---
phase: 19
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md
  - .planning/STATE.md
autonomous: true
requirements: [SIMD-01, SIMD-02, SIMD-03]
tags: [planning, simd, dependency, strategy]

must_haves:
  truths:
    - "The project has a durable Phase 19 strategy artifact that states `github.com/amikos-tech/pure-simdjson v0.1.4` is the selected optional dependency pin."
    - "The strategy artifact records MIT license posture, NOTICE.md/README credit posture, and manual license-drift review on future dependency bumps."
    - "The strategy artifact records shared-library loading as delegated to upstream `purejson.NewParser()` bootstrap, with ami-gin adding only friendlier error guidance."
    - "The strategy artifact records `NewSIMDParser() (Parser, error)` behind `//go:build simdjson` as the v1.3 API shape."
    - "The strategy artifact records default stdlib behavior: no SIMD dependency, no native-library requirement, and no behavior change unless the caller builds with `-tags simdjson` and passes a parser via `WithParser`."
    - "The strategy artifact records hard construction failure semantics plus an explicit caller-owned stdlib fallback recipe."
    - "The strategy artifact records 5-platform `-tags simdjson` CI expectations for linux/amd64, linux/arm64, darwin/amd64, darwin/arm64, and windows/amd64."
    - "The strategy artifact records the stop/fallback table with HARD parity/upstream blockers and SOFT single-platform CI behavior."
  artifacts:
    - path: ".planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md"
      provides: "Durable SIMD dependency and integration decision record for Phase 21 and Phase 22"
      contains: "github.com/amikos-tech/pure-simdjson v0.1.4"
    - path: ".planning/STATE.md"
      provides: "Current-position update showing Phase 19 strategy is complete and Phase 21 is next"
      contains: "Phase 19 strategy complete"
  key_links:
    - from: "Phase 19 strategy"
      to: "Phase 21 SIMD Parser Adapter"
      via: "Pinned dependency, API shape, build tag, and default-stdlib behavior"
      pattern: "NewSIMDParser() (Parser, error)"
    - from: "Phase 19 strategy"
      to: "Phase 22 SIMD Validation, Benchmarks & CI"
      via: "5-platform CI matrix and hard/soft stop policy"
      pattern: "linux/amd64"
---

<objective>
Finalize the SIMD dependency and integration strategy as a durable planning artifact so Phase 21 can implement the parser adapter and Phase 22 can implement validation/CI without re-opening dependency, distribution, API, or stop-condition decisions.

Purpose: satisfy SIMD-01, SIMD-02, and SIMD-03 while keeping Phase 19 documentation-only. No product code, dependency, CI, README, NOTICE, or CHANGELOG changes land in this phase.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-simd-dependency-decision-integration-strategy/19-CONTEXT.md
@.planning/phases/19-simd-dependency-decision-integration-strategy/19-RESEARCH.md
@.planning/phases/19-simd-dependency-decision-integration-strategy/19-PATTERNS.md
@.planning/phases/13-parser-seam-extraction/13-CONTEXT.md
@AGENTS.md

<locked_strategy_values>
- Dependency pin: `github.com/amikos-tech/pure-simdjson v0.1.4`.
- Tag commit: `0f53f3f2e8bb9608d6b79211ffc5fc7b53298617`.
- Constructor signature: `NewSIMDParser() (Parser, error)`.
- Build tag: `//go:build simdjson`.
- Parser name: `"pure-simdjson"`.
- Error wrap text: `initialize pure-simdjson SIMD parser; set PURE_SIMDJSON_LIB_PATH or see docs/simd-deployment.md`.
- CI cache key pattern: `pure-simdjson-${{ matrix.os }}-${{ matrix.arch }}-v0.1.4`.
- Platform set: `linux/amd64`, `linux/arm64`, `darwin/amd64`, `darwin/arm64`, `windows/amd64`.
</locked_strategy_values>

<decision_coverage>
Every trackable Phase 19 CONTEXT.md decision is translated into Task 1's strategy artifact requirements:

| Decision | Strategy Coverage |
|----------|-------------------|
| D-01 | `Dependency: github.com/amikos-tech/pure-simdjson v0.1.4` and tag commit line |
| D-02 | `Bump cadence: pin-and-hold; manual LICENSE and NOTICE review on every bump PR` |
| D-03 | `NOTICE posture: add root NOTICE.md and README dependency credit in Phase 21` |
| D-04 | Manual license and NOTICE review on every bump PR |
| D-05 | `NewSIMDParser` delegates to upstream `purejson.NewParser()` and adds ami-gin-specific error guidance |
| D-06 | CI cache key `pure-simdjson-${{ matrix.os }}-${{ matrix.arch }}-v0.1.4` and auto-download path |
| D-07 | Tiered documentation split between README happy path and `docs/simd-deployment.md` owned by Phase 21 |
| D-08 | No release-time binary-reachability check; bump PR CI is the verification point |
| D-09 | `NewSIMDParser() (Parser, error)` plus explicit `WithParser` selection |
| D-10 | Hard construction error with caller-owned fallback recipe |
| D-11 | 5-platform `-tags simdjson` CI matrix |
| D-12 | Parameter-free constructor for v1.3 |
| D-13 | HARD/SOFT/decide-on-evidence stop table |
</decision_coverage>
</context>

<threat_model>
| ID | Threat | Severity | Mitigation In This Plan |
|----|--------|----------|-------------------------|
| T19-01 | Dependency is added later without license/NOTICE clarity | Medium | Strategy artifact explicitly records MIT license posture, NOTICE.md + README credit posture, and manual license-drift review on bump |
| T19-02 | Native library loading surprises default-build consumers | High | Strategy artifact states default builds remain stdlib-only and SIMD requires both `-tags simdjson` and explicit `NewSIMDParser`/`WithParser` selection |
| T19-03 | Runtime load failure silently degrades and hides missing performance | Medium | Strategy artifact locks hard construction error semantics and documents caller-owned fallback recipe |
| T19-04 | SIMD ships despite correctness-breaking parity failures | High | Stop table marks encoded-byte or query-result parity failures as HARD blockers that halt Phase 21/22 |
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create durable SIMD strategy artifact</name>
  <files>.planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md</files>
  <read_first>
    - .planning/phases/19-simd-dependency-decision-integration-strategy/19-CONTEXT.md
    - .planning/phases/19-simd-dependency-decision-integration-strategy/19-RESEARCH.md
    - .planning/phases/19-simd-dependency-decision-integration-strategy/19-PATTERNS.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/13-parser-seam-extraction/13-CONTEXT.md
  </read_first>
  <action>
    Create `.planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md`.

    The file must be self-contained and include these exact top-level sections:
    - `## Decision Summary`
    - `## SIMD-01: Dependency Source, Pinning, License, and NOTICE`
    - `## SIMD-02: Shared-Library Distribution and Loading`
    - `## SIMD-03: Build Strategy, Opt-In API, CI, and Stop Conditions`
    - `## Downstream Phase 21 Contract`
    - `## Downstream Phase 22 Contract`
    - `## Out of Scope for Phase 19`

    Under `## Decision Summary`, state that Phase 19 makes SIMD executable by locking decisions only; no product code lands in Phase 19.

    Under `## SIMD-01: Dependency Source, Pinning, License, and NOTICE`, include:
    - `Dependency: github.com/amikos-tech/pure-simdjson v0.1.4`
    - `Tag commit: 0f53f3f2e8bb9608d6b79211ffc5fc7b53298617`
    - `License: MIT`
    - `NOTICE posture: add root NOTICE.md and README dependency credit in Phase 21`
    - `Bump cadence: pin-and-hold; manual LICENSE and NOTICE review on every bump PR`

    Under `## SIMD-02: Shared-Library Distribution and Loading`, include:
    - `NewSIMDParser` delegates to upstream `purejson.NewParser()`
    - ami-gin does not reimplement download, cache, mirror, flock, SHA-256 verification, or ABI logic
    - env vars `PURE_SIMDJSON_LIB_PATH`, `PURE_SIMDJSON_BINARY_MIRROR`, `PURE_SIMDJSON_DISABLE_GH_FALLBACK`, and `PURE_SIMDJSON_CACHE_DIR`
    - supported platforms `linux/amd64`, `linux/arm64`, `darwin/amd64`, `darwin/arm64`, `windows/amd64`
    - CI cache key `pure-simdjson-${{ matrix.os }}-${{ matrix.arch }}-v0.1.4`

    Under `## SIMD-03: Build Strategy, Opt-In API, CI, and Stop Conditions`, include:
    - `//go:build simdjson`
    - `NewSIMDParser() (Parser, error)`
    - `Name() == "pure-simdjson"`
    - explicit selection through `WithParser`
    - default stdlib builds remain dependency-free and native-library-free
    - construction errors are hard and wrapped with `initialize pure-simdjson SIMD parser; set PURE_SIMDJSON_LIB_PATH or see docs/simd-deployment.md`
    - a fenced Go fallback recipe showing the caller handling the error and returning `gin.NewBuilder(cfg, numRGs)`
    - a Markdown stop/fallback table with rows for parity failure as HARD, upstream archive/unrecoverable break as HARD, single-platform CI failure as SOFT, and below-expected speedup as decide-on-evidence

    Under `## Downstream Phase 21 Contract`, state Phase 21 owns `go.mod`, `go.sum`, `parser_simd.go`, `NOTICE.md`, `docs/simd-deployment.md`, README dependency credit, and the initial CHANGELOG v1.3 note.

    Under `## Downstream Phase 22 Contract`, state Phase 22 owns SIMD parity tests, realistic benchmark results, `-tags simdjson` CI matrix, release/distribution guidance verification, and stop-table enforcement.

    Under `## Out of Scope for Phase 19`, state exactly that Phase 19 does not edit `go.mod`, `go.sum`, source files, CI workflows, README, NOTICE, CHANGELOG, or runtime docs.
  </action>
  <verify>
    <automated>test -f .planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md && grep -q 'SIMD-01' .planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md && grep -q 'SIMD-02' .planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md && grep -q 'SIMD-03' .planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md && grep -q 'github.com/amikos-tech/pure-simdjson v0.1.4' .planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md && grep -q 'NewSIMDParser() (Parser, error)' .planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md && grep -q '//go:build simdjson' .planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md && grep -q 'PURE_SIMDJSON_LIB_PATH' .planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md` exists.
    - `19-SIMD-STRATEGY.md` contains `Dependency: github.com/amikos-tech/pure-simdjson v0.1.4`.
    - `19-SIMD-STRATEGY.md` contains `Tag commit: 0f53f3f2e8bb9608d6b79211ffc5fc7b53298617`.
    - `19-SIMD-STRATEGY.md` contains `License: MIT`.
    - `19-SIMD-STRATEGY.md` contains `NOTICE posture: add root NOTICE.md and README dependency credit in Phase 21`.
    - `19-SIMD-STRATEGY.md` contains `PURE_SIMDJSON_LIB_PATH`.
    - `19-SIMD-STRATEGY.md` contains `PURE_SIMDJSON_BINARY_MIRROR`.
    - `19-SIMD-STRATEGY.md` contains `PURE_SIMDJSON_DISABLE_GH_FALLBACK`.
    - `19-SIMD-STRATEGY.md` contains `PURE_SIMDJSON_CACHE_DIR`.
    - `19-SIMD-STRATEGY.md` contains `//go:build simdjson`.
    - `19-SIMD-STRATEGY.md` contains `NewSIMDParser() (Parser, error)`.
    - `19-SIMD-STRATEGY.md` contains `Name() == "pure-simdjson"`.
    - `19-SIMD-STRATEGY.md` contains `WithParser`.
    - `19-SIMD-STRATEGY.md` contains `HARD`.
    - `19-SIMD-STRATEGY.md` contains `SOFT`.
    - The focused shell command in `<verify>` exits 0.
  </acceptance_criteria>
  <done>The durable strategy artifact satisfies SIMD-01, SIMD-02, and SIMD-03 decision coverage.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update planning state and run repository sanity check</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md
    - .planning/ROADMAP.md
    - .planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md
  </read_first>
  <action>
    Update `.planning/STATE.md` after the strategy artifact exists.

    Set frontmatter values:
    - `stopped_at: Phase 19 strategy complete — ready for /gsd-execute-phase 21 after planning Phase 21`
    - `last_activity: "2026-04-27 - Phase 19 strategy complete; SIMD dependency, distribution, API, CI, and stop/fallback decisions locked"`

    In `## Current Position`, update:
    - `Phase: 19`
    - `Plan: 19-01 complete — SIMD strategy locked`
    - `Status: Phase 19 strategy complete; next planning target is Phase 21 after Phase 20 dataset planning as scheduled`
    - `Last activity: 2026-04-27 - Phase 19 strategy complete; no product code changed`

    In `### Decisions`, append one bullet that contains exactly `Phase 19 strategy complete` and summarizes the locked decisions: `pure-simdjson v0.1.4`, MIT/NOTICE posture, `NewSIMDParser() (Parser, error)`, `//go:build simdjson`, explicit `WithParser` opt-in, 5-platform SIMD CI expectation, and hard/soft stop policy.

    Do not mark Phase 19 complete in `.planning/ROADMAP.md` in this plan unless `$gsd-execute-phase` workflow normally updates roadmap progress after verification. This task only updates state continuity.

    Run `go test ./...` as a repository sanity check after the planning-only edits.
  </action>
  <verify>
    <automated>grep -q 'Phase 19 strategy complete' .planning/STATE.md && grep -q 'pure-simdjson v0.1.4' .planning/STATE.md && grep -q 'NewSIMDParser() (Parser, error)' .planning/STATE.md && go test ./...</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` contains `Phase 19 strategy complete`.
    - `.planning/STATE.md` contains `pure-simdjson v0.1.4`.
    - `.planning/STATE.md` contains `NewSIMDParser() (Parser, error)`.
    - `.planning/STATE.md` contains `//go:build simdjson`.
    - `.planning/STATE.md` contains `WithParser`.
    - `go test ./...` exits 0.
  </acceptance_criteria>
  <done>Project state points downstream agents at the completed Phase 19 strategy and the repository remains test-green.</done>
</task>

</tasks>

<verification>
Run these commands after executing both tasks:

```bash
test -f .planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md &&
grep -q 'SIMD-01' .planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md &&
grep -q 'SIMD-02' .planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md &&
grep -q 'SIMD-03' .planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md &&
grep -q 'github.com/amikos-tech/pure-simdjson v0.1.4' .planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md &&
grep -q 'NewSIMDParser() (Parser, error)' .planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md &&
grep -q '//go:build simdjson' .planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md &&
grep -q 'PURE_SIMDJSON_LIB_PATH' .planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md
```

```bash
grep -q 'Phase 19 strategy complete' .planning/STATE.md &&
grep -q 'pure-simdjson v0.1.4' .planning/STATE.md &&
grep -q 'NewSIMDParser() (Parser, error)' .planning/STATE.md
```

```bash
go test ./...
```
</verification>

<success_criteria>
- SIMD-01 is covered by exact dependency pin, license/NOTICE posture, bump cadence, and manual license-drift review.
- SIMD-02 is covered by upstream bootstrap delegation, env-var deployment strategy, supported-platform list, CI cache strategy, and unsupported-platform behavior.
- SIMD-03 is covered by build tag, default stdlib behavior, explicit API shape, hard load-failure semantics, fallback recipe, 5-platform CI expectation, and hard/soft stop table.
- Phase 19 remains documentation-only; no product code, dependency graph, CI, README, NOTICE, CHANGELOG, or runtime docs are modified.
- `.planning/STATE.md` gives future agents a clear handoff into Phase 21 and Phase 22.
</success_criteria>
