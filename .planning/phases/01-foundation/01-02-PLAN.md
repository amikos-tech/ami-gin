---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - .gitignore
autonomous: true
requirements:
  - FOUND-03
  - FOUND-04

must_haves:
  truths:
    - "Internal PRD file gin-index-prd.md does not exist in the repository"
    - ".planning/ directory is listed in .gitignore"
    - "No .planning/ files appear in git tracked files"
    - ".planning/ files still exist on local disk for GSD workflow"
  artifacts:
    - path: ".gitignore"
      provides: "Git ignore rules including .planning/"
      contains: ".planning/"
  key_links:
    - from: ".gitignore"
      to: ".planning/"
      via: "gitignore entry prevents tracking"
      pattern: "^.planning/"
---

<objective>
Remove internal PRD artifact and untrack .planning/ directory so no internal process artifacts leak when the repository goes public.

Purpose: Clean up internal references (PRD with "Kiba Team" mentions) and ensure GSD planning artifacts stay local-only.
Output: gin-index-prd.md deleted, .planning/ gitignored and untracked. Two git commits per D-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete internal PRD file (FOUND-03)</name>
  <files>gin-index-prd.md</files>
  <read_first>
    - gin-index-prd.md (confirm it exists and contains internal references before deleting)
  </read_first>
  <action>
Per D-04, this is commit 3 of 4.

Delete the internal PRD file `gin-index-prd.md` from the repository using `git rm`:

```bash
git rm gin-index-prd.md
```

This file is 49KB and contains "Kiba Team" references — it is an internal planning document that must not be in the public repo. Per D-03, this deletion (along with the module rename in Plan 01) handles all material internal references. No additional grep-and-clean pass is needed.

Commit with message: `chore: remove internal PRD document`
  </action>
  <verify>
    <automated>test ! -f gin-index-prd.md && ! git ls-files --error-unmatch gin-index-prd.md 2>/dev/null && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File gin-index-prd.md does not exist on disk
    - `git ls-files gin-index-prd.md` returns empty (not tracked)
    - git log --oneline -1 contains "chore: remove internal PRD document"
  </acceptance_criteria>
  <done>gin-index-prd.md deleted from repository and committed</done>
</task>

<task type="auto">
  <name>Task 2: Add .planning/ to .gitignore and untrack (FOUND-04)</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (current contents — must append, not overwrite existing entries)
  </read_first>
  <action>
Per D-01/D-02/D-04, this is commit 4 of 4. Add `.planning/` to `.gitignore` and remove from git tracking while preserving local files for continued GSD workflow.

IMPORTANT: The order of operations matters (per Research pitfall 4):
1. First, add `.planning/` entry to `.gitignore`
2. Then, run `git rm -r --cached .planning/` to untrack (keeps local files)
3. Commit both changes together

Steps:

1. Append a "GSD workflow" section to `.gitignore`:
```
# GSD workflow (local-only planning artifacts)
.planning/
```

2. Remove .planning/ from git tracking:
```bash
git rm -r --cached .planning/
```

3. Stage .gitignore and commit:
```bash
git add .gitignore
git commit -m "chore: add .planning/ to .gitignore and untrack"
```

4. Verify .planning/ files still exist locally:
```bash
ls .planning/STATE.md  # should exist
```

5. Verify nothing under .planning/ is tracked:
```bash
git ls-files .planning/ | wc -l  # should be 0
```
  </action>
  <verify>
    <automated>grep -q '.planning/' .gitignore && test "$(git ls-files .planning/ | wc -l)" -eq 0 && test -f .planning/STATE.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - .gitignore contains the line `.planning/`
    - `git ls-files .planning/ | wc -l` outputs 0 (no tracked .planning files)
    - .planning/STATE.md exists on local disk (not deleted, just untracked)
    - .planning/ROADMAP.md exists on local disk
    - git log --oneline -1 contains "chore: add .planning/ to .gitignore and untrack"
  </acceptance_criteria>
  <done>.planning/ directory gitignored and untracked, local files preserved for GSD workflow</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `test ! -f gin-index-prd.md` — PRD deleted
2. `grep -q '.planning/' .gitignore` — .planning/ in gitignore
3. `test "$(git ls-files .planning/ | wc -l)" -eq 0` — Nothing tracked under .planning/
4. `test -f .planning/STATE.md` — Local planning files preserved
5. `git log --oneline -4` — Shows all 4 commits in correct order (license, rename, PRD delete, gitignore)
6. `go build ./...` — Build still passes (sanity check)
</verification>

<success_criteria>
- gin-index-prd.md no longer exists in the repository
- .planning/ directory is in .gitignore
- No .planning/ files are git-tracked
- Local .planning/ files still exist for GSD workflow
- Two separate commits exist (one per requirement per D-04)
- Build still passes (no regression from cleanup)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
