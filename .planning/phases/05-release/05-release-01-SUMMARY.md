---
phase: 05-release
plan: 01
subsystem: infra
tags: [goreleaser, github-actions, releases, changelog, readme]
requires:
  - phase: 03-ci-pipeline
    provides: GitHub Actions workflow conventions and the top-level GOTOOLCHAIN local posture
  - phase: 04-contributor-experience
    provides: adopter-focused README structure and minimal public documentation surface
provides:
  - semver tag-triggered GitHub Actions release workflow
  - library-only GoReleaser configuration with grouped changelog output
  - README v0.1.0 known limitations contract
affects: [github-actions, releases, changelog, readme]
tech-stack:
  added: [goreleaser/goreleaser-action@v7, GoReleaser config v2]
  patterns:
    - keep release automation in workflow yaml instead of make
    - use builds skip true for library-only releases
    - keep first-release messaging tag-conditional instead of permanently hardcoded
key-files:
  created:
    - .github/workflows/release.yml
    - .goreleaser.yml
    - .planning/phases/05-release/05-release-01-SUMMARY.md
  modified:
    - README.md
key-decisions:
  - "Kept the release path entirely tag-driven through GitHub Actions plus GoReleaser, with no bootstrap-only script or make target."
  - "Used a tag-conditional GoReleaser release header so v0.1.0 gets first-release wording without leaving stale copy on later tags."
  - "Placed the README limitations section immediately after Quick Start so the scope boundaries stay visible without crowding the top of the document."
patterns-established:
  - "Release automation pattern: semver tag pushes trigger a dedicated Release workflow with full git history and GoReleaser release --clean."
  - "Release notes pattern: grouped changelog sections are generated by GoReleaser while maintainer framing stays concise and tag-conditional."
  - "README scope pattern: release-critical caveats live in README as a short visible limitations section, not a separate release doc."
requirements-completed: [REL-02, REL-03]
duration: 3 min
completed: 2026-04-13
---

# Phase 05 Plan 01: Release Surfaces Summary

**Semver-tag release automation, library-only GoReleaser notes, and a README v0.1.0 limitations contract**

## Performance

- **Duration:** 3 min
- **Started:** 2026-04-13T18:18:41Z
- **Completed:** 2026-04-13T18:22:02Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments

- Added `.github/workflows/release.yml` so future `v*` tags run a dedicated `Release` workflow with `contents: write`, `fetch-depth: 0`, Go `1.26`, and `goreleaser/goreleaser-action@v7`.
- Added `.goreleaser.yml` in library-only mode with `builds: - skip: true`, grouped changelog rules, and a release header that changes between `v0.1.0` and later tags.
- Added a visible `## Known limitations` section to `README.md` with the exact v0.1.0 scope wording for OR/AND composites, index merge, and query-time transformers.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add the dedicated semver-tag GitHub Actions release workflow** - `558554d` (feat)
2. **Task 2: Create the library-only GoReleaser config with grouped first-release notes** - `d01de4f` (feat)
3. **Task 3: Add the public `Known limitations` section to README without bloating the first screen** - `94bef3e` (docs)

**Plan metadata:** Summary-only docs commit recorded after this summary file is written. `STATE.md` and `ROADMAP.md` were intentionally left untouched because the orchestrator owns those writes for this run.

## Files Created/Modified

- `.github/workflows/release.yml` - Dedicated semver-tag release workflow that runs GoReleaser from a full-history checkout with the repo's existing `GOTOOLCHAIN: local` posture.
- `.goreleaser.yml` - Library-first GoReleaser v2 config with grouped changelog buckets, GitHub release targeting for `amikos-tech/ami-gin`, and tag-conditional release preface text.
- `README.md` - Adds the exact `Known limitations` section immediately after Quick Start.
- `.planning/phases/05-release/05-release-01-SUMMARY.md` - Execution summary for Phase 05 Plan 01.

## Decisions Made

- Kept release automation entirely in `.github/workflows/release.yml` plus `.goreleaser.yml`, rather than adding a contributor-facing `make release` path.
- Used GoReleaser templating for the release header so the first public `v0.1.0` wording is specific without becoming stale on later tags.
- Preserved the existing README structure and inserted limitations at the first point where adopters have seen installation plus a basic usage example.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required in this plan.

## Next Phase Readiness

- Plan `05-02` can now rehearse the release candidate SHA with `goreleaser check` and snapshot release flow against concrete repo-local release surfaces.
- The repository now has the local release automation and adopter-facing limitations contract required before the human-gated `v0.1.0` publication steps.
- Shared planning state files were intentionally not updated here because the orchestrator owns those writes after completion.

## Self-Check: PASSED

- Verified `.planning/phases/05-release/05-release-01-SUMMARY.md` exists on disk.
- Verified task commits `558554d`, `d01de4f`, and `94bef3e` resolve in git history.
- Verified the changed files contain no obvious placeholder or TODO-style stub markers.

---
*Phase: 05-release*
*Completed: 2026-04-13*
