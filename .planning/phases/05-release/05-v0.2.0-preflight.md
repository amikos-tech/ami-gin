# v0.2.0 Preflight

## Recommended Tag

v0.2.0

## Candidate Base SHA

bdbd3140f343e3efcc3fe3171636397b8570856f

## Pending Release-Surface Changes

- `README.md`
- `.goreleaser.yml`

## Latest Published Release

- `v0.1.0` (`2026-04-13T19:50:40Z`)

## Commit Delta Since `v0.1.0`

- `d8d0d2b` `Phase 06: Query Path Hot Path (#17)`
- `0b45d75` `build(deps): bump the gomod-minor-and-patch group with 7 updates (#14)`
- `0a638a2` `Phase 07: Builder Parsing & Numeric Fidelity (#18)`
- `306aa40` `Phase 08: Adaptive High-Cardinality Indexing (#19)`
- `bdbd314` `Phase 09: Derived Representations (#20)`

## Verification

- `go build ./...` — PASS
- `go test -v ./...` — PASS
- `goreleaser check` — PASS from a detached worktree carrying the pending release-surface updates
- `goreleaser release --snapshot --clean` — PASS from a detached worktree on `HEAD`
- Local-only `v0.2.0` tag rehearsal — PASS; the `v0.2.0`-specific release header rendered in `dist/config.yaml`

## Release Highlights

- Adaptive high-cardinality indexing now keeps exact pruning for hot values while compacting the long tail.
- Derived representations add companion transformer-backed query aliases via `gin.As(alias, value)`.
- Builder and query-path work since `v0.1.0` improved numeric fidelity, parsing robustness, and hot-path behavior.
- Serialized index compatibility remains strict: indexes built by `v0.1.0` (wire format `v3`) must be rebuilt with `v0.2.0` (wire format `v8`).

## Verdict

READY TO TAG AFTER COMMITTING THE RELEASE-SURFACE CHANGES
