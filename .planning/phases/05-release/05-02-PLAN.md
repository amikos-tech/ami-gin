---
phase: 05-release
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - .planning/phases/05-release/05-v0.1.0-preflight.md
  - .planning/phases/05-release/05-v0.1.0-release-evidence.md
autonomous: false
requirements:
  - REL-01
  - REL-02
user_setup:
  - service: github
    why: "The public GitHub URL for `amikos-tech/ami-gin` currently returns 404, so Phase 5 cannot satisfy the public `go get` contract until the repository is reachable unauthenticated."
    dashboard_config:
      - task: "Make `amikos-tech/ami-gin` publicly reachable, merge the Phase 5 prep PR to `main`, and ensure the latest `CI` run on `main` is green before approving the real `v0.1.0` tag push."
        location: "GitHub repository settings, pull requests, and Actions for amikos-tech/ami-gin"
must_haves:
  truths:
    - The repository is publicly reachable before `v0.1.0` is pushed, so unauthenticated consumers can use the published module.
    - The exact release candidate SHA is rehearsed with a clean worktree, `goreleaser check`, and `goreleaser release --snapshot` before the real tag is created.
    - The real `v0.1.0` tag is created on the rehearsed SHA and pushed once, through the same tag-driven workflow future semver releases will use.
    - The published release is proven by a successful `Release` workflow run, a visible GitHub Release for `v0.1.0`, and a clean-environment `GOPROXY=https://proxy.golang.org go get github.com/amikos-tech/ami-gin@v0.1.0` that succeeds immediately or after a bounded retry window for proxy propagation.
  artifacts:
    - `.planning/phases/05-release/05-v0.1.0-preflight.md` records the candidate SHA, public-access check, commit preview, GoReleaser check, and snapshot rehearsal result.
    - `.planning/phases/05-release/05-v0.1.0-release-evidence.md` records the tag SHA, release workflow evidence, GitHub Release URL, and clean consumer install output.
  key_links:
    - The real tag may only be pushed after the repo is publicly reachable and the default branch is green.
    - The candidate SHA used in the preflight document must match the SHA tagged as `v0.1.0`.
    - If the initial proxy-based `go get` fails with a not-found style error, retry on `proxy.golang.org` for up to 15 minutes and run one `GOPROXY=direct` diagnostic only to distinguish propagation lag from a broken release.
    - If the workflow or the bounded consumer-install verification still fails, stop and capture evidence; do not hand-create a release or invent a second publish path.
---

<objective>
Execute the irreversible Phase 5 release cutover: confirm public repo readiness, rehearse the exact release candidate SHA, and publish `v0.1.0` through the same automated tag flow future semver releases will use.

Purpose: Complete REL-01 and the live-public side of REL-02 while capturing auditable evidence instead of relying on memory or ad hoc release steps.
Output: `05-v0.1.0-preflight.md` and `05-v0.1.0-release-evidence.md`.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-release/05-CONTEXT.md
@.planning/phases/05-release/05-RESEARCH.md
@.planning/phases/05-release/05-VALIDATION.md
@.planning/phases/05-release/05-01-PLAN.md
@.github/workflows/release.yml
@.goreleaser.yml
@README.md
@Makefile
@go.mod
@/Users/tazarov/experiments/telia/tclr/tclr-v2/scripts/release.sh
@AGENTS.md
@CLAUDE.md

<interfaces>
Required public-readiness rule:
```text
Do not push `v0.1.0` until `https://github.com/amikos-tech/ami-gin` is publicly reachable and the latest `CI` run on `main` is green.
```

Required tag command:
```bash
git tag -a v0.1.0 -m "v0.1.0" <candidate-sha>
git push origin v0.1.0
```

Required evidence sections:
```text
# v0.1.0 Preflight
## Candidate SHA
## Repository Visibility
## Last Tag
## Commit Preview
## GoReleaser Check
## Snapshot Rehearsal
## Verdict

# v0.1.0 Release Evidence
## Tagged SHA
## Release Workflow
## GitHub Release
## Consumer Install Check
## Verdict
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Resolve public repo access and default-branch readiness before the first public tag</name>
  <files>NONE - GitHub visibility, merge, and Actions state outside the working tree</files>
  <read_first>
- .planning/STATE.md
- .planning/phases/05-release/05-RESEARCH.md
- .planning/phases/05-release/05-VALIDATION.md
- .github/workflows/ci.yml
- .github/workflows/release.yml
  </read_first>
  <action>Because the unauthenticated public URL `https://github.com/amikos-tech/ami-gin` currently returns `404`, and the state file still records a preceding human checkpoint after Phase 4, complete these readiness steps before continuing. First, make `amikos-tech/ami-gin` publicly reachable or otherwise unauthenticatedly accessible. Second, merge the Phase 5 prep pull request so the release workflow, GoReleaser config, and README limitations are on `main`. Third, wait for the latest `CI` workflow run on `main` to complete successfully. Do not continue until all three are true. Resume only with the exact signal `public-ready-and-main-green`.</action>
  <acceptance_criteria>
- `test "$(gh repo view amikos-tech/ami-gin --json isPrivate -q .isPrivate)" = "false"` succeeds.
- `curl -I https://github.com/amikos-tech/ami-gin | rg -n '^HTTP/.+ 200|^HTTP/.+ 3[0-9][0-9]'` succeeds.
- `test "$(gh run list --workflow ci.yml --branch main --limit 1 --json workflowName -q '.[0].workflowName')" = "CI"` succeeds.
- `test "$(gh run list --workflow ci.yml --branch main --limit 1 --json conclusion -q '.[0].conclusion')" = "success"` succeeds.
- Resume signal must be exactly `public-ready-and-main-green`.
  </acceptance_criteria>
  <verify>
    <automated>gh repo view amikos-tech/ami-gin --json isPrivate,url
curl -I https://github.com/amikos-tech/ami-gin
gh run list --workflow ci.yml --branch main --limit 1 --json workflowName,conclusion,headSha</automated>
  </verify>
  <done>The repository is publicly reachable, the release-prep work is on `main`, and the default branch is green enough to cut a real public tag.</done>
</task>

<task type="auto">
  <name>Task 2: Rehearse the exact candidate SHA in an isolated worktree and record preflight evidence</name>
  <files>.planning/phases/05-release/05-v0.1.0-preflight.md</files>
  <read_first>
- .planning/phases/05-release/05-RESEARCH.md
- .planning/phases/05-release/05-VALIDATION.md
- .github/workflows/release.yml
- .goreleaser.yml
- README.md
- /Users/tazarov/experiments/telia/tclr/tclr-v2/scripts/release.sh
  </read_first>
  <action>After Task 1 returns `public-ready-and-main-green`, create an isolated rehearsal using a temporary detached worktree from the merged default branch instead of disturbing the current working branch. Run `git fetch origin main --tags`, create `TMPDIR="$(mktemp -d)"`, and `git worktree add --detach "$TMPDIR" origin/main`. Inside that worktree, set `CANDIDATE_SHA="$(git rev-parse HEAD)"`, verify the tree is clean with `git diff --quiet`, capture `LAST_TAG="$(git describe --tags --abbrev=0 2>/dev/null || echo none)"`, and capture a commit preview using `git log --oneline --no-decorate | head -20` when `LAST_TAG=none`, otherwise `git log "$LAST_TAG"..HEAD --oneline --no-decorate`. If `goreleaser` is not on `PATH`, install it first with the exact command `go install github.com/goreleaser/goreleaser/v2@v2.14.3`. Then run `goreleaser check` and `goreleaser release --snapshot --clean` in that worktree. Write `.planning/phases/05-release/05-v0.1.0-preflight.md` with these exact sections: `# v0.1.0 Preflight`, `## Candidate SHA`, `## Repository Visibility`, `## Last Tag`, `## Commit Preview`, `## GoReleaser Check`, `## Snapshot Rehearsal`, and `## Verdict`. Under `## Candidate SHA`, write the raw 40-character commit SHA on its own line with no bullet prefix. In `## Verdict`, write the exact line `READY FOR TAG` if and only if the repo visibility check, `goreleaser check`, and the snapshot rehearsal all succeeded. Remove the temporary worktree with `git worktree remove "$TMPDIR" --force` after the evidence file is written. Do not create or push any real tag in this task.</action>
  <acceptance_criteria>
- `test -f .planning/phases/05-release/05-v0.1.0-preflight.md` succeeds.
- `rg -n '^# v0\\.1\\.0 Preflight$' .planning/phases/05-release/05-v0.1.0-preflight.md` returns the title.
- `rg -n '^## Candidate SHA$' .planning/phases/05-release/05-v0.1.0-preflight.md` returns the candidate section.
- `rg -n '^## Repository Visibility$' .planning/phases/05-release/05-v0.1.0-preflight.md` returns the visibility section.
- `rg -n '^## Last Tag$' .planning/phases/05-release/05-v0.1.0-preflight.md` returns the last-tag section.
- `rg -n '^## Commit Preview$' .planning/phases/05-release/05-v0.1.0-preflight.md` returns the commit-preview section.
- `rg -n '^## GoReleaser Check$' .planning/phases/05-release/05-v0.1.0-preflight.md` returns the GoReleaser check section.
- `rg -n '^## Snapshot Rehearsal$' .planning/phases/05-release/05-v0.1.0-preflight.md` returns the snapshot section.
- `rg -n '^## Verdict$' .planning/phases/05-release/05-v0.1.0-preflight.md` returns the verdict section.
- `rg -n '^READY FOR TAG$' .planning/phases/05-release/05-v0.1.0-preflight.md` returns the exact verdict line.
- `rg -n '^[0-9a-f]{40}$' .planning/phases/05-release/05-v0.1.0-preflight.md` returns the rehearsed SHA line.
- `goreleaser check` exits 0 when re-run from a clean merged checkout.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
test -f .planning/phases/05-release/05-v0.1.0-preflight.md
rg -n '^# v0\.1\.0 Preflight$' .planning/phases/05-release/05-v0.1.0-preflight.md
rg -n '^## Candidate SHA$' .planning/phases/05-release/05-v0.1.0-preflight.md
rg -n '^## Repository Visibility$' .planning/phases/05-release/05-v0.1.0-preflight.md
rg -n '^## Last Tag$' .planning/phases/05-release/05-v0.1.0-preflight.md
rg -n '^## Commit Preview$' .planning/phases/05-release/05-v0.1.0-preflight.md
rg -n '^## GoReleaser Check$' .planning/phases/05-release/05-v0.1.0-preflight.md
rg -n '^## Snapshot Rehearsal$' .planning/phases/05-release/05-v0.1.0-preflight.md
rg -n '^## Verdict$' .planning/phases/05-release/05-v0.1.0-preflight.md
rg -n '^READY FOR TAG$' .planning/phases/05-release/05-v0.1.0-preflight.md
rg -n '^[0-9a-f]{40}$' .planning/phases/05-release/05-v0.1.0-preflight.md
goreleaser check</automated>
  </verify>
  <done>The exact candidate SHA has been rehearsed and captured in a permanent preflight record, so the real tag can be cut without guessing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Push the real `v0.1.0` tag from the rehearsed SHA and capture release evidence</name>
  <files>.planning/phases/05-release/05-v0.1.0-release-evidence.md</files>
  <read_first>
- .planning/phases/05-release/05-v0.1.0-preflight.md
- .planning/phases/05-release/05-RESEARCH.md
- .planning/phases/05-release/05-VALIDATION.md
- .github/workflows/release.yml
- .goreleaser.yml
  </read_first>
  <action>Only start this task after Task 2 records the exact verdict `READY FOR TAG`, and after the user explicitly approves cutting the public tag. Parse the candidate SHA from `.planning/phases/05-release/05-v0.1.0-preflight.md`, then create a new temporary detached worktree on that exact SHA. In that worktree, create the exact annotated tag `git tag -a v0.1.0 -m "v0.1.0" "$CANDIDATE_SHA"` and push it with `git push origin v0.1.0`. Wait for the latest run of `.github/workflows/release.yml` whose display title is `v0.1.0` to finish successfully. Verify a GitHub Release exists for `v0.1.0` using `gh release view v0.1.0`. Then create a fresh temp directory, run `go mod init release-check`, and verify public consumer installability with a bounded proxy-aware loop: run `GOPROXY=https://proxy.golang.org go get github.com/amikos-tech/ami-gin@v0.1.0`; if it fails with a not-found or unknown-revision style error, wait 180 seconds and retry, for a maximum of five attempts total. If the proxy-based command is still failing after the fifth attempt, run exactly one diagnostic `GOPROXY=direct go get github.com/amikos-tech/ami-gin@v0.1.0` to distinguish proxy propagation lag from a broken release, and record both the proxy attempts and the direct-fetch result in the evidence file. Write `.planning/phases/05-release/05-v0.1.0-release-evidence.md` with the exact sections `# v0.1.0 Release Evidence`, `## Tagged SHA`, `## Release Workflow`, `## GitHub Release`, `## Consumer Install Check`, and `## Verdict`. Under `## Tagged SHA`, write the raw 40-character SHA on its own line with no bullet prefix. In `## Consumer Install Check`, record the number of proxy attempts, the last proxy error if any, and the direct-fetch diagnostic result if it was needed. In `## Verdict`, write the exact line `PUBLIC RELEASE VERIFIED` only if the workflow succeeded, the GitHub Release exists, and the proxy-based `go get` eventually exited 0 within the bounded retry window. If the direct fetch succeeds but the proxy window expires, record that state explicitly and stop without declaring the release broken or inventing a fallback publish path. Remove the temporary worktree after the evidence file is written. If any other checks fail, stop and capture the failure evidence in the file; do not hand-create a GitHub Release, do not rewrite the tag to another SHA, and do not invent a fallback release path.</action>
  <acceptance_criteria>
- `test -f .planning/phases/05-release/05-v0.1.0-release-evidence.md` succeeds.
- `rg -n '^# v0\\.1\\.0 Release Evidence$' .planning/phases/05-release/05-v0.1.0-release-evidence.md` returns the title.
- `rg -n '^## Tagged SHA$' .planning/phases/05-release/05-v0.1.0-release-evidence.md` returns the tag section.
- `rg -n '^## Release Workflow$' .planning/phases/05-release/05-v0.1.0-release-evidence.md` returns the workflow section.
- `rg -n '^## GitHub Release$' .planning/phases/05-release/05-v0.1.0-release-evidence.md` returns the release section.
- `rg -n '^## Consumer Install Check$' .planning/phases/05-release/05-v0.1.0-release-evidence.md` returns the consumer-install section.
- `rg -n '^## Verdict$' .planning/phases/05-release/05-v0.1.0-release-evidence.md` returns the verdict section.
- `rg -n 'proxy\\.golang\\.org|GOPROXY=direct' .planning/phases/05-release/05-v0.1.0-release-evidence.md` returns the retry or diagnostic evidence.
- `rg -n '^PUBLIC RELEASE VERIFIED$' .planning/phases/05-release/05-v0.1.0-release-evidence.md` returns the exact verdict line.
- `gh release view v0.1.0 --json tagName,isDraft,isPrerelease,url | jq -e '.tagName == "v0.1.0" and .isDraft == false' >/dev/null` succeeds.
- `gh run list --workflow release.yml --limit 10 --json displayTitle,event,conclusion,url | jq -e 'map(select(.displayTitle == "v0.1.0" and .event == "push" and .conclusion == "success")) | length > 0' >/dev/null` succeeds.
- `tmpdir="$(mktemp -d)" && cd "$tmpdir" && go mod init release-check && for attempt in 1 2 3 4 5; do GOPROXY=https://proxy.golang.org go get github.com/amikos-tech/ami-gin@v0.1.0 && exit 0; test "$attempt" = "5" && exit 1; sleep 180; done` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
test -f .planning/phases/05-release/05-v0.1.0-release-evidence.md
rg -n '^# v0\.1\.0 Release Evidence$' .planning/phases/05-release/05-v0.1.0-release-evidence.md
rg -n '^## Tagged SHA$' .planning/phases/05-release/05-v0.1.0-release-evidence.md
rg -n '^## Release Workflow$' .planning/phases/05-release/05-v0.1.0-release-evidence.md
rg -n '^## GitHub Release$' .planning/phases/05-release/05-v0.1.0-release-evidence.md
rg -n '^## Consumer Install Check$' .planning/phases/05-release/05-v0.1.0-release-evidence.md
rg -n '^## Verdict$' .planning/phases/05-release/05-v0.1.0-release-evidence.md
rg -n 'proxy\.golang\.org|GOPROXY=direct' .planning/phases/05-release/05-v0.1.0-release-evidence.md
rg -n '^PUBLIC RELEASE VERIFIED$' .planning/phases/05-release/05-v0.1.0-release-evidence.md
gh release view v0.1.0 --json tagName,isDraft,isPrerelease,url | jq -e '.tagName == "v0.1.0" and .isDraft == false' >/dev/null
gh run list --workflow release.yml --limit 10 --json displayTitle,event,conclusion,url | jq -e 'map(select(.displayTitle == "v0.1.0" and .event == "push" and .conclusion == "success")) | length > 0' >/dev/null
tmpdir="$(mktemp -d)"
cd "$tmpdir"
go mod init release-check
for attempt in 1 2 3 4 5; do
  if GOPROXY=https://proxy.golang.org go get github.com/amikos-tech/ami-gin@v0.1.0; then
    exit 0
  fi
  test "$attempt" = "5" && break
  sleep 180
done
exit 1</automated>
  </verify>
  <done>The real public `v0.1.0` release has been cut from the rehearsed SHA, verified through the automated workflow, and confirmed consumable through a clean `go get`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer approval → public git tag | A human decision crosses into irreversible release state visible to all consumers |
| tagged commit → GitHub Actions release workflow | The pushed tag automatically creates public release artifacts and notes |
| public GitHub repository → Go module proxy / consumers | Public availability becomes the real delivery contract for `go get` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | S/T | tag creation | mitigate | Record the exact candidate SHA in `05-v0.1.0-preflight.md` and require the real tag to target that same SHA. |
| T-05-05 | I | GitHub repository visibility | mitigate | Block the tag until the repository is publicly reachable and the default branch is green. |
| T-05-06 | D/I | published release contract | mitigate | Require both workflow success and a clean-environment `go get` before declaring the public release verified. |
</threat_model>

<verification>
1. Public readiness:
   `gh repo view amikos-tech/ami-gin --json isPrivate,url`
   `curl -I https://github.com/amikos-tech/ami-gin`
   `gh run list --workflow ci.yml --branch main --limit 1 --json workflowName,conclusion,headSha`
2. Preflight:
   `rg -n '^READY FOR TAG$|^[0-9a-f]{40}$' .planning/phases/05-release/05-v0.1.0-preflight.md`
   `goreleaser check`
3. Published release:
   `gh run list --workflow release.yml --limit 10 --json displayTitle,event,conclusion,url | jq -e 'map(select(.displayTitle == "v0.1.0" and .event == "push" and .conclusion == "success")) | length > 0' >/dev/null`
   `gh release view v0.1.0 --json tagName,url`
   `tmpdir="$(mktemp -d)" && cd "$tmpdir" && go mod init release-check && for attempt in 1 2 3 4 5; do GOPROXY=https://proxy.golang.org go get github.com/amikos-tech/ami-gin@v0.1.0 && break; test "$attempt" = "5" && break; sleep 180; done`
   If the proxy retry loop exhausts: `tmpdir="$(mktemp -d)" && cd "$tmpdir" && go mod init release-check && GOPROXY=direct go get github.com/amikos-tech/ami-gin@v0.1.0"`
</verification>

<success_criteria>
The repository is publicly reachable, `v0.1.0` is cut from the rehearsed SHA through the automated tag path, the GitHub Release exists, and a clean consumer install succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/05-release/05-release-02-SUMMARY.md`
</output>
