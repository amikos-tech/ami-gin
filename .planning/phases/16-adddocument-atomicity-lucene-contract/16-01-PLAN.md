---
phase: 16
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - builder.go
  - gin_test.go
autonomous: true
requirements: [ATOMIC-02]
tags: [builder, atomicity, validator, numeric]

must_haves:
  truths:
    - "`validateStagedPaths` rejects both lossy mixed numeric promotion directions before `mergeStagedPaths` mutates shared builder state."
    - "`mergeStagedPaths`, `mergeNumericObservation`, and `promoteNumericPathToFloat` no longer return `error`."
    - "The three merge-layer functions are each preceded by `// MUST_BE_CHECKED_BY_VALIDATOR`."
    - "The hoisted numeric promotion error keeps the exact string `unsupported mixed numeric promotion at $.score`."
  artifacts:
    - path: "builder.go"
      provides: "validator-complete merge pipeline and marker-protected infallible merge signatures"
      contains: "MUST_BE_CHECKED_BY_VALIDATOR"
    - path: "gin_test.go"
      provides: "focused validator regression tests for numeric promotion hoisting"
      contains: "TestValidateStagedPathsRejectsLossyPromotionBeforeMerge"
  key_links:
    - from: "builder.go validateStagedPaths"
      to: "builder.go stageNumericObservation"
      via: "shadow numeric simulation seeded from real b.pathData"
      pattern: "stageNumericObservation\\(path, observation, preview\\)"
    - from: "builder.go mergeDocumentState"
      to: "builder.go mergeStagedPaths"
      via: "validate-before-mutate ordering"
      pattern: "validateStagedPaths\\(state\\)"
---

<objective>
Make the existing validate-before-mutate pipeline complete for Phase 16's one merge-hoisted user failure: unsupported mixed numeric promotion. After this plan, the merge layer is infallible by signature and guarded by validator markers, while validator tests prove lossy promotion is rejected before shared state mutation.

Purpose: satisfy ATOMIC-02 and establish the structural contract that later plans use for tragic recovery and static enforcement.

Output: `builder.go` with marker-protected no-error merge signatures, plus focused `gin_test.go` coverage for pre-merge numeric promotion rejection.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-adddocument-atomicity-lucene-contract/16-CONTEXT.md
@.planning/phases/16-adddocument-atomicity-lucene-contract/16-RESEARCH.md
@.planning/phases/16-adddocument-atomicity-lucene-contract/16-VALIDATION.md
@.planning/phases/16-adddocument-atomicity-lucene-contract/16-PATTERNS.md
@.planning/phases/13-parser-seam-extraction/13-CONTEXT.md
@AGENTS.md
@CLAUDE.md

<interfaces>
Current signatures that this plan changes:

```go
func (b *GINBuilder) validateStagedPaths(state *documentBuildState) error
func (b *GINBuilder) mergeStagedPaths(state *documentBuildState) error
func (b *GINBuilder) mergeNumericObservation(pd *pathBuildData, observation stagedNumericValue, rgID int, path string) error
func (b *GINBuilder) promoteNumericPathToFloat(pd *pathBuildData) error
```

Required target signatures:

```go
func (b *GINBuilder) validateStagedPaths(state *documentBuildState) error
// MUST_BE_CHECKED_BY_VALIDATOR
func (b *GINBuilder) mergeStagedPaths(state *documentBuildState)
// MUST_BE_CHECKED_BY_VALIDATOR
func (b *GINBuilder) mergeNumericObservation(pd *pathBuildData, observation stagedNumericValue, rgID int, path string)
// MUST_BE_CHECKED_BY_VALIDATOR
func (b *GINBuilder) promoteNumericPathToFloat(pd *pathBuildData)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add focused validator tests for lossy numeric promotion hoisting</name>
  <files>gin_test.go</files>
  <read_first>
    - gin_test.go
    - builder.go
    - .planning/phases/16-adddocument-atomicity-lucene-contract/16-CONTEXT.md D-01, D-02, D-04, D-09
    - .planning/phases/16-adddocument-atomicity-lucene-contract/16-RESEARCH.md sections "Validator Coverage Test Shape" and "Common Pitfalls"
    - .planning/phases/16-adddocument-atomicity-lucene-contract/16-VALIDATION.md rows 16-01-01 and 16-01-02
    - .planning/phases/16-adddocument-atomicity-lucene-contract/16-PATTERNS.md sections for `gin_test.go` and `builder.go`
  </read_first>
  <behavior>
    - Test 1: existing unsafe int state plus staged float returns `unsupported mixed numeric promotion at $.score` from `validateStagedPaths`.
    - Test 2: existing float-mixed state plus staged unsafe int returns `unsupported mixed numeric promotion at $.score` from `validateStagedPaths`.
    - Test 3: after a rejected mixed numeric document, the builder remains usable for a subsequent valid document and `numDocs` reflects only successful documents.
  </behavior>
  <action>
    Add focused tests to `gin_test.go`:

    - `TestValidateStagedPathsRejectsLossyPromotionBeforeMerge`
      - create `builder := mustNewBuilder(t, DefaultConfig(), 3)`
      - seed doc 0 with `{"score":9007199254740993}`
      - create `state := newDocumentBuildState(1)`
      - stage the candidate float with `builder.stageJSONNumberLiteral("$.score", "1.5", state)`
      - call `builder.validateStagedPaths(state)`
      - assert the error string contains exactly `unsupported mixed numeric promotion at $.score`
      - assert `builder.numDocs == 1`

    - `TestValidateStagedPathsRejectsUnsafeIntIntoFloatPath`
      - seed doc 0 with `{"score":1.5}`
      - create `state := newDocumentBuildState(1)`
      - stage the candidate int with `builder.stageJSONNumberLiteral("$.score", "9007199254740993", state)`
      - call `builder.validateStagedPaths(state)`
      - assert the error string contains exactly `unsupported mixed numeric promotion at $.score`

    - Extend or add `TestMixedNumericPathRejectsLossyPromotionLeavesBuilderUsable`
      - reject `{"score":1.5}` after seeding `{"score":9007199254740993}`
      - then successfully add `{"status":"ok"}`
      - assert `builder.numDocs == 2`
      - finalize and assert `EQ("$.status", "ok")` returns row group 1 only

    Do not introduce `IngestError`, `IngestFailureMode`, snapshot/restore, or typed path preview helpers; those are out of Phase 16 scope.
  </action>
  <verify>
    <automated>go test -run 'Test(ValidateStagedPathsRejectsLossyPromotionBeforeMerge|ValidateStagedPathsRejectsUnsafeIntIntoFloatPath|MixedNumericPathRejectsLossyPromotionLeavesBuilderUsable)$' ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `gin_test.go` contains `func TestValidateStagedPathsRejectsLossyPromotionBeforeMerge`.
    - `gin_test.go` contains `func TestValidateStagedPathsRejectsUnsafeIntIntoFloatPath`.
    - `gin_test.go` contains `func TestMixedNumericPathRejectsLossyPromotionLeavesBuilderUsable`.
    - `gin_test.go` contains the exact string `unsupported mixed numeric promotion at $.score`.
    - `go test -run 'Test(ValidateStagedPathsRejectsLossyPromotionBeforeMerge|ValidateStagedPathsRejectsUnsafeIntIntoFloatPath|MixedNumericPathRejectsLossyPromotionLeavesBuilderUsable)$' ./... -count=1` exits 0 after Task 2 is implemented.
  </acceptance_criteria>
  <done>Validator-focused tests pin both lossy numeric promotion directions and the post-rejection usable-builder behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make merge-layer numeric functions infallible by signature</name>
  <files>builder.go</files>
  <read_first>
    - builder.go
    - gin_test.go
    - .planning/phases/16-adddocument-atomicity-lucene-contract/16-CONTEXT.md D-01, D-02, D-04, D-09
    - .planning/phases/16-adddocument-atomicity-lucene-contract/16-RESEARCH.md sections "Current Code Audit", "Exact Failure Sites", and "Don't Hand-Roll"
    - .planning/phases/16-adddocument-atomicity-lucene-contract/16-VALIDATION.md rows 16-01-01 and 16-01-02
    - .planning/phases/16-adddocument-atomicity-lucene-contract/16-PATTERNS.md sections for `builder.go`
  </read_first>
  <behavior>
    - `validateStagedPaths` remains the only user-error return point for unsupported mixed numeric promotion.
    - `mergeStagedPaths`, `mergeNumericObservation`, and `promoteNumericPathToFloat` compile without `error` returns.
    - Any impossible missed-validator mixed-promotion branch in merge panics as an internal invariant path, ready for Plan 16-02 recovery.
  </behavior>
  <action>
    Update `builder.go` per D-01, D-02, D-04, and D-09:

    - Addresses review concern: validator coverage may already handle both mixed-promotion directions. After Task 1 adds the focused tests, run `go test -run 'Test(ValidateStagedPathsRejectsLossyPromotionBeforeMerge|ValidateStagedPathsRejectsUnsafeIntIntoFloatPath)$' ./... -count=1` before changing validator logic.
    - If that pre-check exits 0, leave the `validateStagedPaths` body unchanged except for edits required by the no-error merge signatures; perform only the signature changes, marker additions, impossible-branch panics, and call-site rewrites below.
    - If that pre-check fails, make the smallest validator change: ensure `validateStagedPaths` replays every `staged.numericValues` entry through `b.stageNumericObservation(path, observation, preview)` so `seedNumericSimulation` reads the real `b.pathData`. Do not add snapshot/restore, typed path-preview clones, or a shared commit-bool helper.
    - Add `// MUST_BE_CHECKED_BY_VALIDATOR` immediately above these three function declarations:
      - `func (b *GINBuilder) mergeStagedPaths(state *documentBuildState)`
      - `func (b *GINBuilder) mergeNumericObservation(pd *pathBuildData, observation stagedNumericValue, rgID int, path string)`
      - `func (b *GINBuilder) promoteNumericPathToFloat(pd *pathBuildData)`
    - Change the three target function signatures so none returns `error`.
    - Update `mergeDocumentState` so it calls `b.mergeStagedPaths(state)` after successful `validateStagedPaths(state)` and then proceeds to doc bookkeeping. Remove the old `if err := b.mergeStagedPaths(state); err != nil { ... b.poisonErr = err ... }` branch.
    - In `mergeStagedPaths`, replace the `if err := b.mergeNumericObservation(...); err != nil { return err }` branch with a direct call.
    - In `mergeNumericObservation`, replace normal `return nil` statements with `return`.
    - In `mergeNumericObservation`, any branch that still detects `!canRepresentIntAsExactFloat(...)` after validation must `panic(errors.Errorf("validator missed unsupported mixed numeric promotion at %s", path))`.
    - In `promoteNumericPathToFloat`, any branch that still detects non-exact existing integer stats after validation must `panic(errors.New("validator missed unsupported mixed numeric promotion"))`.
    - Keep validator-returned user errors worded exactly as `unsupported mixed numeric promotion at %s`; do not add `numeric:` prefixes or structured error fields.
  </action>
  <verify>
    <automated>go test -run 'Test(ValidateStagedPathsRejectsLossyPromotionBeforeMerge|ValidateStagedPathsRejectsUnsafeIntIntoFloatPath|MixedNumericPathRejectsLossyPromotionLeavesBuilderUsable)$' ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `builder.go` contains `// MUST_BE_CHECKED_BY_VALIDATOR` immediately before `func (b *GINBuilder) mergeStagedPaths`.
    - `builder.go` contains `// MUST_BE_CHECKED_BY_VALIDATOR` immediately before `func (b *GINBuilder) mergeNumericObservation`.
    - `builder.go` contains `// MUST_BE_CHECKED_BY_VALIDATOR` immediately before `func (b *GINBuilder) promoteNumericPathToFloat`.
    - `rg -n 'func .*mergeStagedPaths.*error|func .*mergeNumericObservation.*error|func .*promoteNumericPathToFloat.*error' builder.go` returns no output.
    - `builder.go` does not contain `b.poisonErr = err`.
    - `builder.go` contains `validator missed unsupported mixed numeric promotion`.
    - `16-01-SUMMARY.md` states whether the pre-check showed `validateStagedPaths` already covered both promotion directions before signature edits.
    - `go test -run 'Test(ValidateStagedPathsRejectsLossyPromotionBeforeMerge|ValidateStagedPathsRejectsUnsafeIntIntoFloatPath|MixedNumericPathRejectsLossyPromotionLeavesBuilderUsable)$' ./... -count=1` exits 0.
  </acceptance_criteria>
  <done>The merge-layer functions are signature-infallible and marker-protected, with validator tests proving user-reachable mixed promotion fails before mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller JSON bytes -> builder staging | Untrusted document content enters parser/staging and may contain numeric forms that conflict with existing path state. |
| Staged document state -> shared builder indexes | Validator-approved `documentBuildState` crosses into mutable `pathData`, bloom, HLL, trigrams, and doc bookkeeping. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T16-01 | Tampering | `validateStagedPaths`, `mergeStagedPaths`, numeric stats | mitigate | Task 1 adds tests for both lossy promotion directions; Task 2 routes validation through shadow numeric simulation seeded from real `b.pathData` and removes user-error returns from merge. |
| T16-02 | Denial of Service | merge panic handling | transfer | Plan 16-02 wraps `mergeStagedPaths` in `runMergeWithRecover`; this plan leaves missed-validator branches as internal invariant panics only. |
| T16-03 | Information Disclosure | panic logging | transfer | Plan 16-02 owns recovery logging and must avoid raw document/panic-value attrs. |
| T16-04 | Tampering | marker/signature invariant | transfer | This plan adds the markers; Plan 16-04 wires local and CI checks that marked functions do not return `error`. |
</threat_model>

<verification>
Run:

```bash
go test -run 'Test(ValidateStagedPathsRejectsLossyPromotionBeforeMerge|ValidateStagedPathsRejectsUnsafeIntIntoFloatPath|MixedNumericPathRejectsLossyPromotionLeavesBuilderUsable)$' ./... -count=1
rg -n 'func .*mergeStagedPaths.*error|func .*mergeNumericObservation.*error|func .*promoteNumericPathToFloat.*error' builder.go
```

The `rg` command must return no output.
</verification>

<success_criteria>
ATOMIC-02 is implemented for the current merge-hoisting catalog: validator catches unsupported mixed numeric promotion against real builder state, merge signatures no longer return ordinary user errors, and the marker convention is present for downstream static enforcement.
</success_criteria>

<output>
After completion, create `.planning/phases/16-adddocument-atomicity-lucene-contract/16-01-SUMMARY.md`.
</output>
