---
phase: 16
plan: 03
type: execute
wave: 3
depends_on: [16-01, 16-02, 16-04]
files_modified:
  - atomicity_test.go
autonomous: true
requirements: [ATOMIC-01, ATOMIC-03]
tags: [atomicity, property-testing, gopter, failure-catalog]

must_haves:
  truths:
    - "`atomicity_test.go` exists and uses a bounded gopter budget through `propertyTestParametersWithBudgets(50, 10)` while keeping 1000 attempted documents per generated corpus."
    - "`TestAddDocumentAtomicityEncodeDeterminism` proves the same clean corpus encodes byte-identically across independent builds."
    - "`TestAddDocumentPublicFailuresDoNotSetTragicErr` covers the full Phase 16 public failure-mode catalog and asserts `builder.tragicErr == nil` after each non-tragic error."
    - "`TestAddDocumentAtomicity` builds full-vs-clean corpora of 1000 generated documents with at least 10% guaranteed failing docs and asserts `bytes.Equal` on encoded output."
    - "After Plans 16-01, 16-02, 16-03, and 16-04 complete, `make test`, `make lint`, and `go build ./...` are run as the phase integration gate."
  artifacts:
    - path: "atomicity_test.go"
      provides: "encode determinism sanity, tragic-nil failure catalog, and atomicity property test"
      contains: "func TestAddDocumentAtomicity"
  key_links:
    - from: "atomicity_test.go"
      to: "property_test.go"
      via: "propertyTestParametersWithBudgets normal/short budget"
      pattern: "propertyTestParametersWithBudgets\\(50, 10\\)"
    - from: "atomicity_test.go"
      to: "builder.go AddDocument"
      via: "full corpus and clean subset ingest helpers"
      pattern: "AddDocument\\("
---

<objective>
Prove the Lucene-style per-document contract end to end: non-tragic failed documents do not affect finalized encoded bytes, and the public failure catalog never sets `tragicErr`.

Purpose: satisfy ATOMIC-01 and the public-catalog side of ATOMIC-03 with tests that would fail if failed docs partially mutate builder state.

Output: new `atomicity_test.go` containing deterministic encode sanity, public failure catalog coverage, typed failure-intent generators, and the main gopter atomicity property.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-adddocument-atomicity-lucene-contract/16-CONTEXT.md
@.planning/phases/16-adddocument-atomicity-lucene-contract/16-RESEARCH.md
@.planning/phases/16-adddocument-atomicity-lucene-contract/16-VALIDATION.md
@.planning/phases/16-adddocument-atomicity-lucene-contract/16-PATTERNS.md
@.planning/phases/13-parser-seam-extraction/13-CONTEXT.md
@AGENTS.md
@CLAUDE.md

<interfaces>
Existing property budget helper:

```go
func propertyTestParameters() *gopter.TestParameters
func propertyTestParametersWithBudgets(normal, shortBudget int) *gopter.TestParameters
```

Required test names:

```go
func TestAddDocumentAtomicityEncodeDeterminism(t *testing.T)
func TestAddDocumentPublicFailuresDoNotSetTragicErr(t *testing.T)
func TestAddDocumentAtomicity(t *testing.T)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add encode determinism sanity and shared atomicity helpers</name>
  <files>atomicity_test.go</files>
  <read_first>
    - atomicity_test.go (create)
    - builder.go
    - serialize.go
    - property_test.go
    - generators_test.go
    - .planning/phases/16-adddocument-atomicity-lucene-contract/16-CONTEXT.md D-05
    - .planning/phases/16-adddocument-atomicity-lucene-contract/16-RESEARCH.md sections "Atomicity Property Core Assertion" and "Pitfall 5"
    - .planning/phases/16-adddocument-atomicity-lucene-contract/16-VALIDATION.md rows 16-03-02 and 16-03-03
    - .planning/phases/16-adddocument-atomicity-lucene-contract/16-PATTERNS.md section for `atomicity_test.go`
  </read_first>
  <behavior>
    - Building and encoding the same clean corpus twice returns identical bytes.
    - Helpers return errors instead of calling `t.Fatal` from property callbacks.
    - The test uses `bytes.Equal`, not query-equivalence-only assertions.
  </behavior>
  <action>
    Create `atomicity_test.go` in `package gin` with imports for at least `bytes`, `encoding/json`, `math`, `strconv`, `strings`, `testing`, and gopter packages as needed by later tasks.

    Add:

    - `type atomicityDoc struct { docID DocID; doc []byte; shouldFail bool }`
    - `type atomicityCorpus struct { all []atomicityDoc; cleanOnly []atomicityDoc; failingCount int; numRGs int }`
      - `cleanOnly` must contain the successful `atomicityDoc` entries with their original `docID` values, not compacted loop indexes.
    - `func buildAtomicityIndex(config GINConfig, docs []atomicityDoc, numRGs int) ([]byte, error)`
      - create `NewBuilder(config, numRGs)` after normalizing only impossible capacities such as `< 1`; do not derive capacity from `len(docs)`.
      - call `builder.AddDocument(doc.docID, doc.doc)` for each attempted doc.
      - return an error if `doc.shouldFail == false` returns an error, or if `doc.shouldFail == true` unexpectedly succeeds.
      - call `Finalize()` and `Encode(idx)`
    - `func cleanAtomicityCorpus() []atomicityDoc`
      - return a fixed deterministic corpus containing strings, ints, floats, bools, nulls, arrays, and nested fields
      - include at least one safe numeric path and one strict-transformer-compatible email path
      - assign non-contiguous `docID` values so the determinism test also proves `DocIDMapping` preservation, not just content equality
    - `func TestAddDocumentAtomicityEncodeDeterminism(t *testing.T)`
      - build the same clean corpus twice with the same config
      - pass the same explicit `numRGs` value to both independent builds
      - assert `bytes.Equal(first, second)`
      - on mismatch, fail with `encoded index differs for identical clean corpus`

    Use a config helper that registers a strict companion transformer for `$.email` with alias `strict` and rejects non-string or strings without `@`. The helper must use existing Phase 09 transformer config APIs; do not introduce Phase 17 failure-mode taxonomy.
  </action>
  <verify>
    <automated>go test -run TestAddDocumentAtomicityEncodeDeterminism ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `atomicity_test.go` exists.
    - `atomicity_test.go` contains `func TestAddDocumentAtomicityEncodeDeterminism`.
    - `atomicity_test.go` contains `func buildAtomicityIndex`.
    - `atomicity_test.go` contains `docID DocID`.
    - `atomicity_test.go` contains `cleanOnly []atomicityDoc`.
    - `atomicity_test.go` contains `AddDocument(doc.docID, doc.doc)`.
    - `atomicity_test.go` contains a non-contiguous DocIDMapping assertion or equivalent grep/test text such as `non-contiguous doc ids preserve DocIDMapping`.
    - `atomicity_test.go` contains `bytes.Equal`.
    - `atomicity_test.go` contains `encoded index differs for identical clean corpus`.
    - `go test -run TestAddDocumentAtomicityEncodeDeterminism ./... -count=1` exits 0.
  </acceptance_criteria>
  <done>Encode determinism is pinned before the full atomicity property can use byte-identical output as its oracle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tragic-nil public failure catalog coverage</name>
  <files>atomicity_test.go</files>
  <read_first>
    - atomicity_test.go
    - builder.go
    - parser_test.go
    - transformers_test.go
    - gin_test.go
    - .planning/phases/16-adddocument-atomicity-lucene-contract/16-CONTEXT.md D-03 and D-06
    - .planning/phases/16-adddocument-atomicity-lucene-contract/16-RESEARCH.md sections "Exact Failure Sites" and "Phase Requirements -> Test Map"
    - .planning/phases/16-adddocument-atomicity-lucene-contract/16-VALIDATION.md row 16-03-01
    - .planning/phases/16-adddocument-atomicity-lucene-contract/16-PATTERNS.md sections "Parser Failure Catalog Tests" and "Transformer Failure Tests"
  </read_first>
  <behavior>
    - Parser, stage, transformer, numeric, pre-parser gate, and parser-contract failures return errors without setting `builder.tragicErr`.
    - Each case verifies the builder remains usable or doc bookkeeping is unchanged as appropriate.
    - The catalog does not assert Phase 18 structured error fields.
  </behavior>
  <action>
    Add `func TestAddDocumentPublicFailuresDoNotSetTragicErr(t *testing.T)` to `atomicity_test.go`.

    Cover this D-03 catalog with subtests:

    - parser errors:
      - malformed JSON: `garbage`
      - trailing JSON: `{"a":1} []`
      - non-UTF8/truncated JSON bytes
    - unsupported JSON token type through a package-private test parser that calls `sink.StageScalar(state, "$.bad", complex(1, 2))`
    - hard-mode companion transformer rejection using the strict `$.email` transformer and `{"email":42}`
    - malformed numeric literal through a package-private test parser that calls `sink.StageJSONNumber(state, "$.score", "not-a-number")`
    - non-finite numeric through a package-private test parser that calls `sink.StageNativeNumeric(state, "$.score", math.Inf(1))`
    - validator-rejected numeric promotion:
      - seed `{"score":9007199254740993}`
      - reject `{"score":1.5}`
    - pre-parser gate errors:
      - `position >= numRGs`
      - parser does not call `BeginDocument`
      - parser calls `BeginDocument` twice
      - parser calls `BeginDocument` with the wrong `rgID`
    - unsupported uint overflow through a package-private test parser that calls `sink.StageMaterialized(state, "$.big", uint64(math.MaxUint64), true)`
    - existing unsupported-number regression coverage:
      - add subtest `unsupported-number-without-partial-mutation-regression`
      - mirror `TestAddDocumentRejectsUnsupportedNumberWithoutPartialMutation` with seed `{"name":"stable","score":10}` and rejected doc `{"name":"leak","nested":{"label":"should-not-stick"},"score":9223372036854775808}`
      - assert the rejected doc does not create `$.nested.label`, `builder.numDocs` remains 1, `builder.nextPos` remains 1, and `builder.tragicErr == nil`
      - keep the existing `TestAddDocumentRejectsUnsupportedNumberWithoutPartialMutation` passing unchanged

    For every subtest:

    - assert the operation returns a non-nil error
    - assert `builder.tragicErr == nil`
    - assert `builder.numDocs` and `builder.nextPos` do not advance for the rejected document unless the case intentionally seeded a successful document first
    - where practical, add a subsequent valid document and assert it succeeds
  </action>
  <verify>
    <automated>go test -run 'Test(AddDocumentPublicFailuresDoNotSetTragicErr|AddDocumentRejectsUnsupportedNumberWithoutPartialMutation)$' ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `atomicity_test.go` contains `func TestAddDocumentPublicFailuresDoNotSetTragicErr`.
    - `atomicity_test.go` contains subtest names `malformed-json`, `trailing-json`, `unsupported-token`, `strict-transformer`, `malformed-numeric-literal`, `non-finite-numeric`, `validator-rejected-numeric-promotion`, `position-exceeds-numrgs`, `missing-begin-document`, `double-begin-document`, `wrong-rgid`, `uint64-overflow`, and `unsupported-number-without-partial-mutation-regression`.
    - `atomicity_test.go` contains `builder.tragicErr != nil`.
    - `atomicity_test.go` contains `$.nested.label`.
    - `atomicity_test.go` does not contain `IngestError` or `IngestFailureMode`.
    - `go test -run 'Test(AddDocumentPublicFailuresDoNotSetTragicErr|AddDocumentRejectsUnsupportedNumberWithoutPartialMutation)$' ./... -count=1` exits 0.
  </acceptance_criteria>
  <done>The full public failure catalog is tested as non-tragic and builder-usable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the gopter full-vs-clean AddDocument atomicity property</name>
  <files>atomicity_test.go</files>
  <read_first>
    - atomicity_test.go
    - property_test.go
    - generators_test.go
    - builder.go
    - serialize.go
    - .planning/phases/16-adddocument-atomicity-lucene-contract/16-CONTEXT.md D-05
    - .planning/phases/16-adddocument-atomicity-lucene-contract/16-RESEARCH.md sections "Atomicity Property Core Assertion" and "Test Framework"
    - .planning/phases/16-adddocument-atomicity-lucene-contract/16-VALIDATION.md rows 16-03-02 and 16-03-03
    - .planning/phases/16-adddocument-atomicity-lucene-contract/16-PATTERNS.md sections for property budget and generator conventions
  </read_first>
  <behavior>
    - The generated corpus has exactly 1000 attempted docs per property case.
    - At least 10% of attempted docs are guaranteed failing docs.
    - Full-corpus build ignores expected non-tragic failures, clean-subset build ingests only valid docs, and `Encode` bytes are equal.
    - The property uses `propertyTestParametersWithBudgets(50, 10)`, not `propertyTestParameters()`, because each property iteration ingests roughly 2000 documents plus two `Encode` calls.
  </behavior>
  <action>
    Add these typed failure-intent generators and property:

    - `func genParserMalformedDoc() gopter.Gen`
      - produce raw bytes guaranteed to fail parser.Parse, including malformed JSON, truncated JSON, and trailing-content variants
    - `func genHardTransformerRejectingDoc() gopter.Gen`
      - produce docs such as `{"email":42}` or `{"email":"missing-at"}` that the strict `$.email` transformer rejects
    - `func genNumericPromotionFailingDoc() gopter.Gen`
      - produce an ordered pair or chunk where a clean seed doc with `{"score":9007199254740993}` is followed by a failing `{"score":1.5}` attempt
      - the seed doc must be included in `cleanOnly` with its original `docID`; the failing float doc must be excluded from `cleanOnly`
    - `func genAtomicityCorpus(size int) gopter.Gen`
      - use `gen.SliceOfN(1000, ...)` or `gen.SliceOfN(size, ...)` with `size` passed as `1000`
      - assign stable non-contiguous `docID` values to attempted docs, for example `DocID(i*3 + 1)`, so `cleanOnly` cannot be compacted without changing encoded `DocIDMapping`
      - guarantee `failingCount*10 >= len(corpus.all)` by construction, not probability
      - use deterministic failing slots, for example every index where `i%10 == 9`, so a 1000-document corpus has exactly 100 failing documents before shrinking
      - include parser, transformer, and numeric-promotion failure intents in every generated corpus
      - put one clean unsafe-int seed doc for `$.score` before the first numeric-promotion failing slot, and keep that seed doc in `cleanOnly`
      - set `corpus.numRGs` to the full attempted capacity and use that exact value for both full and clean builds
      - keep `cleanOnly` order identical to the successful-doc order in `all`, preserving each successful entry's original `docID`
    - `func TestAddDocumentAtomicity(t *testing.T)`
      - create `properties := gopter.NewProperties(propertyTestParametersWithBudgets(50, 10))`
      - add a code comment above that line: `Each property iteration ingests roughly 2000 documents plus two Encode calls, so corpus size carries coverage and iteration count stays bounded.`
      - register property name `failed documents do not change encoded index`
      - build `fullBytes` by attempting every `atomicityDoc` in `corpus.all` with `buildAtomicityIndex(config, corpus.all, corpus.numRGs)`, treating errors from `shouldFail == true` as expected and any error from `shouldFail == false` as property failure
      - build `cleanBytes` from `buildAtomicityIndex(config, corpus.cleanOnly, corpus.numRGs)` so both encoded headers use the same `Header.NumRowGroups`
      - assert or comment that both builds use the same `corpus.numRGs` so `Header.NumRowGroups` is equal before the `bytes.Equal` comparison
      - assert `bytes.Equal(fullBytes, cleanBytes)`
      - call `properties.TestingRun(t)`

    The property must not accept query-equivalence-only results; byte-identical `Encode` is required by ATOMIC-01.
  </action>
  <verify>
    <automated>go test -run 'TestAddDocumentAtomicity(EncodeDeterminism)?$' ./... -count=1</automated>
    <automated>make test</automated>
    <automated>make lint</automated>
    <automated>go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `atomicity_test.go` contains `func genParserMalformedDoc`.
    - `atomicity_test.go` contains `func genHardTransformerRejectingDoc`.
    - `atomicity_test.go` contains `func genNumericPromotionFailingDoc`.
    - `atomicity_test.go` contains `func genAtomicityCorpus`.
    - `atomicity_test.go` contains `gen.SliceOfN(1000`.
    - `atomicity_test.go` contains `failingCount*10 >= len(corpus.all)`.
    - `atomicity_test.go` contains `i%10 == 9`.
    - `atomicity_test.go` contains `corpus.numRGs`.
    - `atomicity_test.go` contains `DocIDMapping` or `non-contiguous doc ids preserve DocIDMapping`.
    - `atomicity_test.go` contains `propertyTestParametersWithBudgets(50, 10)`.
    - `atomicity_test.go` contains `Each property iteration ingests roughly 2000 documents plus two Encode calls`.
    - `atomicity_test.go` does not call `propertyTestParameters()` from `TestAddDocumentAtomicity`.
    - `atomicity_test.go` contains `failed documents do not change encoded index`.
    - `atomicity_test.go` contains `bytes.Equal(fullBytes, cleanBytes)`.
    - `go test -run 'TestAddDocumentAtomicity(EncodeDeterminism)?$' ./... -count=1` exits 0.
    - After all four Phase 16 plans have executed, `make test`, `make lint`, and `go build ./...` exit 0.
  </acceptance_criteria>
  <done>The phase has property coverage proving non-tragic failed documents produce byte-identical encoded indexes to a clean-subset build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated corpus -> AddDocument | Property-generated valid and invalid docs exercise parser, transformer, numeric, and pre-parser failures as caller-controlled inputs. |
| Full build -> encoded index oracle | The test compares serialized bytes from full attempted ingest against a clean-subset oracle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T16-01 | Tampering | failed document partial mutation | mitigate | Tasks 1 and 3 assert byte-identical `Encode` between full and clean builds; Task 2 asserts every public user-input failure leaves `tragicErr == nil`. |
| T16-02 | Denial of Service | recovered merge panic | covered | Plan 16-02 implements recovery; this plan verifies ordinary public failures do not use the tragic path. |
| T16-03 | Information Disclosure | failure catalog tests | accept | This plan does not add logging; Plan 16-02 tests recovery logging attrs. |
| T16-04 | Tampering | marker/signature invariant | transfer | Plan 16-04 owns local and CI static enforcement. |
</threat_model>

<verification>
Run:

```bash
go test -run 'Test(AddDocumentAtomicity|AddDocumentPublicFailuresDoNotSetTragicErr)' ./... -count=1
make test
make lint
go build ./...
```
</verification>

<success_criteria>
ATOMIC-01 and the public-catalog portion of ATOMIC-03 are verified: failed documents are skipped without encoded-state differences, all user-input failures stay non-tragic, the existing unsupported-number partial-mutation regression remains covered, and the phase-level `make test` / `make lint` / `go build ./...` integration gate passes after Plans 16-01 through 16-04.
</success_criteria>

<output>
After completion, create `.planning/phases/16-adddocument-atomicity-lucene-contract/16-03-SUMMARY.md`.
</output>
