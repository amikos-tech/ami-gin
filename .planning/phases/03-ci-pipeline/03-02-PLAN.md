---
phase: 03-ci-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/security.yml
  - README.md
autonomous: true
requirements:
  - CI-03
must_haves:
  truths:
    - A weekly and manual GitHub Actions workflow exists for govulncheck SARIF reporting.
    - The README exposes a live CI badge tied to `ci.yml` on the `main` branch.
    - Phase 3 does not add any coverage badge, Codecov dependency, or external coverage service.
  artifacts:
    - `.github/workflows/security.yml` exists with scheduled and manual triggers.
    - `README.md` contains the exact GitHub Actions badge URL for `ci.yml`.
  key_links:
    - `security.yml` runs `golang/govulncheck-action@v1` in `sarif` mode with `repo-checkout: false` and uploads `govulncheck.sarif` through `github/codeql-action/upload-sarif@v4`.
    - `README.md` links the CI badge to `https://github.com/amikos-tech/ami-gin/actions/workflows/ci.yml`.
---

<objective>
Maintain the reporting side of Phase 3: the scheduled SARIF workflow for GitHub Code Scanning and the public CI badge surface in the README.

Purpose: Complete the weekly/manual reporting path for CI-03 and establish the README-visible GitHub Actions signal required by the roadmap, while explicitly removing the stale Codecov branch of the earlier research.
Output: Updated `.github/workflows/security.yml` and updated `README.md` with the CI badge.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-ci-pipeline/03-CONTEXT.md
@.planning/phases/03-ci-pipeline/03-RESEARCH.md
@.planning/phases/03-ci-pipeline/03-REVIEWS.md
@.planning/phases/03-ci-pipeline/03-VALIDATION.md
@README.md
@.github/workflows/security.yml

<interfaces>
Authoritative Phase 3 badge scope:
```text
The only badge required in this phase is the GitHub Actions CI badge for `ci.yml`.
Do not add a coverage badge or Codecov badge in Phase 3.
```

Current `security.yml` state that must be preserved or restored if missing:
```yaml
      - uses: golang/govulncheck-action@v1
        with:
          go-version-input: "1.26"
          go-package: ./...
          repo-checkout: false
          output-format: sarif
          output-file: govulncheck.sarif
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align the scheduled SARIF workflow with the blocking/reporting split</name>
  <files>.github/workflows/security.yml</files>
  <read_first>
- .github/workflows/security.yml
- .planning/ROADMAP.md
- .planning/REQUIREMENTS.md
- .planning/phases/03-ci-pipeline/03-CONTEXT.md
- .planning/phases/03-ci-pipeline/03-REVIEWS.md
- .planning/phases/03-ci-pipeline/03-VALIDATION.md
  </read_first>
  <action>Update `.github/workflows/security.yml` in place so it remains the reporting workflow for CI-03. Keep the exact filename `security.yml`, `name: Security`, triggers `workflow_dispatch` and weekly cron `23 7 * * 1`, top-level `env: { GOTOOLCHAIN: local }`, and workflow-level `permissions: { contents: read }`. Keep one job with the exact job ID `govulncheck-code-scanning`, `runs-on: ubuntu-latest`, and job permissions `contents: read` plus `security-events: write`. Use `actions/checkout@v6`, then `golang/govulncheck-action@v1` with `go-version-input: "1.26"`, `go-package: ./...`, `repo-checkout: false`, `output-format: sarif`, and `output-file: govulncheck.sarif`, then upload the file with `github/codeql-action/upload-sarif@v4` using `sarif_file: govulncheck.sarif` and `category: govulncheck`. Addresses review concern: explicitly keep `repo-checkout: false` in the plan text anywhere `golang/govulncheck-action@v1` is specified. Do not move this SARIF path into `ci.yml` and do not reintroduce Codecov or any external coverage upload.</action>
  <acceptance_criteria>
- `test -f .github/workflows/security.yml` succeeds.
- `rg -n '^name: Security$' .github/workflows/security.yml` returns the workflow name.
- `rg -n 'workflow_dispatch:' .github/workflows/security.yml` returns the manual trigger.
- `rg -n 'cron: "23 7 \\* \\* 1"' .github/workflows/security.yml` returns the weekly schedule.
- `rg -n '^env:$' .github/workflows/security.yml` returns the top-level environment block.
- `rg -n 'GOTOOLCHAIN: local' .github/workflows/security.yml` returns the local toolchain pin.
- `rg -n 'security-events: write' .github/workflows/security.yml` returns the elevated SARIF upload permission.
- `rg -n 'golang/govulncheck-action@v1' .github/workflows/security.yml` returns the govulncheck action pin.
- `rg -n 'repo-checkout: false' .github/workflows/security.yml` returns the explicit checkout override.
- `rg -n 'output-format: sarif' .github/workflows/security.yml` returns the SARIF output mode.
- `rg -n 'output-file: govulncheck\\.sarif' .github/workflows/security.yml` returns the SARIF output filename.
- `rg -n 'github/codeql-action/upload-sarif@v4' .github/workflows/security.yml` returns the SARIF upload action pin.
- `rg -n 'category: govulncheck' .github/workflows/security.yml` returns the SARIF category.
- `! rg -n 'codecov|Codecov' .github/workflows/security.yml` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
actionlint .github/workflows/security.yml
rg -n '^name: Security$' .github/workflows/security.yml
rg -n 'workflow_dispatch:' .github/workflows/security.yml
rg -n 'cron: "23 7 \* \* 1"' .github/workflows/security.yml
rg -n '^env:$' .github/workflows/security.yml
rg -n 'GOTOOLCHAIN: local' .github/workflows/security.yml
rg -n 'security-events: write' .github/workflows/security.yml
rg -n 'golang/govulncheck-action@v1' .github/workflows/security.yml
rg -n 'repo-checkout: false' .github/workflows/security.yml
rg -n 'output-format: sarif' .github/workflows/security.yml
rg -n 'output-file: govulncheck\.sarif' .github/workflows/security.yml
rg -n 'github/codeql-action/upload-sarif@v4' .github/workflows/security.yml
rg -n 'category: govulncheck' .github/workflows/security.yml
! rg -n 'codecov|Codecov' .github/workflows/security.yml</automated>
  </verify>
  <done>The repository contains a separate weekly/manual SARIF workflow that is ready to publish govulncheck results into GitHub Code Scanning once the repository-level prerequisite is enabled, and the action configuration is explicit about `repo-checkout: false`.</done>
</task>

<task type="auto">
  <name>Task 2: Keep README focused on the GitHub Actions CI badge only</name>
  <files>README.md</files>
  <read_first>
- README.md
- .planning/ROADMAP.md
- .planning/phases/03-ci-pipeline/03-CONTEXT.md
- .planning/phases/03-ci-pipeline/03-REVIEWS.md
  </read_first>
  <action>Update `README.md` so the exact CI badge markdown line `[![CI](https://github.com/amikos-tech/ami-gin/actions/workflows/ci.yml/badge.svg?branch=main&event=push)](https://github.com/amikos-tech/ami-gin/actions/workflows/ci.yml)` appears immediately below the `# GIN Index` title. Keep only that badge in Phase 3. Addresses review concern: remove the stale plan text that implied plan 03 would later finalize a Codecov or coverage badge. Do not add `codecov.io`, `app.codecov.io`, or any coverage badge URL; the only badge required in this phase is the GitHub Actions CI badge.</action>
  <acceptance_criteria>
- `rg -n '^# GIN Index$' README.md` returns the title line.
- `rg -n '^\\[!\\[CI\\]\\(https://github\\.com/amikos-tech/ami-gin/actions/workflows/ci\\.yml/badge\\.svg\\?branch=main&event=push\\)\\]\\(https://github\\.com/amikos-tech/ami-gin/actions/workflows/ci\\.yml\\)$' README.md` returns exactly one line.
- `! rg -n 'codecov\\.io|app\\.codecov\\.io|Coverage Badge|Codecov' README.md` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>rg -n '^\\[!\\[CI\\]\\(https://github\\.com/amikos-tech/ami-gin/actions/workflows/ci\\.yml/badge\\.svg\\?branch=main&event=push\\)\\]\\(https://github\\.com/amikos-tech/ami-gin/actions/workflows/ci\\.yml\\)$' README.md
! rg -n 'codecov\\.io|app\\.codecov\\.io|Coverage Badge|Codecov' README.md</automated>
  </verify>
  <done>The README exposes a stable GitHub Actions CI badge directly under the repository title and does not advertise any Codecov or coverage badge work in Phase 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scheduled GitHub workflow → GitHub Code Scanning | Security results cross from a hosted runner into GitHub's security ingestion service. |
| README consumer → badge endpoints | Repository visitors load badge state from GitHub-hosted endpoints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | E | `.github/workflows/security.yml` | mitigate | Grant `security-events: write` only to the SARIF upload job and keep workflow-level permissions at `contents: read`. |
| T-03-06 | R | `.github/workflows/security.yml` | mitigate | Keep SARIF reporting in a separate workflow from the blocking `text`-mode `govulncheck` gate so scheduled reporting cannot silently replace merge enforcement. |
| T-03-07 | I | `README.md` badge links | mitigate | Add only the GitHub-hosted CI badge in Phase 3 and explicitly avoid Codecov or coverage badge URLs. |
</threat_model>

<verification>
1. Workflow structure:
   `actionlint .github/workflows/security.yml`
   `rg -n 'repo-checkout: false|output-format: sarif|upload-sarif@v4|category: govulncheck' .github/workflows/security.yml`
2. Badge surface:
   `rg -n '^\\[!\\[CI\\]\\(https://github\\.com/amikos-tech/ami-gin/actions/workflows/ci\\.yml/badge\\.svg\\?branch=main&event=push\\)\\]\\(https://github\\.com/amikos-tech/ami-gin/actions/workflows/ci\\.yml\\)$' README.md`
   `! rg -n 'codecov\\.io|app\\.codecov\\.io|Coverage Badge|Codecov' README.md`
</verification>

<success_criteria>
The repo has a dedicated weekly/manual SARIF workflow ready for GitHub Code Scanning and the README shows only the GitHub Actions CI badge required for Phase 3.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ci-pipeline/03-ci-pipeline-02-SUMMARY.md`
</output>
