---
phase: 03-ci-pipeline
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - .planning/phases/03-ci-pipeline/ruleset-14266305-before.json
  - .planning/phases/03-ci-pipeline/ruleset-14266305-merged.json
autonomous: false
requirements:
  - CI-01
  - CI-03
  - CI-04
user_setup:
  - service: github
    why: "The private repository currently has Code Security disabled, which blocks SARIF ingestion for CI-03."
    dashboard_config:
      - task: "Either enable GitHub Code Security for amikos-tech/ami-gin or change repo visibility to public before final Phase 3 verification."
        location: "GitHub repository settings for amikos-tech/ami-gin"
must_haves:
  truths:
    - A pull request against `main` produces the exact CI contexts `test (1.25)`, `test (1.26)`, `lint`, `build`, and `govulncheck` before the `Main` ruleset is tightened.
    - Pull-request and push runs publish the GitHub-native artifacts `test-artifacts-go1.25` and `test-artifacts-go1.26`.
    - After the PR is merged to `main`, the default-branch `Security` workflow can be manually dispatched and completes successfully.
    - The active `Main` ruleset preserves `deletion`, `non_fast_forward`, `required_signatures`, `pull_request`, and `required_linear_history`, and adds required status checks for only the exact CI contexts.
    - Branch creation is not blocked by the ruleset change because `do_not_enforce_on_create` stays `true`; merge/push protection on `main` still applies.
    - The current ruleset can be restored from a checked-in backup JSON file if the mutation must be rolled back.
  artifacts:
    - `.planning/phases/03-ci-pipeline/ruleset-14266305-before.json` contains the exported pre-mutation `Main` ruleset.
    - `.planning/phases/03-ci-pipeline/ruleset-14266305-merged.json` contains the validated update payload used for the PUT call.
    - GitHub Actions PR and push runs contain gotestsum artifacts for both matrix legs.
    - GitHub ruleset `14266305` includes a `required_status_checks` rule after verification.
  key_links:
    - Task 2 pushes the working branch and creates or updates a pull request to `main` so `ci.yml` emits the exact contexts before ruleset `14266305` is updated.
    - Task 3 exports the current ruleset, derives a merged payload with `jq`, validates the payload shape, and only then applies the PUT request.
    - Task 3 verifies `do_not_enforce_on_create: true` explicitly so the ruleset does not block branch creation while still protecting merges to `main`.
---

<objective>
Resolve the repo-external prerequisites and GitHub-configuration work that make Phase 3 enforceable, while keeping ruleset mutation reversible and safe.

Purpose: Finish CI-01, CI-03, and CI-04 by proving the live PR/default-branch workflow behavior, unblocking GitHub Code Scanning for this repo, and updating the existing `Main` ruleset only after a backup and payload-shape validation succeed.
Output: Verified PR/default-branch workflow timing, GitHub-native artifacts, GitHub Code Scanning readiness, and a safely updated GitHub ruleset with rollback artifacts.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-ci-pipeline/03-CONTEXT.md
@.planning/phases/03-ci-pipeline/03-RESEARCH.md
@.planning/phases/03-ci-pipeline/03-REVIEWS.md
@.planning/phases/03-ci-pipeline/03-VALIDATION.md
@.github/workflows/ci.yml
@.github/workflows/security.yml

<interfaces>
Current live GitHub state verified during planning:
```json
{
  "private": true,
  "code_security": "disabled",
  "ruleset_id": 14266305,
  "ruleset_name": "Main",
  "rules": [
    "deletion",
    "non_fast_forward",
    "required_signatures",
    "pull_request",
    "required_linear_history"
  ]
}
```

Required check contexts from `ci.yml`:
```text
test (1.25)
test (1.26)
lint
build
govulncheck
```

Authoritative CI-04 for this plan:
```text
`coverage.out` and `unit.xml` are GitHub Actions artifacts; coverage is summarized in GitHub job output via `$GITHUB_STEP_SUMMARY`.
Do not add or require Codecov anywhere in this plan.
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Resolve the GitHub Code Security prerequisite</name>
  <files>NONE — GitHub repository settings outside the working tree</files>
  <read_first>
- .planning/PROJECT.md
- .planning/REQUIREMENTS.md
- .planning/phases/03-ci-pipeline/03-RESEARCH.md
- .planning/phases/03-ci-pipeline/03-REVIEWS.md
- .planning/phases/03-ci-pipeline/03-VALIDATION.md
- .github/workflows/security.yml
  </read_first>
  <action>Because `gh api repos/amikos-tech/ami-gin --jq '.private, .security_and_analysis.code_security.status'` currently returns `true` and `disabled`, and `gh api repos/amikos-tech/ami-gin/code-scanning/default-setup` returns the exact 403 `Code Security must be enabled for this repository to use code scanning`, complete one of these setup paths before resuming. Path A: keep the repo private and enable GitHub Code Security for `amikos-tech/ami-gin`. Path B: make the repo public only after explicitly confirming the pre-public `.planning/` scrub / FOUND-04 review is already complete. Do not continue until one path is complete.</action>
  <acceptance_criteria>
- Private path: `test "$(gh api repos/amikos-tech/ami-gin --jq '.private')" = "true"` succeeds.
- Private path: `test "$(gh api repos/amikos-tech/ami-gin --jq '.security_and_analysis.code_security.status')" = "enabled"` succeeds.
- Public path: `test "$(gh api repos/amikos-tech/ami-gin --jq '.private')" = "false"` succeeds.
- Resume signal must be exactly `private-security-ready` or `public-ready FOUND-04-reviewed`.
  </acceptance_criteria>
  <verify>
    <automated>gh api repos/amikos-tech/ami-gin --jq '.private, .security_and_analysis.code_security.status'</automated>
  </verify>
  <done>The repo is either public or has GitHub Code Security enabled, so SARIF ingestion and default-branch security verification are no longer blocked by repository settings.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the pull-request CI contexts and GitHub-native artifacts on a live run</name>
  <files>NONE — verifies workflow output on the live pull-request run</files>
  <read_first>
- .github/workflows/ci.yml
- .github/workflows/security.yml
- .planning/ROADMAP.md
- .planning/REQUIREMENTS.md
- .planning/phases/03-ci-pipeline/03-CONTEXT.md
- .planning/phases/03-ci-pipeline/03-REVIEWS.md
- .planning/phases/03-ci-pipeline/03-VALIDATION.md
  </read_first>
  <action>Push the working branch and create or update a pull request targeting `main`. If you open a new PR in this task, use the title `feat(ci): add GitHub Actions CI pipeline` and a body that lists `ci.yml`, `security.yml`, GitHub-native artifacts, and later ruleset enforcement as the scope. Wait for the `CI` workflow triggered by `pull_request` to complete successfully on that PR head SHA. Verify that the PR run exposes the exact check contexts `test (1.25)`, `test (1.26)`, `lint`, `build`, and `govulncheck`; those are the only valid contexts for the later ruleset update. In the same PR run, verify that the artifacts `test-artifacts-go1.25` and `test-artifacts-go1.26` exist. Do not attempt `gh workflow run security.yml --ref <branch>` here; `workflow_dispatch` verification remains deferred to Task 3 after the workflow file is present on the default branch.</action>
  <acceptance_criteria>
- `test "$(gh pr view --json baseRefName -q .baseRefName)" = "main"` succeeds.
- `test "$(gh pr view --json state -q .state)" = "OPEN"` succeeds.
- `test "$(gh run list --workflow ci.yml --event pull_request --branch $(git branch --show-current) --limit 1 --json workflowName -q '.[0].workflowName')" = "CI"` succeeds.
- `test "$(gh run list --workflow ci.yml --event pull_request --branch $(git branch --show-current) --limit 1 --json status -q '.[0].status')" = "completed"` succeeds.
- `test "$(gh run list --workflow ci.yml --event pull_request --branch $(git branch --show-current) --limit 1 --json conclusion -q '.[0].conclusion')" = "success"` succeeds.
- `gh api repos/amikos-tech/ami-gin/commits/$(gh pr view --json headRefOid -q .headRefOid)/check-runs | rg '"name":"test \\(1\\.25\\)"'` returns one line.
- `gh api repos/amikos-tech/ami-gin/commits/$(gh pr view --json headRefOid -q .headRefOid)/check-runs | rg '"name":"test \\(1\\.26\\)"'` returns one line.
- `gh api repos/amikos-tech/ami-gin/commits/$(gh pr view --json headRefOid -q .headRefOid)/check-runs | rg '"name":"lint"'` returns one line.
- `gh api repos/amikos-tech/ami-gin/commits/$(gh pr view --json headRefOid -q .headRefOid)/check-runs | rg '"name":"build"'` returns one line.
- `gh api repos/amikos-tech/ami-gin/commits/$(gh pr view --json headRefOid -q .headRefOid)/check-runs | rg '"name":"govulncheck"'` returns one line.
- `gh api repos/amikos-tech/ami-gin/actions/runs/$(gh run list --workflow ci.yml --event pull_request --branch $(git branch --show-current) --limit 1 --json databaseId --jq '.[0].databaseId')/artifacts | rg '"name":"test-artifacts-go1\\.25"'` returns the Go 1.25 artifact name.
- `gh api repos/amikos-tech/ami-gin/actions/runs/$(gh run list --workflow ci.yml --event pull_request --branch $(git branch --show-current) --limit 1 --json databaseId --jq '.[0].databaseId')/artifacts | rg '"name":"test-artifacts-go1\\.26"'` returns the Go 1.26 artifact name.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
BRANCH=$(git branch --show-current)
PR_HEAD=$(gh pr view --json headRefOid -q .headRefOid)
PR_RUN=$(gh run list --workflow ci.yml --event pull_request --branch "$BRANCH" --limit 1 --json databaseId --jq '.[0].databaseId')
PR_CHECKS=$(gh api repos/amikos-tech/ami-gin/commits/"$PR_HEAD"/check-runs)
PR_ARTIFACTS=$(gh api repos/amikos-tech/ami-gin/actions/runs/"$PR_RUN"/artifacts)

echo 'Stage 1: PR state'
test "$(gh pr view --json baseRefName -q .baseRefName)" = "main"
test "$(gh pr view --json state -q .state)" = "OPEN"
test "$(gh run list --workflow ci.yml --event pull_request --branch "$BRANCH" --limit 1 --json workflowName -q '.[0].workflowName')" = "CI"
test "$(gh run list --workflow ci.yml --event pull_request --branch "$BRANCH" --limit 1 --json status -q '.[0].status')" = "completed"
test "$(gh run list --workflow ci.yml --event pull_request --branch "$BRANCH" --limit 1 --json conclusion -q '.[0].conclusion')" = "success"

echo 'Stage 2: required contexts'
printf '%s\n' "$PR_CHECKS" | rg '"name":"test \\(1\\.25\\)"'
printf '%s\n' "$PR_CHECKS" | rg '"name":"test \\(1\\.26\\)"'
printf '%s\n' "$PR_CHECKS" | rg '"name":"lint"'
printf '%s\n' "$PR_CHECKS" | rg '"name":"build"'
printf '%s\n' "$PR_CHECKS" | rg '"name":"govulncheck"'

echo 'Stage 3: artifacts'
printf '%s\n' "$PR_ARTIFACTS" | rg '"name":"test-artifacts-go1\\.25"'
printf '%s\n' "$PR_ARTIFACTS" | rg '"name":"test-artifacts-go1\\.26"'</automated>
  </verify>
  <done>A real `pull_request` run exists with the exact check names needed for ruleset enforcement, and the live PR run publishes the expected GitHub-native artifacts for both matrix legs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Merge to `main`, verify default-branch security, and mutate the ruleset safely</name>
  <files>.planning/phases/03-ci-pipeline/ruleset-14266305-before.json, .planning/phases/03-ci-pipeline/ruleset-14266305-merged.json</files>
  <read_first>
- .github/workflows/ci.yml
- .github/workflows/security.yml
- .planning/phases/03-ci-pipeline/03-REVIEWS.md
- .planning/phases/03-ci-pipeline/03-VALIDATION.md
  </read_first>
  <action>Addresses review concerns: safe ruleset mutation, explicit backup/export, payload-shape validation before PUT, grouped verification, and `do_not_enforce_on_create` rationale. After Task 2 produces a green PR, have the user review and merge that PR into `main`, then resume with the exact signal `merged-to-main`. First, wait for the merge-triggered `CI` run on `main` to complete successfully and verify it is a real `push` event on `main`; verify the artifacts `test-artifacts-go1.25` and `test-artifacts-go1.26` exist on that run. Then trigger `security.yml` with `gh workflow run security.yml --ref main`, wait for the latest `Security` run on `main` to complete successfully through a real `workflow_dispatch` event, and confirm a visible `govulncheck` Code Scanning analysis for `refs/heads/main`.

Only after the main-branch workflow checks are green should you mutate the ruleset. Export the current `Main` ruleset to `.planning/phases/03-ci-pipeline/ruleset-14266305-before.json` with `gh api repos/amikos-tech/ami-gin/rulesets/14266305 > .planning/phases/03-ci-pipeline/ruleset-14266305-before.json`. Build `.planning/phases/03-ci-pipeline/ruleset-14266305-merged.json` from that backup with this exact `jq` transform so the existing protections survive and any pre-existing `required_status_checks` rule is replaced instead of duplicated:

`jq '{name, target, enforcement, conditions, bypass_actors, rules: ((.rules | map(select(.type != "required_status_checks"))) + [{type:"required_status_checks", parameters:{strict_required_status_checks_policy:true, do_not_enforce_on_create:true, required_status_checks:[{context:"test (1.25)"},{context:"test (1.26)"},{context:"lint"},{context:"build"},{context:"govulncheck"}]}}])}' .planning/phases/03-ci-pipeline/ruleset-14266305-before.json > .planning/phases/03-ci-pipeline/ruleset-14266305-merged.json`

Validate the merged payload shape before applying it: run `jq -e '(.name == "Main") and (.target == "branch") and (.enforcement == "active") and any(.rules[]; .type == "deletion") and any(.rules[]; .type == "non_fast_forward") and any(.rules[]; .type == "required_signatures") and any(.rules[]; .type == "pull_request") and any(.rules[]; .type == "required_linear_history") and any(.rules[]; .type == "required_status_checks" and .parameters.strict_required_status_checks_policy == true and .parameters.do_not_enforce_on_create == true and ([.parameters.required_status_checks[].context] | sort == ["build","govulncheck","lint","test (1.25)","test (1.26)"]))' .planning/phases/03-ci-pipeline/ruleset-14266305-merged.json >/dev/null`. Keep `do_not_enforce_on_create: true` intentionally, because the phase goal is to block merges and protected-branch updates, not to block new branch creation. Only after that validation passes should you call `gh api --method PUT repos/amikos-tech/ami-gin/rulesets/14266305 --input .planning/phases/03-ci-pipeline/ruleset-14266305-merged.json`.

If the PR or `main` `govulncheck` run fails because of a newly disclosed upstream advisory with no fix available yet, stop before applying the ruleset change or restore the pre-mutation state from `.planning/phases/03-ci-pipeline/ruleset-14266305-before.json`. Record the advisory IDs in the PR or summary and escalate to the user for a decision on whether the phase should pause until the upstream fix or a dependency replacement lands. Do not remove `govulncheck` from the required checks; D-04 and D-05 require it to stay merge-blocking.</action>
  <acceptance_criteria>
- Resume signal must be exactly `merged-to-main`.
- `test "$(gh run list --workflow ci.yml --branch main --limit 1 --json workflowName -q '.[0].workflowName')" = "CI"` succeeds.
- `test "$(gh run list --workflow ci.yml --branch main --limit 1 --json event -q '.[0].event')" = "push"` succeeds.
- `test "$(gh run list --workflow ci.yml --branch main --limit 1 --json headBranch -q '.[0].headBranch')" = "main"` succeeds.
- `test "$(gh run list --workflow ci.yml --branch main --limit 1 --json conclusion -q '.[0].conclusion')" = "success"` succeeds.
- `gh api repos/amikos-tech/ami-gin/actions/runs/$(gh run list --workflow ci.yml --branch main --limit 1 --json databaseId --jq '.[0].databaseId')/artifacts | rg '"name":"test-artifacts-go1\\.25"'` returns the Go 1.25 artifact name.
- `gh api repos/amikos-tech/ami-gin/actions/runs/$(gh run list --workflow ci.yml --branch main --limit 1 --json databaseId --jq '.[0].databaseId')/artifacts | rg '"name":"test-artifacts-go1\\.26"'` returns the Go 1.26 artifact name.
- `test "$(gh run list --workflow security.yml --branch main --limit 1 --json workflowName -q '.[0].workflowName')" = "Security"` succeeds.
- `test "$(gh run list --workflow security.yml --branch main --limit 1 --json event -q '.[0].event')" = "workflow_dispatch"` succeeds.
- `test "$(gh run list --workflow security.yml --branch main --limit 1 --json headBranch -q '.[0].headBranch')" = "main"` succeeds.
- `test "$(gh run list --workflow security.yml --branch main --limit 1 --json conclusion -q '.[0].conclusion')" = "success"` succeeds.
- `test -f .planning/phases/03-ci-pipeline/ruleset-14266305-before.json` succeeds.
- `test -f .planning/phases/03-ci-pipeline/ruleset-14266305-merged.json` succeeds.
- `jq -e '(.id == 14266305) and (.name == "Main") and any(.rules[]; .type == "deletion") and any(.rules[]; .type == "non_fast_forward") and any(.rules[]; .type == "required_signatures") and any(.rules[]; .type == "pull_request") and any(.rules[]; .type == "required_linear_history")' .planning/phases/03-ci-pipeline/ruleset-14266305-before.json >/dev/null` succeeds.
- `jq -e '(.name == "Main") and (.target == "branch") and (.enforcement == "active") and any(.rules[]; .type == "required_status_checks" and .parameters.strict_required_status_checks_policy == true and .parameters.do_not_enforce_on_create == true and ([.parameters.required_status_checks[].context] | sort == ["build","govulncheck","lint","test (1.25)","test (1.26)"]))' .planning/phases/03-ci-pipeline/ruleset-14266305-merged.json >/dev/null` succeeds.
- `gh api repos/amikos-tech/ami-gin/code-scanning/analyses --paginate --jq '.[] | select(.ref == "refs/heads/main") | .ref' | rg '^refs/heads/main$'` returns the default-branch Code Scanning ref.
- `gh api repos/amikos-tech/ami-gin/code-scanning/analyses --paginate --jq '.[] | select(.ref == "refs/heads/main") | (.tool.name // .tool.driver.name // "")' | rg '^govulncheck$'` returns the main-branch govulncheck analysis tool name.
- `gh api repos/amikos-tech/ami-gin/rulesets/14266305 | rg '"type":"required_status_checks"'` returns one line.
- `gh api repos/amikos-tech/ami-gin/rulesets/14266305 | rg '"strict_required_status_checks_policy":true'` returns one line.
- `gh api repos/amikos-tech/ami-gin/rulesets/14266305 | rg '"do_not_enforce_on_create":true'` returns one line.
- `gh api repos/amikos-tech/ami-gin/rulesets/14266305 | rg '"context":"test \\(1\\.25\\)"'` returns one line.
- `gh api repos/amikos-tech/ami-gin/rulesets/14266305 | rg '"context":"test \\(1\\.26\\)"'` returns one line.
- `gh api repos/amikos-tech/ami-gin/rulesets/14266305 | rg '"context":"lint"'` returns one line.
- `gh api repos/amikos-tech/ami-gin/rulesets/14266305 | rg '"context":"build"'` returns one line.
- `gh api repos/amikos-tech/ami-gin/rulesets/14266305 | rg '"context":"govulncheck"'` returns one line.
- `! gh api repos/amikos-tech/ami-gin/rulesets/14266305 | rg '"context":"[^"]*[Cc]odecov'` succeeds.
- `gh api repos/amikos-tech/ami-gin/rulesets/14266305 | rg '"type":"deletion"'` returns one line.
- `gh api repos/amikos-tech/ami-gin/rulesets/14266305 | rg '"type":"non_fast_forward"'` returns one line.
- `gh api repos/amikos-tech/ami-gin/rulesets/14266305 | rg '"type":"required_signatures"'` returns one line.
- `gh api repos/amikos-tech/ami-gin/rulesets/14266305 | rg '"type":"pull_request"'` returns one line.
- `gh api repos/amikos-tech/ami-gin/rulesets/14266305 | rg '"type":"required_linear_history"'` returns one line.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
MAIN_RUN=$(gh run list --workflow ci.yml --branch main --limit 1 --json databaseId --jq '.[0].databaseId')
MAIN_ARTIFACTS=$(gh api repos/amikos-tech/ami-gin/actions/runs/"$MAIN_RUN"/artifacts)

echo 'Stage 1: main-branch CI run'
test "$(gh run list --workflow ci.yml --branch main --limit 1 --json workflowName -q '.[0].workflowName')" = "CI"
test "$(gh run list --workflow ci.yml --branch main --limit 1 --json event -q '.[0].event')" = "push"
test "$(gh run list --workflow ci.yml --branch main --limit 1 --json headBranch -q '.[0].headBranch')" = "main"
test "$(gh run list --workflow ci.yml --branch main --limit 1 --json conclusion -q '.[0].conclusion')" = "success"
printf '%s\n' "$MAIN_ARTIFACTS" | rg '"name":"test-artifacts-go1\\.25"'
printf '%s\n' "$MAIN_ARTIFACTS" | rg '"name":"test-artifacts-go1\\.26"'

echo 'Stage 2: default-branch Security run'
test "$(gh run list --workflow security.yml --branch main --limit 1 --json workflowName -q '.[0].workflowName')" = "Security"
test "$(gh run list --workflow security.yml --branch main --limit 1 --json event -q '.[0].event')" = "workflow_dispatch"
test "$(gh run list --workflow security.yml --branch main --limit 1 --json headBranch -q '.[0].headBranch')" = "main"
test "$(gh run list --workflow security.yml --branch main --limit 1 --json conclusion -q '.[0].conclusion')" = "success"
gh api repos/amikos-tech/ami-gin/code-scanning/analyses --paginate --jq '.[] | select(.ref == "refs/heads/main") | .ref' | rg '^refs/heads/main$'
gh api repos/amikos-tech/ami-gin/code-scanning/analyses --paginate --jq '.[] | select(.ref == "refs/heads/main") | (.tool.name // .tool.driver.name // "")' | rg '^govulncheck$'

echo 'Stage 3: backup and merged payload'
test -f .planning/phases/03-ci-pipeline/ruleset-14266305-before.json
test -f .planning/phases/03-ci-pipeline/ruleset-14266305-merged.json
jq -e '(.id == 14266305) and (.name == "Main") and any(.rules[]; .type == "deletion") and any(.rules[]; .type == "non_fast_forward") and any(.rules[]; .type == "required_signatures") and any(.rules[]; .type == "pull_request") and any(.rules[]; .type == "required_linear_history")' .planning/phases/03-ci-pipeline/ruleset-14266305-before.json >/dev/null
jq -e '(.name == "Main") and (.target == "branch") and (.enforcement == "active") and any(.rules[]; .type == "required_status_checks" and .parameters.strict_required_status_checks_policy == true and .parameters.do_not_enforce_on_create == true and ([.parameters.required_status_checks[].context] | sort == ["build","govulncheck","lint","test (1.25)","test (1.26)"]))' .planning/phases/03-ci-pipeline/ruleset-14266305-merged.json >/dev/null

echo 'Stage 4: live ruleset'
gh api repos/amikos-tech/ami-gin/rulesets/14266305 | rg '"type":"required_status_checks"'
gh api repos/amikos-tech/ami-gin/rulesets/14266305 | rg '"strict_required_status_checks_policy":true'
gh api repos/amikos-tech/ami-gin/rulesets/14266305 | rg '"do_not_enforce_on_create":true'
gh api repos/amikos-tech/ami-gin/rulesets/14266305 | rg '"context":"test \\(1\\.25\\)"'
gh api repos/amikos-tech/ami-gin/rulesets/14266305 | rg '"context":"test \\(1\\.26\\)"'
gh api repos/amikos-tech/ami-gin/rulesets/14266305 | rg '"context":"lint"'
gh api repos/amikos-tech/ami-gin/rulesets/14266305 | rg '"context":"build"'
gh api repos/amikos-tech/ami-gin/rulesets/14266305 | rg '"context":"govulncheck"'
! gh api repos/amikos-tech/ami-gin/rulesets/14266305 | rg '"context":"[^"]*[Cc]odecov'
gh api repos/amikos-tech/ami-gin/rulesets/14266305 | rg '"type":"deletion"'
gh api repos/amikos-tech/ami-gin/rulesets/14266305 | rg '"type":"non_fast_forward"'
gh api repos/amikos-tech/ami-gin/rulesets/14266305 | rg '"type":"required_signatures"'
gh api repos/amikos-tech/ami-gin/rulesets/14266305 | rg '"type":"pull_request"'
gh api repos/amikos-tech/ami-gin/rulesets/14266305 | rg '"type":"required_linear_history"'</automated>
  </verify>
  <done>The default-branch `Security` workflow is verified through a real `workflow_dispatch` run on `main`, the existing `Main` ruleset is updated from a validated payload with a restorable backup, existing protections are preserved, and merge enforcement covers only the exact CI contexts without blocking branch creation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local execution branch → GitHub repository settings | The executor mutates live branch-protection rules through authenticated APIs. |
| GitHub Actions runner → GitHub Code Scanning | SARIF uploads depend on repository-level security availability outside the working tree. |
| GitHub Actions runner → GitHub Actions artifacts and summaries | gotestsum outputs and coverage summaries are stored and rendered through GitHub-managed services. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | T | GitHub ruleset `14266305` | mitigate | Block the PUT call until the current ruleset is exported to `.planning/phases/03-ci-pipeline/ruleset-14266305-before.json` and the merged payload passes the explicit `jq` shape validation that preserves existing protections. |
| T-03-09 | I | GitHub-native test artifacts and summaries | mitigate | Keep CI-04 inside GitHub artifacts and `$GITHUB_STEP_SUMMARY`; verify the artifact names from both PR and push runs before claiming completion. |
| T-03-10 | D | GitHub Code Scanning integration | mitigate | Block progress on Task 1 until the current Code Security 403 is resolved so SARIF uploads cannot silently fail. |
| T-03-11 | R | Required status check set | mitigate | Verify PR-proven context names before updating ruleset `14266305`, keep `do_not_enforce_on_create: true`, and limit the required-check list to `test (1.25)`, `test (1.26)`, `lint`, `build`, and `govulncheck`. |
</threat_model>

<verification>
1. Repo prerequisite:
   `gh api repos/amikos-tech/ami-gin --jq '.private, .security_and_analysis.code_security.status'`
2. Pull-request evidence:
   `gh pr view --json baseRefName,state,headRefOid`
   `gh run list --workflow ci.yml --event pull_request --branch $(git branch --show-current) --limit 1 --json workflowName,status,conclusion,databaseId`
3. Main-branch evidence:
   `gh run list --workflow ci.yml --branch main --limit 1 --json workflowName,event,headBranch,conclusion,databaseId`
   `gh run list --workflow security.yml --branch main --limit 1 --json workflowName,event,headBranch,conclusion`
   `gh api repos/amikos-tech/ami-gin/code-scanning/analyses --paginate`
4. Ruleset safety:
   `test -f .planning/phases/03-ci-pipeline/ruleset-14266305-before.json`
   `test -f .planning/phases/03-ci-pipeline/ruleset-14266305-merged.json`
   `jq -e '(.name == "Main") and (.target == "branch")' .planning/phases/03-ci-pipeline/ruleset-14266305-merged.json >/dev/null`
   `gh api repos/amikos-tech/ami-gin/rulesets/14266305`
</verification>

<success_criteria>
Phase 3 is externally enforceable: GitHub can ingest SARIF results for the repo’s visibility/security mode, a real PR has emitted the exact CI contexts and GitHub-native artifacts before ruleset enforcement, and the active `Main` ruleset requires those exact checks after a validated, reversible update.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ci-pipeline/03-ci-pipeline-03-SUMMARY.md`
</output>
