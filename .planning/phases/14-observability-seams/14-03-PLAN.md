---
phase: 14
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - parquet.go
  - s3.go
  - serialize.go
  - boundary_observability_test.go
autonomous: true
requirements: [OBS-05, OBS-06, OBS-07]
tags: [observability, parquet, serialization, context, s3, go]

must_haves:
  truths:
    - "`BuildFromParquetContext` exists as an additive sibling and `BuildFromParquet` delegates with `context.Background()`."
    - "Raw serialization exposes `EncodeContext`, `EncodeWithLevelContext`, and `DecodeContext`, while the old `Encode`, `EncodeWithLevel`, and `Decode` functions remain compatibility wrappers."
    - "Parquet/build and serialization instrumentation stays boundary-only; there are no page/row/value-loop spans."
    - "S3-backed build paths compose caller context with existing timeout wrappers instead of resetting everything to `context.Background()`."
  artifacts:
    - path: "parquet.go"
      provides: "context-aware parquet build boundary"
      contains: "func BuildFromParquetContext("
    - path: "serialize.go"
      provides: "context-aware raw serialization boundaries"
      contains: "func EncodeContext("
    - path: "serialize.go"
      provides: "context-aware configurable-compression boundary"
      contains: "func EncodeWithLevelContext("
    - path: "boundary_observability_test.go"
      provides: "wrapper and round-trip behavior tests"
      contains: "func TestBuildFromParquetContextCompatibility"
  key_links:
    - from: "parquet.go (BuildFromParquet)"
      to: "parquet.go (BuildFromParquetContext)"
      via: "compatibility wrapper"
      pattern: "return BuildFromParquetContext\\(context\\.Background\\(\\),"
    - from: "serialize.go (Encode)"
      to: "serialize.go (EncodeContext)"
      via: "compatibility wrapper"
      pattern: "return EncodeContext\\(context\\.Background\\(\\),"
    - from: "serialize.go (EncodeWithLevel)"
      to: "serialize.go (EncodeWithLevelContext)"
      via: "compatibility wrapper"
      pattern: "return EncodeWithLevelContext\\(context\\.Background\\(\\),"
    - from: "s3.go"
      to: "parquet.go build context path"
      via: "caller context composition"
      pattern: "context\\.WithTimeout\\(ctx,"
---

<objective>
Add the remaining public observability boundaries outside the query path: parquet/build and raw serialization. This plan introduces additive context-aware siblings for those boundaries, composes caller context through the S3 build path, and keeps compatibility wrappers untouched.

Purpose: Phase 14 must make build/query/serialize observable. Query is handled in Plan 02; this plan closes the build/parquet and raw encode/decode sides without inventing a global observability object.

Output: `BuildFromParquetContext(...)`, `EncodeContext(...)`, `DecodeContext(...)`, internal reader/S3 context propagation where needed, and tests that prove wrappers preserve old behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-observability-seams/14-CONTEXT.md
@.planning/phases/14-observability-seams/14-RESEARCH.md
@.planning/phases/14-observability-seams/14-PATTERNS.md
@.planning/phases/14-observability-seams/14-01-PLAN.md
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add context-aware parquet/build siblings and keep old wrappers intact</name>
  <files>parquet.go, boundary_observability_test.go</files>
  <read_first>
    - parquet.go:1-220 (`BuildFromParquet`, `BuildFromParquetReader`, sidecar helpers)
    - .planning/phases/14-observability-seams/14-CONTEXT.md D-07 through D-10 and D-15
    - .planning/phases/14-observability-seams/14-RESEARCH.md sections "Pattern 1", "Pattern 3", and "Recommended Project Structure"
    - .planning/phases/14-observability-seams/14-PATTERNS.md parquet section
  </read_first>
  <action>
    Extend `parquet.go` with additive context-aware build entry points:

    - add `BuildFromParquetContext(ctx context.Context, parquetFile string, jsonColumn string, config GINConfig) (*GINIndex, error)`
    - add `BuildFromParquetReaderContext(ctx context.Context, parquetFile string, jsonColumn string, config GINConfig, reader io.ReaderAt, size int64) (*GINIndex, error)`
    - change the existing `BuildFromParquet(...)` and `BuildFromParquetReader(...)` functions to delegate with `context.Background()`

    Instrumentation rules:
    - start one coarse build/parquet boundary around the overall build operation
    - do not add spans or logger calls inside page/value/document loops
    - use config-carried logger/signals only; do not invent a separate observability parameter object
    - attach `Parser.Name()` only to build-boundary spans or guarded debug events, never to INFO-level log attrs, using the builder-side parser identity already cached in Phase 13

    Add tests in `boundary_observability_test.go` that prove:
    - old wrappers delegate to context-aware siblings
    - representative parquet builds produce the same index contents with and without the context-aware path
    - nil/disabled observability does not change behavior
    - build-side observability can surface parser identity without widening INFO-level attrs
  </action>
  <verify>
    <automated>go test -run 'Test(BuildFromParquetContextCompatibility|BuildFromParquetContextPreservesResults|BuildFromParquetContextEmitsParserNameWithoutInfoLeak)$' -count=1 .</automated>
  </verify>
  <acceptance_criteria>
    - `parquet.go` exports `BuildFromParquetContext`.
    - `parquet.go` exports `BuildFromParquetReaderContext`.
    - old parquet build functions delegate with `context.Background()`.
    - no new boundary instrumentation appears inside row/page/value loops.
    - parser identity is observable from the build boundary only via traces or debug-only signals, not INFO-level attrs.
    - representative parquet build results are unchanged.
  </acceptance_criteria>
  <done>The parquet/build boundary has additive context-aware siblings and boundary-only observability wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compose caller context through the S3-backed build path without widening scope into a full S3 API redesign</name>
  <files>s3.go, boundary_observability_test.go</files>
  <read_first>
    - s3.go:1-230 (client creation, timeout wrappers, `BuildFromParquet`)
    - .planning/phases/14-observability-seams/14-CONTEXT.md D-10 and D-15
    - .planning/phases/14-observability-seams/14-RESEARCH.md sections "Recommended Project Structure" and "Common Pitfalls"
    - .planning/phases/14-observability-seams/14-PATTERNS.md S3 section
  </read_first>
  <action>
    Update the S3 build path just enough to preserve caller context for the new build boundary:

    - add `func (c *S3Client) BuildFromParquetContext(ctx context.Context, bucket, key, jsonColumn string, ginCfg GINConfig) (*GINIndex, error)`
    - keep the old `BuildFromParquet(...)` method as a compatibility wrapper over `context.Background()`
    - make the internal per-build `s3ReaderAt` carry the caller context and derive timeout child contexts from it for build-path `HeadObject` / `GetObject` calls; this is an allowed narrow exception because the reader is short-lived, private to one build, and exists only to compose cancellation into the `io.ReaderAt` contract
    - do not broaden this into context-aware variants for every S3 helper unless the build path truly requires them
    - add one code comment near the reader or helper explaining why this scoped context-in-struct exception is intentional, so later refactors do not "fix" it back to `context.Background()`

    Add tests that prove a canceled/expired caller context can stop the context-aware S3 build path and that the legacy wrapper still exists. Use a fake transport / stubbed client or `httptest`-style harness; do not rely on live AWS or real network timing.
  </action>
  <verify>
    <automated>go test -run 'Test(S3BuildFromParquetContextCompatibility|S3BuildFromParquetContextHonorsCancellationWithStubTransport)$' -count=1 .</automated>
  </verify>
  <acceptance_criteria>
    - `s3.go` exports `BuildFromParquetContext`.
    - the legacy S3 build method delegates with `context.Background()`.
    - timeout wrappers used by the S3 build path derive from the caller context after this change, including range reads performed through the internal parquet reader.
    - cancellation coverage is proven with a deterministic stub transport or in-process test server, not a live AWS dependency.
    - Phase 14 does not explode into a full context-aware S3 API sweep unrelated to build observability.
  </acceptance_criteria>
  <done>The S3-backed build path composes caller context into the new public build boundary without unnecessary API churn.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add `EncodeContext` / `DecodeContext` and route raw serialization helpers through them</name>
  <files>serialize.go, parquet.go, s3.go, boundary_observability_test.go</files>
  <read_first>
    - serialize.go:166-330 (`Encode`, `Decode`, header/section flow)
    - parquet.go sidecar/metadata helpers
    - s3.go sidecar helpers
    - .planning/phases/14-observability-seams/14-CONTEXT.md D-11, D-12, D-15
    - .planning/phases/14-observability-seams/14-RESEARCH.md raw serialization notes
  </read_first>
  <action>
    Introduce the research Pattern 2 raw-serialization surface explicitly:

    - add `type EncodeOption func(*encodeRuntime)` and `type DecodeOption func(*decodeRuntime)` in the raw-serialization implementation area
    - add `EncodeContext(ctx context.Context, idx *GINIndex, opts ...EncodeOption) ([]byte, error)`
    - add `EncodeWithLevelContext(ctx context.Context, idx *GINIndex, level CompressionLevel, opts ...EncodeOption) ([]byte, error)`
    - add `DecodeContext(ctx context.Context, data []byte, opts ...DecodeOption) (*GINIndex, error)`
    - change existing `Encode`, `EncodeWithLevel`, and `Decode` to delegate with `context.Background()`
    - seed encode-time observability from `idx.Config` via the shared noop/disabled helpers unless explicit encode options override it; seed decode-time observability from noop/disabled defaults plus explicit decode options

    Instrumentation rules:
    - keep raw serialization boundary-only
    - use coarse attrs such as operation/status/error type and low-cardinality mode/status info only
    - do not reintroduce package-global hooks or a second logging convention

    Update helper surfaces to reuse the new siblings where appropriate:
    - local sidecar write/read helpers in `parquet.go`
    - metadata encode/decode helpers in `parquet.go`
    - S3 sidecar read/write helpers in `s3.go`

    Add tests proving:
    - wrapper compatibility for `Encode`/`EncodeWithLevel`/`Decode`
    - encode/decode round-trip correctness with the context-aware path
    - helper functions still round-trip through sidecar and metadata flows
  </action>
  <verify>
    <automated>go test -run 'Test(EncodeContextCompatibility|EncodeWithLevelContextCompatibility|DecodeContextCompatibility|SerializationContextRoundTrip|MetadataAndSidecarHelpersUseContextSiblings)$' -count=1 .</automated>
  </verify>
  <acceptance_criteria>
    - `serialize.go` exports `EncodeContext`.
    - `serialize.go` exports `EncodeWithLevelContext`.
    - `serialize.go` exports `DecodeContext`.
    - existing `Encode`, `EncodeWithLevel`, and `Decode` delegate with `context.Background()`.
    - raw encode/decode use explicit `EncodeOption` / `DecodeOption` runtime shims rather than positional logger/provider arguments.
    - sidecar and metadata helpers still work after routing through the new siblings.
    - raw serialization remains boundary-only and does not grow package-global logger or tracer state.
  </acceptance_criteria>
  <done>Raw serialization has additive context-aware boundaries, and sidecar/metadata helpers reuse them without reintroducing globals.</done>
</task>

</tasks>

<verification>
  <check>Parquet/build and raw serialization both have additive context-aware siblings with compatibility wrappers intact.</check>
  <check>S3 build context propagation composes with existing timeout wrappers and a deterministic cancellation test harness.</check>
  <check>Instrumentation remains boundary-only outside the query path as well.</check>
  <check>Round-trip behavior through sidecar and metadata helpers is unchanged, including the configurable-compression encode path.</check>
</verification>

<success_criteria>
  - `BuildFromParquetContext` and `BuildFromParquetReaderContext` exist with old wrappers preserved.
  - `EncodeContext`, `EncodeWithLevelContext`, and `DecodeContext` exist with old wrappers preserved.
  - the S3-backed build path can honor caller context without resetting to `context.Background()`.
  - sidecar and metadata helpers continue to work through the new context-aware serialization paths.
</success_criteria>
