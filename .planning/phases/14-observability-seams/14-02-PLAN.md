---
phase: 14
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - query.go
  - benchmark_test.go
  - query_observability_test.go
autonomous: true
requirements: [OBS-02, OBS-06, OBS-07, OBS-08]
tags: [observability, query, telemetry, logging, benchmarks, go]

must_haves:
  truths:
    - "`query.go` exports `EvaluateContext(context.Context, []Predicate) *RGSet` and `Evaluate([]Predicate)` delegates with `context.Background()`."
    - "The package-global `adaptiveInvariantLogger` state and `SetAdaptiveInvariantLogger(*log.Logger)` are gone."
    - "Adaptive invariant violations still fail open to `AllRGs()` while logging through the repo-owned logger seam."
    - "Query instrumentation is boundary-only: one coarse query boundary, no nested spans in the predicate or row-group loops."
    - "Benchmarks/test gates exist for zero-alloc disabled logging and disabled/noop tracer overhead."
  artifacts:
    - path: "query.go"
      provides: "EvaluateContext, compatibility wrapper, logger migration"
      contains: "func (idx *GINIndex) EvaluateContext("
    - path: "benchmark_test.go"
      provides: "query observability perf gates"
      contains: "func BenchmarkEvaluateDisabledLogging"
    - path: "query_observability_test.go"
      provides: "compatibility and invariant-logger migration tests"
      contains: "func TestEvaluateContextCompatibility"
  key_links:
    - from: "query.go (Evaluate)"
      to: "query.go (EvaluateContext)"
      via: "compatibility wrapper"
      pattern: "return idx\\.EvaluateContext\\(context\\.Background\\(\\), predicates\\)"
    - from: "query.go (adaptiveInvariantAllRGs)"
      to: "logging.Logger"
      via: "repo-owned logger seam"
      pattern: "logging\\.(Warn|Error|Info)\\("
    - from: "benchmark_test.go"
      to: "query.go (EvaluateContext)"
      via: "disabled logging/tracing perf budget"
      pattern: "BenchmarkEvaluate(DisabledLogging|WithTracer)"
---

<objective>
Make query evaluation observable through the Phase 14 logger/signals seams, remove the legacy stdlib logger global, and add the performance gates that protect the disabled path.

Purpose: the query path is the highest-risk boundary in the phase. It owns the migration of the only existing logging convention (`SetAdaptiveInvariantLogger`) and the zero-cost benchmark gate from the roadmap.

Output: `EvaluateContext(...)`, `Evaluate(...)` compatibility wrapper, no package-global query logger state, boundary-only query instrumentation, and benchmarks/tests that lock silent/noop overhead.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-observability-seams/14-CONTEXT.md
@.planning/phases/14-observability-seams/14-RESEARCH.md
@.planning/phases/14-observability-seams/14-PATTERNS.md
@.planning/phases/14-observability-seams/14-01-PLAN.md
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `EvaluateContext` and move query observability to one coarse boundary</name>
  <files>query.go, query_observability_test.go</files>
  <read_first>
    - query.go:1-220 (imports, `Evaluate`, predicate resolution)
    - .planning/phases/14-observability-seams/14-CONTEXT.md D-09, D-10, D-15, D-16
    - .planning/phases/14-observability-seams/14-RESEARCH.md sections "Pattern 1", "Pattern 3", and "Summary"
    - .planning/phases/14-observability-seams/14-PATTERNS.md query section
    - telemetry/boundary.go and logging/attrs.go from Plan 14-01
  </read_first>
  <action>
    Update `query.go` to introduce the additive public boundary:

    - add `func (idx *GINIndex) EvaluateContext(ctx context.Context, predicates []Predicate) *RGSet`
    - change the existing `Evaluate(predicates []Predicate)` method to delegate with `context.Background()`
    - normalize observability access through small helpers that safely read logger/signals from `idx.Config`, falling back to noop/disabled behavior when `idx == nil`, `idx.Config == nil`, or config observability fields are zero-valued
    - instrument exactly one coarse query boundary around the whole evaluation
    - if per-predicate details are emitted, record them as parent-span events only and keep them limited to low-cardinality attrs such as `predicate_op` and `path_mode`
    - do not add spans or logger calls inside row-group loops or numeric/string inner loops

    Add focused tests in `query_observability_test.go` that prove:
    - `Evaluate` delegates to `EvaluateContext(context.Background(), ...)`
    - a hand-constructed `NewGINIndex()` with nil config remains silent and safe
    - enabling observability on the config path does not change query results for representative predicates
  </action>
  <verify>
    <automated>go test -run 'Test(EvaluateContextCompatibility|EvaluateContextNilConfigSilent|EvaluateContextPreservesResults)$' -count=1 .</automated>
  </verify>
  <acceptance_criteria>
    - `query.go` exports `EvaluateContext`.
    - `Evaluate` delegates with `context.Background()`.
    - there is exactly one coarse query-boundary entry point and no new `Start(` calls inside predicate or row-group loops.
    - nil-config query evaluation remains safe and silent.
    - representative query results are unchanged with and without observability enabled.
  </acceptance_criteria>
  <done>The query path has an additive context-aware boundary and boundary-only observability wiring with no result drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove `SetAdaptiveInvariantLogger` and route invariant violations through the new logger seam</name>
  <files>query.go, query_observability_test.go</files>
  <read_first>
    - query.go:13-33 (legacy logger state)
    - query.go:220-260 (`adaptiveInvariantAllRGs`)
    - gin_test.go:1065-1115 (existing invariant logger tests)
    - .planning/phases/14-observability-seams/14-CONTEXT.md D-13 and D-14
    - .planning/phases/14-observability-seams/14-RESEARCH.md sections "Summary" and "Common Pitfalls"
  </read_first>
  <action>
    Delete the legacy query logger state entirely:

    - remove the package-global mutex/logger vars
    - remove `SetAdaptiveInvariantLogger(*log.Logger)`
    - remove `currentAdaptiveInvariantLogger()`
    - update `adaptiveInvariantAllRGs(...)` to emit through the repo-owned logger seam instead

    Behavioral rules:
    - keep the safe fallback exactly the same: invariant violations still return `AllRGs()`
    - do not keep a deprecated compatibility bridge
    - INFO-level attrs for the invariant event must stay on the allowlist; do not log raw predicate values, path names, doc IDs, or row-group IDs at INFO

    Replace the old logger tests with new seam-based tests in `query_observability_test.go`:
    - silent by default
    - emits through a supplied logger
    - still returns all row groups on invariant failure
  </action>
  <verify>
    <automated>go test -run 'Test(AdaptiveInvariantViolationUsesLoggerSeam|AdaptiveInvariantViolationSilentByDefault|AdaptiveInvariantViolationStillFailsOpen)$' -count=1 . && ! rg -n 'SetAdaptiveInvariantLogger|adaptiveInvariantLogger|currentAdaptiveInvariantLogger' --glob '*.go' .</automated>
  </verify>
  <acceptance_criteria>
    - `query.go` no longer contains `SetAdaptiveInvariantLogger`.
    - `query.go` no longer imports stdlib `log` for the invariant logger path.
    - no Go source file in the module retains `SetAdaptiveInvariantLogger`, `adaptiveInvariantLogger`, or `currentAdaptiveInvariantLogger`.
    - invariant violations still return `AllRGs()`.
    - supplied logger-based tests pass, and silent-default behavior is preserved.
  </acceptance_criteria>
  <done>The legacy query logger convention is removed; invariant logging now flows through the repo-owned seam without changing failure-open behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add disabled-path performance gates for logging and tracing on query evaluation</name>
  <files>benchmark_test.go, query_observability_test.go</files>
  <read_first>
    - benchmark_test.go:2400-3095 (existing benchmark patterns and query fixtures)
    - .planning/phases/14-observability-seams/14-CONTEXT.md D-15 and D-16
    - .planning/phases/14-observability-seams/14-RESEARCH.md "Validation Architecture"
    - .planning/phases/14-observability-seams/14-PATTERNS.md benchmark/test section
  </read_first>
  <action>
    Extend the benchmark/test surface with explicit query observability perf gates:

    - add `BenchmarkEvaluateDisabledLogging`
      - fixture must use the same representative query and index shape every run
      - benchmark must report allocs
      - add a companion test that uses `testing.AllocsPerRun` and asserts the disabled logger path adds 0 allocs/op

    - add `BenchmarkEvaluateWithTracer`
      - compare a no-tracer baseline and a supplied-but-disabled/noop-tracer path on the same fixture
      - add a companion helper that runs at least 5 repeated samples on the same fixture, forces `runtime.GOMAXPROCS(1)` during the timing window, compares medians, and enforces the roadmap budget (`<= 1.005x`)
      - hard-fail the 0.5% budget only in a strict normalized path (for example `GIN_STRICT_PERF=1`); generic shared CI should still run the benchmark and smoke test without relying on a single-sample wall-clock assertion

    Keep the benchmark/test names stable so later phase evidence can grep them directly.
  </action>
  <verify>
    <automated>GIN_STRICT_PERF=1 go test -run 'Test(EvaluateDisabledLoggingAllocsZero|EvaluateWithTracerWithinBudget)$' -count=1 . && go test -run '^$' -bench 'BenchmarkEvaluate(DisabledLogging|WithTracer)$' -benchmem -count=1 .</automated>
  </verify>
  <acceptance_criteria>
    - `benchmark_test.go` defines `BenchmarkEvaluateDisabledLogging`.
    - `benchmark_test.go` defines `BenchmarkEvaluateWithTracer`.
    - a test gate asserts disabled logging adds 0 allocs/op.
    - a strict perf helper compares median samples on a normalized runtime and asserts the disabled/noop tracer path stays within the 0.5% budget when `GIN_STRICT_PERF=1`.
    - both benchmarks run under `go test -bench ...`.
  </acceptance_criteria>
  <done>Query observability has explicit disabled-path allocation and tracer-overhead gates, not just informal benchmark coverage.</done>
</task>

</tasks>

<verification>
  <check>`EvaluateContext` exists and `Evaluate` remains backward-compatible.</check>
  <check>No package-global query logger state survives the phase.</check>
  <check>Invariant failures still fail open to `AllRGs()`.</check>
  <check>Benchmarks and test helpers exist for zero-alloc disabled logging and no-regression disabled tracing.</check>
</verification>

<success_criteria>
  - `query.go` exports `EvaluateContext` and keeps `Evaluate` as a compatibility wrapper.
  - `SetAdaptiveInvariantLogger` and related global state are removed.
  - invariant logging uses the repo-owned logger seam and remains silent by default.
  - query observability stays boundary-only.
  - `BenchmarkEvaluateDisabledLogging` and `BenchmarkEvaluateWithTracer` exist with companion assertion tests.
</success_criteria>
