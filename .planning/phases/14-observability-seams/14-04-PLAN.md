---
phase: 14
plan: 04
type: execute
wave: 3
depends_on: ["14-01", "14-02", "14-03"]
files_modified:
  - observability_policy_test.go
  - query_observability_test.go
  - boundary_observability_test.go
  - benchmark_test.go
autonomous: true
requirements: [OBS-02, OBS-03, OBS-04, OBS-05, OBS-08]
tags: [observability, policy, verification, benchmarks, tests, go]

must_haves:
  truths:
    - "INFO-level observability attrs are enforced against the frozen allowlist only: `operation`, `predicate_op`, `path_mode`, `status`, `error.type`."
    - "The library exposes no public `*slog.Logger`, no legacy `*log.Logger` query field/state, and no direct OTel SDK/exporter types in the Phase 14 surface."
    - "Finalized and decoded indexes remain silent and safe by default even when observability runtime fields are omitted from the serialized config payload."
    - "Repo-wide perf/policy tests exist in addition to the functional boundary migrations from Plans 02 and 03."
  artifacts:
    - path: "observability_policy_test.go"
      provides: "allowlist, API-leak, and legacy-logger guard tests"
      contains: "func TestInfoLevelAttrAllowlist"
    - path: "benchmark_test.go"
      provides: "phase-level observability perf coverage"
      contains: "BenchmarkEvaluateDisabledLogging"
    - path: "boundary_observability_test.go"
      provides: "finalize/decode/build/query integration checks"
      contains: "func TestObservabilityDefaultsSurviveFinalizeAndDecode"
  key_links:
    - from: "logging/attrs.go"
      to: "observability_policy_test.go"
      via: "allowlist enforcement"
      pattern: "TestInfoLevelAttrAllowlist"
    - from: "query.go"
      to: "observability_policy_test.go"
      via: "legacy logger removal guard"
      pattern: "TestNoLegacyQueryLoggerSurface"
    - from: "serialize.go"
      to: "boundary_observability_test.go"
      via: "decoded config safety"
      pattern: "TestObservabilityDefaultsSurviveFinalizeAndDecode"
---

<objective>
Close Phase 14 with policy and verification gates that prove the new observability seams are safe, silent by default, and consistent with the roadmap constraints.

Purpose: Plans 01 through 03 introduce the actual seams and boundary migrations. This final plan locks the phase-level safety properties: frozen vocabulary enforcement, no legacy logger surface, no backend-type leakage, and silent defaults across finalize/decode/build/query flows.

Output: policy-focused tests, end-to-end default-safety tests, and the final perf/policy verification surface required to treat Phase 14 as complete rather than merely implemented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-observability-seams/14-CONTEXT.md
@.planning/phases/14-observability-seams/14-RESEARCH.md
@.planning/phases/14-observability-seams/14-PATTERNS.md
@.planning/phases/14-observability-seams/14-01-PLAN.md
@.planning/phases/14-observability-seams/14-02-PLAN.md
@.planning/phases/14-observability-seams/14-03-PLAN.md
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add allowlist tests for INFO-level attrs and frozen vocabulary usage</name>
  <files>observability_policy_test.go</files>
  <read_first>
    - logging/attrs.go from Plan 14-01
    - .planning/phases/14-observability-seams/14-CONTEXT.md D-03 and D-04
    - .planning/phases/14-observability-seams/14-RESEARCH.md "Validation Architecture"
  </read_first>
  <action>
    Create `observability_policy_test.go` with focused policy tests that prove:

    - the canonical INFO-level attr allowlist is exactly:
      `operation`, `predicate_op`, `path_mode`, `status`, `error.type`
    - `path_mode` values observed at INFO are limited to `exact`, `bloom-only`, and `adaptive-hybrid`
    - no helper constructors exist for raw path names, predicate values, doc IDs, row-group IDs, term IDs, or other high-cardinality user data
    - representative INFO-level query/build/serialize log emissions from the Phase 14 seams use only the allowlisted keys

    Implement the tests by capturing attrs from a small in-memory test logger rather than by grepping docs. The allowlist must be enforced against actual emitted attrs.
  </action>
  <verify>
    <automated>go test -run 'Test(InfoLevelAttrAllowlist|InfoLevelEmissionsUseOnlyAllowlistedAttrs)$' -count=1 .</automated>
  </verify>
  <acceptance_criteria>
    - `observability_policy_test.go` defines `TestInfoLevelAttrAllowlist`.
    - the allowlist test fails if a new INFO-level attr key is introduced outside the frozen set.
    - the allowlist test fails if INFO-level `path_mode` emits any value outside `exact`, `bloom-only`, or `adaptive-hybrid`.
    - representative emitted INFO-level attrs are validated through a capturing logger, not just source inspection.
  </acceptance_criteria>
  <done>The frozen vocabulary is enforced by executable tests against real emitted attrs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression guards against legacy logger state and backend-type leakage</name>
  <files>observability_policy_test.go</files>
  <read_first>
    - query.go after Plan 14-02
    - gin.go after Plan 14-01
    - go.mod after Plan 14-01
    - .planning/phases/14-observability-seams/14-CONTEXT.md D-02, D-05, D-13
    - .planning/phases/14-observability-seams/14-RESEARCH.md "Common Pitfalls"
  </read_first>
  <action>
    Extend `observability_policy_test.go` with regression guards that prove:

    - `SetAdaptiveInvariantLogger` no longer exists
    - there are no remaining `adaptiveInvariantLogger` globals in the library
    - the core public surface does not expose `*slog.Logger` or any OTel SDK/exporter type directly
    - the root `go.mod` still does not contain `otel/sdk` or OTLP exporter packages

    Implement the backend-type leakage guard against the actual exported root-package API, not a hand-picked file list. Use stdlib `go/parser`, `go/token`, and `go/types` (or equivalent repo-local package inspection) to walk the exported identifiers of the root module and fail if any exported parameter, field, or return type exposes `*slog.Logger`, `*log.Logger`, or `go.opentelemetry.io/otel/sdk/...` types. Keep the checks deterministic and repo-local.
  </action>
  <verify>
    <automated>go test -run 'Test(NoLegacyQueryLoggerSurface|TestNoBackendTypeLeakage|TestRootModuleHasNoOtelSdkOrExporterDeps)$' -count=1 .</automated>
  </verify>
  <acceptance_criteria>
    - policy tests fail if `SetAdaptiveInvariantLogger` reappears.
    - policy tests fail if `*slog.Logger`, `*log.Logger`, or OTel SDK/exporter types leak into the exported root-package surface.
    - policy tests fail if `go.mod` gains `otel/sdk` or OTLP exporter deps.
  </acceptance_criteria>
  <done>The phase has executable guards against slipping back to a legacy logger convention or leaking backend-specific types into the core API.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add finalize/decode and end-to-end default-safety integration tests</name>
  <files>boundary_observability_test.go, query_observability_test.go</files>
  <read_first>
    - builder.go:942+ (`Finalize`)
    - serialize.go `EncodeContext` / `DecodeContext`
    - query.go `EvaluateContext`
    - parquet.go context-aware build path
    - .planning/phases/14-observability-seams/14-CONTEXT.md D-08 through D-12
  </read_first>
  <action>
    Add integration tests that prove the new seam remains silent and safe across the full lifecycle:

    - finalized indexes carry safe observability defaults
    - decoded indexes remain safe even though runtime logger/provider implementations are not serialized
    - query/build/serialize flows all still work with silent defaults
    - enabling a supplied logger/signals path changes observability output only, not functional results

    Cover at least:
    - build -> finalize -> encode -> decode -> evaluate
    - parquet build wrapper path
    - raw encode/decode path
  </action>
  <verify>
    <automated>go test -run 'Test(ObservabilityDefaultsSurviveFinalizeAndDecode|ObservabilityEnabledDoesNotChangeFunctionalResults|ParquetAndSerializationObservabilityRoundTrip)$' -count=1 .</automated>
  </verify>
  <acceptance_criteria>
    - tests exist for finalize/decode default safety.
    - tests exist for observability-enabled functional equivalence.
    - end-to-end build/query/serialize flows pass with silent defaults and with observability enabled.
  </acceptance_criteria>
  <done>The phase has end-to-end proof that observability state is silent by default, safe after decode, and functionally transparent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Re-run and lock the phase-level perf/policy verification surface</name>
  <files>benchmark_test.go, observability_policy_test.go</files>
  <read_first>
    - benchmark_test.go after Plan 14-02
    - observability_policy_test.go after Tasks 1 and 2
    - .planning/phases/14-observability-seams/14-RESEARCH.md validation section
  </read_first>
  <action>
    Ensure the final Phase 14 verification surface is easy to invoke and grep:

    - keep stable benchmark names for disabled logging/tracing
    - keep stable test names for allowlist and legacy-surface guards
    - document both the generic benchmark command and the strict normalized perf-gate command (`GIN_STRICT_PERF=1 ...`) so the 0.5% budget check is reproducible rather than implied
    - if needed, add small helper tests or comments that make the intended verification command obvious and deterministic

    Do not introduce new product behavior here; this task is about making the verification surface explicit and durable.
  </action>
  <verify>
    <automated>GIN_STRICT_PERF=1 go test -run 'Test(InfoLevelAttrAllowlist|NoLegacyQueryLoggerSurface|ObservabilityDefaultsSurviveFinalizeAndDecode|EvaluateDisabledLoggingAllocsZero|EvaluateWithTracerWithinBudget)$' -count=1 . && go test -run '^$' -bench 'BenchmarkEvaluate(DisabledLogging|WithTracer)$' -benchmem -count=1 .</automated>
  </verify>
  <acceptance_criteria>
    - the key perf/policy test and benchmark names are stable and discoverable.
    - the phase docs or helper comments make the strict normalized perf-gate command explicit.
    - repo-local verification commands can exercise the phase without ad-hoc manual steps.
  </acceptance_criteria>
  <done>The Phase 14 verification surface is explicit, stable, and ready for final execute/verify workflows.</done>
</task>

</tasks>

<verification>
  <check>INFO-level attrs are enforced by capturing tests against the frozen allowlist.</check>
  <check>No legacy query logger surface or backend-type leakage survives.</check>
  <check>Finalize/decode/build/query/serialize remain safe and silent by default.</check>
  <check>The disabled-path perf gates and policy tests form a stable phase-level verification surface.</check>
</verification>

<success_criteria>
  - allowlist tests exist and validate real emitted INFO-level attrs.
  - regression guards exist for legacy logger removal and root-module dependency constraints.
  - end-to-end default-safety tests exist across finalize/decode/build/query/serialize flows.
  - the phase-level perf/policy verification commands are stable and repo-local.
</success_criteria>
