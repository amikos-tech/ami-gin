---
phase: 21-simd-parser-adapter
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - NOTICE.md
  - docs/simd-deployment.md
  - README.md
  - CHANGELOG.md
autonomous: true
requirements: [SIMD-04, SIMD-05, SIMD-06]

must_haves:
  truths:
    - "A reader can trace pure-simdjson v0.1.4's MIT license and bundled simdjson Apache-2.0/MIT attribution from a repository-root NOTICE based on the pinned upstream files"
    - "A caller sees separate build-time, construction-time, and runtime activation steps, including a compiling two-step NewSIMDParser error-handling example"
    - "Air-gapped and corporate-mirror operators can find all four upstream bootstrap environment variables and understand which integrity guarantees apply to each loading route"
    - "The accepted D-05 >uint64 BIGINT divergence is described accurately as parser-layer/no-path versus path-aware numeric-layer handling, with document acceptance controlled by different failure-mode knobs"
    - "README and CHANGELOG state that ordinary builds and the default builder remain stdlib-only, without claiming Phase 22 parity, performance, or platform evidence"
  artifacts:
    - path: "NOTICE.md"
      provides: "Pinned upstream license and bundled simdjson attribution chain"
      contains: "pure-simdjson v0.1.4"
    - path: "docs/simd-deployment.md"
      provides: "Build, construction, runtime loading, fallback, integrity, and BIGINT operations guide"
      contains: "PURE_SIMDJSON_DISABLE_GH_FALLBACK"
    - path: "README.md"
      provides: "Optional SIMD parser installation section, dependency credit, and deployment-guide link"
      contains: "Optional SIMD parser"
    - path: "CHANGELOG.md"
      provides: "Unreleased opt-in SIMD adapter entry with unchanged defaults and accurate limitation wording"
      contains: "pure-simdjson"
  key_links:
    - from: "README.md Optional SIMD parser"
      to: "docs/simd-deployment.md"
      via: "relative Markdown link"
      pattern: "docs/simd-deployment.md"
    - from: "parser_simd.go constructor error"
      to: "docs/simd-deployment.md"
      via: "PURE_SIMDJSON_LIB_PATH recovery guidance"
      pattern: "PURE_SIMDJSON_LIB_PATH"
    - from: "NOTICE.md"
      to: "pure-simdjson@v0.1.4/NOTICE"
      via: "versioned attribution sourced from the downloaded module"
      pattern: "simdjson"
---

<objective>
Publish accurate license and operator guidance for the opt-in SIMD adapter, including its activation model, native-asset trust boundaries, explicit fallback, and the accepted BIGINT behavior difference.

Purpose: Make SIMD-04/05 usable without implying a default dependency and document the bounded numeric limitation required by SIMD-06 and D-05.
Output: Root NOTICE, deployment guide, concrete README activation section, and an Unreleased CHANGELOG entry.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md
@.planning/phases/21-simd-parser-adapter/21-CONTEXT.md
@.planning/phases/21-simd-parser-adapter/21-RESEARCH.md
@.planning/phases/21-simd-parser-adapter/21-REVIEWS.md
@.planning/spikes/001-pure-simdjson-api-verification/README.md
@README.md
@CHANGELOG.md

<interfaces>
Locked public activation contract:
  func NewSIMDParser() (Parser, error)
  func WithParser(Parser) BuilderOption
  NewBuilder(config GINConfig, numRGs int, opts ...BuilderOption) (*GINBuilder, error)

Live failure routing:
  upstream Parse BIGINT error -> parser layer, empty path, ParserFailureMode
  stdlib json.Number overflow -> numeric layer, concrete path, NumericFailureMode
  both soft parser and soft numeric failures discard the failed document atomically; neither preserves only the other fields
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source the NOTICE and write deployment guidance with correct failure and trust semantics</name>
  <files>NOTICE.md, docs/simd-deployment.md</files>
  <read_first>
    - NOTICE.md — target file; confirm it does not exist before creation
    - docs/simd-deployment.md — target file; confirm it does not exist before creation
    - .planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md — locked pin, constructor, loading delegation, env vars, and fallback policy
    - .planning/phases/21-simd-parser-adapter/21-CONTEXT.md D-05 — locked acceptance/no-prescan decision and its outdated explanatory consequence
    - .planning/phases/21-simd-parser-adapter/21-REVIEWS.md Plan 21-02 — factual BIGINT, constructor, environment, NOTICE, and integrity corrections
    - builder.go lines 389-449 and 693-738 — live parser-versus-numeric failure routing and atomic soft skips
    - $(go env GOMODCACHE)/github.com/amikos-tech/pure-simdjson@v0.1.4/LICENSE — pinned upstream MIT text; if absent, download from the existing isolated Spike 001 module, never from the root module
    - $(go env GOMODCACHE)/github.com/amikos-tech/pure-simdjson@v0.1.4/NOTICE — exact bundled simdjson Apache-2.0/MIT attribution source
    - $(go env GOMODCACHE)/github.com/amikos-tech/pure-simdjson@v0.1.4/docs/bootstrap.md — resolution order, all four env vars, SHA-256 behavior, air-gap commands, and explicit-path trust boundary
  </read_first>
  <action>
    Address the NOTICE accuracy review by resolving the pinned module directory from `go env GOMODCACHE` and reading its LICENSE and NOTICE before writing. If the cache entry is absent, run `go mod download github.com/amikos-tech/pure-simdjson@v0.1.4` from `.planning/spikes/001-pure-simdjson-api-verification`, whose isolated go.mod already pins that version; do not run the download from the root module or modify root go.mod/go.sum in this parallel plan. Create root `NOTICE.md` that identifies `github.com/amikos-tech/pure-simdjson v0.1.4` as MIT-licensed and reproduces the upstream NOTICE's factual statement that its bundled simdjson source carries Apache-2.0 and MIT texts. Attribute the dependency and chain without saying pure-simdjson itself is Apache-only and without inventing license language from memory.

    Create `docs/simd-deployment.md` with sections for Default behavior, Enable the parser, Automatic bootstrap, Air-gapped/explicit path, Corporate mirror/hermetic loading, Caller-owned stdlib fallback, Native artifact integrity, BIGINT limitation, and Validation scope. Per the Phase 19 locks, distinguish three independent gates: compile with `-tags simdjson`; call `p, err := gin.NewSIMDParser()` and handle the error; pass `p` to `gin.NewBuilder(config, numRGs, gin.WithParser(p))`. Do not show `WithParser(NewSIMDParser())`, because the constructor returns two values. State that no silent fallback occurs and show caller branching after constructor failure. Present v0.1.4 as the consumer-facing Go module version; the recorded tag commit is audit evidence for this repository, not alternate installation syntax.

    Document all four upstream variables exactly: `PURE_SIMDJSON_LIB_PATH`, `PURE_SIMDJSON_BINARY_MIRROR`, `PURE_SIMDJSON_DISABLE_GH_FALLBACK`, and `PURE_SIMDJSON_CACHE_DIR`. Explain the three integrity layers accurately: go.sum authenticates the Go wrapper module; upstream bootstrap verifies automatically downloaded native assets against published SHA-256 metadata; setting PURE_SIMDJSON_LIB_PATH bypasses download/cache verification, so checksum/provenance validation of that operator-supplied library is operator-owned. Link to the pinned upstream bootstrap guidance rather than duplicating every command.

    Preserve D-05's locked decision that >uint64 BIGINT behavior is an accepted SIMD limitation and that ami-gin performs no pre-scan. Resolve the review conflict explicitly in the guide: the CONTEXT explanation that stdlib soft-skips only one field predates the live atomic failure contract and is not the behavior to document. State instead that SIMD fails in upstream Parse before a field path is staged, so AddDocument treats it as a parser-layer failure with empty path governed by ParserFailureMode; stdlib reaches path-aware numeric handling governed by NumericFailureMode; soft mode at either layer discards the failed document atomically. This is a factual correction to D-05's rationale, not a reversal of the accepted divergence or no-prescan choice. State that parity, benchmarks, five-platform runtime CI, and operational verification belong to Phase 22 and make no result claims for them here.
  </action>
  <verify>
    <automated>test -s NOTICE.md &amp;&amp; test -s docs/simd-deployment.md &amp;&amp; rg -q 'pure-simdjson v0\.1\.4' NOTICE.md &amp;&amp; rg -q 'Apache' NOTICE.md &amp;&amp; rg -q 'PURE_SIMDJSON_LIB_PATH' docs/simd-deployment.md &amp;&amp; rg -q 'PURE_SIMDJSON_BINARY_MIRROR' docs/simd-deployment.md &amp;&amp; rg -q 'PURE_SIMDJSON_DISABLE_GH_FALLBACK' docs/simd-deployment.md &amp;&amp; rg -q 'PURE_SIMDJSON_CACHE_DIR' docs/simd-deployment.md &amp;&amp; rg -q 'ParserFailureMode' docs/simd-deployment.md &amp;&amp; rg -q 'NumericFailureMode' docs/simd-deployment.md &amp;&amp; ! rg -q 'WithParser\(NewSIMDParser\(\)\)' docs/simd-deployment.md</automated>
  </verify>
  <acceptance_criteria>
    - NOTICE.md is based on the actual v0.1.4 LICENSE/NOTICE and distinguishes pure-simdjson's MIT license from bundled simdjson's Apache-2.0/MIT attribution.
    - The guide separates build tag, constructor, WithParser selection, and runtime native resolution; its code sample handles both NewSIMDParser and NewBuilder errors.
    - All four upstream bootstrap variables are named and no-silent-fallback is explicit.
    - The integrity section does not claim go.sum authenticates a PURE_SIMDJSON_LIB_PATH artifact.
    - The BIGINT section uses parser-layer/empty-path/ParserFailureMode versus numeric-layer/path/NumericFailureMode wording and states that soft failures discard the document atomically.
    - The guide preserves the accepted D-05 no-prescan decision and does not claim Phase 22 evidence already exists.
  </acceptance_criteria>
  <done>NOTICE and deployment docs are pinned-source accurate, operationally complete, and truthful about BIGINT and supply-chain behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add the concrete README activation section and accurate Unreleased entry</name>
  <files>README.md, CHANGELOG.md</files>
  <read_first>
    - README.md lines 74-113 — exact Installation/Quick Start insertion point
    - README.md lines 634-652 — existing simdjson redistribution/provenance tone
    - CHANGELOG.md — current Unreleased bullet hierarchy and voice
    - docs/simd-deployment.md — Task 1 terminology and relative link target
    - .planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md D-07/D-09/D-10 — README happy path, two-step API, and explicit fallback
    - .planning/phases/21-simd-parser-adapter/21-REVIEWS.md Plan 21-02 — concrete insertion point and constructor-example corrections
  </read_first>
  <action>
    Insert a `### Optional SIMD parser` subsection immediately after the Installation `go get` block and before `## Quick Start`; the repository has no existing Dependencies section, so do not ask the executor to locate one. Credit and link `github.com/amikos-tech/pure-simdjson` v0.1.4, state that the adapter is compiled with `-tags simdjson`, and show the same valid two-step constructor/error handling followed by `WithParser(p)`. Link `docs/simd-deployment.md` for bootstrap, mirrors, air-gap, integrity, fallback, and BIGINT details. State that ordinary builds and default NewBuilder remain stdlib-only.

    Add one Unreleased CHANGELOG bullet covering the same-package `NewSIMDParser() (Parser, error)`, explicit `WithParser(p)` selection, build tag, parser name `pure-simdjson`, unchanged stdlib default, and the documented >uint64 BIGINT difference. Use the corrected layer/path/failure-mode wording from Task 1 rather than saying stdlib preserves the rest of the document. Mention no measured speedup, parity result, or platform certification because those are Phase 22 deliverables. Keep the entry under Unreleased: it may be committed in Wave 1 because the phase branch cannot be considered complete or releasable until Plan 03 lands in Wave 2. Per D-04, add no CLI activation flag or CLI documentation.
  </action>
  <verify>
    <automated>rg -q '^### Optional SIMD parser$' README.md &amp;&amp; rg -q 'docs/simd-deployment\.md' README.md &amp;&amp; rg -q 'pure-simdjson' README.md &amp;&amp; rg -q 'NewSIMDParser' README.md &amp;&amp; rg -q 'WithParser\(p\)' README.md &amp;&amp; ! rg -q 'WithParser\(NewSIMDParser\(\)\)' README.md &amp;&amp; rg -q 'pure-simdjson' CHANGELOG.md &amp;&amp; rg -q 'BIGINT|uint64' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - README has exactly one Optional SIMD parser subsection between Installation and Quick Start, credits the pinned dependency, and links the deployment guide.
    - README's sample assigns NewSIMDParser to `(p, err)`, handles err, and passes only `p` to WithParser.
    - README and CHANGELOG both say default builds/default NewBuilder remain stdlib-only.
    - CHANGELOG names the accepted BIGINT limitation without the incorrect per-field-soft-skip claim and without Phase 22 performance/parity/CI claims.
    - No `--parser=simd` CLI flag or instruction is introduced, per D-04.
  </acceptance_criteria>
  <done>README and CHANGELOG provide a compiling activation path, dependency credit, unchanged-default guarantee, and accurate limitation pointer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go module proxy -> repository | The pinned wrapper module is authenticated by go.sum during dependency resolution |
| Upstream bootstrap -> native cache | Downloaded native assets are checked against upstream SHA-256 metadata before installation |
| Operator path -> process | PURE_SIMDJSON_LIB_PATH loads an operator-supplied binary and bypasses bootstrap verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Tampering | License/NOTICE provenance | mitigate | Source NOTICE.md from the exact v0.1.4 module LICENSE and NOTICE files |
| T-21-05 | Tampering | Native library supply chain | mitigate | Document go.sum wrapper integrity, upstream SHA-256 download verification, and operator ownership for explicit-path artifacts as separate controls |
| T-21-06 | Repudiation | Silent parser fallback | mitigate | Document hard construction errors and require explicit caller fallback so telemetry can distinguish intended stdlib from degraded SIMD |
| T-21-07 | Information Disclosure | Error/document examples | accept | Guidance names variables and failure layers only; it includes no credentials, file contents, or raw document values |
</threat_model>

<verification>
- `test -s NOTICE.md && test -s docs/simd-deployment.md`
- All four PURE_SIMDJSON environment variables are present in the deployment guide.
- README uses a two-step constructor and links the guide; CHANGELOG records unchanged defaults and the corrected BIGINT limitation.
- `go test ./...` remains green after documentation changes.
</verification>

<success_criteria>
- License attribution is derived from the exact pinned dependency files.
- A caller can follow the docs without a compile error and understands all activation and fallback gates.
- Supply-chain guarantees are not overstated, and D-05 is both preserved and described consistently with the live atomic builder.
</success_criteria>

<output>
Create `.planning/phases/21-simd-parser-adapter/21-02-SUMMARY.md` when done.
</output>
