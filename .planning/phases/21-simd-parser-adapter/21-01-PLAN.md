---
phase: 21-simd-parser-adapter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - parser.go
  - parser_sink.go
  - parser_test.go
  - parser_simd_test.go
  - parser_parity_fixtures_test.go
  - testdata/parity-golden/simd-numeric-parity.bin
autonomous: true
requirements: [SIMD-06, SIMD-07]

must_haves:
  truths:
    - "The parser-to-sink boundary exposes typed string, bool, int64, uint64, and float64 methods; a SIMD parser need not call StageScalar(any) or StageNativeNumeric(any) for non-null scalar leaves"
    - "StageFloat64 preserves float classification for 1.0 and 1e18, and every hard non-finite error remains a numeric-stage error rather than being reclassified as a parser error"
    - "StageInt64 preserves 9007199254740993 exactly, while StageUint64 values above math.MaxInt64 follow the existing hard/soft numeric-failure policy without committing partial state"
    - "Every typed-sink comparison commits its document through AddDocument before Finalize and therefore cannot pass by comparing two empty indexes"
    - "The authored simd-numeric-parity fixture keeps incompatible numeric classes on separate paths and regenerates without modifying any existing golden"
  artifacts:
    - path: "parser_sink.go"
      provides: "Five typed scalar methods on parserSink and GINBuilder implementations"
      contains: "StageFloat64"
    - path: "parser.go"
      provides: "Parser contract documenting typed exact-integer routes and the complete Stage method family"
      contains: "StageInt64"
    - path: "parser_test.go"
      provides: "recordingSink implementation of the expanded parserSink contract"
      contains: "func (s *recordingSink) StageFloat64"
    - path: "parser_simd_test.go"
      provides: "Untagged committed-state tests for typed equivalence, non-coercion, exact integers, overflow modes, and non-finite classification"
      contains: "TestTypedSink"
    - path: "parser_parity_fixtures_test.go"
      provides: "Buildable simd-numeric-parity fixture with one numeric class per path"
      contains: "simd-numeric-parity"
    - path: "testdata/parity-golden/simd-numeric-parity.bin"
      provides: "Committed stdlib golden for the authored numeric fixture"
  key_links:
    - from: "parser_simd_test.go"
      to: "builder.go mergeDocumentState"
      via: "typed test Parser -> AddDocument -> mergeDocumentState -> Finalize/Encode"
      pattern: "AddDocument"
    - from: "parser_sink.go StageFloat64"
      to: "builder.go stageNumericObservation"
      via: "direct stagedNumericValue{isInt:false} call wrapped by tagStageError"
      pattern: "tagStageError.*stageNumericObservation"
    - from: "parser_sink.go StageInt64/StageUint64"
      to: "builder.go stageNativeNumeric"
      via: "typed boundary methods reuse the existing numeric policy"
      pattern: "stageNativeNumeric"
---

<objective>
Extend the package-private parser sink with the locked typed scalar surface and prove its semantics through committed document builds. Repair the parser contract and existing test sink at the same time, then add a buildable numeric fixture and isolated golden.

Purpose: Deliver the typed boundary required by SIMD-07 and the exact-int/non-coercion staging behavior required by SIMD-06 before the tagged adapter consumes it.
Output: Expanded sink and parser contract, updated recording sink, committed-state typed wrapper tests, and a deterministic numeric parity fixture/golden.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-simd-parser-adapter/21-CONTEXT.md
@.planning/phases/21-simd-parser-adapter/21-RESEARCH.md
@.planning/phases/21-simd-parser-adapter/21-PATTERNS.md
@.planning/phases/21-simd-parser-adapter/21-REVIEWS.md
@parser.go
@parser_sink.go
@parser_test.go
@builder.go

<interfaces>
Locked D-01 parserSink additions:
  StageString(state *documentBuildState, canonicalPath string, v string) error
  StageBool(state *documentBuildState, canonicalPath string, v bool) error
  StageInt64(state *documentBuildState, canonicalPath string, v int64) error
  StageUint64(state *documentBuildState, canonicalPath string, v uint64) error
  StageFloat64(state *documentBuildState, canonicalPath string, v float64) error

Live staging contracts:
  GINBuilder.Stage* methods must return tagStageError(...) for hard stage failures.
  AddDocument commits only after Parser.Parse succeeds, using mergeDocumentState.
  BeginDocument alone creates documentBuildState but does not mutate pathData.
  stageNativeNumeric preserves int64 and rejects uint64 > math.MaxInt64 according to NumericFailureMode.
  stageNumericObservation accepts stagedNumericValue{isInt:false,floatVal:v} without the stagedNumericFromValue whole-float fold.
  newSoftSkipNumericDocumentError is already classified as a numeric document skip.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement and prove the expanded typed sink contract with committed documents</name>
  <files>parser.go, parser_sink.go, parser_test.go, parser_simd_test.go</files>
  <read_first>
    - parser_simd_test.go — target does not exist; create it untagged for typed-sink tests that never load the native library
    - parser_sink.go — interface, GINBuilder forwarding pattern, and compile assertion being extended
    - parser.go — exact-integer contract and Stage-method list that must describe the new typed routes
    - parser_test.go — recordingSink, existing parser contract tests, and AddDocument test patterns
    - builder.go lines 36-86, 389-449, 526-594, 693-788 — skip/stage tags, AddDocument commit path, scalar/materialized staging, and numeric classification
    - failure_modes_test.go lines 708-825 — hard/soft numeric assertions and requireSoftSkippedDocument pattern
    - .planning/phases/21-simd-parser-adapter/21-CONTEXT.md D-01 and D-02/D-02a — locked signatures and non-coercion behavior
    - .planning/phases/21-simd-parser-adapter/21-REVIEWS.md Plan 21-01 concerns — committed-state, recordingSink, parser contract, error-tagging, and coverage corrections
  </read_first>
  <behavior>
    - StageString and StageBool produce the same committed encoded index as stdlib scalar JSON at the root path.
    - StageInt64(9007199254740993) produces the same committed encoded index as the identical stdlib JSON literal and remains exact.
    - StageFloat64(1.0) and StageFloat64(1e18) match the stdlib raw-number route and differ from StageNativeNumeric(float64(...)), proving the whole-float fold is bypassed.
    - StageUint64(math.MaxInt64+1) returns a numeric IngestError in hard mode; in soft mode AddDocument returns nil, increments the numeric document-skip count, and commits neither a document nor path data. The stdlib oversized literal has the same hard/soft disposition.
    - StageFloat64(NaN/+Inf/-Inf) remains numeric-layer in hard mode and becomes an atomic numeric document skip in soft mode.
  </behavior>
  <action>
    Address the Plan 21-01 review blockers with a RED-GREEN-REFACTOR cycle. In RED, create untagged `parser_simd_test.go` with a package-local test Parser that calls `sink.BeginDocument(rgID)` once and invokes one supplied typed Stage method at canonical root path `$`; drive it through `NewBuilder(..., WithParser(...))` and `AddDocument`, then Finalize/Encode only after AddDocument succeeds. Do not compare builders staged only with BeginDocument: that state is uncommitted and would yield false empty-index equality. Add focused tests named with the `TestTypedSink` prefix for string/bool equivalence, exact int64 >2^53, whole-float non-coercion, hard/soft oversized uint64 behavior, and NaN/Inf hard/soft classification. Assert errors and builder counters/state before byte equality so two failures or two empty indexes cannot satisfy an equivalence check.

    In GREEN, extend `parserSink` with the five exact D-01 signatures. Implement StageString/StageBool through `stageScalarToken`, StageInt64/StageUint64 through `stageNativeNumeric`, and StageFloat64 by constructing `stagedNumericValue{isInt:false,floatVal:v}` and calling `stageNumericObservation` directly per D-02; StageFloat64 must never call `stageNativeNumeric`. For every StageFloat64 exit, including NaN/Inf hard errors, pass the result through `tagStageError`; preserve `newSoftSkipNumericDocumentError(canonicalPath)` under `IngestFailureSoft` and use a numeric-layer `newIngestError` with `errors.New("non-finite numeric value")` under hard mode. This keeps AddDocument from reclassifying a sink failure as parser-layer.

    Update `recordingSink` in parser_test.go with all five methods so it continues to satisfy parserSink; record distinct `string:`, `bool:`, `int64:`, `uint64:`, and `float64:` events. Update parser.go's contract to say raw-text parsers use StageJSONNumber, while exact typed parsers may use StageInt64/StageUint64 and lexeme-classified floats use StageFloat64; list all typed Stage methods in the present-marking contract. Also replace the stale claim that AddDocument returns every Parse error unchanged with the live rule: parser-local errors carry parser context, stage-callback errors retain their layer, and AddDocument applies parser hard/soft classification to non-stage errors. Keep StageScalar and stdlibParser behavior unchanged.

    In REFACTOR, keep the test Parser/helper package-private, ensure each comparison uses a fresh builder, and avoid any runtime dependency on pure-simdjson. This task implements D-01 and D-02; it does not add a tagged parser.
  </action>
  <verify>
    <automated>go test -run '^TestTypedSink' -count=1 . &amp;&amp; go test -run '^TestStdlibParser' -count=1 . &amp;&amp; go build ./... &amp;&amp; go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go test -run '^TestTypedSink' -count=1 .` passes and every encoded comparison reaches AddDocument successfully before Finalize.
    - `parserSink`, `GINBuilder`, and `recordingSink` each implement StageString, StageBool, StageInt64, StageUint64, and StageFloat64 with the locked signatures.
    - StageFloat64 calls stageNumericObservation directly, does not call stageNativeNumeric, and wraps both finite and non-finite hard returns through tagStageError.
    - Tests directly cover StageInt64(9007199254740993), uint64 hard and soft modes, and NaN/+Inf/-Inf hard and soft modes.
    - parser.go documents StageInt64/StageUint64 as exact typed routes and no longer claims that large exact integers must always use StageJSONNumber.
  </acceptance_criteria>
  <done>The typed sink compiles, existing sinks remain valid, and committed-state tests prove exact, non-coercing, correctly classified behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add a buildable numeric parity fixture without perturbing existing goldens</name>
  <files>parser_parity_fixtures_test.go, testdata/parity-golden/simd-numeric-parity.bin</files>
  <read_first>
    - parser_parity_fixtures_test.go — parityFixture shape and existing int64-boundaries fixture
    - testdata/parity-golden/simd-numeric-parity.bin — target does not exist; generate it only through the fixture-specific regenerator
    - parity_goldens_test.go — regeneration loop and AddDocument failure behavior
    - parser_parity_test.go lines 40-74 — authored fixture golden comparison
    - builder.go lines 776-850 — mixed numeric promotion rejection that makes one shared path invalid for this fixture
    - .planning/phases/21-simd-parser-adapter/21-CONTEXT.md D-02 fixture set and D-05 exclusion
    - .planning/phases/21-simd-parser-adapter/21-REVIEWS.md Plan 21-01 fixture and golden-status findings
  </read_first>
  <action>
    Address the review finding that the original same-path fixture cannot build. Add `simd-numeric-parity` with `DefaultConfig`, `NumRGs: 1`, and one JSON object whose distinct fields are `plain_int:1`, `whole_float:1.0`, `exp_float:1e18`, `fraction:1.5`, and `exact_large_int:9007199254740993`. Keeping each numeric class on a separate canonical path prevents the float-mixed path from rejecting the >2^53 integer while preserving every D-02 fixture literal for Phase 22's tagged parity consumer.

    Per D-05, do not add `18446744073709551615` or a >uint64 BIGINT to this byte-golden fixture: the former is covered by Task 1's hard/soft rejection tests, and the latter is the explicitly documented parser-layer divergence. Before regenerating, require `git diff HEAD --exit-code -- testdata/parity-golden` so pre-existing tracked golden changes are not overwritten. Run the tagged regenerator, then prove all tracked pre-existing goldens are byte-identical with `git diff HEAD --exit-code -- testdata/parity-golden ':(exclude)testdata/parity-golden/simd-numeric-parity.bin'`; retain the new non-empty golden only.
  </action>
  <verify>
    <automated>git diff HEAD --exit-code -- testdata/parity-golden ':(exclude)testdata/parity-golden/simd-numeric-parity.bin' &amp;&amp; go test -tags regenerate_goldens -run '^TestRegenerateParityGoldens/simd-numeric-parity$' -count=1 . &amp;&amp; go test -run '^TestParserParity_AuthoredFixtures/simd-numeric-parity$' -count=1 . &amp;&amp; test -s testdata/parity-golden/simd-numeric-parity.bin &amp;&amp; git diff HEAD --exit-code -- testdata/parity-golden ':(exclude)testdata/parity-golden/simd-numeric-parity.bin'</automated>
  </verify>
  <acceptance_criteria>
    - The fixture uses five distinct fields and contains the exact literals 1, 1.0, 1e18, 1.5, and 9007199254740993.
    - The fixture contains neither 18446744073709551615 nor a >uint64 BIGINT.
    - The fixture-specific regeneration and authored parity tests pass, and simd-numeric-parity.bin exists with non-zero size.
    - `git diff HEAD --exit-code -- testdata/parity-golden ':(exclude)testdata/parity-golden/simd-numeric-parity.bin'` passes after regeneration, including staged modifications to tracked goldens.
  </acceptance_criteria>
  <done>The numeric fixture builds deterministically, covers every parity-safe D-02 literal, and leaves all existing goldens unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parser -> sink | Untrusted JSON-derived scalar values cross a package-private typed staging boundary |
| Sink -> AddDocument | Stage errors must retain numeric classification so atomic hard/soft policy is applied correctly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | StageFloat64 numeric classification | mitigate | Direct non-folding stageNumericObservation route plus committed-byte tests for 1.0 and 1e18 |
| T-21-02 | Tampering | Stage error provenance | mitigate | tagStageError on every hard StageFloat64 exit; tests require IngestLayerNumeric rather than parser-layer wrapping |
| T-21-03 | Denial of Service | Oversized/non-finite numerics | mitigate | Existing hard rejection or atomic numeric soft skip; tests assert no document/path commit after rejection |
</threat_model>

<verification>
- `go test -run '^TestTypedSink' -count=1 .`
- `go test -run '^TestParserParity_AuthoredFixtures/simd-numeric-parity$' -count=1 .`
- `go build ./... && go vet ./...`
- `make test`
</verification>

<success_criteria>
- Five locked typed scalar methods exist across the sink contract, GINBuilder, and recordingSink.
- Tests exercise the real AddDocument commit path and prove non-coercion, exact int64, hard/soft uint64 rejection, and numeric-layer NaN/Inf handling.
- The numeric fixture/golden is buildable because incompatible numeric classes use separate paths, and no tracked golden changes.
</success_criteria>

<output>
Create `.planning/phases/21-simd-parser-adapter/21-01-SUMMARY.md` when done.
</output>
