---
phase: 21-simd-parser-adapter
plan: 03
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - go.mod
  - go.sum
  - parser_simd.go
  - Makefile
autonomous: true
requirements: [SIMD-04, SIMD-05, SIMD-06, SIMD-07]

must_haves:
  truths:
    - "A tagged caller can construct NewSIMDParser() (Parser, error), observe Name()==pure-simdjson, and select it through the existing WithParser seam without any CLI activation path"
    - "Every successful upstream Parse closes its Doc exactly once after walking, including walker-error paths, so a reusable parser does not become ErrParserBusy on the next document"
    - "Non-null scalar leaves cross the parserSink boundary through the D-01 typed methods and numeric accessors are selected strictly from Element.Type()"
    - "Transform-buffered subtrees preserve stdlib json.Number semantics, including float lexeme classification and transformer input preparation, instead of re-entering the native-float whole-number fold"
    - "SIMD object handling is last-key-wins for duplicate keys and uses the same raw path construction as stdlibParser; containers and both array path variants re-check transforms before staging"
    - "Default package/test dependency graphs exclude pure-simdjson, and every product Go importer has the exact first-line //go:build simdjson constraint"
  artifacts:
    - path: "parser_simd.go"
      provides: "Tagged lifecycle-safe same-package adapter, typed hybrid walker, and json.Number materializer"
      contains: "//go:build simdjson"
    - path: "go.mod"
      provides: "Exact pure-simdjson v0.1.4 pin"
      contains: "github.com/amikos-tech/pure-simdjson v0.1.4"
    - path: "go.sum"
      provides: "Module checksums for the pinned wrapper and transitives"
      contains: "github.com/amikos-tech/pure-simdjson v0.1.4"
    - path: "Makefile"
      provides: "simd-isolation-check source and default dependency-graph guard"
      contains: "simd-isolation-check"
  key_links:
    - from: "parser_simd.go walkElement"
      to: "parser_sink.go typed methods"
      via: "TypeString/Bool/Int64/Uint64/Float64 dispatch"
      pattern: "Stage(String|Bool|Int64|Uint64|Float64)"
    - from: "parser_simd.go materializeElement"
      to: "builder.go stageMaterializedValue"
      via: "canonical json.Number values passed through StageMaterialized(state, rawPath, value, true)"
      pattern: "json.Number"
    - from: "parser_simd.go Parse"
      to: "purejson.Doc.Close"
      via: "deferred close with propagated close error"
      pattern: "doc.Close"
    - from: "Makefile simd-isolation-check"
      to: "default Go dependency graph"
      via: "go list -deps -test ./... exact-package absence assertion"
      pattern: "go list -deps -test"
---

<objective>
Pin pure-simdjson and implement the tagged same-package adapter with correct native document lifecycle, exact numeric/transform semantics, duplicate-key parity, and a robust default-build isolation gate.

Purpose: Deliver explicit parser selection (SIMD-04) while proving the default graph stays stdlib-only (SIMD-05) and consuming the exact typed/numeric contracts from Plan 01 (SIMD-06/07).
Output: go.mod/go.sum pin, parser_simd.go, and a contributor-facing simd-isolation-check target.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md
@.planning/phases/21-simd-parser-adapter/21-CONTEXT.md
@.planning/phases/21-simd-parser-adapter/21-RESEARCH.md
@.planning/phases/21-simd-parser-adapter/21-PATTERNS.md
@.planning/phases/21-simd-parser-adapter/21-REVIEWS.md
@.planning/spikes/001-pure-simdjson-api-verification/README.md
@.planning/phases/21-simd-parser-adapter/21-01-SUMMARY.md
@parser_stdlib.go
@parser.go
@parser_sink.go
@builder.go

<interfaces>
Parser seam after Plan 01:
  Parser { Name() string; Parse(jsonDoc []byte, rgID int, sink parserSink) error }
  typed sink methods: StageString, StageBool, StageInt64, StageUint64, StageFloat64
  null remains StageScalar(state, canonicalPath, nil)
  transformed subtrees use StageMaterialized(state, rawPath, value, true)

Pinned upstream v0.1.4 lifecycle/API:
  purejson.NewParser() (*purejson.Parser, error)
  (*purejson.Parser).Parse([]byte) (*purejson.Doc, error)
  one live Doc per Parser; a second Parse before Doc.Close returns ErrParserBusy
  (*purejson.Doc).Close() error clears the parser's live-document state and is idempotent
  Element.Type() discriminates Null/Bool/Int64/Uint64/Float64/String/Array/Object
  Element.AsObject() (Object, error); Element.AsArray() (Array, error)
  Object.Iter()/Array.Iter() expose Next, Key/Value, Err
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin pure-simdjson and implement the lifecycle-safe parity-preserving adapter</name>
  <files>go.mod, go.sum, parser_simd.go</files>
  <read_first>
    - go.mod — existing direct require block and Go version
    - go.sum — existing checksum style; do not hand-edit
    - parser_simd.go — target does not exist; create it at the module root
    - parser_stdlib.go — authoritative BeginDocument, transform guard, presence, raw-path, duplicate-key, array dual-path, and error-context behavior
    - parser.go — live parser contract after Plan 01
    - parser_sink.go — typed methods created by Plan 01 and tagStageError ownership
    - builder.go lines 389-510, 526-615, 693-788 — AddDocument classification, decodeAny/prepareTransformerValue, StageMaterialized json.Number behavior, and native-float fold to avoid
    - .planning/phases/21-simd-parser-adapter/21-CONTEXT.md D-02/D-02a/D-03/D-04/D-05 — strict typed routing, hybrid walk, no CLI, and no BIGINT pre-scan
    - .planning/phases/21-simd-parser-adapter/21-REVIEWS.md Plan 21-03 — Doc.Close, transform materialization, duplicate keys, path handling, invalid types, and error policy
    - $(go env GOMODCACHE)/github.com/amikos-tech/pure-simdjson@v0.1.4/parser.go — ErrParserBusy single-live-Doc invariant
    - $(go env GOMODCACHE)/github.com/amikos-tech/pure-simdjson@v0.1.4/doc.go — Doc.Close contract and returned error
    - $(go env GOMODCACHE)/github.com/amikos-tech/pure-simdjson@v0.1.4/element.go — exact accessor, TypeInvalid, AsObject, and AsArray signatures
    - $(go env GOMODCACHE)/github.com/amikos-tech/pure-simdjson@v0.1.4/iterator.go — iterator terminal-error behavior
  </read_first>
  <action>
    Add the exact locked dependency with `go get github.com/amikos-tech/pure-simdjson@v0.1.4`, then run `go mod tidy`; never hand-edit go.sum. Require the direct v0.1.4 line to remain and let Go record `ebitengine/purego` and `x/sys` transitives. The Phase 21 Package Legitimacy Audit marks this same-organization, commit-anchored Go module approved; no npm/PyPI legitimacy checkpoint applies.

    Create root `parser_simd.go` with exact first line `//go:build simdjson`, then a blank line and `package gin`. Define `simdParserName = "pure-simdjson"`, an unexported simdParser holding `*purejson.Parser`, `Name`, the `var _ Parser = (*simdParser)(nil)` assertion, and parameter-free `NewSIMDParser() (Parser, error)`. The constructor delegates only to `purejson.NewParser()` and wraps failure with the locked message `initialize pure-simdjson SIMD parser; set PURE_SIMDJSON_LIB_PATH or see docs/simd-deployment.md`. Keep the message as a local constant, add no success log, and add no fallback or CLI flag per D-04.

    Implement Parse with parser-local error context matching parser_stdlib.go: wrap upstream Parse failure once as `failed to parse JSON`; do not pre-scan BIGINT input per D-05. After a successful upstream Parse, arrange a single deferred `doc.Close()` before BeginDocument/walk returns. The deferred closure must run for both success and walker errors. If Close alone fails, return it wrapped as `close pure-simdjson document`; if walking and closing both fail, return the close failure with the prior walk error included as context so the lifecycle failure is not silently discarded. Then call sink.BeginDocument exactly once and walk doc.Root at raw path `$`. This addresses the review's ErrParserBusy blocker while leaving parser-handle cleanup to upstream's locked constructor/API design.

    Implement a recursive `walkElement(element, rawPath, state, sink)` that normalizes rawPath and calls ShouldBufferForTransform before inspecting every element kind, including object/array nodes and every indexed and wildcard array traversal. On a transform match, call a local `materializeElement` and pass its result to `StageMaterialized(state, rawPath, value, true)`; use the raw path exactly as parser_stdlib does. Otherwise switch strictly once on Element.Type per D-02a. Route string/bool/int64/uint64/float64 through their matching accessor and typed sink method, null through StageScalar(nil), and return accessor errors with type and canonical-path context. Handle TypeInvalid and unknown values explicitly with `errors.Errorf`. AsObject and AsArray return values plus errors; check those errors, check iterator Err after every loop, and wrap iterator/container errors with the canonical path. Return sink errors unchanged because GINBuilder already tags their stage provenance.

    For typed object walking, MarkPresent before children, collect iterator entries into `map[string]purejson.Element` so later duplicate occurrences replace earlier ones, sort the surviving keys, then recurse with the exact stdlib raw expression `rawPath+"."+key`. This resolves duplicate-key last-write-wins parity. It also resolves the path-escaping review by deliberately mirroring the live stdlib expression rather than introducing a different JSONPath policy in only one parser. For arrays, MarkPresent first, preserve iterator order, and recurse on both `fmt.Sprintf("%s[%d]", rawPath, i)` and `rawPath+"[*]"`; each recursive call performs its own transform check.

    Implement `materializeElement` as the D-03 whole-subtree fallback with stdlib-compatible dynamic values. Return nil/string/bool directly. Return int64 and uint64 tags as `json.Number` built from decimal `strconv.FormatInt`/`FormatUint`, not native integers, so StageMaterialized and prepareTransformerValue see the same shape as decodeAny with UseNumber. For TypeFloat64, reject NaN/Inf defensively, format with `strconv.FormatFloat(v, 'g', -1, 64)`, and append `.0` when the formatted text contains none of `.eE`; wrap it as json.Number. This preserves the TypeFloat64 lexeme class for 1.0 instead of re-entering stagedNumericFromValue's whole-float fold while still preparing transformer input as float64 like stdlib. Materialize arrays to []any and objects to map[string]any with iterator-order assignment so duplicate keys are last-write-wins; check AsObject/AsArray/accessor/iterator errors and handle TypeInvalid/default explicitly. Do not use raw int64/uint64/float64 values in this fallback.

    Keep all pure-simdjson imports in this file. Do not add tagged runtime parity tests, benchmarks, cross-platform CI, distribution verification, or CLI activation; ROADMAP assigns those evidence tasks to Phase 22, while this task must ship the complete D-02/D-03 semantics they will exercise.
  </action>
  <verify>
    <automated>go mod tidy &amp;&amp; rg -q '^\s*github\.com/amikos-tech/pure-simdjson v0\.1\.4$' go.mod &amp;&amp; test "$(sed -n '1p' parser_simd.go)" = '//go:build simdjson' &amp;&amp; go build -tags simdjson ./... &amp;&amp; go vet -tags simdjson ./... &amp;&amp; go test -tags simdjson -run '^$' ./...</automated>
  </verify>
  <acceptance_criteria>
    - go.mod contains exact direct pin github.com/amikos-tech/pure-simdjson v0.1.4 and go.sum contains v0.1.4 plus transitive hashes after tidy.
    - parser_simd.go's first line is exactly `//go:build simdjson`; NewSIMDParser has the locked signature/message and Name returns exactly `pure-simdjson`.
    - Parse closes every successfully parsed Doc on success and walker-error paths and propagates a Close error; no second Parse can remain blocked solely because this adapter leaked the prior Doc.
    - walkElement checks ShouldBufferForTransform before every type switch, handles AsObject/AsArray and iterator errors, and has explicit TypeInvalid/default errors.
    - Direct objects collapse duplicate keys last-write-wins and sort surviving keys; raw object and array paths use the same expressions as parser_stdlib.go.
    - materializeElement represents every numeric tag as json.Number, forces TypeFloat64 whole values to retain a dot/exponent marker, and never returns native numeric values.
    - `go build`, `go vet`, and compile-only `go test` with `-tags simdjson` pass without invoking NewSIMDParser or requiring a native shared library.
    - No CLI flag, fallback, runtime parity test, benchmark, or CI workflow is added.
  </acceptance_criteria>
  <done>The exact pin and tagged adapter compile with reusable-Doc lifecycle, strict typed routing, transform-safe numbers, and last-key-wins object semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Prove source-tag and default dependency-graph isolation</name>
  <files>Makefile</files>
  <read_first>
    - Makefile — per-target .PHONY declarations, POSIX shell style, and manually maintained help output
    - parser_simd.go — the expected tagged importer created by Task 1
    - .planning/spikes/001-pure-simdjson-api-verification/main.go — untagged isolated-module importer that must be excluded from the product scan
    - .planning/phases/21-simd-parser-adapter/21-REVIEWS.md consensus isolation findings
    - .planning/phases/21-simd-parser-adapter/21-RESEARCH.md V8 and Common Pitfall 5
  </read_first>
  <action>
    Add `.PHONY: simd-isolation-check`, the target, and a matching line in the existing manual `help` output. Address both reviewers' isolation findings with two independent assertions.

    First, inspect tracked product Go files from `git ls-files '*.go' ':(exclude).planning/**'`; this deliberately excludes the isolated Spike 001 module that imports pure-simdjson without the product tag. For every file containing the exact import path `github.com/amikos-tech/pure-simdjson`, require its first line to equal exactly `//go:build simdjson`; reject broader/tautological expressions such as `simdjson || !simdjson`, print the offending file, and require at least one tagged importer so the check cannot pass vacuously. Do not scan docs, .git, module caches, or planning prose.

    Second, run `go list -deps -test ./...` without tags and fail if any output line equals `github.com/amikos-tech/pure-simdjson`; use an `if grep -Fxq ...; then ... exit 1; fi` form so absence succeeds under POSIX make shells. Follow with untagged `go build ./...` and `go vet ./...`. This proves the default package and test graph excludes the dependency rather than relying on source text alone. Keep the target standalone for Phase 22 CI consumption; do not alter existing test/lint target dependencies.
  </action>
  <verify>
    <automated>make simd-isolation-check &amp;&amp; make help | rg -q '^  simd-isolation-check '</automated>
  </verify>
  <acceptance_criteria>
    - Makefile declares simd-isolation-check phony and help lists it.
    - The source scan excludes `.planning/**`, requires at least one importer, and accepts only the exact first-line `//go:build simdjson` constraint for every product importer.
    - The target uses `go list -deps -test ./...` with an exact-line negative match for github.com/amikos-tech/pure-simdjson.
    - The target runs untagged go build and go vet, and `make simd-isolation-check` exits zero in the completed tree.
    - The target would print a file and exit non-zero for any tracked product importer without the exact tag; Spike 001 is not reported.
  </acceptance_criteria>
  <done>The default source and dependency graph are both guarded from accidental SIMD linkage, without false-positive scanning the isolated spike.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted JSON -> native parser | Document bytes cross into the runtime-loaded simdjson implementation |
| Native Doc -> Go adapter | Typed views remain valid only until Doc.Close and must not leak across parses |
| Adapter -> builder sink | Numeric, duplicate-key, transform, path, and stage-error semantics determine index correctness |
| Build graph -> optional module | Default builds must exclude the tagged wrapper and native bootstrap path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-08 | Denial of Service | Leaked upstream Doc / ErrParserBusy | mitigate | Close every successful Parse result on all return paths and propagate Close failures |
| T-21-09 | Tampering | Transform numeric coercion | mitigate | Canonical json.Number materializer preserves int/float class and stdlib transformer preparation |
| T-21-10 | Tampering | Duplicate object keys | mitigate | Last-write-wins maps in direct walk and materializer match encoding/json behavior |
| T-21-11 | Tampering | Default build isolation | mitigate | Exact source-tag guard plus default go-list dependency-graph absence check |
| T-21-12 | Information Disclosure | Native/parser errors | accept | Context contains parser operation and canonical path only; raw document bytes are not appended by adapter wrappers |
| T-21-SC | Tampering | Go module/native supply chain | mitigate | Exact v0.1.4 tag + go.sum for wrapper; Phase 19 legitimacy audit; upstream SHA-256 bootstrap and explicit-path operator trust documented by Plan 02 |
</threat_model>

<verification>
- `go test -run '^TestTypedSink|^TestParserParity_AuthoredFixtures' -count=1 .`
- `go build ./... && go vet ./...`
- `go build -tags simdjson ./... && go vet -tags simdjson ./...`
- `go test -tags simdjson -run '^$' ./...`
- `make simd-isolation-check`
- `make test`
</verification>

<success_criteria>
- Tagged code exposes the exact locked constructor/name and integrates through WithParser only.
- A reusable simdParser releases each Doc, routes leaves strictly by Type, preserves transform numeric semantics, and matches stdlib duplicate-key/path behavior by construction.
- Default build/test dependency graphs contain no pure-simdjson package, and all product importers carry the exact tag.
- D-04/D-05 boundaries hold: no CLI flag, no silent fallback, and no BIGINT pre-scan.
</success_criteria>

<output>
Create `.planning/phases/21-simd-parser-adapter/21-03-SUMMARY.md` when done.
</output>
