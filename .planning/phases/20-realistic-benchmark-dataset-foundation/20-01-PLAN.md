---
phase: 20
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - testdata/phase20/generate.go
  - testdata/phase20/README.md
  - testdata/phase20/nested_high_cardinality.jsonl
  - testdata/phase20/mixed_type_arrays.jsonl
  - testdata/phase20/number_heavy.jsonl
  - testdata/phase20/combined.jsonl
  - benchmark_test.go
autonomous: true
requirements: [DATA-01, DATA-02]
tags: [fixtures, benchmark, jsonl, provenance]

must_haves:
  truths:
    - "Four bounded, deterministic, synthesized JSONL smoke fixtures are committed and usable without network access or an upstream download."
    - "The committed corpus covers nested/high-cardinality values, mixed wildcard-array value kinds, and literal number-boundary/range cases across more than one 32-document row group."
    - "combined.jsonl preserves a 96-cycle nested/array/number round-robin of the three focused fixtures as raw JSONL records."
    - "Fixture provenance documents source inspiration, license/NOTICE handling, size limits, and the offline default without making Markdown formatting an executable test contract."
  artifacts:
    - path: "testdata/phase20/generate.go"
      provides: "Deterministic refresh-only writer for the four committed JSONL fixture files"
      contains: "phase20_source_kind"
    - path: "testdata/phase20/README.md"
      provides: "Phase-20 fixture policy, provenance, limits, and optional-local boundary"
      contains: "synthesized-from-shape"
    - path: "testdata/phase20/combined.jsonl"
      provides: "Interleaved end-to-end smoke stream"
    - path: "benchmark_test.go"
      provides: "Focused Phase-20 raw-JSONL loader and integrity test"
      contains: "TestPhase20SmokeFixtures"
  key_links:
    - from: "testdata/phase20/generate.go"
      to: "testdata/phase20/*.jsonl"
      via: "explicit go run refresh command; checked-in JSONL remains the default consumer input"
      pattern: "combined.jsonl"
    - from: "TestPhase20SmokeFixtures"
      to: "testdata/phase20/*.jsonl"
      via: "JSON validity, row-count, cap, raw interleaving, and semantic-shape assertions"
      pattern: "json.Valid"
---

<objective>
Create a compact, governed Phase-20 JSONL corpus and prove the offline fixture contract before benchmark wiring.

Purpose: satisfy DATA-01 and DATA-02 using the established Phase-11 checked-in-fixture pattern. The JSONL files are the default source of truth; the refresh helper only regenerates those checked-in files. Do not add downloading, SIMD parser work, CI changes, parser-selection changes, or Phase-13 golden changes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-realistic-benchmark-dataset-foundation/20-CONTEXT.md
@.planning/phases/20-realistic-benchmark-dataset-foundation/20-RESEARCH.md
@.planning/phases/20-realistic-benchmark-dataset-foundation/20-PATTERNS.md
@.planning/phases/20-realistic-benchmark-dataset-foundation/20-VALIDATION.md
@.planning/phases/20-realistic-benchmark-dataset-foundation/20-REVIEWS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Generate and document the bounded synthesized JSONL corpus</name>
  <files>testdata/phase20/generate.go, testdata/phase20/README.md, testdata/phase20/nested_high_cardinality.jsonl, testdata/phase20/mixed_type_arrays.jsonl, testdata/phase20/number_heavy.jsonl, testdata/phase20/combined.jsonl</files>
  <read_first>
    - AGENTS.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/20-realistic-benchmark-dataset-foundation/20-CONTEXT.md
    - .planning/phases/20-realistic-benchmark-dataset-foundation/20-RESEARCH.md
    - .planning/phases/20-realistic-benchmark-dataset-foundation/20-PATTERNS.md
    - .planning/phases/20-realistic-benchmark-dataset-foundation/20-REVIEWS.md
    - testdata/phase11/README.md
    - testdata/phase11/github_archive_smoke.jsonl
    - parser_parity_fixtures_test.go
    - cmd/gin-index/experiment.go
    - testdata/phase20/generate.go
    - testdata/phase20/README.md
    - testdata/phase20/nested_high_cardinality.jsonl
    - testdata/phase20/mixed_type_arrays.jsonl
    - testdata/phase20/number_heavy.jsonl
    - testdata/phase20/combined.jsonl
  </read_first>
  <action>
    Create `testdata/phase20/generate.go` as the sole refresh helper for the four fixture files. Running `go run ./testdata/phase20/generate.go` from the repository root must deterministically overwrite only `testdata/phase20/nested_high_cardinality.jsonl`, `mixed_type_arrays.jsonl`, `number_heavy.jsonl`, and `combined.jsonl`; it must not fetch data, read environment variables, or write outside `testdata/phase20/`. Commit the generated JSONL files and treat them, not generator output at test time, as the input consumed by default tests and benchmarks.

    Generate exactly 96 JSON objects in each focused file and exactly 288 JSON objects in `combined.jsonl`. Every committed line must be one nonblank standalone JSON object. Generate numeric literals directly into JSON text so `9223372036854775807`, `-9223372036854775807`, and `9007199254740993` remain unquoted number lexemes after regeneration. Limit every focused file to 128 KiB and `combined.jsonl` to 384 KiB.

    Set the top-level `phase20_source_kind` on every focused record to exactly `nested-high-cardinality`, `mixed-type-arrays`, or `number-heavy`. Generate `combined.jsonl` by writing raw generated record bytes in 96 cycles: focused nested record `i`, mixed-array record `i`, then number-heavy record `i`. Do not produce a fourth schema or concatenate the three files. This contract gives every focused fixture three row groups and the combined fixture nine when `phase20DocsPerRowGroup` is 32.

    Make nested records include repeated top-level event categories plus nested actor/user, repository, URL, timestamp, and searchable-text fields; vary IDs, users, repositories, URLs, and text across the 96 records. Make mixed-array records use one repeated array family containing string, integer and decimal number, boolean, null, object, and empty-array cases under the same wildcard path. Make number-heavy records include the three stated integer literals, decimal literals, RFC3339 and epoch fields, and repeatable low/mid/high numeric range cohorts. Do not emit numbers outside the current parser's accepted range.

    Create `testdata/phase20/README.md` in the compact style of `testdata/phase11/README.md`. Include a fixture table with paths, `synthesized-from-shape` origin, document counts, current byte counts, and byte caps; byte counts are provenance metadata, not constants parsed by tests. State that simdjson `jsonexamples` is structural inspiration, no upstream rows or text are copied, committed fixtures are the offline default, and tests/benchmarks do not download data. State that any future direct upstream redistribution requires a license/NOTICE review at a chosen pinned revision, link simdjson LICENSE and LICENSE-MIT as review points, and document exactly `GIN_PHASE20_ENABLE_SIMDJSON_EXTERNAL=1` plus `GIN_PHASE20_SIMDJSON_DIR=/path/to/simdjson/jsonexamples`. Do not vendor upstream license files or data.
  </action>
  <verify>
    <automated>go run ./testdata/phase20/generate.go &amp;&amp; git diff --exit-code -- testdata/phase20/nested_high_cardinality.jsonl testdata/phase20/mixed_type_arrays.jsonl testdata/phase20/number_heavy.jsonl testdata/phase20/combined.jsonl &amp;&amp; rg -n 'synthesized-from-shape|GIN_PHASE20_ENABLE_SIMDJSON_EXTERNAL|GIN_PHASE20_SIMDJSON_DIR|LICENSE|NOTICE|offline' testdata/phase20/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `go run ./testdata/phase20/generate.go` exits 0 and leaves the four committed JSONL files unchanged on a second run.
    - The four named fixture files exist, every line is nonblank valid JSON, each focused file has 96 records and is at most 131072 bytes, and `combined.jsonl` has 288 records and is at most 393216 bytes.
    - The focused streams contain the required nested/high-cardinality, mixed-array, and numeric boundary/range data, including unquoted `9223372036854775807`, `-9223372036854775807`, and `9007199254740993` literals.
    - `combined.jsonl` contains raw record copies from the focused streams in the 96 repeating nested/array/number cycles.
    - `testdata/phase20/README.md` documents source inspiration, synthesized provenance, current counts and caps, offline/no-download behavior, the future pinned-revision LICENSE/NOTICE review rule, and both Phase-20 external variables.
  </acceptance_criteria>
  <done>The repository contains deterministic, bounded, provenance-documented realistic JSONL fixtures that can be refreshed without hand-authoring hundreds of records.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add raw JSONL loading and fixture-integrity coverage</name>
  <files>benchmark_test.go</files>
  <read_first>
    - AGENTS.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/20-realistic-benchmark-dataset-foundation/20-CONTEXT.md
    - .planning/phases/20-realistic-benchmark-dataset-foundation/20-RESEARCH.md
    - .planning/phases/20-realistic-benchmark-dataset-foundation/20-PATTERNS.md
    - .planning/phases/20-realistic-benchmark-dataset-foundation/20-REVIEWS.md
    - benchmark_test.go
    - testdata/phase20/README.md
    - testdata/phase20/generate.go
    - testdata/phase20/nested_high_cardinality.jsonl
    - testdata/phase20/mixed_type_arrays.jsonl
    - testdata/phase20/number_heavy.jsonl
    - testdata/phase20/combined.jsonl
    - testdata/phase11/README.md
    - parser_parity_fixtures_test.go
    - cmd/gin-index/experiment.go
  </read_first>
  <action>
    In `benchmark_test.go`, add Phase-20-scoped fixture descriptors and raw-document helpers; do not generalize the Phase-11 typed corpus code. Define `phase20DocsPerRowGroup = 32`, the four stable repository-relative fixture paths, expected record counts, byte caps, source-kind labels, and fixed smoke-query metadata. Do not duplicate exact byte counts in Go constants and do not parse the Phase-20 README Markdown table.

    Add a local-file JSONL reader returning `[][]byte`. It must reject blank lines in committed Phase-20 fixtures, validate each retained raw line with `json.Valid`, preserve raw bytes for future `Builder.AddDocument` use, and report the cleaned fixture path plus one-based line number for malformed input. Use `github.com/pkg/errors` for new error context in new helpers.

    Add `TestPhase20SmokeFixtures` as a table-driven integrity test over all four descriptors. Assert file existence, record count, byte cap, no blank lines, JSON validity, and more than one packed row group using `phase20DocsPerRowGroup`. Decode only for semantic inspection: verify nested fields, repeated categories, multiple users/repos/URLs, and searchable text; verify every required mixed-array kind under the same array family; verify the numeric literals, decimal, timestamp/epoch, and low/mid/high cohorts. For `combined.jsonl`, compare each raw line byte-for-byte with the corresponding focused record at its cycle position and then verify the decoded `phase20_source_kind` sequence. This must reject both a simple concatenation and a combined file whose decoded labels happen to match but whose raw records drift.

    Assert the README contains policy markers for `synthesized-from-shape`, simdjson `jsonexamples`, no copied upstream rows/text, offline/no-download behavior, the pinned-revision LICENSE/NOTICE review rule, and both external variable names. Treat this as token presence only; table layout, prose wrapping, current-byte metadata, and Markdown formatting must not affect the Go test. Keep benchmark registration, external-directory discovery, environment reads, downloading, parser changes, SIMD build tags, CI edits, and Phase-13 golden changes out of this task.
  </action>
  <verify>
    <automated>go test ./... -run '^TestPhase20SmokeFixtures$' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `benchmark_test.go` defines `phase20DocsPerRowGroup` as `32`, descriptors for the four fixtures, a raw `[][]byte` JSONL loader, and `TestPhase20SmokeFixtures`.
    - The loader reports fixture path and one-based line number for a malformed or blank committed JSONL line and keeps valid documents as raw bytes.
    - The integrity test enforces record counts, byte caps, JSON validity, semantic shape coverage, and more than one row group without parsing README table cells or asserting duplicated exact byte constants.
    - The integrity test proves the combined stream's raw records are the prescribed 96 nested/array/number cycles rather than a concatenation or independently regenerated lookalike records.
    - The policy-marker checks verify required provenance and offline wording but remain insensitive to Markdown table formatting and documented byte-count changes.
    - `go test ./... -run '^TestPhase20SmokeFixtures$' -count=1` exits 0.
  </acceptance_criteria>
  <done>The checked-in corpus is protected from malformed input, size growth, semantic-shape loss, and interleaving drift without coupling test correctness to README formatting.</done>
</task>

</tasks>

## Truths — Decision Traceability

- D-01: Task 1 establishes the hybrid policy: checked-in synthesized smoke fixtures are required, while exact upstream simdjson examples remain optional local inputs.
- D-02: Task 1 documents simdjson structural inspiration while prohibiting copied upstream rows or text in the committed smoke fixtures.
- D-03: Tasks 1 and 2 make the committed JSONL corpus the offline default and prohibit downloading during default tests and smoke benchmarks.
- D-04: Tasks 1 and 2 require provenance markers covering source inspiration, synthesized origin, counts and caps, license/NOTICE review, and the optional external variables.
- D-05: Task 1 documents that any optional exact upstream simdjson input is local-path and explicitly enabled only; it is never downloaded or vendored.
- D-06: Task 1 creates three focused checked-in JSONL fixtures plus one checked-in combined fixture.
- D-07: Task 1 gives `nested_high_cardinality.jsonl` repeated event categories and varied nested actor/user, repository, URL, timestamp, and searchable-text fields.
- D-08: Task 1 gives `mixed_type_arrays.jsonl` a repeated wildcard array family containing strings, numbers, booleans, nulls, objects, and empty arrays.
- D-09: Task 1 gives `number_heavy.jsonl` int64 and exact-float-boundary literals, decimals, RFC3339 and epoch fields, and repeated numeric range cohorts.
- D-10: Task 1 creates `combined.jsonl` as raw nested/array/number round-robin cycles, while Task 2 rejects concatenation or record drift.
- D-11: Task 1 adds a deterministic refresh helper while retaining checked-in JSONL as the source consumed by default tests and smoke benchmarks.

<verification>
Run after the plan tasks:

```bash
go run ./testdata/phase20/generate.go
git diff --exit-code -- testdata/phase20/nested_high_cardinality.jsonl testdata/phase20/mixed_type_arrays.jsonl testdata/phase20/number_heavy.jsonl testdata/phase20/combined.jsonl
go test ./... -run '^TestPhase20SmokeFixtures$' -count=1
go test ./... -count=1
```
</verification>

<success_criteria>
- DATA-01 is satisfied by a Phase-20 fixture policy covering synthesized provenance, size limits, license/NOTICE handling, and offline default behavior.
- DATA-02 is satisfied by three focused JSONL streams plus a raw-interleaved combined stream and structural integrity coverage.
- No downloader, upstream fixture rows, SIMD implementation, parser change, CI edit, or Phase-13 golden refresh is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/20-realistic-benchmark-dataset-foundation/20-01-SUMMARY.md`.
</output>
