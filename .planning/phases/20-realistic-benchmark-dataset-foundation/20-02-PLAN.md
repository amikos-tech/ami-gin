---
phase: 20
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - benchmark_test.go
  - README.md
autonomous: true
requirements: [DATA-01, DATA-03]
tags: [benchmarks, offline, fixtures, documentation]

must_haves:
  truths:
    - "BenchmarkPhase20RealisticJSON runs Build, Encode, and Query actions for each checked-in fixture without network access or Phase-20 external environment variables."
    - "An external local-input tier is registered unconditionally but reads a directory only when GIN_PHASE20_ENABLE_SIMDJSON_EXTERNAL equals 1."
    - "The external Query action derives a valid query from the discovered local documents rather than assuming an upstream schema."
    - "README commands select existing smoke and local-external benchmark leaves and explain the offline/default versus manual-local workflow."
  artifacts:
    - path: "benchmark_test.go"
      provides: "Offline Phase-20 smoke benchmark and explicitly gated local external tier"
      contains: "BenchmarkPhase20RealisticJSON"
    - path: "README.md"
      provides: "Contributor-facing offline and optional-local Phase-20 benchmark commands"
      contains: "GIN_PHASE20_ENABLE_SIMDJSON_EXTERNAL"
  key_links:
    - from: "BenchmarkPhase20RealisticJSON/tier=smoke"
      to: "testdata/phase20/*.jsonl"
      via: "Plan-01 raw JSONL loader and descriptor paths"
      pattern: "fixture=combined"
    - from: "GIN_PHASE20_ENABLE_SIMDJSON_EXTERNAL"
      to: "GIN_PHASE20_SIMDJSON_DIR"
      via: "exact enable gate before local-directory lookup"
      pattern: "fixture=local-example"
    - from: "README.md"
      to: "BenchmarkPhase20RealisticJSON"
      via: "anchored smoke selector and direct local-example selector"
      pattern: "tier=external/fixture=local-example"
---

<objective>
Wire the committed Phase-20 corpus into an offline smoke benchmark, provide a strictly opt-in local external-data tier, and document both workflows.

Purpose: satisfy DATA-03 and finish the contributor-facing part of DATA-01. Reuse the Phase-11 local-file and `b.Skipf` pattern, but use data-derived query selection for arbitrary local inputs and avoid a second benchmark framework.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-realistic-benchmark-dataset-foundation/20-CONTEXT.md
@.planning/phases/20-realistic-benchmark-dataset-foundation/20-RESEARCH.md
@.planning/phases/20-realistic-benchmark-dataset-foundation/20-PATTERNS.md
@.planning/phases/20-realistic-benchmark-dataset-foundation/20-VALIDATION.md
@.planning/phases/20-realistic-benchmark-dataset-foundation/20-REVIEWS.md
@.planning/phases/20-realistic-benchmark-dataset-foundation/20-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add offline smoke and explicitly gated local benchmark tiers</name>
  <files>benchmark_test.go</files>
  <read_first>
    - AGENTS.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/20-realistic-benchmark-dataset-foundation/20-CONTEXT.md
    - .planning/phases/20-realistic-benchmark-dataset-foundation/20-RESEARCH.md
    - .planning/phases/20-realistic-benchmark-dataset-foundation/20-PATTERNS.md
    - .planning/phases/20-realistic-benchmark-dataset-foundation/20-REVIEWS.md
    - .planning/phases/20-realistic-benchmark-dataset-foundation/20-01-PLAN.md
    - benchmark_test.go
    - testdata/phase20/README.md
    - testdata/phase20/nested_high_cardinality.jsonl
    - testdata/phase20/mixed_type_arrays.jsonl
    - testdata/phase20/number_heavy.jsonl
    - testdata/phase20/combined.jsonl
    - parser_parity_fixtures_test.go
  </read_first>
  <action>
    Extend the Plan-01 helpers in `benchmark_test.go`; do not modify Phase-11 corpus types, projections, gzip rules, or serializer metrics. Add `BenchmarkPhase20RealisticJSON` with smoke fixture nodes named `tier=smoke/fixture=nested-high-cardinality`, `tier=smoke/fixture=mixed-type-arrays`, `tier=smoke/fixture=number-heavy`, and `tier=smoke/fixture=combined`. Under every fixture node, add exactly named `Build`, `Encode`, and `Query` action leaves.

    For smoke actions, load the committed raw JSONL documents through the Plan-01 loader, use `phase20DocsPerRowGroup` and `DocID(docIndex/phase20DocsPerRowGroup)` for `Builder.AddDocument`, and complete loading/index construction outside timed loops. Use `b.ReportAllocs()` and `b.ResetTimer()` in each timed leaf. `Build` measures building from the preloaded raw documents; `Encode` measures the existing encode API against a finalized index; `Query` evaluates its descriptor's fixed representative predicate once before timing and fails if candidate count is zero. Do not add Phase-11 prefix-compression projections or metrics.

    Define constants exactly named `GIN_PHASE20_ENABLE_SIMDJSON_EXTERNAL` and `GIN_PHASE20_SIMDJSON_DIR`. Register `tier=external/fixture=local-example` and its `Build`, `Encode`, and `Query` leaves unconditionally. At the start of each action path, call a shared gate that checks `os.Getenv("GIN_PHASE20_ENABLE_SIMDJSON_EXTERNAL") == "1"` before reading, cleaning, statting, or otherwise accessing `GIN_PHASE20_SIMDJSON_DIR`; any unset or non-`1` value must call `b.Skipf` with guidance naming both variables. Do not hide the gate around parent benchmark registration.

    After enablement, require a nonempty existing local directory and deterministically discover only sorted top-level `*.ndjson`, `*.jsonl`, and `*.json` files. Read NDJSON/JSONL as nonblank `json.Valid` documents and each `.json` file as one `json.Valid` document. Reject an empty directory, unsupported files only, invalid JSON, and unreadable inputs with errors naming both variables and accepted formats. Do not recurse, run shell commands, fetch URLs, download simdjson, or use local external files in smoke actions.

    Build the external Query predicate from the loaded raw documents: inspect documents in sorted file and line order, select the first non-null scalar leaf with a supported JSONPath, and create an `IsNotNull` predicate for that path. Evaluate it once after index construction and fail only when no candidate exists or no supported scalar leaf can be selected. This makes the local tier schema-independent while keeping its query action meaningful.

    Add `TestPhase20ExternalTier` with `t.TempDir()` and `t.Setenv()`. Unit-test the shared gate/discovery helpers: unset and `true` enable values must return the disabled result without reading or requiring the directory variable; only `1` enables discovery. In enabled mode, verify error messages for absent, empty, unsupported, and invalid local input name both variables; verify a temporary valid `.jsonl` and a temporary valid `.json` are accepted in sorted path order; and verify a derived scalar-path query is available from a valid fixture. Do not spawn `go test` subprocesses from this test and do not depend on a real simdjson checkout. Use the direct benchmark commands in the plan verification to prove registered leaves and skip behavior.
  </action>
  <verify>
    <automated>go test ./... -run '^TestPhase20ExternalTier$' -count=1 &amp;&amp; env -u GIN_PHASE20_ENABLE_SIMDJSON_EXTERNAL -u GIN_PHASE20_SIMDJSON_DIR go test ./... -run '^$' -bench '^BenchmarkPhase20RealisticJSON/tier=smoke' -benchtime=1x -count=1 -benchmem &amp;&amp; env -u GIN_PHASE20_SIMDJSON_DIR GIN_PHASE20_ENABLE_SIMDJSON_EXTERNAL=true go test ./... -run '^$' -bench 'BenchmarkPhase20RealisticJSON/tier=external/fixture=local-example' -benchtime=1x -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `BenchmarkPhase20RealisticJSON` exposes four smoke fixtures and `Build`, `Encode`, and `Query` leaves under each fixture.
    - Smoke leaves use only committed Phase-20 paths, report allocations, keep setup outside timed loops, and run successfully with both external variables absent.
    - The external local-example `Build`, `Encode`, and `Query` leaves are registered even when external input is disabled; the disabled command emits those leaves as skipped and succeeds without `GIN_PHASE20_SIMDJSON_DIR`.
    - The external gate enables discovery if and only if `GIN_PHASE20_ENABLE_SIMDJSON_EXTERNAL` is exactly `1`, and no disabled code path accesses the directory variable.
    - Enabled discovery accepts only sorted top-level `.ndjson`, `.jsonl`, and `.json` files and returns actionable errors naming both variables for invalid input without downloading data.
    - The external Query action selects an `IsNotNull` predicate from an observed non-null scalar leaf and validates a nonzero candidate before timing; it does not assume a smoke-fixture field exists in arbitrary local data.
    - `go test ./... -run '^TestPhase20ExternalTier$' -count=1`, the smoke benchmark command, and the disabled external benchmark command in `<verify>` all exit 0.
  </acceptance_criteria>
  <done>The Phase-20 benchmark family measures committed realistic data offline and exposes a visible, deterministic, local-only external tier without flaky nested test invocations.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Document the offline and manual-local benchmark workflows</name>
  <files>README.md</files>
  <read_first>
    - AGENTS.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/20-realistic-benchmark-dataset-foundation/20-CONTEXT.md
    - .planning/phases/20-realistic-benchmark-dataset-foundation/20-RESEARCH.md
    - .planning/phases/20-realistic-benchmark-dataset-foundation/20-PATTERNS.md
    - .planning/phases/20-realistic-benchmark-dataset-foundation/20-REVIEWS.md
    - README.md
    - benchmark_test.go
    - testdata/phase20/README.md
    - testdata/phase11/README.md
  </read_first>
  <action>
    Add `### Phase 20 Realistic JSON Workflow` next to the existing Phase-11 real-corpus workflow in `README.md`. State that the default corpus is the four checked-in synthesized `testdata/phase20/*.jsonl` fixtures and runs offline without downloading data.

    Document this smoke command exactly: `go test ./... -run '^$' -bench '^BenchmarkPhase20RealisticJSON/tier=smoke' -benchtime=1x -count=1 -benchmem`. Explain that `-benchtime=1x -count=1` provides one reproducible pass over the finite prepared fixture work.

    Document this manual-local command exactly: `GIN_PHASE20_ENABLE_SIMDJSON_EXTERNAL=1 GIN_PHASE20_SIMDJSON_DIR=/path/to/simdjson/jsonexamples go test ./... -run '^$' -bench 'BenchmarkPhase20RealisticJSON/tier=external/fixture=local-example' -benchtime=1x -count=1 -benchmem`. State that it selects the registered Build, Encode, and Query leaves; that local input may be top-level `.ndjson`, `.jsonl`, or single-document `.json`; and that the Query action derives a scalar-path presence query from the local input rather than expecting a particular simdjson example schema.

    State that this repository neither downloads simdjson data nor vendors copied upstream example rows. State that a future direct redistribution requires a pinned-revision LICENSE/NOTICE review. Do not describe the workflow as SIMD parser selection, a parser parity result, a Phase-11 prefix-compression benchmark, or a CI requirement.
  </action>
  <verify>
    <automated>rg -n "Phase 20 Realistic JSON Workflow|BenchmarkPhase20RealisticJSON/tier=smoke|BenchmarkPhase20RealisticJSON/tier=external/fixture=local-example|GIN_PHASE20_ENABLE_SIMDJSON_EXTERNAL|GIN_PHASE20_SIMDJSON_DIR|download" README.md &amp;&amp; go test ./... -run '^TestPhase20' -count=1 &amp;&amp; env -u GIN_PHASE20_ENABLE_SIMDJSON_EXTERNAL -u GIN_PHASE20_SIMDJSON_DIR go test ./... -run '^$' -bench '^BenchmarkPhase20RealisticJSON/tier=smoke' -benchtime=1x -count=1 -benchmem</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `### Phase 20 Realistic JSON Workflow` adjacent to the Phase-11 workflow and identifies the four checked-in fixtures as synthesized, offline, no-download defaults.
    - `README.md` contains the exact smoke command and direct local-example benchmark selector specified in `<action>`.
    - The external documentation names both variables, accepted local file formats, and the data-derived external query behavior.
    - The documentation states that direct future upstream redistribution requires a pinned-revision LICENSE/NOTICE review.
    - `go test ./... -run '^TestPhase20' -count=1` and the documented smoke command exit 0 with external variables absent.
  </acceptance_criteria>
  <done>Contributors can run the default realistic benchmark offline and deliberately opt into local external examples without ambiguity about downloads, fixture provenance, or query schema.</done>
</task>

</tasks>

## Truths — Decision Traceability

- D-12: Task 1 extends the existing Phase-11 tier pattern in `benchmark_test.go` instead of introducing a standalone Phase-20 fixture system.
- D-13: Task 1 provides a checked-in offline smoke tier and an explicitly gated optional local external tier.
- D-14: Tasks 1 and 2 use and document the Phase-20-specific `GIN_PHASE20_ENABLE_SIMDJSON_EXTERNAL` and `GIN_PHASE20_SIMDJSON_DIR` variables.
- D-15: Tasks 1 and 2 keep the workflow simple: the small smoke benchmark always works, while richer exact upstream data requires a deliberate local command.
- D-16: Task 1's Build, Encode, and Query leaves make the corpus reusable by Phase 22 without adding a parser choice, SIMD parity, or SIMD comparison in Phase 20.

<verification>
Run after the plan tasks:

```bash
go test ./... -run '^TestPhase20' -count=1
env -u GIN_PHASE20_ENABLE_SIMDJSON_EXTERNAL -u GIN_PHASE20_SIMDJSON_DIR go test ./... -run '^$' -bench '^BenchmarkPhase20RealisticJSON/tier=smoke' -benchtime=1x -count=1 -benchmem
env -u GIN_PHASE20_SIMDJSON_DIR GIN_PHASE20_ENABLE_SIMDJSON_EXTERNAL=true go test ./... -run '^$' -bench 'BenchmarkPhase20RealisticJSON/tier=external/fixture=local-example' -benchtime=1x -count=1 -v
go test ./... -count=1
```
</verification>

<success_criteria>
- DATA-03 is satisfied: default Phase-20 smoke benchmark leaves use only checked-in fixtures and run without external environment input or network access.
- DATA-01 is contributor-visible: documentation distinguishes synthesized committed fixtures from optional local external examples and records the license/NOTICE rule.
- External Build, Encode, and Query leaves remain registered when disabled, use deterministic local discovery when enabled, and derive the Query predicate from observed input data.
- No downloader, vendored upstream rows, SIMD parser implementation, parser-selection change, SIMD CI, or Phase-13 golden change is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/20-realistic-benchmark-dataset-foundation/20-02-SUMMARY.md`.
</output>
