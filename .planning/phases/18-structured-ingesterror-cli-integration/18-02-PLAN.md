---
phase: 18
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - failure_modes_test.go
  - ingest_error_guard_test.go
autonomous: true
requirements: [IERR-02]
tags: [tests, guard, ingest]

must_haves:
  truths:
    - "Per-layer `errors.As` behavior is covered by a named table-driven matrix."
    - "The enforcement guard is focused on ingest functions and does not add a broad repo-wide grep."
    - "Parser contract, tragic/internal, and Phase 17 soft-mode exceptions are explicitly tested."
  artifacts:
    - path: "failure_modes_test.go"
      provides: "Behavioral guard for parser, transformer, numeric, schema, soft, and contract cases"
      contains: "TestHardIngestFailuresReturnIngestError"
    - path: "ingest_error_guard_test.go"
      provides: "Low-noise AST guard for direct plain error returns in named hard-ingest functions"
      contains: "TestHardIngestFunctionsDoNotReturnPlainErrors"
  key_links:
    - from: "ingest_error_guard_test.go"
      to: "builder.go"
      via: "AST scan of named ingest functions"
      pattern: "newIngestError"
    - from: "failure_modes_test.go"
      to: "builder.go validateStagedPaths"
      via: "numeric_mixed_promotion case"
      pattern: "numeric_mixed_promotion"
---

<objective>
Add low-noise enforcement around the structured ingest error contract so hard ingest sites do not drift back to opaque errors.

Purpose: satisfy the guard portion of IERR-02 without the brittle Makefile historical-string guard flagged in review.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-structured-ingesterror-cli-integration/18-CONTEXT.md
@.planning/phases/18-structured-ingesterror-cli-integration/18-RESEARCH.md
@.planning/phases/18-structured-ingesterror-cli-integration/18-PATTERNS.md
@.planning/phases/18-structured-ingesterror-cli-integration/18-REVIEWS.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden behavior matrix and exception coverage</name>
  <files>failure_modes_test.go</files>
  <read_first>
    - failure_modes_test.go
    - builder.go
    - atomicity_test.go
    - .planning/phases/18-structured-ingesterror-cli-integration/18-CONTEXT.md D-16 through D-23
    - .planning/phases/18-structured-ingesterror-cli-integration/18-REVIEWS.md "Plan 18-02" concerns
  </read_first>
  <action>
    Ensure `TestHardIngestFailuresReturnIngestError` has these exact named subtests from Plan 18-01:
    - `parser_unknown_path`
    - `transformer_source_path`
    - `numeric_malformed_literal`
    - `numeric_mixed_promotion`
    - `schema_unsupported_token`

    Each subtest must assert returned error is non-nil, `errors.As(errors.Wrap(err, "outer"), &ingestErr)` is true, expected layer/path/value semantics are present, `ingestErr.Err != nil`, and the builder remains usable after the hard public failure where the fixture can reuse the builder.

    Keep separate non-`IngestError` exception tests:
    - parser contract errors remain hard implementation errors
    - tragic state/refusal errors remain non-`IngestError`
    - recovered merge panic errors remain non-`IngestError`
    - Phase 17 soft parser/numeric/transformer cases return nil and do not expose `IngestError`

    Move any missing subtest-name requirements from this plan into the actual test names so Plan 18-03 and later verification do not depend on ambiguous naming.

    Addresses review concern: tight coupling to unnamed tests is resolved by pinning the names in the first wave's acceptance and rechecking them here.
  </action>
  <verify>
    <automated>go test ./... -run 'Test(HardIngestFailuresReturnIngestError|ParserContractErrorsRemainNonIngestError|SoftFailureModesDoNotReturnIngestError|ParserFailureModeSoftKeepsTragicStateHard|NumericFailureModeSoftKeepsMergeRecoveryTragic)$' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `failure_modes_test.go` contains `parser_unknown_path`.
    - `failure_modes_test.go` contains `transformer_source_path`.
    - `failure_modes_test.go` contains `numeric_malformed_literal`.
    - `failure_modes_test.go` contains `numeric_mixed_promotion`.
    - `failure_modes_test.go` contains `schema_unsupported_token`.
    - `failure_modes_test.go` contains `errors.As(errors.Wrap(err, "outer"), &`.
    - Focused behavior command exits 0.
  </acceptance_criteria>
  <done>The behavior matrix proves the hard failure contract and the explicit non-structured-error exceptions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace brittle Makefile guard with focused AST enforcement test</name>
  <files>ingest_error_guard_test.go</files>
  <read_first>
    - builder.go
    - ingest_error.go
    - Makefile
    - .planning/phases/18-structured-ingesterror-cli-integration/18-RESEARCH.md "Static Guard"
    - .planning/phases/18-structured-ingesterror-cli-integration/18-REVIEWS.md consensus guard feedback
  </read_first>
  <action>
    Do not add `check-ingest-error-wrapping` to the Makefile. The review consensus found a historical-string Makefile guard brittle; replace it with a focused Go test using only stdlib packages (`go/parser`, `go/ast`, `go/token`, `testing`, `strings` as needed).

    Create `ingest_error_guard_test.go` with `TestHardIngestFunctionsDoNotReturnPlainErrors`.

    The test parses `builder.go` and inspects only these hard-ingest functions:
    - `stageScalarToken`
    - `stageMaterializedValue`
    - `stageCompanionRepresentations`
    - `stageJSONNumberLiteral`
    - `stageNativeNumeric`
    - `stageNumericObservation`
    - `validateStagedPaths`

    Guard rule: within those functions, any `return` expression that directly calls `errors.New`, `errors.Errorf`, `errors.Wrap`, or `errors.Wrapf` must fail unless the same return expression wraps through `newIngestError` or `newIngestErrorString`. The test may intentionally ignore `AddDocument` because it contains parser contract/tragedy gates; those are covered behaviorally by Task 1.

    The failure message must include the function name and source position. Keep the test scoped to the functions above so legitimate non-ingest error creation elsewhere in the repo is not blocked. Add a comment explaining the blindspot: this AST guard catches direct plain error returns in current hard-ingest functions; new functions still need behavior-matrix coverage.

    Addresses review concerns: replaces brittle POSIX string guard with concrete low-noise Go enforcement and documents blindspots.
  </action>
  <verify>
    <automated>go test ./... -run 'TestHardIngestFunctionsDoNotReturnPlainErrors$' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `ingest_error_guard_test.go` contains `func TestHardIngestFunctionsDoNotReturnPlainErrors`.
    - `ingest_error_guard_test.go` contains `go/parser`.
    - `ingest_error_guard_test.go` contains `stageScalarToken`.
    - `ingest_error_guard_test.go` contains `stageMaterializedValue`.
    - `ingest_error_guard_test.go` contains `stageCompanionRepresentations`.
    - `ingest_error_guard_test.go` contains `stageJSONNumberLiteral`.
    - `ingest_error_guard_test.go` contains `stageNativeNumeric`.
    - `ingest_error_guard_test.go` contains `stageNumericObservation`.
    - `ingest_error_guard_test.go` contains `validateStagedPaths`.
    - `go test ./... -run 'TestHardIngestFunctionsDoNotReturnPlainErrors$' -count=1` exits 0.
  </acceptance_criteria>
  <done>A focused AST test guards current hard-ingest functions without adding Makefile portability or false-positive risk.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Future code changes -> ingest error API | New hard ingest returns could bypass `IngestError` if enforcement is too broad or too weak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T18-05 | Tampering | future hard ingest sites | mitigate | Behavior matrix plus focused AST test guards named ingest functions against direct plain error returns. |
| T18-06 | Denial of Service | enforcement false positives | mitigate | No broad grep or Makefile shell portability dependency; guard is scoped to named functions and stdlib Go parsing. |
| T18-07 | Repudiation | documented exceptions | mitigate | Parser contract, tragic, recovered panic, and soft-mode cases are explicit behavior tests rather than implicit allowlist strings. |
</threat_model>

<verification>
Run:

```bash
go test ./... -run 'Test(HardIngestFailuresReturnIngestError|ParserContractErrorsRemainNonIngestError|SoftFailureModesDoNotReturnIngestError|HardIngestFunctionsDoNotReturnPlainErrors)$' -count=1
```
</verification>

<success_criteria>
IERR-02 is guarded by behavior tests and a focused AST enforcement test that is strong enough for current ingest functions without noisy repo-wide error restrictions.
</success_criteria>

<output>
After completion, create `.planning/phases/18-structured-ingesterror-cli-integration/18-02-SUMMARY.md`.
</output>
