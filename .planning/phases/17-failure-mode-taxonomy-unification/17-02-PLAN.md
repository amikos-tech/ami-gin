---
phase: 17
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - serialize.go
  - serialize_security_test.go
autonomous: true
requirements: [FAIL-01]
tags: [serialization, compatibility, v9, failure-modes]

must_haves:
  truths:
    - "Parser and numeric failure modes are not serialized into finalized index config."
    - "Transformer metadata still accepts legacy v9 `strict` and `soft_fail` tokens."
    - "Newly encoded v9 transformer metadata preserves legacy failure-mode wire tokens."
    - "Unknown transformer failure-mode wire tokens are rejected during decode."
    - "`Version` remains unchanged at v9."
  artifacts:
    - path: "serialize.go"
      provides: "Transformer failure-mode wire-token compatibility without parser/numeric config serialization"
      contains: "SerializedConfig"
    - path: "serialize_security_test.go"
      provides: "Legacy-token decode and v9 wire-token regression coverage"
      contains: "TestDecodeLegacyTransformerFailureModeTokens"
  key_links:
    - from: "serialize.go writeConfig"
      to: "TransformerSpec.FailureMode"
      via: "private wire-token mapping before JSON marshal"
      pattern: "soft_fail"
    - from: "serialize.go readConfig"
      to: "gin.go normalizeTransformerFailureMode"
      via: "legacy token normalization"
      pattern: "normalizeTransformerFailureMode"
    - from: "serialize.go SerializedConfig"
      to: "GINConfig"
      via: "deliberate omission of parser/numeric builder-time modes"
      pattern: "Transformers"
---

<objective>
Preserve binary v9 compatibility while the public transformer failure-mode API is renamed.

Purpose: satisfy D-12 through D-14 and the FAIL-01 serialization requirement without adding parser/numeric builder-time choices to finalized index config.

Output: `serialize.go` wire-token mapping plus tests proving legacy transformer tokens decode, new v9 encodes retain old token spelling, and parser/numeric modes stay out of `SerializedConfig`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-failure-mode-taxonomy-unification/17-CONTEXT.md
@.planning/phases/17-failure-mode-taxonomy-unification/17-RESEARCH.md
@.planning/phases/17-failure-mode-taxonomy-unification/17-VALIDATION.md
@.planning/phases/17-failure-mode-taxonomy-unification/17-PATTERNS.md
@.planning/phases/17-failure-mode-taxonomy-unification/17-01-SUMMARY.md
@AGENTS.md
@CLAUDE.md

<interfaces>
Serializer boundary that must remain builder-time-mode free:

```go
type SerializedConfig struct {
	BloomFilterSize         uint32            `json:"bloom_filter_size"`
	// ...
	FTSPaths                []string          `json:"fts_paths,omitempty"`
	Transformers            []TransformerSpec `json:"transformers,omitempty"`
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve transformer wire tokens without serializing parser/numeric modes</name>
  <files>serialize.go</files>
  <read_first>
    - serialize.go
    - gin.go
    - transformer_registry.go
    - .planning/phases/17-failure-mode-taxonomy-unification/17-CONTEXT.md D-12, D-13, D-14
    - .planning/phases/17-failure-mode-taxonomy-unification/17-REVIEWS.md Plan 17-02 wire-token compatibility feedback
    - .planning/phases/17-failure-mode-taxonomy-unification/17-PATTERNS.md section `serialize.go`
    - .planning/phases/17-failure-mode-taxonomy-unification/17-VALIDATION.md row 17-01-02
  </read_first>
  <behavior>
    - `SerializedConfig` has no `ParserFailureMode` or `NumericFailureMode` fields.
    - `writeConfig` writes transformer failure modes as `strict` or `soft_fail` while public config uses `IngestFailureHard` or `IngestFailureSoft`.
    - `writeRepresentations` writes representation metadata with the same legacy token spelling.
    - Write-side wire-token mapping does not mutate live `GINConfig` representation specs.
    - `readConfig` and `readRepresentations` decode legacy `strict` / `soft_fail` tokens into the new public type.
    - `Version` is not bumped.
  </behavior>
  <action>
    Update `serialize.go` per D-12 through D-14:

    - Do not add parser or numeric failure-mode fields to `SerializedConfig`; these modes are builder-time routing options only per D-12.
    - Keep `Version` unchanged. If `Version` is currently `9`, it must remain `9`.
    - Add a private helper near the config serialization helpers, for example `transformerFailureModeWireToken(mode IngestFailureMode) IngestFailureMode`, that returns:
      - `transformerFailureWireStrict` or `IngestFailureMode("strict")` for normalized `IngestFailureHard`
      - `transformerFailureWireSoft` or `IngestFailureMode("soft_fail")` for normalized `IngestFailureSoft`
    - In `writeConfig`, before appending each serializable transformer to `sc.Transformers`, copy the `TransformerSpec` value into a local variable, set the copied `TransformerSpec.FailureMode = transformerFailureModeWireToken(transformer.FailureMode)`, and append only that copied spec.
    - In `writeRepresentations`, marshal a copied slice of `RepresentationSpec` values, not the original slice. For each element, deep-copy the embedded `Transformer` field into the copied `RepresentationSpec` before setting `copied[i].Transformer.FailureMode = transformerFailureModeWireToken(copied[i].Transformer.FailureMode)`. This addresses review concern: copying only the outer slice is not enough if the embedded transformer metadata can otherwise alias or mutate live config.
    - Leave `readConfig` and `readRepresentations` using `normalizeTransformerFailureMode` and `validateTransformerFailureMode`, relying on Plan 17-01's helper behavior to accept both public values and legacy wire tokens.
    - Do not add a format-version history entry, because this task preserves v9 token spelling per the preferred default in D-14.
  </action>
  <verify>
    <automated>go test ./... -run 'TestRepresentationFailureModeRoundTrip$' -count=1 && rg -n 'const Version = 9|Version\\s*=\\s*9' gin.go</automated>
  </verify>
  <acceptance_criteria>
    - `serialize.go` contains no `ParserFailureMode` field inside `type SerializedConfig`.
    - `serialize.go` contains no `NumericFailureMode` field inside `type SerializedConfig`.
    - `serialize.go` contains `soft_fail`.
    - `serialize.go` contains `strict`.
    - `writeConfig` maps transformer failure modes through a private wire-token helper before appending to `sc.Transformers`.
    - `writeRepresentations` maps copied representation metadata through a private wire-token helper before `json.Marshal`.
    - `writeRepresentations` deep-copies or reassigns the copied `RepresentationSpec.Transformer` field before changing `FailureMode`.
    - `rg -n 'const Version = 9|Version\s*=\s*9' gin.go` exits 0.
    - `go test ./... -run 'TestRepresentationFailureModeRoundTrip$' -count=1` exits 0.
  </acceptance_criteria>
  <done>New v9 encodes keep legacy transformer failure-mode token spelling and parser/numeric failure modes remain absent from serialized config.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add v9 legacy-token decode and wire-format regression tests</name>
  <files>serialize_security_test.go</files>
  <read_first>
    - serialize_security_test.go
    - serialize.go
    - gin.go
    - transformer_registry.go
    - .planning/phases/17-failure-mode-taxonomy-unification/17-CONTEXT.md D-12, D-13, D-14
    - .planning/phases/17-failure-mode-taxonomy-unification/17-REVIEWS.md Plan 17-02 wire-token compatibility feedback
    - .planning/phases/17-failure-mode-taxonomy-unification/17-VALIDATION.md row 17-01-02
    - .planning/phases/17-failure-mode-taxonomy-unification/17-PATTERNS.md section `serialize_security_test.go`
  </read_first>
  <behavior>
    - Hand-built v9 config payloads using `strict` and `soft_fail` decode into `IngestFailureHard` and `IngestFailureSoft`.
    - Hand-built v9 config payloads using an unknown transformer failure-mode token are rejected during decode.
    - Round-tripped transformer metadata still behaves as soft when reloaded into a builder.
    - Encoded config and representation metadata expose transformer `failure_mode` values through exact structured JSON field inspection, not substring checks.
    - Encoded config and representation metadata do not contain parser/numeric failure-mode JSON keys.
  </behavior>
  <action>
    Extend `serialize_security_test.go`:

    - Update `TestRepresentationFailureModeRoundTrip` to call `WithTransformerFailureMode(IngestFailureSoft)` and assert decoded metadata equals `IngestFailureSoft`.
    - Add `TestDecodeLegacyTransformerFailureModeTokens`:
      - hand-build a `SerializedConfig` with two `TransformerSpec` values on distinct paths, one with `FailureMode: IngestFailureMode("strict")` and one with `FailureMode: IngestFailureMode("soft_fail")`.
      - marshal it into the length-prefixed config payload pattern already used by `TestReadConfigRejectsDuplicateTransformerAlias`.
      - call `readConfig(&buf)`.
      - assert the decoded transformer specs contain `IngestFailureHard` for the `strict` token and `IngestFailureSoft` for the `soft_fail` token.
    - Add `TestReadConfigRejectsUnknownTransformerFailureMode`:
      - hand-build a `SerializedConfig` with one `TransformerSpec` value using `FailureMode: IngestFailureMode("panic")`.
      - marshal it into the same length-prefixed config payload pattern.
      - call `readConfig(&buf)`.
      - assert the returned error is non-nil and contains `invalid transformer failure mode`.
    - Add `TestTransformerFailureModeWireTokensStayV9`:
      - build a config with `WithToLowerTransformer("$.email", "lower", WithTransformerFailureMode(IngestFailureSoft))`.
      - build and encode a one-document index.
      - read the config section and representation metadata using existing helpers such as `locateRepresentationSection` or direct section readers.
      - parse the serialized config JSON into a struct or `map[string]any` and inspect the `transformers[].failure_mode` field exactly; assert the value is exactly `soft_fail`.
      - parse representation metadata JSON the same way and inspect each `transformer.failure_mode` field exactly; assert the value is exactly `soft_fail`.
      - Do not use raw substring assertions for `soft` or `soft_fail`; `soft` is contained inside `soft_fail` and that was called out by review as fragile.
      - assert serialized JSON does not contain `parser_failure_mode` or `numeric_failure_mode` keys.
      - assert `Version == 9`.
    - Ensure the focused tests also run the parity golden suite if already covered by `go test ./...`; no parity golden files under `testdata/parity-golden/` should be regenerated for this phase because v9 wire spelling is preserved.
    - Keep the test strings scoped to public API names and wire tokens. Do not add internal repository/company references to test names, comments, or failure messages.
  </action>
  <verify>
    <automated>go test ./... -run 'Test(RepresentationFailureModeRoundTrip|DecodeLegacyTransformerFailureModeTokens|ReadConfigRejectsUnknownTransformerFailureMode|TransformerFailureModeWireTokensStayV9)$' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `serialize_security_test.go` contains `func TestDecodeLegacyTransformerFailureModeTokens`.
    - `serialize_security_test.go` contains `func TestReadConfigRejectsUnknownTransformerFailureMode`.
    - `serialize_security_test.go` contains `func TestTransformerFailureModeWireTokensStayV9`.
    - `serialize_security_test.go` contains `IngestFailureMode("strict")`.
    - `serialize_security_test.go` contains `IngestFailureMode("soft_fail")`.
    - `serialize_security_test.go` contains `IngestFailureMode("panic")`.
    - `serialize_security_test.go` asserts decoded legacy tokens equal `IngestFailureHard` and `IngestFailureSoft`.
    - `serialize_security_test.go` asserts unknown transformer failure-mode tokens return an error containing `invalid transformer failure mode`.
    - `serialize_security_test.go` parses serialized config JSON and asserts transformer `failure_mode` equals `soft_fail` exactly.
    - `serialize_security_test.go` parses representation metadata JSON and asserts transformer `failure_mode` equals `soft_fail` exactly.
    - `serialize_security_test.go` checks that `parser_failure_mode` and `numeric_failure_mode` are absent from encoded metadata.
    - `go test ./... -run 'Test(RepresentationFailureModeRoundTrip|DecodeLegacyTransformerFailureModeTokens|ReadConfigRejectsUnknownTransformerFailureMode|TransformerFailureModeWireTokensStayV9)$' -count=1` exits 0.
  </acceptance_criteria>
  <done>Serialization tests prove v9 compatibility, legacy token normalization, and deliberate omission of parser/numeric builder-time modes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Encoded index bytes -> decoder | Untrusted persisted index bytes may contain legacy transformer failure-mode tokens. |
| Builder config -> serialized index metadata | Runtime config crosses into durable wire format where accidental drift can break compatibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering / Repudiation | decoded config defaults | transfer | Plan 17-01 validates hard defaults. This plan ensures decoded transformer metadata normalizes legacy tokens into explicit hard/soft values. |
| T-17-02 | Tampering | partially indexed rejected documents | transfer | Plan 17-03 owns whole-document soft-skip behavior; serialization does not route ingest failures. |
| T-17-03 | Tampering / Denial of Service | recovered merge panic handling | transfer | Plan 17-03 keeps merge recovery tragic; this plan does not touch merge recovery. |
| T-17-04 | Tampering | `writeConfig`, `writeRepresentations`, `readConfig`, `readRepresentations` | mitigate | Tasks 1-2 preserve legacy `strict` / `soft_fail` wire tokens, decode them into `IngestFailureMode`, and verify `Version` remains v9 without format drift. |
| T-17-05 | Information Disclosure | serialized metadata tests | mitigate | Tests inspect enum tokens only and do not log or serialize raw document payloads beyond fixed public examples. |
</threat_model>

<verification>
Run:

```bash
go test ./... -run 'Test(RepresentationFailureModeRoundTrip|DecodeLegacyTransformerFailureModeTokens|ReadConfigRejectsUnknownTransformerFailureMode|TransformerFailureModeWireTokensStayV9)$' -count=1
rg -n 'const Version = 9|Version\s*=\s*9' gin.go
! rg -n 'ParserFailureMode|NumericFailureMode' serialize.go
```
</verification>

<success_criteria>
FAIL-01's breaking rename is compatible with existing v9 transformer metadata: legacy tokens decode, new encodes retain legacy token spelling, parser/numeric builder-time options are not serialized, and no version bump is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/17-failure-mode-taxonomy-unification/17-02-SUMMARY.md`.
</output>
