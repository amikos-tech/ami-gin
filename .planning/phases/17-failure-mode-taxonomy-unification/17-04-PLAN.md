---
phase: 17
plan: 04
type: execute
wave: 3
depends_on: [17-02, 17-03]
files_modified:
  - CHANGELOG.md
  - examples/failure-modes/main.go
  - examples/failure-modes/main_test.go
autonomous: true
requirements: [FAIL-01, FAIL-02]
tags: [docs, examples, integration, migration]

must_haves:
  truths:
    - "The breaking rename is documented in a root `CHANGELOG.md` Unreleased section."
    - "The failure-modes example demonstrates hard rejection and soft skipping with deterministic output."
    - "Focused tests, example smoke, lint, and build gates pass after all phase work is integrated."
  artifacts:
    - path: "CHANGELOG.md"
      provides: "One-line migration note for the breaking public API rename"
      contains: "TransformerFailureMode"
    - path: "examples/failure-modes/main.go"
      provides: "Runnable hard-vs-soft ingest failure-mode example"
      contains: "WithParserFailureMode"
    - path: "examples/failure-modes/main_test.go"
      provides: "CI-style exact output regression for the failure-modes example"
      contains: "TestFailureModesExampleOutput"
  key_links:
    - from: "examples/failure-modes/main.go"
      to: "gin.go WithParserFailureMode"
      via: "soft config demonstration"
      pattern: "WithParserFailureMode\\(gin.IngestFailureSoft\\)"
    - from: "examples/failure-modes/main.go"
      to: "gin.go WithNumericFailureMode"
      via: "soft config demonstration"
      pattern: "WithNumericFailureMode\\(gin.IngestFailureSoft\\)"
    - from: "examples/failure-modes/main.go"
      to: "WithTransformerFailureMode"
      via: "soft transformer demonstration"
      pattern: "WithTransformerFailureMode\\(gin.IngestFailureSoft\\)"
---

<objective>
Document the breaking rename and add the public example that shows the unified failure-mode mental model in action.

Purpose: satisfy D-05, D-17, and the Phase 17 roadmap example criterion after the API, serialization, and builder behavior are in place.

Output: root `CHANGELOG.md`, deterministic `examples/failure-modes/main.go`, and final integration verification for the phase.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-failure-mode-taxonomy-unification/17-CONTEXT.md
@.planning/phases/17-failure-mode-taxonomy-unification/17-RESEARCH.md
@.planning/phases/17-failure-mode-taxonomy-unification/17-VALIDATION.md
@.planning/phases/17-failure-mode-taxonomy-unification/17-PATTERNS.md
@.planning/phases/17-failure-mode-taxonomy-unification/17-01-SUMMARY.md
@.planning/phases/17-failure-mode-taxonomy-unification/17-02-SUMMARY.md
@.planning/phases/17-failure-mode-taxonomy-unification/17-03-SUMMARY.md
@AGENTS.md
@CLAUDE.md

<interfaces>
Example must compile against:

```go
gin.WithParserFailureMode(gin.IngestFailureSoft)
gin.WithNumericFailureMode(gin.IngestFailureSoft)
gin.WithTransformerFailureMode(gin.IngestFailureSoft)
gin.IngestFailureHard
gin.IngestFailureSoft
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the breaking rename migration note</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md
    - .planning/phases/17-failure-mode-taxonomy-unification/17-CONTEXT.md D-02, D-05
    - .planning/REQUIREMENTS.md FAIL-01
    - AGENTS.md
  </read_first>
  <action>
    Create root `CHANGELOG.md` if it does not exist, or update the existing root file if one exists:

    - Add a top-level `# Changelog` heading if creating the file.
    - Add a `## Unreleased` section.
    - Add exactly one breaking-change bullet that includes this migration mapping per D-05:
      `TransformerFailureMode` / `TransformerFailureStrict` / `TransformerFailureSoft` were replaced by `IngestFailureMode` / `IngestFailureHard` / `IngestFailureSoft`.
    - Add a compact before/after snippet only if it stays focused on the public API symbols:
      - before: `gin.WithTransformerFailureMode(gin.TransformerFailureSoft)`
      - after: `gin.WithTransformerFailureMode(gin.IngestFailureSoft)`
    - Do not include repository-hosting names, company-internal information, PR links, or commit hashes.
    - Keep the changelog limited to the breaking rename and migration snippet.
  </action>
  <verify>
    <automated>rg -n 'TransformerFailureMode.*IngestFailureMode|TransformerFailureStrict.*IngestFailureHard|TransformerFailureSoft.*IngestFailureSoft' CHANGELOG.md && ! rg -n 'https?://|/Users/' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `CHANGELOG.md` exists at the repository root.
    - `CHANGELOG.md` contains `## Unreleased`.
    - `CHANGELOG.md` contains `TransformerFailureMode`.
    - `CHANGELOG.md` contains `IngestFailureMode`.
    - `CHANGELOG.md` contains `TransformerFailureStrict`.
    - `CHANGELOG.md` contains `IngestFailureHard`.
    - `CHANGELOG.md` contains `TransformerFailureSoft`.
    - `CHANGELOG.md` contains `IngestFailureSoft`.
    - `rg -n 'TransformerFailureMode.*IngestFailureMode|TransformerFailureStrict.*IngestFailureHard|TransformerFailureSoft.*IngestFailureSoft' CHANGELOG.md` exits 0.
    - `rg -n 'https?://|/Users/' CHANGELOG.md` returns no output.
  </acceptance_criteria>
  <done>The breaking API rename is documented with a concise migration note and no forbidden internal references.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic hard-vs-soft failure modes example</name>
  <files>examples/failure-modes/main.go, examples/failure-modes/main_test.go</files>
  <read_first>
    - examples/failure-modes/main.go
    - examples/failure-modes/main_test.go
    - examples/basic/main.go
    - examples/transformers/main.go
    - gin.go
    - builder.go
    - .planning/phases/17-failure-mode-taxonomy-unification/17-CONTEXT.md D-06, D-07, D-10, D-11, D-17
    - .planning/phases/17-failure-mode-taxonomy-unification/17-REVIEWS.md Plan 17-04 example/docs feedback
    - .planning/phases/17-failure-mode-taxonomy-unification/17-PATTERNS.md section `examples/failure-modes/main.go`
    - .planning/phases/17-failure-mode-taxonomy-unification/17-VALIDATION.md row 17-03-02
  </read_first>
  <action>
    Create `examples/failure-modes/main.go` following the existing example structure:

    - Use `package main`.
    - Use `main()` calling `run()` and write errors to stderr with `fmt.Fprintf(os.Stderr, "error: %v\n", err)`.
    - Use `github.com/pkg/errors` for wrapping.
    - Define fixed attempted documents in this order:
      1. `DocID(0)`: `{"email":"alice@example.com","score":9007199254740993}`
      2. `DocID(1)`: `{"email":42}`
      3. `DocID(2)`: `{"email":"bob@example.com","score":1.5}`
      4. `DocID(3)`: `not-json`
      5. `DocID(4)`: `{"email":"carol@example.com","score":9007199254740992}`
    - Hard configuration:
      - create config with `gin.WithEmailDomainTransformer("$.email", "domain")`.
      - ingest in order until the first error.
      - print exactly `hard: stopped after 1 indexed document: companion transformer "domain" on $.email failed to produce a value`.
      - Add a code comment explaining the hard config stops at `DocID(1)` because `{"email":42}` triggers transformer rejection after `DocID(0)` was accepted.
    - Soft configuration:
      - create config with `gin.WithParserFailureMode(gin.IngestFailureSoft)`, `gin.WithNumericFailureMode(gin.IngestFailureSoft)`, and `gin.WithEmailDomainTransformer("$.email", "domain", gin.WithTransformerFailureMode(gin.IngestFailureSoft))`.
      - ingest all attempted docs and return an error only if `AddDocument` returns a non-nil error.
      - finalize and print exactly `soft: indexed 2 documents`.
      - evaluate `gin.EQ("$.email", gin.As("domain", "example.com"))` and print exactly `soft: email-domain example.com row groups [0 1]`.
      - Add a code comment explaining the soft config accepts only `DocID(0)` and `DocID(4)`, and those original DocIDs pack densely into row groups `[0 1]` because soft skips do not consume positions.
    - Keep output deterministic: no random values, timing, environment-derived values, or map iteration output.
    - Keep the example limited to the two configs and printed outcomes described above.
    - Add `examples/failure-modes/main_test.go` with `TestFailureModesExampleOutput`:
      - Run the example using `exec.Command("go", "run", ".")` from the example directory, or call the example helper directly if the implementation exposes a buffer-friendly helper.
      - Assert the complete stdout equals exactly these three lines and trailing newline:
        `hard: stopped after 1 indexed document: companion transformer "domain" on $.email failed to produce a value`
        `soft: indexed 2 documents`
        `soft: email-domain example.com row groups [0 1]`
      - Assert stderr is empty on success.
      - Do not use substring checks for this test; exact output is the regression contract.
  </action>
  <verify>
    <automated>go test ./examples/failure-modes -run 'TestFailureModesExampleOutput$' -count=1 && go run ./examples/failure-modes/main.go > /tmp/gin-failure-modes-example.out && printf 'hard: stopped after 1 indexed document: companion transformer "domain" on $.email failed to produce a value\nsoft: indexed 2 documents\nsoft: email-domain example.com row groups [0 1]\n' > /tmp/gin-failure-modes-example.expected && cmp -s /tmp/gin-failure-modes-example.expected /tmp/gin-failure-modes-example.out</automated>
  </verify>
  <acceptance_criteria>
    - `examples/failure-modes/main.go` exists.
    - `examples/failure-modes/main.go` contains `WithParserFailureMode(gin.IngestFailureSoft)`.
    - `examples/failure-modes/main.go` contains `WithNumericFailureMode(gin.IngestFailureSoft)`.
    - `examples/failure-modes/main.go` contains `WithTransformerFailureMode(gin.IngestFailureSoft)`.
    - `examples/failure-modes/main.go` contains `hard: stopped after 1 indexed document`.
    - `examples/failure-modes/main.go` contains `soft: indexed 2 documents`.
    - `examples/failure-modes/main.go` contains `soft: email-domain example.com row groups`.
    - `examples/failure-modes/main.go` contains `DocID(1)`.
    - `examples/failure-modes/main.go` contains `DocID(0) and DocID(4)`.
    - `examples/failure-modes/main.go` contains `row groups [0 1]`.
    - `examples/failure-modes/main_test.go` contains `func TestFailureModesExampleOutput`.
    - `examples/failure-modes/main_test.go` compares stdout to the exact three-line output, not substrings.
    - `go test ./examples/failure-modes -run 'TestFailureModesExampleOutput$' -count=1` exits 0.
    - `go run ./examples/failure-modes/main.go` exits 0 and `cmp -s` proves it prints exactly the three expected lines.
  </acceptance_criteria>
  <done>The example demonstrates rejecting on any failure and skipping on configured soft failures with predictable output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public docs/examples -> caller behavior | Documentation and examples influence whether callers intentionally choose hard or soft failure modes. |
| Fixed example input -> stdout | Example code processes malformed and invalid documents and must not leak any sensitive runtime data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering / Repudiation | `CHANGELOG.md`, `examples/failure-modes/main.go` | mitigate | Tasks make hard the default documented behavior and require explicit parser/numeric/transformer soft options in the example. |
| T-17-02 | Tampering | example soft output | mitigate | Example output proves only two valid documents are indexed and accepted row groups pack densely as `[0 1]`. |
| T-17-03 | Tampering / Denial of Service | example scope | transfer | Runtime tragic recovery remains covered by Plan 17-03 tests; the example does not exercise or soften internal panics. |
| T-17-04 | Tampering | docs/example serialization claims | mitigate | Tasks do not mention serialized parser/numeric config and avoid implying a format change. Plan 17-02 owns wire-format tests. |
| T-17-05 | Information Disclosure | `CHANGELOG.md`, example stdout/stderr | mitigate | Example uses fixed public sample documents and prints counts/results only; changelog avoids internal references and raw private data. |
</threat_model>

<verification>
Run:

```bash
go test ./examples/failure-modes -run 'TestFailureModesExampleOutput$' -count=1
go run ./examples/failure-modes/main.go
go test ./... -run 'Test(IngestFailureMode|ParserFailureMode|TransformerFailureMode|NumericFailureMode|RepresentationFailureMode|DecodeLegacyTransformerFailureModeTokens|TransformerFailureModeWireTokensStayV9|SoftFailureModesMatchCleanCorpus|AddDocumentPublicFailuresDoNotSetTragicErr)' -count=1
make test
make lint
go build ./...
```
</verification>

<success_criteria>
Phase 17 is fully integrated: the breaking rename is documented, the hard-vs-soft example runs with deterministic output, focused behavior tests pass, and full test/lint/build gates are green.
</success_criteria>

<output>
After completion, create `.planning/phases/17-failure-mode-taxonomy-unification/17-04-SUMMARY.md`.
</output>
