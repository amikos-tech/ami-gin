---
phase: 22-simd-validation-benchmarks-ci
plan: 04
type: execute
wave: 4
depends_on: ["22-03"]
files_modified:
  - docs/simd-deployment.md
  - simd_example_test.go
  - .goreleaser.yml
  - simd_docs_guard_test.go
  - Makefile
autonomous: true
requirements: [SIMD-10, SIMD-11]

must_haves:
  truths:
    - "The encoded-byte/query parity and bounded fuzz gates from Plans 22-02 and 22-03 are green before public deployment guidance or release-header claims change."
    - "Per D-04, public validation guidance uses the exact qualified claim: identical encoded indexes and query results for documents that ingest without a parser-layer error, and names malformed failure-layer attribution as an explicit tested exclusion."
    - "Per D-05, guidance lists exactly the five supported platform pairs and explains two required versus three advisory tiers, including darwin/amd64 as tier 2."
    - "Per D-15, `make check-simd-docs` derives the effective module version and directory with `go list -m`, validates exactly four loading environment variables, and fails on guide-path, cross-reference, pinned-link, release-header, or snippet drift."
    - "Per D-16, the guide's enablement snippet has a tagged compile-checked Example counterpart with no `// Output:` comment, so tests compile but never load the native library."
    - "Per D-18, generic release notes signal the optional native SIMD dependency and link `docs/simd-deployment.md` without creating release assets."
    - "Per D-19, the upstream bootstrap link is pinned to the effective module tag rather than `/blob/main/`, with no fixed version embedded in guard logic."
  artifacts:
    - path: "docs/simd-deployment.md"
      provides: "Safe activation, platform policy, qualified parity, and version-pinned upstream guidance"
    - path: "simd_example_test.go"
      provides: "Tagged compile-only consumer example synchronized with the guide"
      exports: ["ExampleNewSIMDParser"]
    - path: "simd_docs_guard_test.go"
      provides: "Positive and mutation-negative documentation contract tests"
      exports: ["TestSIMDDocumentationContract"]
    - path: "Makefile"
      provides: "check-simd-docs target wired into lint"
      contains: "check-simd-docs"
    - path: ".goreleaser.yml"
      provides: "Release-header signal for opt-in native SIMD"
  key_links:
    - from: "simd_docs_guard_test.go"
      to: "docs/simd-deployment.md"
      via: "effective-version link, exact four-variable allowlist, guide path, and marker-delimited snippet checks"
      pattern: "TestSIMDDocumentationContract"
    - from: "simd_example_test.go"
      to: "docs/simd-deployment.md"
      via: "matching marker-delimited enablement body"
      pattern: "SIMD_EXAMPLE_(START|END)"
    - from: "Makefile"
      to: "simd_docs_guard_test.go"
      via: "canonical check-simd-docs and lint dependency"
      pattern: "lint:.*check-simd-docs"
---

<objective>
Make the consumer enablement and release guidance executable so API, native-loading variables, guide paths, and upstream version pins cannot silently drift.

Purpose: Let consumers enable SIMD without guesswork while keeping upstream bootstrap responsibilities and explicit-path provenance boundaries accurate.
Output: Updated deployment/release guidance, a compile-only tagged example, and a Make/Go drift guard wired into local lint.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-CONTEXT.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-RESEARCH.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-PATTERNS.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-VALIDATION.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-01-SUMMARY.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-03-SUMMARY.md

<interfaces>
Existing contracts this plan guards rather than changes:

- `func NewSIMDParser() (CloseableParser, error)`
- `func WithParser(p Parser) BuilderOption`
- `const initializationContext = "initialize pure-simdjson SIMD parser; set PURE_SIMDJSON_LIB_PATH or see docs/simd-deployment.md"`
- Existing guard helpers in `notice_version_guard_test.go`: `repositoryRoot`, `readTestFile`, `replaceFileText`, `appendFileText`, `requireOutputContains`, and `ciJobSection`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct and compile-check consumer and release guidance</name>
  <files>docs/simd-deployment.md, simd_example_test.go, .goreleaser.yml</files>
  <read_first>
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-CONTEXT.md` — D-04, D-05, D-10, D-15, D-16, D-18, and D-19; preserve all scope fences.
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-03-SUMMARY.md` — confirm the parity and bounded fuzz hard gates are green before changing public shipping guidance.
    - `docs/simd-deployment.md` — retain accurate ownership, bootstrap, explicit-path integrity, depth, numeric, and fallback content.
    - `parser_simd.go` — exact constructor return type and `initializationContext` guide path; do not modify parser behavior.
    - `README.md` — existing public example and guide cross-reference; it is a guard input, not an edit target.
    - `CHANGELOG.md` — existing guide cross-reference; it is a guard input, not an edit target.
    - `.goreleaser.yml` — modify only the generic `release.header`; preserve `builds: skip: true` and all changelog rules.
    - `.planning/milestones/v1.0-phases/11-real-corpus-prefix-compression-benchmarking/11-REAL-CORPUS-REPORT.md` — use its evidence-language restraint, not its dataset specifics.
  </read_first>
  <action>Resolve the effective pure-simdjson module version at execution time with `go list -m -f '{{if .Replace}}{{.Replace.Version}}{{else}}{{.Version}}{{end}}' github.com/amikos-tech/pure-simdjson`; require a complete versioned tag and use that result to replace the stale `/blob/main/docs/bootstrap.md` target in `docs/simd-deployment.md`. Do not copy a version literal from this plan or older Phase 19 artifacts. In the guide, retain the exact four loading variables and upstream ownership boundary; state that explicit `PURE_SIMDJSON_LIB_PATH` users own transport/checksum/provenance. Rewrite the validation section using D-04's exact phrase, name malformed-input failure-layer attribution as the tested exclusion, list linux/amd64 and darwin/arm64 as required plus linux/arm64, darwin/amd64, and windows/amd64 as advisory, and distinguish controlled committed benchmark evidence from shared-runner noisy trend artifacts without claiming a result that Plan 22-06 has not measured. Marker-delimit the existing full constructor/builder/cleanup example and create `simd_example_test.go` with `//go:build simdjson` on line 1, external-package consumer syntax, `ExampleNewSIMDParser`, the matching marker-delimited body, and no `// Output:` comment; it must compile but never execute native construction under ordinary `go test`. Update only the generic `.goreleaser.yml` release header to state SIMD is opt-in, requires an optional native dependency, and point to `docs/simd-deployment.md`; do not add assets or change release workflow behavior. This implements D-04, D-05, D-16, D-18, and D-19 while preserving Phase 19 upstream bootstrap ownership.</action>
  <verify>
    <automated>simd_effective_version="$(go list -m -f '{{if .Replace}}{{.Replace.Version}}{{else}}{{.Version}}{{end}}' github.com/amikos-tech/pure-simdjson)" &amp;&amp; rg -Fq "/blob/${simd_effective_version}/docs/bootstrap.md" docs/simd-deployment.md &amp;&amp; ! rg -Fq '/blob/main/docs/bootstrap.md' docs/simd-deployment.md &amp;&amp; go test -tags simdjson -run '^ExampleNewSIMDParser$' .</automated>
  </verify>
  <acceptance_criteria>
    - The bootstrap link contains the effective tag returned by `go list -m`, and `/blob/main/` is absent.
    - The guide contains D-04's exact qualified claim and explicitly names malformed failure-layer attribution as the exclusion.
    - The guide names all five platform pairs and the exact 2-required/3-advisory policy.
    - `simd_example_test.go` has the SIMD tag on line 1, compiles the public ownership path, and contains no `// Output:` comment.
    - `.goreleaser.yml` still has `builds: skip: true` and now points adopters to the deployment guide.
  </acceptance_criteria>
  <done>Consumer and release guidance accurately describes the opt-in path and has a compile-checked example without loading native code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce the SIMD documentation contract through Make and Go</name>
  <files>simd_docs_guard_test.go, Makefile</files>
  <read_first>
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-CONTEXT.md` — D-15's exact machine-readable inputs and four-variable allowlist.
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-PATTERNS.md` — executable documentation guard and temp-copy mutation patterns.
    - `notice_version_guard_test.go` — reuse its repository root, file helpers, effective-version/replacement behavior, diagnostics, and canonical Make invocation style.
    - `Makefile` — preserve required targets and add the new guard beside `check-notice-version`.
    - `docs/simd-deployment.md`, `simd_example_test.go`, `.goreleaser.yml`, `README.md`, `CHANGELOG.md`, and `parser_simd.go` — all are explicit guard inputs.
    - Effective module sources: `library_loading.go`, `internal/bootstrap/bootstrap.go`, and `internal/bootstrap/cache.go` under the directory returned by `go list -m -f '{{if .Replace}}{{.Replace.Dir}}{{else}}{{.Dir}}{{end}}'`.
  </read_first>
  <behavior>
    - The aligned repository passes `make check-simd-docs`.
    - Changing any one of the four loading variables, guide path, README/CHANGELOG link, effective-tag bootstrap link, release-header link, or snippet body causes a specific guard failure.
    - Adding unrelated upstream diagnostic tokens such as `PURE_SIMDJSON_WARN_LEAKS` does not expand the documented loading-variable allowlist.
    - Versioned replacements are resolved effectively; an unversioned local replacement cannot produce a supposedly pinned upstream link.
  </behavior>
  <action>Add a dependency-free `check-simd-docs` target following `check-notice-version`'s fail-closed style. Derive the effective module version and directory with `go list -m`, including replacements, and reject an unversioned effective replacement for pinned-link validation. Read the source values for exactly `libraryEnvPath`, `mirrorEnvVar`, `disableGHEnvVar`, and `cacheDirEnvVar` from the three locked upstream files; compare the resulting exact allowlist to the guide and do not sweep all `PURE_SIMDJSON_*` tokens. Verify `parser_simd.go`'s `initializationContext` points at the existing guide, README and CHANGELOG each cross-reference it, the bootstrap link uses the effective tag, the generic release header mentions opt-in native SIMD and the guide, and the marker-delimited guide/Example bodies match. Keep the pure-simdjson module path split in untagged Go source so `simd-isolation-check` does not treat the guard as a product importer. Add `simd_docs_guard_test.go` with one aligned temp-copy case plus mutation-negative subtests for every contract family, reusing helpers from `notice_version_guard_test.go` instead of adding YAML/Markdown libraries. Wire `check-simd-docs` into the existing `lint` prerequisites and add an exact help line; preserve every AGENTS-required target. Do not edit `.github/workflows/ci.yml` here—Plan 22-07 owns the dedicated lint-CI step and its ordering contract.</action>
  <verify>
    <automated>make check-simd-docs &amp;&amp; go test -run '^TestSIMDDocumentationContract$' . &amp;&amp; go test ./... &amp;&amp; make simd-isolation-check</automated>
  </verify>
  <acceptance_criteria>
    - `make check-simd-docs` passes on aligned sources and the focused Go test proves one-mutation failures with contract-specific diagnostics.
    - The guard's loading-variable set contains exactly `PURE_SIMDJSON_LIB_PATH`, `PURE_SIMDJSON_BINARY_MIRROR`, `PURE_SIMDJSON_DISABLE_GH_FALLBACK`, and `PURE_SIMDJSON_CACHE_DIR`.
    - Effective version and module directory come from `go list -m`; no current pure-simdjson version literal exists in Make or guard logic.
    - `lint` depends on both existing checks and `check-simd-docs`, and all required Make targets remain present.
    - Default dependency isolation remains green despite module-source inspection.
  </acceptance_criteria>
  <done>Documentation, release copy, env names, guide path, version pin, and example body are enforced by the canonical local lint path.</done>
</task>

</tasks>

<threat_model>
## Assets

- Consumer loading and provenance guidance.
- Release-note accuracy.
- The effective module/version contract used by documentation.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Effective module cache → repository documentation | Upstream constant values and version determine the documented contract. |
| Operator-supplied library → runtime | Explicit-path loading bypasses upstream download checksum verification; ownership must remain explicit. |
| Public API snippet → consumer code | Documentation must compile against the tagged API without executing native loading during tests. |

## STRIDE Threat Register

| Threat ID | Category | Asset / Boundary | Severity | Disposition | Mitigation | Verification |
|-----------|----------|------------------|----------|-------------|------------|--------------|
| T-22-02 | Tampering | Wrong module version → pinned docs | HIGH | mitigate | Derive effective version/directory through `go list -m`, reject unversioned replacements, and guard the exact tagged bootstrap link. | `make check-simd-docs` plus replacement and stale-link mutation tests. |
| T-22-04 | Tampering / Information Disclosure | Module source and explicit-path guidance | HIGH | mitigate | Exact four-variable allowlist, approved module, operator provenance warning, no environment dump, no new parser/docs dependency. | Guard negative tests, tagged compile-only Example, and default isolation check. |
</threat_model>

<verification>
- Run `make check-simd-docs`, the focused documentation contract test, and the tagged Example compile test from `22-VALIDATION.md`.
- Run the full default suite and `make simd-isolation-check`.
- Confirm `git diff -- README.md CHANGELOG.md parser_simd.go` is empty; those files are guard inputs, not edit targets.
</verification>

<success_criteria>
- A consumer can follow one compile-checked path from build tag through constructor ownership and explicit builder selection.
- Loading variables, guide path, public cross-links, release note, and upstream version pin fail closed on drift.
- No downstream native downloader, checksum verifier, cache installer/locker, ABI validator, parser behavior, or CLI feature is introduced.
</success_criteria>

<output>
Create `.planning/phases/22-simd-validation-benchmarks-ci/22-04-SUMMARY.md` when done.
</output>
