---
phase: 22-simd-validation-benchmarks-ci
plan: 03
type: execute
wave: 3
depends_on: ["22-02"]
files_modified:
  - parser_parity_fuzz_simd_test.go
  - testdata/fuzz/FuzzParserParity/authored-int-boundaries
  - testdata/fuzz/FuzzParserParity/authored-transformer-nested
  - testdata/fuzz/FuzzParserParity/phase20-nested
  - testdata/fuzz/FuzzParserParity/phase20-mixed-array
  - testdata/fuzz/FuzzParserParity/known-malformed-layer-asymmetry
autonomous: true
requirements: [SIMD-08]

must_haves:
  truths:
    - "Per D-02, `FuzzParserParity` is a tagged differential whose committed seeds run as an ordinary deterministic test in CI; timed fuzzing remains manual and no new fuzz job is added."
    - "Per D-02a, inputs longer than 4096 bytes or with array nesting depth greater than 8 are rejected before either parser arm executes."
    - "Per D-02b, one caller-owned SIMD parser is constructed before `f.Fuzz`, reused sequentially, and closed through `f.Cleanup`; there is no poisoned-parser recovery mechanism."
    - "Per D-02c, both-ingest compares encoded bytes and hard-fails on divergence, exactly-one-ingests records the D-04 exclusion, and both-reject is agreement."
    - "Per D-03, fuzz discovery does not introduce Phase 20 binary goldens or a parser registry."
    - "Per D-04, the malformed `1e400 garbage` failure-layer attribution case is represented and does not weaken the qualified byte/query parity gate."
  artifacts:
    - path: "parser_parity_fuzz_simd_test.go"
      provides: "Bounded tagged three-way differential fuzz target"
      exports: ["FuzzParserParity"]
    - path: "testdata/fuzz/FuzzParserParity/"
      provides: "Standard Go seed corpus sourced from authored and Phase 20 fixtures plus the known exclusion"
  key_links:
    - from: "parser_parity_fuzz_simd_test.go"
      to: "parser_simd_integration_test.go"
      via: "newTestSIMDParser(f) lifecycle and required-host policy"
      pattern: 'newTestSIMDParser\(f\)'
    - from: "parser_parity_fuzz_simd_test.go"
      to: "parser_parity_test.go"
      via: "fresh-builder encode comparison and byte-difference diagnostics"
      pattern: "Encode|assertByteIdentical"
    - from: "testdata/fuzz/FuzzParserParity/"
      to: "FuzzParserParity"
      via: "Go's standard testdata/fuzz corpus discovery"
      pattern: "go test fuzz v1"
---

<objective>
Add a bounded differential discovery net around the hard parity gate without allowing deep-array inputs to exhaust fuzzing CPU.

Purpose: Preserve newly discovered parser equivalence cases as deterministic regressions while respecting the known malformed-input attribution boundary.
Output: One tagged fuzz target and a small standard Go corpus drawn from both authored and realistic fixtures.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-CONTEXT.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-RESEARCH.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-PATTERNS.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-VALIDATION.md
@.planning/spikes/002-simd-differential-fuzz-harness/README.md
@.planning/spikes/002-simd-differential-fuzz-harness/harness_test.go
@.planning/phases/22-simd-validation-benchmarks-ci/22-02-SUMMARY.md

<interfaces>
Use the interfaces established by plan 22-02:

- `func newTestSIMDParser(tb testing.TB) CloseableParser`
- `func buildAndEncodeWithParser(t *testing.T, fx parityFixture, parser Parser) []byte`
- `func authoredParityFixtures() []parityFixture`
- `func phase20LoadRawJSONL(path string) ([][]byte, error)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit a bounded standard Go fuzz seed corpus</name>
  <files>testdata/fuzz/FuzzParserParity/authored-int-boundaries, testdata/fuzz/FuzzParserParity/authored-transformer-nested, testdata/fuzz/FuzzParserParity/phase20-nested, testdata/fuzz/FuzzParserParity/phase20-mixed-array, testdata/fuzz/FuzzParserParity/known-malformed-layer-asymmetry</files>
  <read_first>
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-CONTEXT.md` — D-02 seed-source requirement, D-02a bounds, and D-04 exclusion.
    - `.planning/spikes/002-simd-differential-fuzz-harness/harness_test.go` — accepted seed shapes and three-way outcome semantics; do not copy its runtime file loader.
    - `parser_parity_fixtures_test.go` — choose deterministic documents from the authored numeric and nested/transformer fixtures.
    - `testdata/phase20/nested_high_cardinality.jsonl` — choose one deterministic line without altering the fixture.
    - `testdata/phase20/mixed_type_arrays.jsonl` — choose one deterministic line without altering the fixture.
    - `parser_simd_integration_test.go` — preserve the exact malformed `1e400 garbage` D-04 case.
  </read_first>
  <action>Create exactly the five named corpus files using Go's standard `go test fuzz v1` corpus encoding for the single `[]byte` argument. Source two entries verbatim from existing authored parity documents (one numeric-boundary entry and one nested/transformer-buffered entry), two entries verbatim from deterministic lines in the named Phase 20 JSONL fixtures, and one entry containing the existing malformed trailing-number attribution case. Validate each chosen input is at most 4096 bytes and has array depth at most 8. Do not invent a corpus loader, duplicate entire fixture files, store encoded indexes, add generated crashers that have not been reproduced, or edit the source fixtures.</action>
  <verify>
    <automated>test "$(find testdata/fuzz/FuzzParserParity -type f | wc -l | tr -d ' ')" -eq 5 &amp;&amp; rg -l '^go test fuzz v1$' testdata/fuzz/FuzzParserParity/* | wc -l | tr -d ' ' | grep -Fxq '5'</automated>
  </verify>
  <acceptance_criteria>
    - Exactly five corpus files exist at the listed paths and each uses Go's standard fuzz corpus header.
    - At least two seeds originate from `authoredParityFixtures` and at least two originate from Phase 20 JSONL lines.
    - The known malformed attribution input is a dedicated seed.
    - No input exceeds the D-02a byte or array-depth bound.
    - No source fixture or parity golden changes.
  </acceptance_criteria>
  <done>A small reproducible corpus covers authored, realistic, and qualified-exclusion inputs without custom seed infrastructure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the bounded three-way differential fuzz target</name>
  <files>parser_parity_fuzz_simd_test.go</files>
  <read_first>
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-CONTEXT.md` — D-02, D-02a, D-02b, D-02c, D-03, and D-04 are all non-negotiable.
    - `.planning/spikes/002-simd-differential-fuzz-harness/harness_test.go` — `softConfig`, `buildEncode`, `FuzzParserParity`, and the string-aware `arrayNestingDepth` reference.
    - `.planning/spikes/002-simd-differential-fuzz-harness/README.md` — empirical reason for depth 8 and one-parser reuse.
    - `parser_simd_integration_test.go` — use `newTestSIMDParser(f)` and do not create a second construction policy.
    - `parser_parity_test.go` — reuse existing encoded-byte comparison conventions.
    - `builder.go` — read `stageMaterializedValue` array recursion only to preserve the guard rationale; do not optimize it in this phase.
  </read_first>
  <behavior>
    - Brackets inside JSON strings, including strings with escaped quotes and backslashes, do not contribute to array nesting depth.
    - Inputs of exactly 4096 bytes and exactly depth 8 pass the boundary predicate; 4097-byte and depth-9 inputs are rejected.
    - Rejected over-limit inputs return before either injected stdlib or SIMD parser/build arm is invoked.
    - A seed over 4096 bytes returns before stdlib or SIMD builder construction.
    - A seed whose string-aware array nesting exceeds 8 returns before either parser arm.
    - When both arms ingest, encoded bytes must match or the test fails as a hard-stop class.
    - When exactly one arm ingests, the target records the D-04 attribution class and does not claim byte divergence.
    - When both arms reject, the target records agreement and returns.
  </behavior>
  <action>Create `parser_parity_fuzz_simd_test.go` with `//go:build simdjson` exactly on line 1. Define `fuzzMaxInputBytes = 4096` and `fuzzMaxArrayDepth = 8`; implement the spike's string/escape-aware `arrayNestingDepth` helper plus a `fuzzInputWithinBounds` predicate used by the fuzz target. Add `TestArrayNestingDepthStringAware` proving brackets inside strings and escaped quote/backslash sequences are ignored, and `TestFuzzInputWithinBounds` proving the exact 4096-byte/depth-8 inclusive boundaries versus 4097-byte/depth-9 rejection. Extract the per-input differential body behind a test-only arm seam in this tagged `_test.go` file and add `TestFuzzParserParityRejectsOverLimitBeforeArms`: inject counter or fail-fast callbacks for the stdlib and SIMD parser/build arms, assert both counters remain zero for both over-limit classes, then assert an in-limit input reaches the seam so a dead test hook cannot pass. `FuzzParserParity` must route through that same bounded helper, obtain one parser through `newTestSIMDParser(f)` before `f.Fuzz`, reuse it sequentially, and rely on its cleanup; do not call `t.Parallel`, start goroutines, use `RunParallel`, reconstruct per iteration, or add poisoned-parser recovery. Configure both parser and numeric failure modes soft, and create a fresh builder per arm/iteration so rejected input cannot leave shared index state. Apply both bounds before either build helper runs. Classify D-02c outcomes exactly: both ingest and finalize to bytes means byte equality is mandatory and a mismatch calls `t.Fatalf` with the input and first-difference/length context; exactly one ingests logs the stdlib/SIMD errors as the D-04 malformed parser-layer attribution class and returns; both reject returns as agreement. Let the committed `testdata/fuzz/FuzzParserParity` directory supply seeds automatically—no runtime glob or fixture loader. The ordinary CI invocation must omit `-fuzz`; timed `go test -tags simdjson -fuzz=FuzzParserParity` remains an on-demand developer command only. Keep all seams and focused tests in `parser_parity_fuzz_simd_test.go`; do not change production code, `builder.go`, or implement the deferred nested-array optimization.</action>
  <verify>
    <automated>AMI_GIN_SIMD_REQUIRED=1 go test -tags simdjson -run '^(TestArrayNestingDepthStringAware|TestFuzzInputWithinBounds|TestFuzzParserParityRejectsOverLimitBeforeArms|FuzzParserParity)$' -count=1 . &amp;&amp; go test ./... &amp;&amp; make simd-isolation-check</automated>
  </verify>
  <acceptance_criteria>
    - Ordinary `go test` replays all five seeds and passes without a `-fuzz` flag.
    - Focused scanner and boundary tests cover quoted/escaped brackets plus both inclusive limits, and the injected arm seam proves both over-limit classes return before either parser/build arm.
    - One parser is constructed before `f.Fuzz`, no concurrency APIs appear, and cleanup is inherited from `newTestSIMDParser`.
    - Both-ingest divergence is fatal; exactly-one and both-reject branches remain distinct and diagnostic.
    - No parser behavior, builder behavior, CI schedule, binary golden, or nested-array optimization is added.
  </acceptance_criteria>
  <done>The deterministic fuzz seed suite enforces bounded differential parity and preserves the known failure-layer exclusion.</done>
</task>

</tasks>

<threat_model>
## Assets

- Fuzzing availability and CPU budget.
- The encoded-byte parity oracle.
- Native parser handle integrity across repeated iterations.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted fuzz bytes → builder staging | Mutated inputs can trigger exponential array staging on both parsers. |
| Fuzz iteration → reused native parser | Sequential ownership must be preserved across fresh builders. |
| Corpus files → CI evidence | Committed seeds must be standard, reviewable, and bounded. |

## STRIDE Threat Register

| Threat ID | Category | Asset / Boundary | Severity | Disposition | Mitigation | Verification |
|-----------|----------|------------------|----------|-------------|------------|--------------|
| T-22-03 | Denial of Service | Fuzz bytes → common builder staging | HIGH | mitigate | Enforce 4096-byte and depth-8 guards before either arm; use a string-aware depth scan; keep timed fuzzing out of CI. | Focused scanner/boundary tests plus injected arm counters prove over-limit inputs return before either build call; ordinary replay completes within the focused command. |
| T-22-04 | Tampering | Corpus/dependency → parity evidence | HIGH | mitigate | Standard Go corpus format, reviewed source fixtures, approved pinned native module, no custom loader or generated binary oracle. | Five named corpus files, `22-01-SUMMARY.md`, focused tagged test, and isolation check. |
</threat_model>

<verification>
- Run the focused scanner, boundary, early-return-seam, and `FuzzParserParity` seed-only command mapped from `22-VALIDATION.md` Task 22-01-02.
- Run the full default suite and isolation check after adding the tagged importer.
- Inspect `git diff -- testdata/fuzz/FuzzParserParity parser_parity_fuzz_simd_test.go` to confirm only the bounded harness and named seeds landed.
</verification>

<success_criteria>
- Seed replay covers both parser arms without resource-exhaustion inputs.
- Any in-scope byte divergence blocks subsequent benchmark and release recommendation plans.
- The D-04 exactly-one-ingests class is visible, recorded, and not mislabeled as parity success or byte failure.
</success_criteria>

<output>
Create `.planning/phases/22-simd-validation-benchmarks-ci/22-03-SUMMARY.md` when done.
</output>
