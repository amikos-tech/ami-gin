---
phase: 22-simd-validation-benchmarks-ci
plan: 08
type: execute
wave: 8
depends_on: ["22-06"]
files_modified: []
autonomous: false
requirements: [SIMD-08, SIMD-09, SIMD-10, SIMD-11]

must_haves:
  truths:
    - "The exact Phase 22 full default, required tagged-race, and security-scan gate is green before either human approval begins."
    - "A completed remote run shows all five supported legs attempted real SIMD construction and explicit-path loading; required legs are green and advisory outcomes are recorded rather than silently skipped."
    - "External repository rules require only the stable `SIMD (linux/amd64, required)` and `SIMD (darwin/arm64, required)` check names; all three advisory SIMD names remain non-blocking."
    - "Repository rules are inspected read-only and are not mutated without separate explicit authority."
    - "A human confirms the controlled committed results—not shared-runner trend output—support the report's single ship, defer, or narrow recommendation."
    - "The final evidence retains D-04's qualified parity claim and gives correctness priority over any measured speedup."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Stable five-leg check names and remote execution policy to inspect"
    - path: ".planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-RESULTS.md"
      provides: "Controlled authoritative measurements"
    - path: ".planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-REPORT.md"
      provides: "Single evidence-backed recommendation"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "external repository rules"
      via: "stable exact required-tier check names inspected without mutation"
      pattern: 'SIMD \((linux/amd64|darwin/arm64), required\)'
    - from: ".planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-REPORT.md"
      to: ".planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-RESULTS.md"
      via: "human-reviewed source evidence and recommendation traceability"
      pattern: "Recommendation class"
---

<objective>
Close the two external/manual evidence gaps that source code cannot prove: merge-rule binding for stable check names and human acceptance of the controlled performance recommendation.

Purpose: Ensure the operational policy outside the worktree matches the tested YAML and that noisy CI data cannot override controlled evidence.
Output: Recorded human approval of remote matrix/ruleset state and controlled benchmark interpretation; no repository-rule mutation.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-CONTEXT.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-RESEARCH.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-VALIDATION.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-06-SUMMARY.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-07-SUMMARY.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-RESULTS.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-REPORT.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the final automated phase and security gate</name>
  <files>None (verification only; do not modify repository files)</files>
  <read_first>
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-VALIDATION.md` — use the Full suite command exactly, including the required tagged race suite and security scan.
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-06-SUMMARY.md` — confirm controlled evidence and its recommendation were produced after the isolated benchmark wave.
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-07-SUMMARY.md` — confirm the five-leg workflow contract and local implementation suites are complete.
  </read_first>
  <action>Before presenting either human checkpoint, run the exact full Phase 22 gate from `22-VALIDATION.md`: the default suite, the required tagged race suite with its 30-minute timeout, and the repository security scan. Do not reorder, weaken, omit, or replace any segment. If any segment fails, stop before the approvals and report the failing command and diagnostics; do not treat remote matrix results or benchmark evidence as a substitute.</action>
  <verify>
    <automated>go test ./... &amp;&amp; AMI_GIN_SIMD_REQUIRED=1 go test -tags simdjson -race -timeout=30m ./... &amp;&amp; make security-scan</automated>
  </verify>
  <acceptance_criteria>
    - The exact three-segment command completes successfully in order.
    - Default tests, the required tagged race suite, and `make security-scan` are each green.
    - Neither human checkpoint begins after a failed or skipped segment.
    - No repository file is modified by this verification task.
  </acceptance_criteria>
  <done>The complete automated Phase 22 and security gate is green before external/manual acceptance begins.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify remote SIMD checks and merge-rule bindings</name>
  <files>None (read-only external verification; do not mutate repository rulesets)</files>
  <read_first>
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-CONTEXT.md` — D-05 through D-08 and D-17; required/advisory policy is locked.
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-VALIDATION.md` — manual matrix and required-status-check verification obligations.
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-07-SUMMARY.md` — stable check names, matrix policy, explicit-path step, and local contract-test result.
    - `.github/workflows/ci.yml` — exact five names and job behavior now present in source.
    - `simd_workflow_contract_test.go` — local assertions already proving the YAML shape.
  </read_first>
  <action>After the stable job names from Plan 22-07 have landed and at least one pull-request matrix run has completed, inspect that remote run and the repository's required-status-check/ruleset configuration. Confirm exactly these five checks were created: `SIMD (linux/amd64, required)`, `SIMD (darwin/arm64, required)`, `SIMD (linux/arm64, advisory)`, `SIMD (darwin/amd64, advisory)`, and `SIMD (windows/amd64, advisory)`. Inspect logs/status for each leg: both required legs must be green; advisory outcomes must be recorded; all five must set `AMI_GIN_SIMD_REQUIRED=1`, attempt native construction, run tagged tests, and run the `PURE_SIMDJSON_LIB_PATH` pass without an allowed supported-platform skip. Then inspect external merge rules and confirm only the two required names block merge and none of the three advisory names is required. This checkpoint is read-only: do not add, remove, rename, or reconfigure required checks. If the external state differs, report the exact mismatch and pause for explicit authority instead of mutating it.</action>
  <verify>
    <automated>go test -run '^TestSIMDWorkflowContract$' . &amp;&amp; make simd-isolation-check</automated>
    <human-check>Human records the completed run reference, outcomes for all five exact names, and confirms only the two required-tier names block merge; reply `approved` or describe the mismatch.</human-check>
  </verify>
  <acceptance_criteria>
    - A concrete completed remote run is identified and all five stable check names/outcomes are recorded.
    - Both required legs passed; each advisory leg's result is explicitly recorded.
    - Logs show real required-flag construction and explicit-path execution, not a supported-host skip.
    - External rules require exactly the two required-tier names and no advisory-tier name.
    - No external ruleset mutation occurred during verification.
  </acceptance_criteria>
  <done>Remote platform execution and external merge blocking match the tested two-required/three-advisory policy.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Approve the controlled SIMD evidence recommendation</name>
  <files>None (review only; evidence corrections must return to Plan 22-06)</files>
  <read_first>
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-RESULTS.md` — raw samples, exact commands, environment metadata, and benchstat output.
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-REPORT.md` — parity scope, per-fixture deltas, variance, and recommendation class.
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-CONTEXT.md` — D-04, D-10, D-11, D-12, and D-13 evidence policy.
    - `.planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md` — correctness HARD stop and decide-on-evidence performance rule.
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-06-SUMMARY.md` — commands and evidence-generation outcome.
  </read_first>
  <action>Review the committed controlled raw results and report together. Confirm the report uses the exact qualified claim "identical encoded indexes and query results for documents that ingest without a parser-layer error," names malformed failure-layer attribution as the tested exclusion, and gives any parity failure precedence over performance. Trace every fixture delta to the raw `COUNT=10` paired output and exact-version benchstat results; confirm ns/op, B/op, allocs/op, and input-JSON MB/s are not conflated. Confirm shared-runner artifacts are labeled noisy trend-only and were not used as authoritative evidence, no regression threshold was introduced per D-12, and the final class is exactly one of ship, defer, or narrow with reasoning supported by the observed fixtures and variance. Reply `approved` if the recommendation is supported; otherwise describe the exact evidence or interpretation defect so execution returns to Plan 22-06 rather than editing during this checkpoint.</action>
  <verify>
    <automated>test -s .planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-RESULTS.md &amp;&amp; test -s .planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-REPORT.md &amp;&amp; rg -Fq 'identical encoded indexes and query results for documents that ingest without a parser-layer error' .planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-REPORT.md &amp;&amp; rg -q '^Recommendation class: \*\*(ship|defer|narrow)\*\*$' .planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-REPORT.md</automated>
    <human-check>Human replies `approved` after tracing the recommendation to controlled evidence, or identifies the exact result/report correction required.</human-check>
  </verify>
  <acceptance_criteria>
    - Human confirms parity is the first and hard gate under D-04's exact scope.
    - Every reported delta is traceable to the controlled raw document and all four metric classes are interpreted correctly.
    - Shared-runner output is trend-only, no threshold exists, and exactly one recommendation class is supported.
    - Any requested correction is routed back to Plan 22-06 instead of silently changing evidence here.
  </acceptance_criteria>
  <done>The controlled measurement record and its ship/defer/narrow interpretation have explicit human acceptance.</done>
</task>

</tasks>

<threat_model>
## Assets

- External merge-policy binding for required SIMD checks.
- Remote five-platform execution evidence.
- Human-approved controlled benchmark recommendation.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workflow check names → external ruleset | Source-controlled names must be bound correctly outside the worktree. |
| Remote logs/status → human verification | A green check must represent actual SIMD execution rather than a skip. |
| Controlled documents → human shipping decision | Raw evidence must remain authoritative over noisy trend artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Asset / Boundary | Severity | Disposition | Mitigation | Verification |
|-----------|----------|------------------|----------|-------------|------------|--------------|
| T-22-01 | Spoofing / Repudiation | Remote check → merge policy | HIGH | mitigate | Inspect concrete five-leg run logs and exact external required-check names; block on mismatch; no self-approval through YAML alone. | Task 1 human approval records run and ruleset outcomes. |
| T-22-02 | Tampering | Remote cache/platform → check result | HIGH | mitigate | Verify normal plus explicit-path pass on every leg and rely on Plan 22-07 exact-cache contract. | Task 1 remote log review plus local workflow-contract test. |
| T-22-04 | Tampering / Information Disclosure | Trend/raw artifacts → decision | HIGH | mitigate | Human traces report to controlled text-only evidence, rejects env/native/cache disclosure, and treats shared runners as trend-only. | Task 2 approval and artifact/source checks. |
</threat_model>

<source_coverage>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Prove SIMD is correct, measurable, and operationally shippable | 02-08 | COVERED | Parity → fuzz → docs → benchmark → evidence → CI → external approval. |
| REQ | SIMD-08 | Identical encoded indexes and query results across authored and realistic fixtures | 02, 03, 06 | COVERED | Existing goldens, live Phase 20 differential, shared Evaluate cases, bounded fuzz, parity-first evidence. |
| REQ | SIMD-09 | CPU, allocation, and bytes/op deltas on realistic fixtures | 01, 05, 06, 08 | COVERED | Approved benchstat, paired benchmark, controlled results/report, human review. |
| REQ | SIMD-10 | Default plus tagged CI with explicit platform/library behavior | 02, 07, 08 | COVERED | Three-state helper, exact five-leg matrix/cache/path, remote/ruleset review. |
| REQ | SIMD-11 | Safe runtime loading and release/distribution guidance | 04, 07 | COVERED | Compile-checked guide, drift guard, release header, lint and explicit-path CI. |
| RESEARCH | R-01 | Package legitimacy gates for both `[SUS]` modules | 01 | COVERED | Two separate blocking-human checkpoints; no persistent workaround. |
| RESEARCH | R-02 | Layered authored/realistic/query parity architecture | 02 | COVERED | Reuses current helpers and keeps Phase 20 goldens rejected. |
| RESEARCH | R-03 | Bounded three-way differential fuzzing | 03 | COVERED | Standard corpus, 4096/depth-8 bounds, one parser, three outcomes. |
| RESEARCH | R-04 | Executable documentation and compile-only Example | 04 | COVERED | Effective-version/env/path/link/snippet/release guard. |
| RESEARCH | R-05 | Same-process paired benchmark and controlled evidence | 05, 06 | COVERED | One output, exact benchstat projection, raw/report split. |
| RESEARCH | R-06 | Five-platform matrix, exact cache, explicit path, and noisy trend artifact | 07, 08 | COVERED | Source contract plus remote/external verification. |
| CONTEXT | D-01 | Tagged live differential plus Evaluate cross-check | 02 | COVERED | Task 2 cites D-01 and all three oracle layers. |
| CONTEXT | D-02 | Tagged fuzz target; seed-only CI; manual timed fuzz | 03 | COVERED | No CI `-fuzz` or new job. |
| CONTEXT | D-02a | 4096-byte and depth-8 pre-arm guard | 03 | COVERED | Task action and T-22-03 mitigation. |
| CONTEXT | D-02b | One reused parser; no poison recovery | 03 | COVERED | Sequential `newTestSIMDParser(f)` lifecycle. |
| CONTEXT | D-02c | Three-way differential classification | 03 | COVERED | Both-ingest hard diff; one-ingest exclusion; both-reject agreement. |
| CONTEXT | D-03 | Reject Phase 20 goldens | 02, 03 | COVERED | Explicit no-golden actions and acceptance checks. |
| CONTEXT | D-04 | Exact qualified parity claim and malformed attribution exclusion | 02, 03, 04, 06, 08 | COVERED | Test, docs, evidence, and review all retain exact wording. |
| CONTEXT | D-05 | Exact five runners; two required, three advisory | 04, 07, 08 | COVERED | Docs, workflow contract, remote ruleset review. |
| CONTEXT | D-06 | Race on required tier only | 07 | COVERED | Exact two-race/three-non-race rows and test. |
| CONTEXT | D-07 | Three-state supported-platform construction | 02, 07 | COVERED | Shared helper and all-five required flag. |
| CONTEXT | D-08 | Effective-version/platform cache with no restore prefixes | 07 | COVERED | Exact key/path contract; no version literal. |
| CONTEXT | D-09 | One tagged dual-arm paired benchmark | 05 | COVERED | One process/output and `parser=` dimension. |
| CONTEXT | D-10 | Committed raw results plus synthesized recommendation | 06, 08 | COVERED | Phase 11 shape and human acceptance. |
| CONTEXT | D-11 | Main/dispatch non-gating noisy CI artifact | 07, 08 | COVERED | No PR trigger; text only; disclaimer; controlled evidence authoritative. |
| CONTEXT | D-12 | Reject benchstat regression gate | 06, 07, 08 | COVERED | No threshold in evidence, workflow, or review. |
| CONTEXT | D-13 | Allocation and input-throughput reporting | 05, 06, 08 | COVERED | B/op, allocs/op, MB/s, ns/op explicitly distinguished. |
| CONTEXT | D-14 | Smoke committed; external opt-in via exact two env vars | 05, 06 | COVERED | Existing loaders only; committed evidence smoke-only. |
| CONTEXT | D-15 | Machine-derived four-variable docs guard wired to lint | 04, 07 | COVERED | Exact allowlist, effective module sources, Make and CI lint steps. |
| CONTEXT | D-16 | Tagged compile-only synchronized Example | 04 | COVERED | No Output comment or native execution. |
| CONTEXT | D-17 | Narrow explicit-path CI proof only | 07, 08 | COVERED | Already-fetched path; no mirror/checksum matrix. |
| CONTEXT | D-18 | Release-header SIMD/native signal | 04 | COVERED | Generic header only; builds remain skipped. |
| CONTEXT | D-19 | Effective-tag bootstrap link | 04 | COVERED | Derived at execution and guard-enforced. |

Excluded without a gap: every item under `22-CONTEXT.md` Deferred Ideas, all parser/API/default/CLI behavior changes, the nested-array optimization, nightly/scheduled fuzz or matrix runs, a benchmark regression threshold, a parser registry, Phase 20 binary goldens, and downstream bootstrap/checksum/cache/ABI implementation.
</source_coverage>

<verification>
- The exact full suite command from `22-VALIDATION.md`, including `make security-scan`, is green before either checkpoint.
- Both checkpoint tasks are approved and recorded in `22-08-SUMMARY.md`.
- The remote run/ruleset record names all five checks and distinguishes required from advisory.
- The evidence review names the exact controlled files and approved recommendation class.
- No repository or external ruleset files are modified by this plan.
</verification>

<success_criteria>
- Source-controlled CI behavior and external merge policy agree exactly.
- Controlled benchmark evidence and its recommendation have explicit human acceptance.
- All four requirements and D-01 through D-19 are covered; all deferred/out-of-scope items remain excluded.
- No HIGH threat remains unmitigated or unverified.
</success_criteria>

<output>
Create `.planning/phases/22-simd-validation-benchmarks-ci/22-08-SUMMARY.md` when done.
</output>
