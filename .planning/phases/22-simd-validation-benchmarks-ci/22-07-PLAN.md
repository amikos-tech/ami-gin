---
phase: 22-simd-validation-benchmarks-ci
plan: 07
type: execute
wave: 6
depends_on: ["22-03", "22-04", "22-05"]
files_modified:
  - .github/workflows/ci.yml
  - simd_workflow_contract_test.go
  - Makefile
autonomous: true
requirements: [SIMD-09, SIMD-10, SIMD-11]

must_haves:
  truths:
    - "Per D-05, every PR runs exactly five SIMD legs on `ubuntu-latest`, `ubuntu-24.04-arm`, `macos-15-intel`, `macos-15`, and `windows-latest`, with two required and three `continue-on-error` advisory legs and `fail-fast: false`."
    - "Per D-06, only required linux/amd64 and darwin/arm64 run `-race`; all three advisory legs still construct SIMD and run the tagged suite without race."
    - "Per D-07, all five supported legs set `AMI_GIN_SIMD_REQUIRED=1`, so native construction failure is fatal and no supported leg can silently skip."
    - "Per D-08, the native cache key is exactly version/platform-specific using the effective `go list -m` version, caches only `PURE_SIMDJSON_CACHE_DIR`, and has no `restore-keys`."
    - "Per D-11, Linux/amd64 benchmark trend artifacts run only on push to main and workflow_dispatch, are non-gating, upload text only, and carry a shared-runner-noisy disclaimer."
    - "Per D-12, workflow code and tests contain no benchmark threshold or regression gate."
    - "Per D-17, every SIMD leg reruns tagged tests through `PURE_SIMDJSON_LIB_PATH` pointing at the already-fetched version/platform cache file, with no second network path or downstream bootstrap reimplementation."
    - "The lint job executes `make check-simd-docs` before golangci-lint, while default build/test/isolation jobs remain intact."
    - "The canonical `make security-scan` covers both the default and `-tags simdjson` Go call graphs, so the optional path is included in the final Phase 22 vulnerability gate."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Five-platform required/advisory SIMD matrix, explicit-path proof, docs guard, and non-PR benchmark trend job"
    - path: "simd_workflow_contract_test.go"
      provides: "Dependency-free exact workflow policy regression tests"
      exports: ["TestSIMDWorkflowContract"]
    - path: "Makefile"
      provides: "Default plus simdjson-tagged govulncheck security gate"
      contains: "govulncheck -tags simdjson ./..."
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "parser_simd_integration_test.go"
      via: "AMI_GIN_SIMD_REQUIRED=1 on all five matrix rows"
      pattern: "AMI_GIN_SIMD_REQUIRED"
    - from: ".github/workflows/ci.yml"
      to: "Makefile"
      via: "check-simd-docs and bench-simd canonical targets"
      pattern: "make (check-simd-docs|bench-simd)"
    - from: "simd_workflow_contract_test.go"
      to: ".github/workflows/ci.yml"
      via: "exact matrix, cache, trigger, explicit-path, least-privilege, and artifact assertions"
      pattern: "TestSIMDWorkflowContract"
    - from: "Makefile"
      to: "parser_simd.go"
      via: "govulncheck -tags simdjson call-graph scan without loading the native library"
      pattern: "govulncheck -tags simdjson"
---

<objective>
Convert the single-host SIMD job into the locked five-platform operational contract, prove explicit-path loading, and publish noisy trend data without weakening correctness or merge policy.

Purpose: Make supported-platform failures visible and cache-safe while keeping shared-runner performance non-authoritative.
Output: Updated CI workflow plus an executable workflow-policy guard.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-CONTEXT.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-RESEARCH.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-PATTERNS.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-VALIDATION.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-03-SUMMARY.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-04-SUMMARY.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-05-SUMMARY.md

<interfaces>
Workflow-facing contracts established earlier:

- Required flag: `AMI_GIN_SIMD_REQUIRED=1`
- Docs guard: `make check-simd-docs`
- Benchmark target: `make bench-simd COUNT=... BENCHTIME=...`
- Cache filenames: linux `libpure_simdjson.so`, darwin `libpure_simdjson.dylib`, windows `pure_simdjson-msvc.dll` under `&lt;cache&gt;/&lt;effective-v-version&gt;/&lt;goos&gt;-&lt;goarch&gt;/`.
- Existing narrow workflow parser: `ciJobSection(t, workflow, job string) string` in `notice_version_guard_test.go`.
- Canonical security entry point: `make security-scan`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce the exact five-leg required/advisory matrix and cache</name>
  <files>.github/workflows/ci.yml, simd_workflow_contract_test.go</files>
  <read_first>
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-CONTEXT.md` — D-05, D-06, D-07, and D-08 exact runner/tier/race/cache policy.
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-RESEARCH.md` — `Required-vs-Advisory Matrix`, `Exact-Version Native Cache`, and pitfalls 1-4.
    - `.github/workflows/ci.yml` — preserve `contents: read`, default jobs, action majors, effective-version replacement handling, and upstream bootstrap invocation.
    - `parser_simd_integration_test.go` — required flag and fatal supported-host behavior from Plan 22-02.
    - `notice_version_guard_test.go` — reuse `repositoryRoot`, `readTestFile`, and `ciJobSection`; do not add a YAML dependency.
    - Effective upstream `internal/bootstrap/cache.go` and `url.go` — confirm cache layout and platform library filenames; do not duplicate their implementation.
  </read_first>
  <behavior>
    - The matrix contains exactly five include rows with the locked runner, GOOS/GOARCH, tier, advisory, race, release-label, and cache-library values.
    - Required check names are stable as `SIMD (linux/amd64, required)` and `SIMD (darwin/arm64, required)`; advisory names use the same pattern.
    - Required legs run race; advisory legs do not; every leg runs tagged tests with the required flag.
    - The cache key includes runner OS, runner architecture, and effective module version and has no restore prefix.
  </behavior>
  <action>Replace only the existing `simd` job with a five-row `matrix.include`, keep Bash, `fail-fast: false`, job-level `continue-on-error: ${{ matrix.advisory }}`, and stable name `SIMD (${{ matrix.goos }}/${{ matrix.goarch }}, ${{ matrix.tier }})`. Rows are exact: (1) `ubuntu-latest`, linux/amd64, required, false, race true, `linux-amd64`, `libpure_simdjson.so`; (2) `macos-15`, darwin/arm64, required, false, true, `darwin-arm64`, `libpure_simdjson.dylib`; (3) `ubuntu-24.04-arm`, linux/arm64, advisory, true, false, `linux-arm64`, `libpure_simdjson.so`; (4) `macos-15-intel`, darwin/amd64, advisory, true, false, `darwin-amd64`, `libpure_simdjson.dylib`; (5) `windows-latest`, windows/amd64, advisory, true, false, `windows-amd64-msvc`, `pure_simdjson-msvc.dll`. Preserve replacement-aware `go list -m` version derivation as `steps.simd-version.outputs.version` and invoke upstream bootstrap at that exact version. Cache only `${{ runner.temp }}/pure-simdjson` with key `pure-simdjson-${{ runner.os }}-${{ runner.arch }}-${{ steps.simd-version.outputs.version }}` and no restore keys. Required rows run the full tagged race suite; advisory rows run the full tagged non-race suite. Every row sets `AMI_GIN_SIMD_REQUIRED: "1"`, cache dir, and leak warning. Create untagged `simd_workflow_contract_test.go`. Read the complete workflow for top-level `permissions: contents: read`, trigger shape, and presence of the existing `test`, `lint`, `build`, `simd`, and `govulncheck` jobs; use `ciJobSection(..., "simd")` only for job-local matrix/cache assertions. Assert semantic anchors and exact locked row values/counts, but do not require irrelevant whitespace or key ordering. This directly fixes the review's impossible section-scope contract. Do not add a YAML dependency, supported-host skip, literal module version, step-local error tolerance, default-job change, or downstream bootstrap implementation.</action>
  <verify>
    <automated>go test -run '^TestSIMDWorkflowContract/matrix_and_cache$' . &amp;&amp; go test ./... &amp;&amp; make simd-isolation-check</automated>
  </verify>
  <acceptance_criteria>
    - The contract test counts exactly five locked runners, two required/race rows, and three advisory/non-race rows.
    - All five rows set `AMI_GIN_SIMD_REQUIRED=1`; no supported construction failure is represented as an allowed skip.
    - Cache path/key are exact and version-derived, and `restore-keys` is absent.
    - The bootstrap remains upstream-owned and invoked at the effective version.
    - Full-workflow assertions prove default test, lint, build/isolation, and vulnerability jobs remain present and `permissions: contents: read` is unchanged; job-local assertions inspect only the `simd` section.
  </acceptance_criteria>
  <done>Every supported PR leg attempts real SIMD under the locked tier/race policy and cannot restore the wrong platform/version cache.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove explicit-path loading, tagged security scanning, and non-PR trend artifacts</name>
  <files>.github/workflows/ci.yml, simd_workflow_contract_test.go, Makefile</files>
  <read_first>
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-CONTEXT.md` — D-11, D-12, D-15, and D-17 exact trigger, disclaimer, no-gate, lint, and narrow recipe policy.
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-01-SUMMARY.md` — confirm exact x/perf tool approval before CI invokes it.
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-04-SUMMARY.md` — `check-simd-docs` exists and is green.
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-05-SUMMARY.md` — `bench-simd` exists and emits one paired output.
    - `.github/workflows/ci.yml` — build on Task 1's exact matrix; do not create a second SIMD matrix.
    - `simd_workflow_contract_test.go` — extend the narrow assertions without a YAML parser.
    - `Makefile` — preserve required targets and extend only the canonical security scan/help contract for the tagged call graph.
    - Effective upstream `internal/bootstrap/cache.go` and `url.go` — use the fetched cache path and filename, not the release asset filename.
  </read_first>
  <behavior>
    - Every matrix row reruns tagged tests with `PURE_SIMDJSON_LIB_PATH` pointing at its already-fetched effective-version cache file and performs no new download.
    - The lint job runs `make check-simd-docs` after setup and before golangci-lint.
    - The benchmark trend job can run only on push to `main` or `workflow_dispatch`, never pull_request, and cannot fail a merge.
    - The trend artifact contains benchmark and benchstat text only and labels itself shared-runner-noisy/trend-only.
    - `make security-scan` invokes `govulncheck ./...` and `govulncheck -tags simdjson ./...` in order.
  </behavior>
  <action>After upstream bootstrap, add a `simd-library` resolver step that constructs `${{ runner.temp }}/pure-simdjson/${{ steps.simd-version.outputs.version }}/${{ matrix.goos }}-${{ matrix.goarch }}/${{ matrix.library }}`, fails immediately unless exactly that file exists, and exports the validated path; this executable check closes the review's cache-layout assumption before `PURE_SIMDJSON_LIB_PATH` is used. Add one explicit-path smoke after the normal full tagged run, using the exported file, `AMI_GIN_SIMD_REQUIRED=1`, and only `TestSIMDParserGoldenAuthoredFixtures|TestSIMDParserPhase20Parity` (with `-race` on required rows only). Do not fetch again or add mirror/fallback/checksum/error-text cases. Add workflow-level `workflow_dispatch` and acknowledge in comments/tests that all jobs can be manually triggered; guard the dedicated Linux/amd64 trend job to dispatch or push-main only, keep it `continue-on-error: true`, bootstrap/cache at the exact derived version, explicitly unset both Phase 20 external env vars, and redirect `make bench-simd COUNT=1 BENCHTIME=1s` to one text file. Run the approved exact benchstat command on that file, write `shared-runner-noisy, trend only; controlled committed Phase 22 evidence is authoritative` to the step summary, and upload only the two text files with `actions/upload-artifact@v7` and `if-no-files-found: error`. Add `make check-simd-docs` before golangci-lint. Extend `Makefile`'s existing `security-scan` target to run `govulncheck ./...` followed by `govulncheck -tags simdjson ./...`, and update its help text; this is a static tagged call-graph scan and must not load the native library. Extend workflow contract subtests for the validated library step, focused no-network explicit-path pass, docs order, trigger/job condition, text-only artifact, disclaimer, least privilege, no threshold/schedule, and dual-call-graph Make target. Use whole-workflow text only for top-level/cross-job facts and job sections for local facts; avoid formatting-only assertions.</action>
  <verify>
    <automated>go test -run '^TestSIMDWorkflowContract$' . &amp;&amp; make check-simd-docs &amp;&amp; rg -Uq 'security-scan:\n\s+govulncheck \./\.\.\n\s+govulncheck -tags simdjson \./\.\.' Makefile &amp;&amp; go test ./... &amp;&amp; make simd-isolation-check</automated>
  </verify>
  <acceptance_criteria>
    - Each matrix row has one normal full tagged run plus one focused explicit-path parity smoke using a path that a preceding `test -f` resolver validated from the already-fetched cache.
    - No mirror, disable-fallback, checksum, lock, ABI, or second-download test was added downstream.
    - Benchmark job condition excludes pull_request and schedule, permits only main push/dispatch, and is `continue-on-error: true`.
    - Artifact upload paths end in text outputs only, with an explicit noisy-trend disclaimer and no threshold logic.
    - Lint runs `make check-simd-docs` before golangci-lint and the full workflow contract test passes.
    - `make security-scan` retains the default scan and adds exactly one `govulncheck -tags simdjson ./...` scan for the optional call graph.
  </acceptance_criteria>
  <done>CI proves normal and air-gapped-style explicit loading on five platforms and emits clearly non-authoritative trend text off the PR path.</done>
</task>

</tasks>

<threat_model>
## Assets

- Required SIMD check integrity across five supported platforms.
- Native cache correctness and explicit-path selection.
- CI credentials, logs, and uploaded artifacts.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Matrix policy → merge checks | YAML job outcomes exist separately from external required-check configuration. |
| Effective Go module version/platform → native cache | Cache selection must not cross version or architecture boundaries. |
| GitHub-hosted runner → uploaded artifact | Only non-sensitive benchmark text may leave the runner. |
| Already-fetched cache → explicit path | The second run must prove the documented local path without another network route. |

## STRIDE Threat Register

| Threat ID | Category | Asset / Boundary | Severity | Disposition | Mitigation | Verification |
|-----------|----------|------------------|----------|-------------|------------|--------------|
| T-22-01 | Spoofing / Repudiation | Supported runner → green SIMD check | HIGH | mitigate | All five legs set the required flag; two stable required check names; no supported construction skip; exact workflow contract. | Local contract test and final remote five-leg/ruleset checkpoint in Plan 22-08. |
| T-22-02 | Tampering | Cache → native library | HIGH | mitigate | Effective-version plus runner OS/arch exact key, no restore prefixes, matrix-specific path, upstream bootstrap verification, and a fail-closed `test -f` resolver before explicit use. | Workflow contract asserts key/resolver/path/no restore; explicit-path smoke exercises the validated file. |
| T-22-04 | Tampering / Information Disclosure | Tools/artifacts/optional call graph | HIGH | mitigate | Approved exact x/perf tool, `contents: read`, text-only artifact, cleared external tier, no env/cache/native upload, non-gating trend job, and default plus tagged govulncheck. | Workflow/Make contracts plus Plan 22-08 `make security-scan` execution. |
</threat_model>

<verification>
- Run `go test -run '^TestSIMDWorkflowContract$' .`, the default suite, docs guard, and isolation check locally.
- Inspect the workflow diff for exactly five runner labels and no `restore-keys`, `schedule`, performance threshold, or upstream-bootstrap reimplementation.
- Remote matrix and ruleset semantics are deliberately deferred to the blocking Plan 22-08 checkpoint after stable names land.
</verification>

<success_criteria>
- Default CI remains unchanged in purpose and tagged CI covers all five locked supported platforms.
- Required/advisory, race/no-race, cache, explicit-path, lint, trigger, disclaimer, artifact, and tagged vulnerability-scan contracts are executable and regression-tested.
- No HIGH Phase 22 CI threat remains without a concrete mitigation and verification path.
</success_criteria>

<output>
Create `.planning/phases/22-simd-validation-benchmarks-ci/22-07-SUMMARY.md` when done.
</output>
