---
phase: 22-simd-validation-benchmarks-ci
plan: 05
type: execute
wave: 5
depends_on: ["22-02", "22-03", "22-04"]
files_modified:
  - benchmark_simd_test.go
  - Makefile
autonomous: true
requirements: [SIMD-09]

must_haves:
  truths:
    - "The encoded-byte/query parity and bounded seed gates from plans 22-02 and 22-03 are green before the benchmark path is used."
    - "Per D-09, stdlib and SIMD typed-sink ingest run as paired `parser=` sub-benchmarks in one tagged process and produce one comparison-ready output file."
    - "Per D-13, every arm reports ns/op, B/op, allocs/op, and input-JSON MB/s, with setup and fixture I/O outside the timed region."
    - "Per D-20, the single shared SIMD parser is confirmed unbiased by Spike 003 (no order dependence, no shared-vs-fresh difference, byte-identical output); keep this lifecycle and do not switch to a per-fixture parser."
    - "Per D-14, checked-in Phase 20 fixtures form the always-on smoke tier, while the optional external tier uses only `GIN_PHASE20_ENABLE_SIMDJSON_EXTERNAL` and `GIN_PHASE20_SIMDJSON_DIR`."
    - "The canonical `bench-simd` target is anchored to the SIMD benchmark, preserves BENCHTIME/BENCH_TIMEOUT/COUNT overrides, and never runs the full tagged benchmark suite accidentally."
  artifacts:
    - path: "benchmark_simd_test.go"
      provides: "Tagged paired stdlib/SIMD typed-sink ingest benchmark"
      exports: ["BenchmarkSIMDTypedSinkIngest"]
    - path: "Makefile"
      provides: "Scoped bench-simd contributor target"
      contains: "bench-simd"
  key_links:
    - from: "benchmark_simd_test.go"
      to: "benchmark_test.go"
      via: "phase20SmokeFixtures, phase20LoadRawJSONL, phase20ExternalBenchmarkFixture, and variadic phase20BuildBenchmarkIndex"
      pattern: "phase20(BuildBenchmarkIndex|SmokeFixtures|ExternalBenchmarkFixture)"
    - from: "Makefile"
      to: "BenchmarkSIMDTypedSinkIngest"
      via: "anchored -bench selector with -tags simdjson"
      pattern: "-bench.*BenchmarkSIMDTypedSinkIngest"
---

<objective>
Create one reproducible paired benchmark surface that measures stdlib versus SIMD typed-sink ingest over the realistic Phase 20 fixture tiers.

Purpose: Produce comparable CPU, allocation, bytes/op, and input-throughput samples only after correctness is established.
Output: A tagged dual-arm Go benchmark and a narrowly scoped `make bench-simd` command.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-CONTEXT.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-RESEARCH.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-PATTERNS.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-VALIDATION.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-02-SUMMARY.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-03-SUMMARY.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-04-SUMMARY.md

<interfaces>
Interfaces available after upstream plans:

- `func newTestSIMDParser(tb testing.TB) CloseableParser`
- `func phase20BuildBenchmarkIndex(docs [][]byte, opts ...BuilderOption) (*GINIndex, error)`
- `func phase20LoadRawJSONL(path string) ([][]byte, error)`
- `func phase20ExternalBenchmarkFixture(b *testing.B) phase20BenchmarkFixture`
- `var phase20SmokeFixtures []phase20SmokeFixture`
- `const phase20ExternalEnableEnvVar = "GIN_PHASE20_ENABLE_SIMDJSON_EXTERNAL"`
- `const phase20ExternalDirectoryEnvVar = "GIN_PHASE20_SIMDJSON_DIR"`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the paired typed-sink ingest benchmark</name>
  <files>benchmark_simd_test.go</files>
  <read_first>
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-CONTEXT.md` — D-09, D-13, and D-14; preserve same-process pairing and tier boundaries.
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-RESEARCH.md` — paired benchmark shape, benchmark hygiene, and benchmark-report metrics.
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-02-SUMMARY.md` and `22-03-SUMMARY.md` — confirm hard parity and seed replay are green before measuring.
    - `benchmark_test.go` — `phase20SmokeFixtures`, loaders, external opt-in helper, row-group packing, and `SetBytes` precedent.
    - `parser_simd_integration_test.go` — use the shared `testing.TB` constructor; never create another native lifecycle policy.
    - `testdata/phase20/README.md` — fixture identity, counts, byte caps, and offline/external policy.
    - `.planning/spikes/003-simd-benchmark-parser-reuse/README.md` — D-20's evidence that the shared-parser lifecycle is unbiased, plus `harness_test.go` as the public-API mirror of the Phase 20 loaders and builder.
  </read_first>
  <behavior>
    - Every checked-in Phase 20 fixture emits adjacent `parser=stdlib` and `parser=simd` sub-benchmarks in one process.
    - Both arms build/finalize the same documents with the same row-group packing.
    - Each leaf reports allocations and sets processed bytes to the total input JSON bytes per iteration.
    - The external tier skips unless both existing Phase 20 opt-in variables are valid, then emits the same two parser arms.
  </behavior>
  <action>Create `benchmark_simd_test.go` with `//go:build simdjson` exactly on line 1 and `BenchmarkSIMDTypedSinkIngest` as the only top-level benchmark. Construct one caller-owned SIMD parser with `newTestSIMDParser(b)` before any timed region; define two sequential arms named exactly `stdlib` and `simd`, with no goroutines or `RunParallel`. For each `phase20SmokeFixtures` entry, load JSONL before entering the timed sub-benchmark, sum document byte lengths once, and run names in the form `tier=smoke/fixture=&lt;fixture&gt;/parser=stdlib|simd`. Inside each leaf call `ReportAllocs`, `SetBytes(totalInputJSONBytes)`, and `ResetTimer`, then repeatedly call `phase20BuildBenchmarkIndex` with no parser option for stdlib and `WithParser(simdParser)` for SIMD. Add `tier=external/fixture=local-example/parser=...` using only `phase20ExternalBenchmarkFixture`; retain its existing skip/error/size/depth behavior and do not add a new loader or env var. Measure build/finalize only: parser construction, fixture I/O, byte summing, and external discovery remain outside timing. Do not use two tag-set invocations, benchmark parser construction, add a threshold, or change runtime/parser code.</action>
  <verify>
    <automated>AMI_GIN_SIMD_REQUIRED=1 go test -tags simdjson -run '^$' -bench '^BenchmarkSIMDTypedSinkIngest$' -benchmem -benchtime=100ms -count=1 .</automated>
  </verify>
  <acceptance_criteria>
    - Smoke output contains both parser arms for every registered Phase 20 fixture.
    - Output contains `ns/op`, `B/op`, `allocs/op`, and `MB/s` for each arm.
    - MB/s is derived from total input JSON bytes, not encoded-index bytes.
    - Parser construction and file loading occur before `ResetTimer`; no parallel benchmark uses the shared parser.
    - With external env vars unset, only the external sub-benchmark skips and smoke results still run offline.
  </acceptance_criteria>
  <done>One tagged process produces paired, allocation-aware, input-throughput benchmark samples over the locked fixture tiers.</done>
</task>

<task type="auto">
  <name>Task 2: Add the scoped bench-simd Make target</name>
  <files>Makefile</files>
  <read_first>
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-CONTEXT.md` — D-14's exact target and external-tier policy.
    - `Makefile` — preserve `BENCHTIME`, `BENCH_TIMEOUT`, `COUNT`, required targets, the new `check-simd-docs` dependency, and help formatting from Plan 22-04.
    - `benchmark_simd_test.go` — exact top-level benchmark name added in Task 1.
    - `benchmark_test.go` — `bench-phase20` scoping precedent.
  </read_first>
  <action>Add a `.PHONY` `bench-simd` target that runs the root package with `-tags simdjson`, `-run '^$$'`, the anchored selector `-bench '^BenchmarkSIMDTypedSinkIngest$$'`, `-benchmem`, and the existing `$(BENCHTIME)`, `$(BENCH_TIMEOUT)`, and `$(COUNT)` controls. Do not use `-bench .`, add a second output-stitching command, or change `COUNT ?= 1`; controlled evidence will override `COUNT=10` later. Add one help line that says smoke fixtures run by default and names only `GIN_PHASE20_ENABLE_SIMDJSON_EXTERNAL=1` plus `GIN_PHASE20_SIMDJSON_DIR=&lt;path&gt;` for the optional local tier. Preserve the Plan 22-04 `check-simd-docs` target and every AGENTS-required target.</action>
  <verify>
    <automated>AMI_GIN_SIMD_REQUIRED=1 make bench-simd COUNT=1 BENCHTIME=100ms &amp;&amp; make help | rg -q '^  bench-simd '</automated>
  </verify>
  <acceptance_criteria>
    - `make bench-simd COUNT=1 BENCHTIME=100ms` runs only `BenchmarkSIMDTypedSinkIngest` under the SIMD tag.
    - The target emits paired smoke results with all four Go benchmark metrics.
    - `COUNT`, `BENCHTIME`, and `BENCH_TIMEOUT` remain overridable and retain their existing defaults.
    - Help names the two existing external-tier variables and no additional variable.
    - All required Make targets and `check-simd-docs` remain intact.
  </acceptance_criteria>
  <done>Contributors have one narrow, overridable command for paired SIMD evidence without accidentally running the full benchmark suite.</done>
</task>

</tasks>

<threat_model>
## Assets

- Comparable benchmark measurements.
- The approved native parser and its single-owner lifecycle.
- Offline smoke-fixture behavior.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture files → timed benchmark | I/O and discovery must not contaminate parser timing. |
| Approved native module → benchmark process | One non-concurrent parser is reused under the existing ownership contract. |
| Optional local corpus → benchmark helper | Existing bounded Phase 20 validation remains the only external-input gate. |

## STRIDE Threat Register

| Threat ID | Category | Asset / Boundary | Severity | Disposition | Mitigation | Verification |
|-----------|----------|------------------|----------|-------------|------------|--------------|
| T-22-04 | Tampering / Information Disclosure | External module/input → benchmark output | HIGH | mitigate | Require Plan 22-01 approval, reuse bounded Phase 20 loaders, keep setup outside timing, emit metrics only, and add no dependency or environment dump. | Focused benchmark, offline smoke run, unchanged module files, and later evidence artifact checks. |
</threat_model>

<verification>
- Re-run the parity commands from plans 22-02 and 22-03 before accepting benchmark output.
- Run `make bench-simd COUNT=1 BENCHTIME=100ms` and confirm every smoke fixture has both parser arms and all required metrics.
- Run `go test ./...` and `make simd-isolation-check` after the new tagged importer and Makefile edit.
</verification>

<success_criteria>
- The benchmark is paired, realistic, offline by default, and scoped to typed-sink ingest.
- CPU, allocation bytes/op, allocation count, and input throughput are simultaneously visible.
- No performance threshold, parser change, new fixture framework, or persistent tool dependency is introduced.
</success_criteria>

<output>
Create `.planning/phases/22-simd-validation-benchmarks-ci/22-05-SUMMARY.md` when done.
</output>
