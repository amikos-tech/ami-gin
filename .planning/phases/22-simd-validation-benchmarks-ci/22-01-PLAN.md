---
phase: 22-simd-validation-benchmarks-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [SIMD-08, SIMD-09, SIMD-10, SIMD-11]

must_haves:
  truths:
    - "A human approves the already-pinned github.com/amikos-tech/pure-simdjson module after reviewing the Phase 22 [SUS] provenance evidence, before any tagged SIMD evidence command runs."
    - "A human approves golang.org/x/perf at v0.0.0-20260709024250-82a0b07e230d before the first pinned benchstat invocation."
    - "No persistent dependency is added or changed merely to bypass either legitimacy checkpoint."
  artifacts:
    - path: "go.mod"
      provides: "Existing pure-simdjson module pin reviewed without mutation"
    - path: ".planning/phases/22-simd-validation-benchmarks-ci/22-RESEARCH.md"
      provides: "Package Legitimacy Audit and selected x/perf pseudo-version"
  key_links:
    - from: ".planning/phases/22-simd-validation-benchmarks-ci/22-RESEARCH.md"
      to: "22-02 through 22-07 execution"
      via: "blocking human approvals recorded in 22-01-SUMMARY.md"
      pattern: "Package Legitimacy Audit"
---

<objective>
Clear the mandatory package-provenance gates before Phase 22 relies on the native SIMD module or the benchmark-analysis tool.

Purpose: Prevent tool or module substitution from entering correctness, performance, CI, or documentation evidence.
Output: Two explicit human approvals recorded in `22-01-SUMMARY.md`; no product or dependency-file changes.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-CONTEXT.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-RESEARCH.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-VALIDATION.md
@go.mod
@go.sum
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve the pinned pure-simdjson module provenance</name>
  <files>None (checkpoint only; do not modify `go.mod` or `go.sum`)</files>
  <read_first>
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-RESEARCH.md` — read `Package Legitimacy Audit`, including the `[SUS]` reason and verified source origin.
    - `go.mod` — inspect the currently effective `github.com/amikos-tech/pure-simdjson` requirement; do not copy a version from an older planning artifact.
    - `go.sum` — confirm checksum records already exist for the pinned module.
    - `.planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md` — confirm the existing upstream-ownership and license/NOTICE boundary.
  </read_first>
  <action>Before any `-tags simdjson` test, native bootstrap command, module download, or evidence capture, present the research audit for `github.com/amikos-tech/pure-simdjson`: the module is already pinned, the resolved source origin is the public `amikos-tech/pure-simdjson` repository, and slopcheck returned `[SUS]` because of young-package/source-metadata heuristics rather than `[SLOP]`. Ask the human to inspect the module path, the version currently written in `go.mod`, its source/tag, LICENSE, NOTICE, and the existing `go.sum` entries. Do not change the version, add a replacement, run the native bootstrap, or introduce an alternative dependency. Resume only after the human explicitly approves this module.</action>
  <verify>
    <automated>rg -q '^\s*github.com/amikos-tech/pure-simdjson\s+v' go.mod &amp;&amp; rg -q '^github.com/amikos-tech/pure-simdjson\s' go.sum</automated>
    <human-check>Human records `approved` for `github.com/amikos-tech/pure-simdjson`, or describes the provenance concern and leaves the checkpoint blocked.</human-check>
  </verify>
  <acceptance_criteria>
    - The approval names `github.com/amikos-tech/pure-simdjson` and the effective version visible in `go.mod`.
    - The approval confirms the public source/tag and license/NOTICE posture were inspected.
    - `go.mod` and `go.sum` are byte-unchanged by this checkpoint.
  </acceptance_criteria>
  <done>The native SIMD module has an explicit human provenance approval and no dependency mutation occurred.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Approve the exact x/perf benchstat tool version</name>
  <files>None (checkpoint only; do not add `golang.org/x/perf` to `go.mod`)</files>
  <read_first>
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-RESEARCH.md` — read `Standard Stack`, `Package Legitimacy Audit`, and the exact selected x/perf pseudo-version.
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-CONTEXT.md` — read D-09 through D-13, especially the one-file benchstat comparison and no-regression-gate boundary.
    - `go.mod` — confirm x/perf is not being introduced as a persistent project dependency.
  </read_first>
  <action>Before the first `go run golang.org/x/perf/cmd/benchstat@...` command, present the research audit for `golang.org/x/perf@v0.0.0-20260709024250-82a0b07e230d`: the module resolves to the official Go performance repository and slopcheck returned `[SUS]` because of young pseudo-version/source-metadata heuristics, not `[SLOP]`. Ask the human to inspect the exact pseudo-version and official source origin. Do not install an unpinned latest version, add x/perf to `go.mod`, or substitute a custom statistics package. Resume only after explicit approval.</action>
  <verify>
    <automated>rg -q 'v0\.0\.0-20260709024250-82a0b07e230d' .planning/phases/22-simd-validation-benchmarks-ci/22-RESEARCH.md &amp;&amp; ! rg -q '^\s*golang.org/x/perf\s' go.mod</automated>
    <human-check>Human records `approved` for `golang.org/x/perf@v0.0.0-20260709024250-82a0b07e230d`, or describes the provenance concern and leaves the checkpoint blocked.</human-check>
  </verify>
  <acceptance_criteria>
    - The approval names the exact pseudo-version `v0.0.0-20260709024250-82a0b07e230d` and the official Go source origin.
    - No unpinned `benchstat` command has run before approval.
    - `golang.org/x/perf` remains absent from `go.mod`.
  </acceptance_criteria>
  <done>The exact benchstat tool version has explicit human approval without becoming a persistent dependency.</done>
</task>

</tasks>

<threat_model>
## Assets

- Correctness and benchmark evidence that subsequent plans will generate.
- The existing Go module graph and checksum records.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Research audit → human approval | Heuristic package warnings are converted into an explicit provenance decision. |
| Public module source → local evidence tooling | External code must be the reviewed module/version before it executes. |

## STRIDE Threat Register

| Threat ID | Category | Asset / Boundary | Severity | Disposition | Mitigation | Verification |
|-----------|----------|------------------|----------|-------------|------------|--------------|
| T-22-04 | Tampering | External module/tool → evidence | HIGH | mitigate | Separate blocking-human review for both `[SUS]` modules; exact x/perf pseudo-version; existing pure-simdjson pin/checksums; no replacement or persistent workaround. | Both approvals are recorded, automated pin checks pass, and `go.mod`/`go.sum` remain unchanged. |
</threat_model>

<verification>
- Both blocking approvals are present in `22-01-SUMMARY.md` before any dependent plan starts.
- `rg -n 'pure-simdjson|golang.org/x/perf' go.mod go.sum .planning/phases/22-simd-validation-benchmarks-ci/22-RESEARCH.md` shows the reviewed records and no added x/perf requirement.
</verification>

<success_criteria>
- The two `[SUS]` findings are accepted explicitly rather than silently bypassed.
- Plans 22-02 through 22-07 may use the approved dependencies under their locked versions and ownership boundaries.
- No repository source, dependency, or workflow file is modified by this plan.
</success_criteria>

<output>
Create `.planning/phases/22-simd-validation-benchmarks-ci/22-01-SUMMARY.md` when done.
</output>
