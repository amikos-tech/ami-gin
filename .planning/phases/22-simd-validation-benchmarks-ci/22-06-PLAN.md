---
phase: 22-simd-validation-benchmarks-ci
plan: 06
type: execute
wave: 7
depends_on: ["22-05", "22-07"]
files_modified:
  - .planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-RESULTS.md
  - .planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-REPORT.md
autonomous: true
requirements: [SIMD-08, SIMD-09]

must_haves:
  truths:
    - "The controlled COUNT=10 benchmark executes in its own wave after Plan 22-07's CPU-intensive implementation suites, with no same-wave implementation plan contaminating the evidence."
    - "Parity is rechecked first; any encoded-byte or query mismatch for documents that ingest without a parser-layer error halts this plan as a HARD stop before measurement or a ship recommendation."
    - "Per D-10, controlled committed results and a synthesized report are the authoritative evidence, following the Phase 11 raw-results/report split."
    - "Per D-12, neither the committed evidence nor benchstat introduces a performance regression threshold; speed below expectations produces an evidence-based ship, defer, or narrow decision."
    - "Per D-13, results distinguish CPU (`ns/op`), allocation bytes (`B/op`), allocation count (`allocs/op`), and input JSON throughput (`MB/s`)."
    - "Per D-21, the report states that committed numbers are steady-state (b.N lands at 61-312, so warm-up is amortized) and that `B/op` counts the Go heap only, so a favourable `B/op` is never reported as a total-memory win."
    - "Per D-22, no single-run number is authoritative: a COUNT=1 run was measured 3x wrong, so the COUNT=10 controlled snapshot and the shared-runner-noisy disclaimer may not be relaxed."
    - "Per D-23, the report is authored neutrally against a measured prior that SIMD is 1.45x-1.84x slower with 18-36% more Go heap on byte-identical output and no document-size crossover; a defer or narrow recommendation is a legitimate, expected outcome, not a phase failure."
    - "Per D-14, only checked-in smoke-tier fixture numbers are committed; the optional external tier remains a local escape hatch."
    - "The exact approved x/perf pseudo-version analyzes one paired benchmark output with `benchstat -col /parser`."
  artifacts:
    - path: ".planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-RESULTS.md"
      provides: "Pinned raw controlled-run metadata, samples, and benchstat output"
    - path: ".planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-REPORT.md"
      provides: "Parity-first per-fixture interpretation and ship/defer/narrow recommendation"
  key_links:
    - from: ".planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-REPORT.md"
      to: ".planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-RESULTS.md"
      via: "source-artifact link and derived per-fixture deltas"
      pattern: "22-SIMD-BENCHMARK-RESULTS.md"
    - from: ".planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-RESULTS.md"
      to: "BenchmarkSIMDTypedSinkIngest"
      via: "COUNT=10 paired run and exact benchstat parser projection"
      pattern: "benchstat.*-col /parser"
---

<objective>
Capture controlled paired benchmark results and turn them into the evidence-backed SIMD shipping recommendation required by the Phase 19 stop table.

Purpose: Make performance measurable and auditable without converting noisy data into a brittle gate.
Output: One raw-results document and one synthesized ship/defer/narrow report.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-CONTEXT.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-RESEARCH.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-VALIDATION.md
@.planning/milestones/v1.0-phases/11-real-corpus-prefix-compression-benchmarking/11-BENCHMARK-RESULTS.md
@.planning/milestones/v1.0-phases/11-real-corpus-prefix-compression-benchmarking/11-REAL-CORPUS-REPORT.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-03-SUMMARY.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-05-SUMMARY.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the controlled paired benchmark record</name>
  <files>.planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-RESULTS.md</files>
  <read_first>
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-CONTEXT.md` — D-04, D-09 through D-14, and D-20 through D-23; parity precedes measurement, smoke evidence is authoritative, and the recommendation is authored neutrally.
    - `.planning/spikes/003-simd-benchmark-parser-reuse/README.md` — measured priors for D-21/D-22/D-23: steady-state semantics, Go-heap-only `B/op`, the 3x single-run error, and the stdlib-vs-SIMD deltas with no document-size crossover.
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-01-SUMMARY.md` — confirm explicit approval of the exact x/perf pseudo-version and native module.
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-03-SUMMARY.md` — confirm seed replay and hard parity are green.
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-05-SUMMARY.md` — exact benchmark/Make target delivered.
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-07-SUMMARY.md` — confirm the CPU-intensive CI implementation suites completed in the preceding wave before controlled measurement begins.
    - `.planning/milestones/v1.0-phases/11-real-corpus-prefix-compression-benchmarking/11-BENCHMARK-RESULTS.md` — mirror its command/env/machine/revision/raw-table shape.
    - `testdata/phase20/README.md` — record the checked-in fixture identities and provenance without external-tier results.
    - `go.mod` — derive the effective native module version at run time.
  </read_first>
  <action>Run this controlled capture only after Plan 22-07 completes and without any concurrently executing Phase 22 implementation plan or Go suite. Before measuring, rerun the Plan 22-02/22-03 focused tagged parity command, including authored goldens, Phase 20 byte/query parity, the 24-case Evaluate matrix, known malformed attribution test, and `FuzzParserParity` seed replay. If an encoded-byte or query-result mismatch occurs for a document that ingests without a parser-layer error, stop immediately as the Phase 19 HARD condition; do not capture performance or recommend shipping. With parity green, create a temporary output path outside the repository, run `AMI_GIN_SIMD_REQUIRED=1 make bench-simd COUNT=10 BENCHTIME=1s` once so both arms remain in one output, then invoke exactly `go run golang.org/x/perf/cmd/benchstat@v0.0.0-20260709024250-82a0b07e230d -col /parser &lt;that-single-output&gt;`. Write `22-SIMD-BENCHMARK-RESULTS.md` in the Phase 11 raw-results shape. Record repository revision/dirty status, Go version, effective pure-simdjson version derived with `go list -m`, OS/architecture/CPU/machine metadata, exact commands, only relevant SIMD loading variable values (never a full environment dump), fixture names/counts, all raw paired samples, and full benchstat output. Include a table per fixture exposing parser, ns/op, B/op, allocs/op, and MB/s; state explicitly that MB/s is input JSON throughput, that B/op is Go-heap allocated bytes per benchmark operation and excludes pure-simdjson's native buffers (D-21), and that all committed numbers are steady-state because b.N amortizes warm-up (D-21). Commit only smoke-tier numbers; do not enable the external tier or use shared-runner output.</action>
  <verify>
    <automated>test -s .planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-RESULTS.md &amp;&amp; rg -q 'COUNT=10' .planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-RESULTS.md &amp;&amp; rg -q 'benchstat@v0\.0\.0-20260709024250-82a0b07e230d -col /parser' .planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-RESULTS.md &amp;&amp; rg -q 'ns/op' .planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-RESULTS.md &amp;&amp; rg -q 'B/op' .planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-RESULTS.md &amp;&amp; rg -q 'allocs/op' .planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-RESULTS.md &amp;&amp; rg -q 'MB/s' .planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - The focused parity/seed command is recorded green before the benchmark command.
    - Raw evidence comes from one `COUNT=10` paired output and one exact-version `benchstat -col /parser` invocation.
    - Revision, dirty state, Go/native-module version, OS/architecture/CPU, relevant loading values, fixtures, raw samples, and comparison output are recorded.
    - Tables include ns/op, B/op, allocs/op, and input-JSON MB/s for both parser arms on every smoke fixture.
    - No full environment, secret, native binary, cache contents, external corpus measurement, or shared-runner result is embedded.
  </acceptance_criteria>
  <done>The repository contains a reproducible controlled raw record for all paired smoke measurements.</done>
</task>

<task type="auto">
  <name>Task 2: Synthesize the parity-first shipping recommendation</name>
  <files>.planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-REPORT.md</files>
  <read_first>
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-RESULTS.md` — sole authoritative numeric source for this report.
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-CONTEXT.md` — D-04 exact parity phrase, D-10 report shape, D-12 no-threshold decision, D-13 metric meanings, and D-21/D-23 metric-semantics and neutral-authoring obligations.
    - `.planning/phases/19-simd-dependency-decision-integration-strategy/19-SIMD-STRATEGY.md` — hard/soft/decide-on-evidence stop table.
    - `.planning/milestones/v1.0-phases/11-real-corpus-prefix-compression-benchmarking/11-REAL-CORPUS-REPORT.md` — source link, Helps/Flat interpretation, evidence-qualified inference, and recommendation ending.
  </read_first>
  <action>Create `22-SIMD-BENCHMARK-REPORT.md` linked to the raw-results document. Lead with the parity gate and reproduce D-04's exact scope: "identical encoded indexes and query results for documents that ingest without a parser-layer error," followed immediately by the tested malformed-input failure-layer attribution exclusion. Summarize each smoke fixture's stdlib-versus-SIMD deltas for ns/op, B/op, allocs/op, and input JSON MB/s from benchstat; separate observed values from inference, discuss sample variance, and explain how CPU and throughput trade against allocations in whichever direction the measurements actually fall. Per D-23, use symmetric language throughout and presuppose no speedup; per D-21, state that the numbers are steady-state and that `B/op` counts the Go heap only, so neither arm's `B/op` represents total process memory. Treat committed controlled evidence as authoritative and any future CI artifact as shared-runner-noisy trend data only. Per D-12, do not define or imply a regression threshold. End with a `## Recommendation` section and exactly one line `Recommendation class: **ship**`, `**defer**`, or `**narrow**`, supported by the measured workload context. A parity failure forces `defer` regardless of speed; otherwise choose on evidence without inventing a target. Do not alter production code or claim unsupported platforms/default selection.</action>
  <verify>
    <automated>test -s .planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-REPORT.md &amp;&amp; rg -q '22-SIMD-BENCHMARK-RESULTS\.md' .planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-REPORT.md &amp;&amp; rg -Fq 'identical encoded indexes and query results for documents that ingest without a parser-layer error' .planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-REPORT.md &amp;&amp; rg -q '^## Recommendation$' .planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-REPORT.md &amp;&amp; rg -q '^Recommendation class: \*\*(ship|defer|narrow)\*\*$' .planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - The report links the raw source, begins with parity, and preserves the exact D-04 qualification and tested exclusion.
    - Every smoke fixture has CPU, allocation-byte, allocation-count, and input-throughput interpretation derived from the raw document.
    - Shared-runner numbers are explicitly non-authoritative and no performance gate or threshold appears.
    - The report ends in exactly one evidence-backed `ship`, `defer`, or `narrow` class.
  </acceptance_criteria>
  <done>The measured SIMD path has a concise, auditable stop-table recommendation grounded in controlled evidence.</done>
</task>

</tasks>

<threat_model>
## Assets

- Authoritative performance evidence and recommendation.
- Exact tool/module versions used to produce it.
- Local machine metadata that must be sufficient but not over-disclosed.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Benchmark process → committed raw document | Samples must remain paired, complete, and tied to the exact revision/environment. |
| Exact x/perf tool → statistical comparison | An external ephemeral tool interprets the single output file. |
| Local environment → public evidence | Required metadata crosses into committed docs; unrelated environment data must not. |

## STRIDE Threat Register

| Threat ID | Category | Asset / Boundary | Severity | Disposition | Mitigation | Verification |
|-----------|----------|------------------|----------|-------------|------------|--------------|
| T-22-02 | Tampering | Wrong native version → evidence | HIGH | mitigate | Record effective version from `go list -m`, revision/dirty state, and run after exact-cache parity plans. | Raw document metadata and Plan 22-02/22-03 green summaries. |
| T-22-04 | Tampering / Information Disclosure | Tool/environment → committed evidence | HIGH | mitigate | Human-approved exact x/perf pseudo-version, one unstitched output, full raw samples, no environment dump/native artifact/cache, controlled host only. | Exact command/version and required tables in the raw document; final manual evidence review in Plan 22-08. |
</threat_model>

<verification>
- Confirm both evidence documents are non-empty and internally linked.
- Re-run the focused parity command; no evidence is valid if it fails.
- Check all values in the report trace to the raw document and all four metric classes are labeled correctly.
- Run `git diff --check -- .planning/phases/22-simd-validation-benchmarks-ci/22-SIMD-BENCHMARK-*.md`.
</verification>

<success_criteria>
- Controlled evidence is reproducible from pinned commands, metadata, and raw samples.
- The report makes one stop-table recommendation without a shared-runner threshold.
- Correctness remains the non-negotiable prerequisite for any ship or narrow recommendation.
</success_criteria>

<output>
Create `.planning/phases/22-simd-validation-benchmarks-ci/22-06-SUMMARY.md` when done.
</output>
