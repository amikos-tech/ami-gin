---
phase: 22-simd-validation-benchmarks-ci
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - parser_simd_integration_test.go
  - parser_parity_simd_test.go
  - parser_parity_test.go
  - benchmark_test.go
  - parser_parity_phase20_simd_test.go
autonomous: true
requirements: [SIMD-08, SIMD-10]

must_haves:
  truths:
    - "Per D-01 and D-03, the 12 authored fixtures retain their existing golden oracle while all four Phase 20 fixtures are compared live in one tagged process; no Phase 20 binary goldens are added."
    - "Per D-01, stdlib and SIMD return identical encoded bytes, the same 24 Evaluate-matrix results, and the same per-fixture Phase 20 query results."
    - "Per D-04, the parity claim is exactly: identical encoded indexes and query results for documents that ingest without a parser-layer error; malformed-input failure-layer attribution remains an explicit tested exclusion."
    - "Per D-07, every native-dependent tagged test routes construction through one shared helper: unsupported platforms skip before construction, supported local hosts may skip with remediation when SIMD is unavailable, and `AMI_GIN_SIMD_REQUIRED=1` turns any supported-host construction failure into a classified fatal error."
    - "The three-state policy is proven without depending on the local native installation: all five supported pairs, representative unsupported pairs, all six named sentinels, and an unknown error have deterministic table-driven coverage."
    - "An AST-based executable guard scans every `//go:build simdjson` test file, recognizes both unqualified and qualified calls whose final callee name is `NewSIMDParser`, requires the sole same-package call to remain inside `newTestSIMDParser`, and permits only the optional compile-only `ExampleNewSIMDParser` selector call when it has no output directive; no executable tagged test path can bypass D-07."
    - "Any encoded-byte or query-result mismatch in the qualified parity scope is a Phase 19 HARD stop; later benchmark and shipping evidence cannot proceed."
  artifacts:
    - path: "parser_simd_integration_test.go"
      provides: "Shared testing.TB SIMD constructor, native-free three-state policy tests, and identifier/selector raw-constructor call guard"
      contains: "AMI_GIN_SIMD_REQUIRED"
    - path: "parser_parity_simd_test.go"
      provides: "Existing tagged soft-skip parity routed through the shared D-07 constructor policy"
    - path: "parser_parity_phase20_simd_test.go"
      provides: "Tagged Phase 20 byte and query differential"
      exports: ["TestSIMDParserPhase20Parity", "TestSIMDParserEvaluateParity"]
    - path: "parser_parity_test.go"
      provides: "Reusable Evaluate cases and parser-option build seam"
    - path: "benchmark_test.go"
      provides: "Variadic Phase 20 builder seam preserving existing callers"
  key_links:
    - from: "parser_parity_phase20_simd_test.go"
      to: "benchmark_test.go"
      via: "phase20SmokeFixtures, phase20LoadRawJSONL, and phase20BuildBenchmarkIndex"
      pattern: "phase20(SmokeFixtures|LoadRawJSONL|BuildBenchmarkIndex)"
    - from: "parser_parity_phase20_simd_test.go"
      to: "parser_parity_test.go"
      via: "assertByteIdentical and shared Evaluate matrix"
      pattern: "assertByteIdentical|evaluateMatrixCases"
    - from: "parser_simd_integration_test.go"
      to: ".github/workflows/ci.yml"
      via: "AMI_GIN_SIMD_REQUIRED consumed later by every supported CI leg"
      pattern: "AMI_GIN_SIMD_REQUIRED"
    - from: "parser_parity_simd_test.go"
      to: "parser_simd_integration_test.go"
      via: "newTestSIMDParser plus the exact identifier/selector raw-constructor allowlist"
      pattern: "newTestSIMDParser"
    - from: "simd_example_test.go"
      to: "parser_simd_integration_test.go"
      via: "optional external-package selector exception restricted to package gin_test ExampleNewSIMDParser with no runnable output directive"
      pattern: "ExampleNewSIMDParser|NewSIMDParser"
---

<objective>
Turn the already-observed SIMD parity result into a durable hard gate over authored fixtures, realistic Phase 20 data, and query evaluation, while preventing supported CI hosts from silently skipping SIMD.

Purpose: Establish correctness before any performance or release recommendation is allowed.
Output: A shared three-state SIMD test constructor, generalized existing helper seams, and tagged realistic byte/query parity tests.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-CONTEXT.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-RESEARCH.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-PATTERNS.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-VALIDATION.md
@.planning/phases/22-simd-validation-benchmarks-ci/22-01-SUMMARY.md

<interfaces>
Current seams to generalize without changing production behavior:

- `func newTestSIMDParser(t *testing.T) CloseableParser`
- `func buildEvaluateMatrixIndex(t *testing.T) *GINIndex`
- `func phase20BuildBenchmarkIndex(docs [][]byte) (*GINIndex, error)`
- `func (idx *GINIndex) Evaluate(predicates []Predicate) *RGSet`
- `func WithParser(p Parser) BuilderOption`
- `var phase20SmokeFixtures []phase20SmokeFixture`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce the three-state SIMD test construction policy</name>
  <files>parser_simd_integration_test.go, parser_parity_simd_test.go</files>
  <read_first>
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-CONTEXT.md` — D-05 through D-08, especially D-07's exact three states and sentinel set.
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-RESEARCH.md` — `Three-State Construction Helper`, security threat T-22-01, and package-approval dependency.
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-01-SUMMARY.md` — confirm pure-simdjson approval before executing tagged tests.
    - `parser_simd_integration_test.go` — preserve every existing tagged test and cleanup expectation.
    - `parser_parity_simd_test.go` — migrate its direct native construction to the shared helper without changing the soft-skip parity fixture or byte oracle.
    - `parser_simd_lifecycle_test.go` — reuse the phase's native-free callback/policy-test style; do not make failure-branch tests depend on an installed library.
    - `parser_simd.go` — `NewSIMDParser`, `CloseableParser`, and the existing construction-remediation path; do not change this production file.
    - `go.mod` — use the currently pinned module; never copy a version literal into this helper.
  </read_first>
  <behavior>
    - `supportedSIMDPlatform` returns true for exactly linux/amd64, linux/arm64, darwin/amd64, darwin/arm64, and windows/amd64 and false for representative unsupported pairs.
    - The policy result for an unsupported host is skip-before-construction; supported local construction failure is skip-with-remediation; supported required construction failure is fatal.
    - `classifySIMDLoadError` identifies all six D-07 sentinels through wrapped errors and returns an explicit unknown class for an unrelated error.
    - Successful construction registers exactly one cleanup and reports a close failure through the caller's test boundary.
    - Every native-dependent test in `parser_simd_integration_test.go` and `parser_parity_simd_test.go` obtains its parser through `newTestSIMDParser`; `parser_simd_lifecycle_test.go` remains native-free.
    - The tagged-test constructor guard normalizes both `ast.Ident` and `ast.SelectorExpr` callees to their final identifier, requires exactly one unqualified `NewSIMDParser` call enclosed by `newTestSIMDParser`, and rejects every other raw call except an optional qualified call in the exact compile-only `ExampleNewSIMDParser` with no recognized output directive.
    - A native-free external-package mutation containing `gin.NewSIMDParser()` inside a tagged `Test*` function fails the guard with a file/function diagnostic, while the exact no-output compile-only Example shape remains accepted for Plan 22-04.
  </behavior>
  <action>First add table-driven native-free tests in `parser_simd_integration_test.go` for `supportedSIMDPlatform`, `classifySIMDLoadError`, and a pure `simdConstructionPolicy(goos, goarch, required, err)` result that `newTestSIMDParser` will consume. Cover exactly the five supported pairs, at least linux/386 and freebsd/amd64 as unsupported, wrapped instances of `purejson.ErrCPUUnsupported`, `ErrABIVersionMismatch`, `ErrChecksumMismatch`, `ErrAllSourcesFailed`, `ErrInvalidHandle`, and `ErrClosed`, plus one unknown error. Then change `newTestSIMDParser` to accept `testing.TB` so tests, fuzz targets, and benchmarks share one lifecycle policy. Define the CI-only flag as `AMI_GIN_SIMD_REQUIRED` (planner discretion resolved). Evaluate platform support before calling `NewSIMDParser`: outside the locked set call `Skip` with `pure-simdjson unsupported platform`; on a supported host where construction fails and the flag is not `1`, skip with the error and `docs/simd-deployment.md` remediation; on a supported host with `AMI_GIN_SIMD_REQUIRED=1`, fail fatally with the pure helper's sentinel class. Preserve immediate `Cleanup` registration and report any `Close` error. Inventory all root-package `_test.go` files whose first line is exactly `//go:build simdjson`: `parser_simd_integration_test.go` and `parser_parity_simd_test.go` are native-dependent, while `parser_simd_lifecycle_test.go` uses injected native-free seams. Replace the direct constructor in `TestSIMDParserSoftSkipAndMalformedJSONByteParity` and the direct constructors in `TestSIMDParserCloseIsIdempotent`, `TestSIMDParserCloseErrorPropagatesWhenParserBusy`, and `TestSIMDParserParseAfterExternalCloseReturnsUsableBuilderError` with `newTestSIMDParser`, preserving manual-close, concrete-type, busy-error, and post-close assertions; redundant cleanup after an intentional successful close relies on the already-tested idempotent close contract. Add `TestSIMDConstructorCallPolicy` using Go's standard `go/parser`, `go/ast`, and `go/doc`: discover every SIMD-tagged test file, parse comments, ignore constructor-like text in comments and string literals, and inspect each call expression whose callee is either an `ast.Ident` or an `ast.SelectorExpr` with final identifier `NewSIMDParser`. Require exactly one same-package unqualified call in `parser_simd_integration_test.go` enclosed by `newTestSIMDParser`. Permit zero or one additional call only when it is a qualified selector in exact file `simd_example_test.go`, package `gin_test`, top-level function `ExampleNewSIMDParser`, and `go/doc` Example metadata proves there is no `Output:` or `Unordered output:` directive, including an empty directive; this exception is optional so Plan 22-02 passes before Plan 22-04 creates the file. Reject every other matched call regardless of enclosing declaration, including `Test*`, `Benchmark*`, `Fuzz*`, `TestMain`, `init`, helper functions, package-level initializers, and any runnable Example. Extract the scan behind a native-free validator that accepts a directory or parsed source set. In `TestSIMDConstructorCallPolicy`, add a temp-source mutation subtest named `external_qualified_test_mutation` whose tagged external-package `TestExternalConstructorBypass` calls `gin.NewSIMDParser()` and assert the validator fails with the file and function in its diagnostic; add a positive exact compile-only Example fixture and a negative copy with an empty output directive so Plan 22-04's narrow exception is proven rather than assumed. Constructor-policy tests themselves receive no raw-call exception. Do not match error strings, change `NewSIMDParser`, add another constructor wrapper, add fallback behavior, or duplicate upstream download/checksum/cache/ABI logic. This addresses both the deterministic failure-branch review concern and every unqualified or qualified D-07 bypass path.</action>
  <verify>
    <automated>go test -tags simdjson -run '^(TestSupportedSIMDPlatform|TestClassifySIMDLoadError|TestSIMDConstructionPolicy|TestSIMDConstructorCallPolicy)$' -count=1 . &amp;&amp; AMI_GIN_SIMD_REQUIRED=1 go test -tags simdjson -run '^TestSIMDParser' -count=1 . &amp;&amp; make simd-isolation-check &amp;&amp; go test ./...</automated>
  </verify>
  <acceptance_criteria>
    - `newTestSIMDParser` accepts `testing.TB`, constructs once, and registers caller-owned cleanup once.
    - The supported-platform allowlist contains exactly the five D-05 GOOS/GOARCH pairs.
    - With `AMI_GIN_SIMD_REQUIRED=1`, no supported-host constructor error can become a skip.
    - Failure classification uses `errors.Is` with all six D-07 sentinels and no string matching.
    - Native-free policy tests prove unsupported skip, supported-local skip, supported-required fatal, successful construction, and unknown-error classification without altering the installed native library.
    - Every existing native-dependent tagged test uses `newTestSIMDParser`; the lifecycle file remains native-free, and the full `^TestSIMDParser` run exercises the migrated parity and manual-close tests under `AMI_GIN_SIMD_REQUIRED=1`.
    - `TestSIMDConstructorCallPolicy` scans all SIMD-tagged test files, recognizes unqualified and qualified constructor callees, and proves the only allowed calls are the sole same-package helper call plus the optional exact no-output compile-only Example selector; every other call fails without a broad package, filename, or external-test exception.
    - The `external_qualified_test_mutation` subtest objectively proves a tagged external-package `Test*` containing `gin.NewSIMDParser()` is rejected, and the paired Example fixtures prove that absence of an output directive is required for Plan 22-04's exception.
    - Default dependency isolation remains green.
  </acceptance_criteria>
  <done>Tagged evidence has one reusable constructor policy that distinguishes unsupported, optional-local, and required-CI execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin realistic encoded-byte and query parity</name>
  <files>parser_parity_test.go, benchmark_test.go, parser_parity_phase20_simd_test.go</files>
  <read_first>
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-CONTEXT.md` — D-01, D-03, and D-04, including the exact qualified parity claim and rejection of Phase 20 goldens.
    - `.planning/phases/22-simd-validation-benchmarks-ci/22-PATTERNS.md` — `parser_parity_test.go`, `benchmark_test.go`, and `parser_parity_phase20_simd_test.go` analogs.
    - `parser_parity_test.go` — `assertByteIdentical`, `evaluateMatrixFixture`, `buildEvaluateMatrixIndex`, and the current 24 cases.
    - `benchmark_test.go` — `phase20SmokeFixtures`, `phase20LoadRawJSONL`, `phase20DocsPerRowGroup`, and `phase20BuildBenchmarkIndex`.
    - `parser_parity_fixtures_test.go` — authored fixture inventory already covered by the existing golden test.
    - `parser_simd_integration_test.go` — use the Task 1 constructor and preserve `TestSIMDParserMalformedTrailingNumericKnownPolicyAsymmetry`.
    - `gin.go` — non-serialized derived-state note supporting bytes-imply-Evaluate, without changing index behavior.
  </read_first>
  <behavior>
    - All four entries in `phase20SmokeFixtures` build successfully through stdlib and one reused SIMD parser and encode byte-for-byte identically.
    - Each fixture's registered `phase20SmokeQuery.predicate` returns the same row-group slice under both parser arms.
    - The existing 24 matching/pruning cases return the same expected row groups under stdlib and SIMD.
    - The existing authored golden test and malformed trailing-number attribution test remain green.
  </behavior>
  <action>First generalize existing untagged seams without breaking current callers: change `buildEvaluateMatrixIndex(t)` to `buildEvaluateMatrixIndex(t, opts ...BuilderOption)` and forward `opts...` into `NewBuilder`; extract the complete 24-case table into one reusable untagged `evaluateMatrixCases` helper while keeping the known-path/pruning comment and existing stdlib test; change `phase20BuildBenchmarkIndex(docs)` to `phase20BuildBenchmarkIndex(docs, opts ...BuilderOption)` and forward options while preserving row-group packing and `pkg/errors` wrapping. Then create `parser_parity_phase20_simd_test.go` with `//go:build simdjson` byte-for-byte on line 1. In `TestSIMDParserPhase20Parity`, construct one SIMD parser, iterate every `phase20SmokeFixtures` entry, load documents through `phase20LoadRawJSONL`, build stdlib and SIMD indexes in the same process with identical configuration, encode both, and call `assertByteIdentical` with fixture-qualified diagnostics; compare `Evaluate` results for the fixture predicate. In `TestSIMDParserEvaluateParity`, build the shared matrix once per parser and evaluate the extracted 24 cases, asserting both the expected row groups and cross-parser equality. Per D-03, do not create or regenerate any Phase 20 `.bin` golden. Per D-04, any byte/query mismatch for an input that ingests without parser-layer error is fatal; retain the malformed failure-layer attribution test as the explicit non-byte exclusion. Do not change parser behavior, public API, CLI behavior, defaults, or fixture generation.</action>
  <verify>
    <automated>AMI_GIN_SIMD_REQUIRED=1 go test -tags simdjson -run '^(TestSIMDParserGoldenAuthoredFixtures|TestSIMDParserPhase20Parity|TestSIMDParserEvaluateParity|TestSIMDParserMalformedTrailingNumericKnownPolicyAsymmetry)$' -count=1 . &amp;&amp; go test ./... &amp;&amp; make simd-isolation-check</automated>
  </verify>
  <acceptance_criteria>
    - The focused command executes the existing authored oracle, all four Phase 20 fixture differentials, all 24 shared Evaluate cases, and the known malformed attribution exclusion.
    - `testdata/parity-golden/` has no new Phase 20 files and no existing golden changes.
    - Existing untagged callers compile unchanged through variadic helper defaults.
    - Failures identify fixture, parser arm or byte offset, predicate case, and actual/expected row groups.
    - No production parser, parser API, CLI, fixture generator, or default behavior is modified.
  </acceptance_criteria>
  <done>Encoded-byte and query parity are a deterministic hard gate over authored and realistic fixtures under the exact D-04 scope.</done>
</task>

</tasks>

<threat_model>
## Assets

- The correctness oracle for encoded indexes and query pruning.
- Supported-platform CI enforcement semantics.
- Default-build dependency isolation.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture bytes → stdlib/SIMD builders | Identical untrusted documents must reach both arms with identical configuration. |
| Native constructor → tagged test harness | Local absence may skip, but required supported CI must fail visibly. |
| Tagged files → default dependency graph | Optional native imports must remain absent from ordinary builds. |

## STRIDE Threat Register

| Threat ID | Category | Asset / Boundary | Severity | Disposition | Mitigation | Verification |
|-----------|----------|------------------|----------|-------------|------------|--------------|
| T-22-01 | Spoofing / Repudiation | Supported CI → parity evidence | HIGH | mitigate | Exact five-platform allowlist plus `AMI_GIN_SIMD_REQUIRED=1` fatal policy, sentinel-classified constructor diagnostics, and an identifier/selector AST guard whose only non-helper exception is the no-output compile-only Example. | Focused tagged command with the external-package selector mutation; later workflow contract requires the flag on every supported leg. |
| T-22-04 | Tampering | Tagged dependency → evidence | HIGH | mitigate | Plan 22-01 provenance approval, existing pin/checksums, build-tag isolation, and no new dependency. | `22-01-SUMMARY.md`, `make simd-isolation-check`, and unchanged `go.mod`/`go.sum`. |
</threat_model>

<verification>
- Run the deterministic three-state policy plus AST constructor-guard command, the full existing `^TestSIMDParser` migration command, and the parity command mapped to `22-VALIDATION.md` Tasks 22-02-01 and 22-02-02.
- Run `go test ./...` to prove generalized helpers preserve default behavior.
- Run `make simd-isolation-check` to prove new tagged imports do not enter the default graph.
- If any in-scope byte or query mismatch appears, stop the phase as a HARD failure; do not proceed to plans 22-05 or 22-06.
</verification>

<success_criteria>
- Authored goldens, four realistic fixture byte comparisons, 24 Evaluate cases, and four fixture predicates pass under the approved native module.
- The only retained qualified exclusion is malformed-input failure-layer attribution, pinned by its existing test and described with D-04's exact claim.
- Supported CI cannot report success merely because native parser construction skipped, and no tagged test can silently reintroduce an unqualified or qualified constructor bypass; the sole external selector exception remains compile-only.
</success_criteria>

<output>
Create `.planning/phases/22-simd-validation-benchmarks-ci/22-02-SUMMARY.md` when done.
</output>
