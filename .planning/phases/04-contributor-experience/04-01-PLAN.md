---
phase: 04-contributor-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CONTRIBUTING.md
  - SECURITY.md
  - README.md
autonomous: true
requirements:
  - CONTR-01
  - CONTR-02
  - CONTR-03
must_haves:
  truths:
    - New contributors can discover the local workflow by running `make help` near the start of `CONTRIBUTING.md`.
    - A task-first root `CONTRIBUTING.md` documents the exact contributor commands `make build`, `make test`, `make integration-test`, `make lint`, `make lint-fix`, `make security-scan`, and `make clean`.
    - A root `SECURITY.md` tells reporters to use GitHub private vulnerability reporting when available, publishes `security@amikos.tech` as fallback, and tells reporters not to disclose vulnerabilities in public issues, discussions, or pull requests.
    - The first non-empty line after `# GIN Index` in `README.md` is a minimal badge row containing exactly `CI`, `Go Reference`, and `MIT`.
  artifacts:
    - `CONTRIBUTING.md` exists with `Development Setup`, `Local Checks`, `Pull Request Workflow`, and `Security` sections plus an early `make help` discovery step.
    - `SECURITY.md` exists with reporting channels, disclosure expectations, and supported-version wording.
    - `README.md` contains the existing CI badge plus a `pkg.go.dev` Go Reference badge and an MIT badge linked to `LICENSE`.
  key_links:
    - `CONTRIBUTING.md` links back to `README.md` for installation/examples and to `SECURITY.md` for disclosure guidance instead of duplicating those sections.
    - The first non-empty line after the README title is the only badge row used for Phase 4, and it contains exactly three badge markers.
---

<objective>
Add the public-facing contributor documentation surface for Phase 4: a task-first contributor guide, a real security disclosure policy, and the final minimal trust badge row in README.

Purpose: Complete CONTR-01, CONTR-02, and CONTR-03 without bloating README or inventing new local command entrypoints.
Output: New `CONTRIBUTING.md`, new `SECURITY.md`, and an updated `README.md`.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-contributor-experience/04-CONTEXT.md
@.planning/phases/04-contributor-experience/04-RESEARCH.md
@.planning/phases/04-contributor-experience/04-VALIDATION.md
@.planning/phases/04-contributor-experience/04-REVIEWS.md
@.planning/phases/03-ci-pipeline/03-CONTEXT.md
@AGENTS.md
@CLAUDE.md
@README.md
@Makefile
@.github/workflows/ci.yml
@.github/workflows/security.yml

<interfaces>
Required README badge row:
```text
Keep the existing GitHub Actions CI badge and add only two more badges:
- Go Reference -> https://pkg.go.dev/github.com/amikos-tech/ami-gin
- MIT -> LICENSE
The first non-empty line after `# GIN Index` must be the only Phase 4 badge row.
```

Required contributor command surface:
```text
make help
make build
make test
make integration-test
make lint
make lint-fix
make security-scan
make clean
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a task-first contributor guide with explicit `make help` discovery and fenced command blocks</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
- CONTRIBUTING.md
- README.md
- Makefile
- .planning/phases/04-contributor-experience/04-CONTEXT.md
- .planning/phases/04-contributor-experience/04-RESEARCH.md
- .planning/phases/04-contributor-experience/04-REVIEWS.md
- .planning/phases/03-ci-pipeline/03-CONTEXT.md
- AGENTS.md
- CLAUDE.md
  </read_first>
  <action>Create a new root `CONTRIBUTING.md` with these exact top-level sections: `# Contributing`, `## Development Setup`, `## Local Checks`, `## Pull Request Workflow`, and `## Security`. Addresses review concern: clarify command formatting by using fenced `bash` blocks, not one-command-per-line inline backtick entries, so the plan and checks agree. In `## Development Setup`, add one short introductory sentence telling contributors to run `make help` first, then include a fenced `bash` block whose first command is exactly `make help`. State that contributors need Go installed locally and should also have `golangci-lint` and `govulncheck` available; note that `make test` installs `gotestsum` automatically through the existing `Makefile`. In `## Local Checks`, add one fenced `bash` block containing these exact commands on separate lines and in this order: `make build`, `make test`, `make integration-test`, `make lint`, `make lint-fix`, `make security-scan`, `make clean`. In `## Pull Request Workflow`, describe the minimal `branch from main -> run local checks -> open PR` flow and explicitly tell contributors to keep README adopter-focused by linking back to README for installation, examples, and architecture instead of duplicating them here. In `## Security`, link directly to `SECURITY.md` for disclosure guidance. Do not add release, benchmark, or architecture sections.</action>
  <acceptance_criteria>
- `test -f CONTRIBUTING.md` succeeds.
- `rg -n '^# Contributing$' CONTRIBUTING.md` returns one match.
- `rg -n '^## Development Setup$' CONTRIBUTING.md` succeeds.
- `rg -n '^## Local Checks$' CONTRIBUTING.md` succeeds.
- `rg -n '^## Pull Request Workflow$' CONTRIBUTING.md` succeeds.
- `rg -n '^## Security$' CONTRIBUTING.md` succeeds.
- `test "$(rg -n '^```bash$' CONTRIBUTING.md | wc -l | tr -d ' ')" -ge 2` succeeds.
- `setup_section="$(sed -n '/^## Development Setup$/,/^## Local Checks$/p' CONTRIBUTING.md)"` captures the setup section.
- `printf '%s\n' "$setup_section" | rg -n 'Go.*installed locally|installed locally.*Go|need Go'` succeeds, proving the Go prerequisite is documented.
- `printf '%s\n' "$setup_section" | rg -n 'golangci-lint'` succeeds.
- `printf '%s\n' "$setup_section" | rg -n 'govulncheck'` succeeds.
- `printf '%s\n' "$setup_section" | rg -n 'gotestsum'` succeeds.
- `first_setup_cmd="$(printf '%s\n' "$setup_section" | awk '/^```bash$/{in_block=1; next} in_block && /^```$/{exit} in_block {print; exit}')" && test "$first_setup_cmd" = "make help"` succeeds.
- `expected_local_checks="$(cat <<'EOF'\nmake build\nmake test\nmake integration-test\nmake lint\nmake lint-fix\nmake security-scan\nmake clean\nEOF\n)"` defines the required ordered Local Checks block.
- `local_checks_block="$(sed -n '/^## Local Checks$/,/^## Pull Request Workflow$/p' CONTRIBUTING.md | awk '/^```bash$/{in_block=1; next} in_block && /^```$/{exit} in_block {print}')" && test "$local_checks_block" = "$expected_local_checks"` succeeds.
- `pr_section="$(sed -n '/^## Pull Request Workflow$/,/^## Security$/p' CONTRIBUTING.md)"` captures the PR workflow section.
- `printf '%s\n' "$pr_section" | rg -n 'branch from main'` succeeds.
- `printf '%s\n' "$pr_section" | rg -n 'run local checks'` succeeds.
- `printf '%s\n' "$pr_section" | rg -n 'open PR'` succeeds.
- `rg -n '\[README\.md\]\(README\.md\)' CONTRIBUTING.md` returns the explicit README markdown link.
- `rg -n '\[SECURITY\.md\]\(SECURITY\.md\)' CONTRIBUTING.md` returns the explicit security policy markdown link.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
test -f CONTRIBUTING.md
rg -n '^# Contributing$' CONTRIBUTING.md
rg -n '^## Development Setup$' CONTRIBUTING.md
rg -n '^## Local Checks$' CONTRIBUTING.md
rg -n '^## Pull Request Workflow$' CONTRIBUTING.md
rg -n '^## Security$' CONTRIBUTING.md
test "$(rg -n '^```bash$' CONTRIBUTING.md | wc -l | tr -d ' ')" -ge 2
setup_section="$(sed -n '/^## Development Setup$/,/^## Local Checks$/p' CONTRIBUTING.md)"
printf '%s\n' "$setup_section" | rg -n 'Go.*installed locally|installed locally.*Go|need Go'
printf '%s\n' "$setup_section" | rg -n 'golangci-lint'
printf '%s\n' "$setup_section" | rg -n 'govulncheck'
printf '%s\n' "$setup_section" | rg -n 'gotestsum'
first_setup_cmd="$(printf '%s\n' "$setup_section" | awk '/^```bash$/{in_block=1; next} in_block && /^```$/{exit} in_block {print; exit}')"
test "$first_setup_cmd" = "make help"
expected_local_checks="$(cat <<'EOF'
make build
make test
make integration-test
make lint
make lint-fix
make security-scan
make clean
EOF
)"
local_checks_block="$(sed -n '/^## Local Checks$/,/^## Pull Request Workflow$/p' CONTRIBUTING.md | awk '/^```bash$/{in_block=1; next} in_block && /^```$/{exit} in_block {print}')"
test "$local_checks_block" = "$expected_local_checks"
pr_section="$(sed -n '/^## Pull Request Workflow$/,/^## Security$/p' CONTRIBUTING.md)"
printf '%s\n' "$pr_section" | rg -n 'branch from main'
printf '%s\n' "$pr_section" | rg -n 'run local checks'
printf '%s\n' "$pr_section" | rg -n 'open PR'
rg -n '\[README\.md\]\(README\.md\)' CONTRIBUTING.md
rg -n '\[SECURITY\.md\]\(SECURITY\.md\)' CONTRIBUTING.md</automated>
  </verify>
  <done>The repository has a root contributor guide that reflects the real local workflow, starts with `make help` discovery, and keeps README adopter-focused.</done>
</task>

<task type="auto">
  <name>Task 2: Publish a real security disclosure policy without inventing a bug bounty program</name>
  <files>SECURITY.md</files>
  <read_first>
- SECURITY.md
- .planning/phases/04-contributor-experience/04-CONTEXT.md
- .planning/phases/04-contributor-experience/04-RESEARCH.md
- .planning/phases/04-contributor-experience/04-REVIEWS.md
- .github/workflows/security.yml
- README.md
  </read_first>
  <action>Create a new root `SECURITY.md` with these exact top-level sections: `# Security Policy`, `## Reporting a Vulnerability`, `## Preferred Channel`, `## Fallback Contact`, `## Disclosure Expectations`, and `## Supported Versions`. In `## Preferred Channel`, say that when the public repository exposes GitHub private vulnerability reporting, reporters should use that private GitHub flow first. In `## Fallback Contact`, publish the exact email address `security@amikos.tech`. In `## Reporting a Vulnerability`, explicitly tell reporters not to open public issues, public pull requests, or public discussions for suspected vulnerabilities. In `## Disclosure Expectations`, use exact low-overhead OSS timing language: acknowledge reports within `5 business days` and send follow-up status updates as triage progresses. In `## Supported Versions`, use exact lightweight wording that support currently applies to the latest development line on `main` and, once releases exist, the latest tagged release. Addresses review concern: keep the policy factual and do not add bug bounty, CVE reward, or compensation language because Phase 4 should not invent maintainer policy that the user did not choose.</action>
  <acceptance_criteria>
- `test -f SECURITY.md` succeeds.
- `rg -n '^# Security Policy$' SECURITY.md` returns one match.
- `rg -n '^## Reporting a Vulnerability$' SECURITY.md` succeeds.
- `rg -n '^## Preferred Channel$' SECURITY.md` succeeds.
- `rg -n '^## Fallback Contact$' SECURITY.md` succeeds.
- `rg -n '^## Disclosure Expectations$' SECURITY.md` succeeds.
- `rg -n '^## Supported Versions$' SECURITY.md` succeeds.
- `preferred_section="$(sed -n '/^## Preferred Channel$/,/^## Fallback Contact$/p' SECURITY.md)"` captures the preferred-channel section.
- `printf '%s\n' "$preferred_section" | rg -n 'GitHub private vulnerability reporting.*first|first.*GitHub private vulnerability reporting'` succeeds, proving GitHub private reporting is the preferred first channel.
- `rg -n 'security@amikos\\.tech' SECURITY.md` returns the fallback email.
- `rg -n 'public issues' SECURITY.md` returns the no-public-disclosure issues guidance.
- `rg -n 'public pull requests' SECURITY.md` returns the no-public-disclosure PR guidance.
- `rg -n 'public discussions' SECURITY.md` returns the no-public-disclosure discussion guidance.
- `disclosure_section="$(sed -n '/^## Disclosure Expectations$/,/^## Supported Versions$/p' SECURITY.md)"` captures the disclosure-expectations section.
- `printf '%s\n' "$disclosure_section" | rg -n '5 business days'` returns the acknowledgement timing.
- `printf '%s\n' "$disclosure_section" | rg -n 'follow-up'` succeeds.
- `printf '%s\n' "$disclosure_section" | rg -n 'status updates'` succeeds.
- `printf '%s\n' "$disclosure_section" | rg -n 'triage progresses'` succeeds, proving follow-up updates are promised during triage.
- `rg -n 'latest development line' SECURITY.md` returns the supported-version wording.
- `rg -n 'main' SECURITY.md` returns the supported-version branch wording.
- `rg -n 'latest tagged release' SECURITY.md` returns the supported-version release wording.
- `! rg -n 'bug bounty|bounty|reward' SECURITY.md` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
test -f SECURITY.md
rg -n '^# Security Policy$' SECURITY.md
rg -n '^## Reporting a Vulnerability$' SECURITY.md
rg -n '^## Preferred Channel$' SECURITY.md
rg -n '^## Fallback Contact$' SECURITY.md
rg -n '^## Disclosure Expectations$' SECURITY.md
rg -n '^## Supported Versions$' SECURITY.md
preferred_section="$(sed -n '/^## Preferred Channel$/,/^## Fallback Contact$/p' SECURITY.md)"
printf '%s\n' "$preferred_section" | rg -n 'GitHub private vulnerability reporting.*first|first.*GitHub private vulnerability reporting'
rg -n 'security@amikos\.tech' SECURITY.md
rg -n 'public issues' SECURITY.md
rg -n 'public pull requests' SECURITY.md
rg -n 'public discussions' SECURITY.md
disclosure_section="$(sed -n '/^## Disclosure Expectations$/,/^## Supported Versions$/p' SECURITY.md)"
printf '%s\n' "$disclosure_section" | rg -n '5 business days'
printf '%s\n' "$disclosure_section" | rg -n 'follow-up'
printf '%s\n' "$disclosure_section" | rg -n 'status updates'
printf '%s\n' "$disclosure_section" | rg -n 'triage progresses'
rg -n 'latest development line' SECURITY.md
rg -n 'main' SECURITY.md
rg -n 'latest tagged release' SECURITY.md
! rg -n 'bug bounty|bounty|reward' SECURITY.md</automated>
  </verify>
  <done>The repository exposes a real coordinated-disclosure policy with a preferred private path, a stable fallback email, and no speculative bounty language.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the README trust row to exactly CI, Go Reference, and MIT with less brittle verification</name>
  <files>README.md</files>
  <read_first>
- README.md
- CONTRIBUTING.md
- SECURITY.md
- LICENSE
- .planning/phases/04-contributor-experience/04-CONTEXT.md
- .planning/phases/04-contributor-experience/04-RESEARCH.md
- .planning/phases/04-contributor-experience/04-REVIEWS.md
- .planning/phases/03-ci-pipeline/03-ci-pipeline-02-SUMMARY.md
  </read_first>
  <action>Update `README.md` in place so the first non-empty line after `# GIN Index` is a single three-badge markdown row containing these exact badge links and no others: `[![CI](https://github.com/amikos-tech/ami-gin/actions/workflows/ci.yml/badge.svg?branch=main&event=push)](https://github.com/amikos-tech/ami-gin/actions/workflows/ci.yml)`, `[![Go Reference](https://pkg.go.dev/badge/github.com/amikos-tech/ami-gin.svg)](https://pkg.go.dev/github.com/amikos-tech/ami-gin)`, and `[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)`. Addresses review concern: keep badge scope locked to exactly three badges but make verification semantic rather than a single full-line regex, so spacing between badge links may vary while the first badge row still stays minimal. Add this exact short discovery sentence immediately below the badge row: `See [CONTRIBUTING.md](CONTRIBUTING.md) for local contributor workflows and [SECURITY.md](SECURITY.md) for disclosure guidance.` Keep the existing README product narrative intact. Use the standard `pkg.go.dev` badge URL even if preview rendering stays limited until the repo is public; do not replace it with another badge service.</action>
  <acceptance_criteria>
- `rg -n '^# GIN Index$' README.md` returns the title line.
- `badge_row="$(awk 'NR==1{next} NF{print; exit}' README.md)"` captures the first non-empty line after the title.
- `printf '%s\n' "$badge_row" | rg '\[!\[CI\]\(https://github\.com/amikos-tech/ami-gin/actions/workflows/ci\.yml/badge\.svg\?branch=main&event=push\)\]\(https://github\.com/amikos-tech/ami-gin/actions/workflows/ci\.yml\)'` succeeds.
- `printf '%s\n' "$badge_row" | rg '\[!\[Go Reference\]\(https://pkg\.go\.dev/badge/github\.com/amikos-tech/ami-gin\.svg\)\]\(https://pkg\.go\.dev/github\.com/amikos-tech/ami-gin\)'` succeeds.
- `printf '%s\n' "$badge_row" | rg '\[!\[License: MIT\]\(https://img\.shields\.io/badge/License-MIT-yellow\.svg\)\]\(LICENSE\)'` succeeds.
- `test "$(printf '%s\n' "$badge_row" | rg -o '\[!\[' | wc -l | tr -d ' ')" = "3"` succeeds.
- `rg -n '^See \[CONTRIBUTING\.md\]\(CONTRIBUTING\.md\) for local contributor workflows and \[SECURITY\.md\]\(SECURITY\.md\) for disclosure guidance\.$' README.md` returns one match.
- `! sed -n '1,8p' README.md | rg -n 'Coverage|Codecov|Go Report Card|OpenSSF|Stars|Downloads'` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
rg -n '^# GIN Index$' README.md
badge_row="$(awk 'NR==1{next} NF{print; exit}' README.md)"
printf '%s\n' "$badge_row" | rg '\[!\[CI\]\(https://github\.com/amikos-tech/ami-gin/actions/workflows/ci\.yml/badge\.svg\?branch=main&event=push\)\]\(https://github\.com/amikos-tech/ami-gin/actions/workflows/ci\.yml\)'
printf '%s\n' "$badge_row" | rg '\[!\[Go Reference\]\(https://pkg\.go\.dev/badge/github\.com/amikos-tech/ami-gin\.svg\)\]\(https://pkg\.go\.dev/github\.com/amikos-tech/ami-gin\)'
printf '%s\n' "$badge_row" | rg '\[!\[License: MIT\]\(https://img\.shields\.io/badge/License-MIT-yellow\.svg\)\]\(LICENSE\)'
test "$(printf '%s\n' "$badge_row" | rg -o '\[!\[' | wc -l | tr -d ' ')" = "3"
rg -n '^See \[CONTRIBUTING\.md\]\(CONTRIBUTING\.md\) for local contributor workflows and \[SECURITY\.md\]\(SECURITY\.md\) for disclosure guidance\.$' README.md
! sed -n '1,8p' README.md | rg -n 'Coverage|Codecov|Go Report Card|OpenSSF|Stars|Downloads'
go test ./... -run TestQueryEQ -count=1
make help</automated>
  </verify>
  <done>The README header exposes the exact three Phase 4 trust badges, keeps the first screen minimal, and points contributors to the new policy docs without losing adopter focus.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public repo docs -> outside contributors | Contributors rely on README and CONTRIBUTING for the correct local commands and workflows. |
| Vulnerability reporter -> maintainer intake channels | Reporters rely on SECURITY.md to choose a private channel instead of public disclosure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | `CONTRIBUTING.md` command surface | mitigate | Document only literal existing `make` targets, surface `make help` as the discovery entrypoint, and verify the exact commands against the current Makefile contract. |
| T-04-02 | I | `SECURITY.md` reporting guidance | mitigate | State the no-public-disclosure rule explicitly, publish `security@amikos.tech` as fallback, and avoid speculative bounty language so reporters always have a private, factual reporting path. |
| T-04-03 | S | README badge row | mitigate | Keep the first badge row constrained to exactly three verified badge links tied to the real CI workflow, package import path, and repository license file. |
</threat_model>

<verification>
1. Contributor guide static checks:
   `test -f CONTRIBUTING.md`
   `sed -n '/^## Development Setup$/,/^## Local Checks$/p' CONTRIBUTING.md | rg -n '^make help$'`
   `sed -n '/^## Local Checks$/,/^## Pull Request Workflow$/p' CONTRIBUTING.md | rg -n '^make build$|^make test$|^make integration-test$|^make lint$|^make lint-fix$|^make security-scan$|^make clean$'`
2. Security policy static checks:
   `test -f SECURITY.md`
   `rg -n 'GitHub private vulnerability reporting|security@amikos\\.tech|Do not open public issues|public pull requests|public discussions|5 business days' SECURITY.md`
   `! rg -n 'bug bounty|bounty|reward' SECURITY.md`
3. README trust row:
   `badge_row="$(awk 'NR==1{next} NF{print; exit}' README.md)"`
   `printf '%s\n' "$badge_row" | rg '\[!\[CI\]\(|\[!\[Go Reference\]\(|\[!\[License: MIT\]\('`
   `test "$(printf '%s\n' "$badge_row" | rg -o '\[!\[' | wc -l | tr -d ' ')" = "3"`
4. Repo smoke:
   `go test ./... -run TestQueryEQ -count=1`
   `make help`
</verification>

<success_criteria>
The repository exposes contributor and security docs that match the real local workflow, and the first README badge row contains exactly the three Phase 4 trust badges without turning the page into a badge wall.
</success_criteria>

<output>
After completion, create `.planning/phases/04-contributor-experience/04-contributor-experience-01-SUMMARY.md`
</output>
