---
phase: 09-derived-representations
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - README.md
  - examples/transformers/main.go
  - examples/transformers-advanced/main.go
  - transformers_test.go
autonomous: true
requirements:
  - DERIVE-04
must_haves:
  truths:
    - Public docs/examples show additive raw-plus-companion semantics instead of replacement-only behavior.
    - Date/time, normalized text, and extracted-subfield patterns all demonstrate explicit alias queries.
    - Example code keeps raw path queries valid and uses helper-style transformer registration with aliases.
    - All three required pattern families prove encode/decode alias parity, not just one representative sample.
  artifacts:
    - `README.md` documents additive transformer registration, raw default behavior, alias targeting, and the custom-transformer serialization caveat.
    - `examples/transformers/main.go` demonstrates date/time companions with raw-plus-alias querying.
    - `examples/transformers-advanced/main.go` demonstrates normalized text and extracted-subfield aliases.
    - `transformers_test.go` contains acceptance coverage for date/time, normalized text, and extracted-subfield alias patterns.
  key_links:
    - Current README and examples still teach `WithFieldTransformer(path, fn)` replacement-only semantics.
    - DERIVE-04 requires tests and examples, not just docs.
    - The new public query contract should optimize for helper discoverability and never require public hidden target paths.
    - Plan 01 may do minimal example/test call-site migration for compile safety; this plan still owns the final public story and polished acceptance evidence.
---

<objective>
Finish Phase 09 with public-facing evidence and acceptance coverage.

Purpose: update README/examples/tests so the shipped feature is understandable and verifiable for date/time, normalized text, and extracted-subfield derived patterns.
Output: additive alias-aware docs, runnable examples, and focused acceptance tests for DERIVE-04.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-derived-representations/09-CONTEXT.md
@.planning/phases/09-derived-representations/09-DISCUSSION-LOG.md
@.planning/phases/09-derived-representations/09-RESEARCH.md
@.planning/phases/09-derived-representations/09-VALIDATION.md
@.planning/phases/09-derived-representations/09-01-PLAN.md
@.planning/phases/09-derived-representations/09-02-PLAN.md
@AGENTS.md
@README.md
@examples/transformers/main.go
@examples/transformers-advanced/main.go
@transformers_test.go

<interfaces>
Current README / example style still uses replacement-only helpers:
```go
gin.WithFieldTransformer("$.created_at", gin.ISODateToEpochMs)
gin.EQ("$.email", "alice@example.com")
```

Phase 09 public direction:
```go
gin.WithISODateTransformer("$.created_at", "epoch_ms")
gin.EQ("$.created_at", gin.As("epoch_ms", july2024))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README and runnable examples around additive alias-aware semantics</name>
  <files>README.md, examples/transformers/main.go, examples/transformers-advanced/main.go</files>
  <read_first>
- README.md
- examples/transformers/main.go
- examples/transformers-advanced/main.go
- .planning/phases/09-derived-representations/09-CONTEXT.md
- .planning/phases/09-derived-representations/09-DISCUSSION-LOG.md
- .planning/phases/09-derived-representations/09-RESEARCH.md
  </read_first>
  <behavior>
    - Docs/examples use additive helper-style registration with explicit aliases.
    - Docs/examples show raw-path queries remaining raw by default and alias queries using `gin.As(alias, value)`.
    - Docs/examples never teach public `__derived:` target paths.
  </behavior>
  <action>In `README.md`, replace replacement-only transformer examples with additive alias-aware ones using the exact helper style `gin.WithISODateTransformer(path, alias)`, `gin.WithToLowerTransformer(path, alias)`, `gin.WithEmailDomainTransformer(path, alias)`, and `gin.WithRegexExtractTransformer(path, alias, pattern, group)`. Add one concise section that explains the public contract in plain terms: raw source path queries remain raw by default, companion representations are queried explicitly with `gin.As(alias, value)`, and hidden `__derived:` paths are internal-only. Include one explicit note describing the custom-transformer serialization caveat from Plan `09-02`: opaque custom companions registered through `WithCustomTransformer(...)` are not serializable and `Encode()` rejects them. Update `examples/transformers/main.go` to demonstrate a date/time companion alias such as `epoch_ms` while still showing that raw `$.created_at` queries remain string/raw semantics. Update `examples/transformers-advanced/main.go` to cover normalized text aliases (`lower`, `domain`) and extracted-subfield aliases (`error_code`, `order_number`) with `gin.As(alias, value)` queries. Do not leave any runnable example using only `WithFieldTransformer(path, fn)` for built-in additive patterns.</action>
  <acceptance_criteria>
- `README.md` contains the exact string `gin.As(`.
- `README.md` contains the exact string `WithCustomTransformer(` and explicitly states that opaque custom companions are not serializable.
- `README.md` does not instruct users to query the hidden `__derived:` path format.
- `examples/transformers/main.go` contains an alias registration for a date/time companion and at least one `gin.As(` query.
- `examples/transformers-advanced/main.go` contains alias registrations for normalized text and extracted-subfield patterns and at least one `gin.As(` query for each family.
- Built-in additive examples no longer rely on `WithFieldTransformer(path, fn)` as the primary API.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
rg -n 'gin\\.As\\(|WithCustomTransformer\\(' README.md examples/transformers/main.go examples/transformers-advanced/main.go
if rg -n '__derived:' README.md examples/transformers/main.go examples/transformers-advanced/main.go; then
  echo 'public docs/examples must not expose hidden __derived: paths'
  exit 1
fi
go run ./examples/transformers/main.go
go run ./examples/transformers-advanced/main.go</automated>
  </verify>
  <done>The public docs and runnable examples reflect the actual additive alias-aware feature instead of the old replacement-only story.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add DERIVE-04 acceptance coverage for date, normalized text, and extracted-subfield aliases</name>
  <files>transformers_test.go</files>
  <read_first>
- transformers_test.go
- README.md
- examples/transformers/main.go
- examples/transformers-advanced/main.go
- .planning/phases/09-derived-representations/09-RESEARCH.md
  </read_first>
  <behavior>
    - Acceptance tests cover the three required derived-pattern families end to end.
    - Coverage proves raw paths remain queryable while companion aliases are also queryable explicitly.
    - Each required pattern family includes before/after `Encode()` / `Decode()` parity rather than leaving parity to a single representative test.
  </behavior>
  <action>In `transformers_test.go`, add focused acceptance regressions named `TestDateTransformerAliasCoverage`, `TestNormalizedTextAliasCoverage`, and `TestRegexExtractAliasCoverage`. `TestDateTransformerAliasCoverage` must build an index with a date/time companion alias such as `epoch_ms`, assert that the raw source path remains queryable as raw/string data where appropriate, and assert that `gin.As("epoch_ms", <unix-ms>)` range/equality queries hit the expected row groups before and after `Encode()` / `Decode()`. `TestNormalizedTextAliasCoverage` must use one source path like `$.email` or `$.username` with at least two sibling aliases such as `lower` and `domain`, verify raw-path queries remain raw, verify alias queries hit the normalized companions explicitly, and compare those alias-query results before and after `Encode()` / `Decode()`. `TestRegexExtractAliasCoverage` must use an extracted-subfield alias such as `error_code` or `order_number`, prove explicit alias routing, keep raw source values queryable, and also compare alias-query behavior before and after `Encode()` / `Decode()`. Keep these as end-to-end acceptance tests rooted in realistic docs/example patterns, not only config unit tests.</action>
  <acceptance_criteria>
- `transformers_test.go` contains the exact test names `TestDateTransformerAliasCoverage`, `TestNormalizedTextAliasCoverage`, and `TestRegexExtractAliasCoverage`.
- Each of those tests contains at least one `gin.As(` query.
- Each of those tests asserts raw-path queries remain queryable alongside companion alias queries.
- Each of those tests compares alias-query results before and after `Encode()` / `Decode()`.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
go test ./... -run 'Test(DateTransformerAliasCoverage|NormalizedTextAliasCoverage|RegexExtractAliasCoverage)' -count=1</automated>
  </verify>
  <done>DERIVE-04 is locked by acceptance tests that mirror the public docs/examples for the three required pattern families.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| implementation -> public docs/examples | stale examples can teach the wrong API or semantics |
| docs/examples -> user queries | if raw-vs-alias behavior is not explicit, users will issue ambiguous or incorrect queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | I | README / examples | mitigate | Rewrite examples around explicit aliases and state that hidden target paths are internal-only. |
| T-09-08 | R | DERIVE-04 evidence | mitigate | Add acceptance tests for date/time, normalized text, and extracted-subfield alias patterns, including raw-path coexistence. |
</threat_model>

<verification>
1. Acceptance coverage:
   `go test ./... -run 'Test(DateTransformerAliasCoverage|NormalizedTextAliasCoverage|RegexExtractAliasCoverage)' -count=1`
2. Example smoke:
   `go run ./examples/transformers/main.go`
   `go run ./examples/transformers-advanced/main.go`
3. Full suite:
   `go test ./... -count=1`
</verification>

<success_criteria>
- README and examples teach additive alias-aware transformers instead of replacement-only behavior.
- Date/time, normalized text, and extracted-subfield patterns all have explicit alias-query evidence.
- Raw source paths remain queryable alongside companions in both docs and acceptance coverage, and all three pattern families prove encode/decode parity.
</success_criteria>
