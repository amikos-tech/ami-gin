---
phase: 11-real-corpus-prefix-compression-benchmarking
plan: 03
type: execute
wave: 3
depends_on:
  - 11-02
files_modified:
  - README.md
  - .planning/phases/11-real-corpus-prefix-compression-benchmarking/11-REAL-CORPUS-REPORT.md
autonomous: true
requirements: []
must_haves:
  truths:
    - The final write-up states explicitly where prefix compaction helps, where it is flat or no-win, and whether more format work is justified.
    - README documentation keeps the default benchmark path lightweight while still telling maintainers exactly how to reproduce the opt-in external tiers.
    - The report grounds every conclusion in the checked-in raw-results artifact instead of vague prose.
    - The reproduction docs and report both carry forward the pinned dataset revision and explain whether zstd preserved or masked the raw prefix-compaction wins.
  artifacts:
    - `.planning/phases/11-real-corpus-prefix-compression-benchmarking/11-REAL-CORPUS-REPORT.md` contains the interpretive recommendation.
    - `README.md` documents the exact env vars and the supported external-corpus acquisition path.
  key_links:
    - Phase 11 roadmap success criteria explicitly require the final write-up to say where compaction helps, where it is flat, and whether further work is justified.
    - The raw-results artifact from 11-02 is the evidence base for the final report and must remain linked from the narrative.
    - The default repo benchmark surface must remain smoke-only even after docs updates.
---

<objective>
Publish the final Phase 11 benchmark narrative and reproduction guidance.

Purpose: turn the checked-in raw results into a maintainer-facing recommendation and document the exact opt-in workflow for reproducing the external real-corpus runs.
Output: one checked-in Phase 11 report and a concise README benchmark subsection for the smoke/subset/large corpus workflow.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-real-corpus-prefix-compression-benchmarking/11-CONTEXT.md
@.planning/phases/11-real-corpus-prefix-compression-benchmarking/11-RESEARCH.md
@.planning/phases/11-real-corpus-prefix-compression-benchmarking/11-VALIDATION.md
@.planning/phases/11-real-corpus-prefix-compression-benchmarking/11-BENCHMARK-RESULTS.md
@testdata/phase11/README.md
@.planning/phases/10-serialization-compaction/10-03-SUMMARY.md
@.planning/phases/10-serialization-compaction/10-VERIFICATION.md
@README.md
@AGENTS.md

<interfaces>
The report must be written from the results artifact generated by 11-02, not from ad hoc reruns.

Recommended external acquisition example from research:
```python
from huggingface_hub import snapshot_download
snapshot_download(
    repo_id="common-pile/github_archive",
    repo_type="dataset",
    allow_patterns="gharchive/v0/documents/*.jsonl.gz",
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the checked-in interpretive Phase 11 report</name>
  <files>.planning/phases/11-real-corpus-prefix-compression-benchmarking/11-REAL-CORPUS-REPORT.md</files>
  <read_first>
- .planning/phases/11-real-corpus-prefix-compression-benchmarking/11-BENCHMARK-RESULTS.md
- .planning/phases/11-real-corpus-prefix-compression-benchmarking/11-CONTEXT.md
- .planning/ROADMAP.md
- .planning/phases/10-serialization-compaction/10-03-SUMMARY.md
- .planning/phases/10-serialization-compaction/10-VERIFICATION.md
- testdata/phase11/README.md
  </read_first>
  <behavior>
    - The report distinguishes measured wins from flat/no-win cases explicitly.
    - The recommendation on future format work is tied to the recorded evidence, not guesswork.
    - The report remains phase-local and auditable.
  </behavior>
  <action>Create `.planning/phases/11-real-corpus-prefix-compression-benchmarking/11-REAL-CORPUS-REPORT.md` with the exact section headings `## Dataset`, `## Tier Matrix`, `## Helps`, `## Flat / No-Win`, and `## Recommendation`. Use `.planning/phases/10-serialization-compaction/10-VERIFICATION.md` as the primary analog for the report shape: keep the narrative headings required by Phase 11, but under those headings include explicit evidence tables or command-backed bullet lists rather than prose alone. In `## Dataset`, include the pinned dataset revision, the fixture origin, and the exact snapshot layout `gharchive/v0/documents/*.jsonl.gz`. In `## Tier Matrix`, summarize smoke, subset, and large runs for both `projection=structured` and `projection=text-heavy` using the recorded numbers from `11-BENCHMARK-RESULTS.md`, including `docs_indexed`, `shards_loaded`, and the observed `B/op` or `allocs/op` signals where relevant. In `## Helps`, identify the projections and tiers where compact ordered strings materially improved `legacy_raw_bytes` versus `compact_raw_bytes`. In `## Flat / No-Win`, call out any projection or tier where `bytes_saved_pct` was negligible or negative and state whether `default_zstd_bytes` preserved that raw-string win or largely masked it. In `## Recommendation`, make one explicit decision sentence about whether further serialization-format work is justified now, later, or not at all, and anchor that sentence to the measured results rather than qualitative opinion.</action>
  <acceptance_criteria>
- `.planning/phases/11-real-corpus-prefix-compression-benchmarking/11-REAL-CORPUS-REPORT.md` exists.
- The report contains the exact headings `## Dataset`, `## Tier Matrix`, `## Helps`, `## Flat / No-Win`, and `## Recommendation`.
- The report references `projection=structured` and `projection=text-heavy`.
- The report references `11-BENCHMARK-RESULTS.md`.
- The report references `Dataset revision:` or the pinned revision value from the results artifact.
- The report discusses `default_zstd_bytes` or states whether zstd masked/preserved the raw savings.
- The recommendation is an explicit yes/later/no decision about further format work.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
rg -n '^## (Dataset|Tier Matrix|Helps|Recommendation)$|^## Flat\\s*/\\s*No-Win$' .planning/phases/11-real-corpus-prefix-compression-benchmarking/11-REAL-CORPUS-REPORT.md
rg -n 'projection=structured|projection=text-heavy|11-BENCHMARK-RESULTS\\.md|Dataset revision:|default_zstd_bytes|zstd' .planning/phases/11-real-corpus-prefix-compression-benchmarking/11-REAL-CORPUS-REPORT.md</automated>
  </verify>
  <done>Phase 11 has a checked-in recommendation report grounded in the measured real-corpus evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Document the opt-in workflow in README without changing the default benchmark surface</name>
  <files>README.md</files>
  <read_first>
- README.md
- .planning/phases/11-real-corpus-prefix-compression-benchmarking/11-RESEARCH.md
- .planning/phases/11-real-corpus-prefix-compression-benchmarking/11-BENCHMARK-RESULTS.md
- .planning/phases/11-real-corpus-prefix-compression-benchmarking/11-REAL-CORPUS-REPORT.md
  </read_first>
  <behavior>
    - README explains exactly how to run the smoke tier and how to opt into subset/large.
    - README does not claim the external tiers run by default.
    - External acquisition guidance stays concrete and limited to the locked Hugging Face corpus.
  </behavior>
  <action>Update `README.md` by adding a concise Phase 11 benchmark subsection under the existing benchmark documentation. Include the exact env var names `GIN_PHASE11_GITHUB_ARCHIVE_ROOT`, `GIN_PHASE11_ENABLE_SUBSET`, and `GIN_PHASE11_ENABLE_LARGE`. Show the default smoke command `go test ./... -run '^$' -bench 'BenchmarkPhase11RealCorpus/tier=smoke' -benchtime=1x -count=1 -benchmem`. Add one supported acquisition example using the `huggingface_hub.snapshot_download` snippet from research, but pin it with the exact `revision=\"<dataset_revision_from_testdata_readme>\"` value rather than leaving the latest revision implicit; keep the corpus locked to `common-pile/github_archive` and the file pattern locked to `gharchive/v0/documents/*.jsonl.gz`. Add one short maintainer-facing resource note per tier so readers know smoke is the default path and subset/large are opt-in only with materially larger local disk/RAM/time cost. Finally, link the reader to both `.planning/phases/11-real-corpus-prefix-compression-benchmarking/11-BENCHMARK-RESULTS.md` and `.planning/phases/11-real-corpus-prefix-compression-benchmarking/11-REAL-CORPUS-REPORT.md` for the pinned provenance and interpretation.</action>
  <acceptance_criteria>
- `README.md` contains the exact env var names `GIN_PHASE11_GITHUB_ARCHIVE_ROOT`, `GIN_PHASE11_ENABLE_SUBSET`, and `GIN_PHASE11_ENABLE_LARGE`.
- `README.md` contains the exact smoke benchmark command `go test ./... -run '^$' -bench 'BenchmarkPhase11RealCorpus/tier=smoke' -benchtime=1x -count=1 -benchmem`.
- `README.md` references `common-pile/github_archive`, `gharchive/v0/documents/*.jsonl.gz`, and `revision=`.
- `README.md` references `11-REAL-CORPUS-REPORT.md`.
- `README.md` references `11-BENCHMARK-RESULTS.md`.
- `README.md` explicitly states that subset and large tiers are opt-in only.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
rg -n 'GIN_PHASE11_GITHUB_ARCHIVE_ROOT|GIN_PHASE11_ENABLE_SUBSET|GIN_PHASE11_ENABLE_LARGE|BenchmarkPhase11RealCorpus/tier=smoke|common-pile/github_archive|gharchive/v0/documents/\\*\\.jsonl\\.gz|revision=|11-BENCHMARK-RESULTS\\.md|11-REAL-CORPUS-REPORT\\.md|opt-in only' README.md</automated>
  </verify>
  <done>The README documents how to reproduce Phase 11 external corpus benchmarks without making them part of the default benchmark path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw results -> narrative report | an uncited recommendation could drift away from the numbers |
| README workflow -> maintainer behavior | unclear docs could cause accidental large-tier execution or inconsistent data acquisition |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | R | interpretive report | mitigate | Require explicit `Helps`, `Flat / No-Win`, and `Recommendation` sections tied to `11-BENCHMARK-RESULTS.md`, including whether zstd preserved or masked raw wins. |
| T-11-09 | T/R | README workflow | mitigate | Document exact env vars, the pinned dataset revision, the smoke command, and tier resource notes while stating subset/large are opt-in only. |
</threat_model>

<verification>
1. Report structure:
   `rg -n '^## (Dataset|Tier Matrix|Helps|Flat / No-Win|Recommendation)$' .planning/phases/11-real-corpus-prefix-compression-benchmarking/11-REAL-CORPUS-REPORT.md`
2. README workflow smoke:
   `rg -n 'GIN_PHASE11_GITHUB_ARCHIVE_ROOT|GIN_PHASE11_ENABLE_SUBSET|GIN_PHASE11_ENABLE_LARGE|BenchmarkPhase11RealCorpus/tier=smoke|common-pile/github_archive|gharchive/v0/documents/\\*\\.jsonl\\.gz|11-REAL-CORPUS-REPORT\\.md|opt-in only' README.md`
</verification>

<success_criteria>
- Phase 11 has a checked-in report that explicitly distinguishes help versus flat/no-win cases.
- README documents the exact opt-in workflow and keeps the default benchmark surface smoke-only.
- The final recommendation about further format work is grounded in the recorded Phase 11 results artifact.
</success_criteria>
