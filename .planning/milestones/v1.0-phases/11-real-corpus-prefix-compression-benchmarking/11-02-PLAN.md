---
phase: 11-real-corpus-prefix-compression-benchmarking
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - benchmark_test.go
  - .planning/phases/11-real-corpus-prefix-compression-benchmarking/11-BENCHMARK-RESULTS.md
autonomous: false
requirements: []
user_setup:
  - service: local-filesystem
    why: "Subset and large evidence require a caller-provided local snapshot of the pinned `common-pile/github_archive` revision."
    dashboard_config:
      - task: "Download or point to the pinned dataset revision so `${GIN_PHASE11_GITHUB_ARCHIVE_ROOT}/gharchive/v0/documents/*.jsonl.gz` exists before the external evidence task resumes."
        location: "Local benchmark workstation"
must_haves:
  truths:
    - Real-corpus evidence reuses the exact Phase 10 accounting model instead of inventing a new size vocabulary.
    - The checked-in raw-results artifact records smoke, subset, and large runs separately for both `projection=structured` and `projection=text-heavy`.
    - External tiers operate only on a caller-provided local `common-pile/github_archive` snapshot root; no benchmark timing includes network download work.
    - The raw-results artifact captures enough provenance to reproduce the run: commands, env vars, the pinned dataset revision, shard counts, projection names, doc counts, and the exact benchmark config.
  artifacts:
    - `benchmark_test.go` reports exact Phase 11 metrics for every tier/projection combination.
    - `.planning/phases/11-real-corpus-prefix-compression-benchmarking/11-BENCHMARK-RESULTS.md` captures the measured numbers from actual external runs.
  key_links:
    - Phase 10 already proved the exact raw-wire accounting model on current serializer state; Phase 11 should extend that model to real corpora.
    - Phase 11 context requires both raw string-section deltas and final encoded artifact size.
    - The final report cannot be written honestly unless the raw results remain checked in and reproducible.
---

<objective>
Capture the real-corpus size evidence for Phase 11.

Purpose: extend the benchmark harness so real `common-pile/github_archive` projections report exact raw-vs-compact-vs-zstd size metrics, then run the opt-in subset and large tiers and record the resulting evidence in a checked-in artifact.
Output: Phase 11 size metrics in the benchmark harness plus a reproducible `11-BENCHMARK-RESULTS.md` artifact for smoke, subset, and large corpus runs.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-real-corpus-prefix-compression-benchmarking/11-CONTEXT.md
@.planning/phases/11-real-corpus-prefix-compression-benchmarking/11-RESEARCH.md
@.planning/phases/11-real-corpus-prefix-compression-benchmarking/11-VALIDATION.md
@.planning/phases/11-real-corpus-prefix-compression-benchmarking/11-PATTERNS.md
@.planning/phases/10-serialization-compaction/10-03-SUMMARY.md
@.planning/phases/10-serialization-compaction/10-VERIFICATION.md
@.planning/phases/11-real-corpus-prefix-compression-benchmarking/11-01-PLAN.md
@AGENTS.md
@benchmark_test.go
@serialize.go
@prefix.go

<interfaces>
Existing Phase 10 accounting helpers in `benchmark_test.go`:
```go
func phase10LegacyStringPayloadBytes(idx *GINIndex) int
func phase10CompactStringPayloadBytes(idx *GINIndex) int
func phase10ReportMetrics(b *testing.B, metrics phase10BenchmarkMetrics)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the benchmark to report exact real-corpus string-section and artifact-size metrics</name>
  <files>benchmark_test.go</files>
  <read_first>
- benchmark_test.go
- serialize.go
- prefix.go
- .planning/phases/11-real-corpus-prefix-compression-benchmarking/11-RESEARCH.md
- .planning/phases/10-serialization-compaction/10-03-SUMMARY.md
  </read_first>
  <behavior>
    - Metric names stay aligned with Phase 10 where practical.
    - Both projections report exact raw and compressed size signals.
    - The benchmark exposes enough metadata to make the results artifact meaningful without re-running code inspection.
  </behavior>
  <action>In `benchmark_test.go`, reuse the existing Phase 10 helpers to compute `legacy_raw_bytes`, `compact_raw_bytes`, `default_zstd_bytes`, and `bytes_saved_pct` for every `tier=` and `projection=` branch in `BenchmarkPhase11RealCorpus`. Add explicit string-section reporting using the exact metric names `legacy_string_payload_bytes` and `compact_string_payload_bytes`, plus `docs_indexed` and `shards_loaded` so the scale of each run is visible in benchmark output. Call `b.ReportAllocs()` inside the Phase 11 benchmark branches so the later checked-in artifact can record allocation pressure alongside the size metrics. Keep the benchmark repo-local: size metrics come from `EncodeWithLevel(idx, CompressionNone)`, `Encode(idx)`, and the existing ordered-string payload helpers, not from a second serializer or external tool. Preserve the projection names from 11-01 exactly; do not invent new projections in this wave.</action>
  <acceptance_criteria>
- `benchmark_test.go` reports `legacy_raw_bytes`, `compact_raw_bytes`, `default_zstd_bytes`, and `bytes_saved_pct` for Phase 11 real-corpus branches.
- `benchmark_test.go` reports `legacy_string_payload_bytes` and `compact_string_payload_bytes`.
- `benchmark_test.go` reports `docs_indexed` and `shards_loaded`.
- `benchmark_test.go` calls `b.ReportAllocs()` inside the Phase 11 benchmark family.
- Phase 11 metrics are emitted for both `projection=structured` and `projection=text-heavy`.
- The implementation reuses Phase 10 accounting helpers or their exact logic; it does not add a parallel legacy serializer.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
go test ./... -run '^$' -bench 'BenchmarkPhase11RealCorpus/tier=smoke' -benchtime=1x -count=1 -benchmem
go test ./... -count=1
rg -n 'BenchmarkPhase11RealCorpus|ReportAllocs|legacy_string_payload_bytes|compact_string_payload_bytes|docs_indexed|shards_loaded|projection=structured|projection=text-heavy' benchmark_test.go</automated>
  </verify>
  <done>The benchmark harness can report exact string-section deltas and final artifact-size metrics on the real-corpus projections.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Prepare the pinned external corpus snapshot root</name>
  <files>NONE — local Hugging Face snapshot outside the working tree</files>
  <read_first>
- .planning/phases/11-real-corpus-prefix-compression-benchmarking/11-RESEARCH.md
- .planning/phases/11-real-corpus-prefix-compression-benchmarking/11-01-PLAN.md
- testdata/phase11/README.md
  </read_first>
  <behavior>
    - The code-local metric work from Task 1 can land before the external snapshot exists.
    - The external evidence run only resumes once the local root matches the pinned dataset revision and required shard layout.
    - The resume signal is explicit so the later artifact task can assume the snapshot is ready.
  </behavior>
  <action>Obtain or point to a local Hugging Face snapshot root for the exact dataset revision pinned in `testdata/phase11/README.md`. The root passed via `GIN_PHASE11_GITHUB_ARCHIVE_ROOT` must contain `gharchive/v0/documents/*.jsonl.gz` for that same revision; do not continue with a latest-only or unpinned snapshot. Once the root is ready, resume with the exact signal `phase11-snapshot-ready` and preserve the chosen root path for Task 3.</action>
  <acceptance_criteria>
- `GIN_PHASE11_GITHUB_ARCHIVE_ROOT` points to a directory containing `gharchive/v0/documents/*.jsonl.gz`.
- The snapshot matches the exact `Dataset revision:` pinned in `testdata/phase11/README.md`.
- Resume signal must be exactly `phase11-snapshot-ready`.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
test -n "${GIN_PHASE11_GITHUB_ARCHIVE_ROOT:-}"
test -d "${GIN_PHASE11_GITHUB_ARCHIVE_ROOT}/gharchive/v0/documents"
find "${GIN_PHASE11_GITHUB_ARCHIVE_ROOT}/gharchive/v0/documents" -name '*.jsonl.gz' | head -1 | grep -q '.jsonl.gz$'</automated>
  </verify>
  <done>The pinned external snapshot root is ready, so the results-capture task can run without blocking on acquisition work.</done>
</task>

<task type="auto">
  <name>Task 3: Run smoke, subset, and large tiers and check in the raw results artifact</name>
  <files>.planning/phases/11-real-corpus-prefix-compression-benchmarking/11-BENCHMARK-RESULTS.md, benchmark_test.go</files>
  <read_first>
- .planning/phases/11-real-corpus-prefix-compression-benchmarking/11-RESEARCH.md
- .planning/phases/11-real-corpus-prefix-compression-benchmarking/11-01-PLAN.md
- .planning/phases/11-real-corpus-prefix-compression-benchmarking/11-VALIDATION.md
- benchmark_test.go
- testdata/phase11/README.md
  </read_first>
  <behavior>
    - The results artifact records exact commands, env vars, pinned provenance, and metric tables.
    - Smoke, subset, and large results all remain visible; no tier is overwritten or collapsed into a single average.
    - The artifact distinguishes the structured and text-heavy projections clearly and records the resource shape of the run.
  </behavior>
  <action>After Task 1 verification passes and Task 2 resumes with `phase11-snapshot-ready`, run all three Phase 11 tiers against the real local snapshot root already exported in `GIN_PHASE11_GITHUB_ARCHIVE_ROOT`; do not replace it with a placeholder path. Capture the output in `.planning/phases/11-real-corpus-prefix-compression-benchmarking/11-BENCHMARK-RESULTS.md` with a top-level `## Benchmark Configuration` section followed by sections named `## Smoke`, `## Subset`, and `## Large`. In `## Benchmark Configuration`, record the exact pinned dataset revision from `testdata/phase11/README.md`, the root path used, and the exact benchmark config sentence `GIN config: default config, no extra transformers, no derived aliases`. Under each tier section, include the exact benchmark command, the env vars used, the effective shard count, the effective document count, the benchmark `B/op` and `allocs/op` figures, and one metric table per projection containing `legacy_raw_bytes`, `compact_raw_bytes`, `default_zstd_bytes`, `legacy_string_payload_bytes`, `compact_string_payload_bytes`, and `bytes_saved_pct`. Use the exact smoke command `go test ./... -run '^$' -bench 'BenchmarkPhase11RealCorpus/tier=smoke' -benchtime=1x -count=1 -benchmem`, then run the subset and large tiers by prepending the already-set `GIN_PHASE11_GITHUB_ARCHIVE_ROOT` plus `GIN_PHASE11_ENABLE_SUBSET=1` or `GIN_PHASE11_ENABLE_LARGE=1` rather than rewriting the root value inline. If `docs_indexed` does not increase strictly from Smoke to Subset to Large, stop and fix the tier selection or provenance before checking in the artifact.</action>
  <acceptance_criteria>
- `.planning/phases/11-real-corpus-prefix-compression-benchmarking/11-BENCHMARK-RESULTS.md` exists.
- The results artifact contains sections named `## Benchmark Configuration`, `## Smoke`, `## Subset`, and `## Large`.
- The results artifact contains both `projection=structured` and `projection=text-heavy`.
- The results artifact includes the exact metric names `legacy_raw_bytes`, `compact_raw_bytes`, `default_zstd_bytes`, `legacy_string_payload_bytes`, `compact_string_payload_bytes`, `bytes_saved_pct`, `docs_indexed`, and `shards_loaded`.
- The results artifact records `Dataset revision:`, `GIN config: default config, no extra transformers, no derived aliases`, and benchmark memory figures (`B/op` and `allocs/op`).
- `docs_indexed` increases strictly from Smoke to Subset to Large.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
go test ./... -run '^$' -bench 'BenchmarkPhase11RealCorpus/tier=smoke' -benchtime=1x -count=1 -benchmem
test -n "${GIN_PHASE11_GITHUB_ARCHIVE_ROOT:-}"
GIN_PHASE11_ENABLE_SUBSET=1 go test ./... -run '^$' -bench 'BenchmarkPhase11RealCorpus/tier=subset' -benchtime=1x -count=1 -benchmem
GIN_PHASE11_ENABLE_LARGE=1 go test ./... -run '^$' -bench 'BenchmarkPhase11RealCorpus/tier=large' -benchtime=1x -count=1 -benchmem
rg -n '^## Benchmark Configuration$|^## Smoke$|^## Subset$|^## Large$|Dataset revision:|GIN config: default config, no extra transformers, no derived aliases|projection=structured|projection=text-heavy|legacy_raw_bytes|compact_raw_bytes|default_zstd_bytes|legacy_string_payload_bytes|compact_string_payload_bytes|bytes_saved_pct|docs_indexed|shards_loaded|B/op|allocs/op' .planning/phases/11-real-corpus-prefix-compression-benchmarking/11-BENCHMARK-RESULTS.md</automated>
  </verify>
  <done>Phase 11 has a checked-in raw-results artifact grounded in actual subset and large external corpus runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark metrics -> artifact claims | missing or renamed metrics make the checked-in evidence unverifiable |
| local snapshot root -> corpus scale | a different shard selection changes the story unless provenance is recorded |
| raw results -> later report | if the raw artifact is incomplete, the narrative can drift away from the measurement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | R | benchmark metrics | mitigate | Emit exact Phase 10 metric names plus explicit string-payload metrics for every branch. |
| T-11-05 | R | snapshot provenance | mitigate | Gate external capture on a pinned revision and record the root path, shard count, and doc count in `11-BENCHMARK-RESULTS.md`. |
| T-11-06 | D/R | resource drift | mitigate | Record `B/op`, `allocs/op`, and tier-by-tier document counts so oversized or unexpectedly tiny runs are caught before the report is written. |
| T-11-07 | I | interpretive drift | mitigate | Keep raw results checked in separately from the narrative report. |
</threat_model>

<verification>
1. Smoke evidence command:
   `go test ./... -run '^$' -bench 'BenchmarkPhase11RealCorpus/tier=smoke' -benchtime=1x -count=1 -benchmem`
2. External subset evidence:
   `GIN_PHASE11_GITHUB_ARCHIVE_ROOT=/path/to/snapshot GIN_PHASE11_ENABLE_SUBSET=1 go test ./... -run '^$' -bench 'BenchmarkPhase11RealCorpus/tier=subset' -benchtime=1x -count=1 -benchmem`
3. External large evidence:
   `GIN_PHASE11_GITHUB_ARCHIVE_ROOT=/path/to/snapshot GIN_PHASE11_ENABLE_LARGE=1 go test ./... -run '^$' -bench 'BenchmarkPhase11RealCorpus/tier=large' -benchtime=1x -count=1 -benchmem`
</verification>

<success_criteria>
- Phase 11 benchmark output reuses the exact raw/compact/zstd accounting style from Phase 10.
- The checked-in results artifact records smoke, subset, and large evidence separately for both projections.
- External corpus evidence is reproducible from the recorded commands and env vars.
</success_criteria>
