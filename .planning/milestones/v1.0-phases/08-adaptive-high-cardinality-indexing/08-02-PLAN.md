---
phase: 08-adaptive-high-cardinality-indexing
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - gin.go
  - serialize.go
  - serialize_security_test.go
  - cmd/gin-index/main.go
  - cmd/gin-index/main_test.go
  - README.md
autonomous: true
requirements:
  - HCARD-02
  - HCARD-04
must_haves:
  truths:
    - Adaptive configuration is explicit, additive, and survives encode/decode.
    - Adaptive per-path metadata round-trips without ambiguity and the binary format evolution is explicit.
    - `gin-index info` distinguishes exact, bloom-only, and adaptive-hybrid paths with compact summary counters.
    - Public docs describe the new hybrid behavior and configuration without pretending high-cardinality paths are bloom-only only.
  artifacts:
    - `serialize.go` persists adaptive config and per-path adaptive metadata/index sections explicitly.
    - `serialize_security_test.go` or equivalent decode tests cover adaptive round-trip and malformed adaptive section handling.
    - `cmd/gin-index/main.go` and `cmd/gin-index/main_test.go` show mode-aware path info.
    - `README.md` documents adaptive-high-cardinality behavior and the additive config knobs.
  key_links:
    - Current `SerializedConfig` does not persist any adaptive knobs.
    - Current CLI info prints only path name, types, and cardinality.
    - Current README still describes high-cardinality behavior as bloom-only fallback.
---

<objective>
Persist and surface adaptive-hybrid behavior end to end.

Purpose: make the Phase 08 adaptive mode explicit in the binary format, CLI output, and public docs instead of leaving it as an in-memory-only behavior.
Output: adaptive config serialization, adaptive path metadata round-trip, mode-aware `gin-index info`, and README coverage for the new high-cardinality behavior.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-adaptive-high-cardinality-indexing/08-CONTEXT.md
@.planning/phases/08-adaptive-high-cardinality-indexing/08-RESEARCH.md
@.planning/phases/08-adaptive-high-cardinality-indexing/08-VALIDATION.md
@.planning/phases/08-adaptive-high-cardinality-indexing/08-01-PLAN.md
@AGENTS.md
@serialize.go
@serialize_security_test.go
@cmd/gin-index/main.go
@README.md

<interfaces>
Current serialized config in `serialize.go`:
```go
type SerializedConfig struct {
	BloomFilterSize   uint32
	BloomFilterHashes uint8
	EnableTrigrams    bool
	TrigramMinLength  int
	HLLPrecision      uint8
	PrefixBlockSize   int
	FTSPaths          []string
	Transformers      []TransformerSpec
}
```

Current path output in `cmd/gin-index/main.go`:
```go
fmt.Printf("  %s (id=%d, types=%s, cardinality=%d)\n", pe.PathName, pe.PathID, types, pe.Cardinality)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist adaptive config and index metadata with explicit format handling</name>
  <files>gin.go, serialize.go, serialize_security_test.go</files>
  <read_first>
- gin.go
- serialize.go
- .planning/phases/08-adaptive-high-cardinality-indexing/08-RESEARCH.md
- .planning/phases/08-adaptive-high-cardinality-indexing/08-VALIDATION.md
- AGENTS.md
  </read_first>
  <behavior>
    - Adaptive config knobs and per-path adaptive indexes survive `Encode()` / `Decode()`.
    - Format evolution is explicit rather than silently writing new sections under an old assumption set.
    - Decode rejects malformed adaptive metadata safely.
  </behavior>
  <action>In `gin.go`, make the wire-format change explicit by bumping `Version` from `4` to `5`; do not leave adaptive sections under the old binary contract. In `serialize.go`, extend the serialized config payload only for the global adaptive knobs (`AdaptiveMinRGCoverage`, `AdaptivePromotedTermCap`, `AdaptiveCoverageCeiling`, `AdaptiveBucketCount`) and keep the per-path adaptive data in dedicated binary section helpers such as `writeAdaptiveStringIndexes()` / `readAdaptiveStringIndexes()`. Place the adaptive section immediately after `writeStringIndexes()` / `readStringIndexes()` and before `writeStringLengthIndexes()` / `readStringLengthIndexes()` so the string-path layout stays grouped and deterministic. The per-path section must persist promoted exact terms, promoted `RGSet` bitmaps, bucket count, bucket `RGSet`s, and any compact summary counters surfaced to the CLI. Update config serialization so the new global adaptive knobs are included in `SerializedConfig` and reconstructed in `readConfig()`. Add defensive bounds for adaptive path count, promoted-term count, bucket count, and oversized adaptive bucket sections. In `serialize_security_test.go` or the closest serialization test file, add round-trip coverage for adaptive paths and malformed-input guards for adaptive section counts or bucket sizes that exceed sane bounds.</action>
  <acceptance_criteria>
- `gin.go` contains the exact line `Version    = uint16(5)` or an equally explicit version bump/compatibility branch for the adaptive layout.
- `serialize.go` writes and reads adaptive config fields in addition to the existing config payload.
- `serialize.go` contains explicit adaptive section helpers rather than smuggling adaptive state through existing string-index code paths.
- `serialize.go` places adaptive section reads/writes between the string-index and string-length-index sections.
- `SerializedConfig` only carries global adaptive knobs; promoted terms and bucket bitmaps are persisted through the dedicated adaptive binary section.
- `serialize_security_test.go` or equivalent contains tests named `TestAdaptiveConfigRoundTrip` and `TestAdaptivePathMetadataRoundTrip`.
- At least one serialization-focused test rejects malformed adaptive metadata or oversized adaptive bucket sections.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
go test ./... -run 'Test(AdaptiveConfigRoundTrip|AdaptivePathMetadataRoundTrip)' -count=1</automated>
  </verify>
  <done>Adaptive configuration and metadata round-trip cleanly and the binary format change is explicit and test-backed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose adaptive mode and summary counters in CLI info output</name>
  <files>cmd/gin-index/main.go, cmd/gin-index/main_test.go</files>
  <read_first>
- cmd/gin-index/main.go
- gin.go
- serialize.go
- .planning/phases/08-adaptive-high-cardinality-indexing/08-CONTEXT.md
- .planning/phases/08-adaptive-high-cardinality-indexing/08-RESEARCH.md
  </read_first>
  <behavior>
    - CLI info clearly reports whether each path is exact, bloom-only, or adaptive-hybrid.
    - Adaptive paths show compact counters only: promoted-term count, bucket count, and relevant threshold/cap summary.
    - CLI output remains readable for exact and bloom-only paths.
  </behavior>
  <action>In `cmd/gin-index/main.go`, replace the current path print line with a mode-aware formatter. Pull the per-path rendering out of file-loading logic into a helper that accepts `io.Writer` and `*GINIndex`, then have `infoSingleFile()` call that helper so `cmd/gin-index/main_test.go` can assert mode output directly without S3 or remote-path plumbing. For exact paths, print mode `exact`; for pure bloom-only paths, print mode `bloom-only`; for hybrid paths, print mode `adaptive-hybrid` and include compact counters such as `promoted=<n>`, `buckets=<n>`, `threshold=<n>`, and `cap=<n>` or their exact equivalent metadata fields. Add `cmd/gin-index/main_test.go` if it does not exist and cover exact, pure bloom-only, and adaptive-hybrid paths so the info output contract is locked. Do not dump raw promoted values or a giant diagnostics surface in CLI output; if you add a temp-file smoke test, keep it local-only and secondary to the pure formatter test.</action>
  <acceptance_criteria>
- `cmd/gin-index/main.go` no longer prints only `path/type/cardinality` for every path.
- CLI output contains the exact strings `mode=exact`, `mode=bloom-only`, and `mode=adaptive-hybrid` in the appropriate cases.
- Adaptive-hybrid CLI output includes compact counters such as `promoted=` and `buckets=`.
- `cmd/gin-index/main_test.go` exercises info rendering through a local helper or temp-file path without requiring S3/network access.
- `cmd/gin-index/main_test.go` contains tests named `TestPathInfoReportsAdaptiveMode` and `TestCLIInfoShowsAdaptiveSummary`.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
go test ./... -run 'Test(PathInfoReportsAdaptiveMode|CLIInfoShowsAdaptiveSummary)' -count=1</automated>
  </verify>
  <done>The CLI makes adaptive mode visible and debuggable without overwhelming output.</done>
</task>

<task type="auto">
  <name>Task 3: Update README configuration and behavior docs for adaptive high-cardinality indexing</name>
  <files>README.md</files>
  <read_first>
- README.md
- .planning/ROADMAP.md
- .planning/phases/08-adaptive-high-cardinality-indexing/08-CONTEXT.md
- .planning/phases/08-adaptive-high-cardinality-indexing/08-RESEARCH.md
  </read_first>
  <behavior>
    - Public docs describe exact, bloom-only, and adaptive-hybrid path behavior correctly.
    - The configuration section mentions the new additive adaptive knobs.
    - Architecture prose no longer claims high-cardinality paths are always bloom-only.
  </behavior>
  <action>In `README.md`, update the configuration example and high-cardinality explanation so it reflects the three-mode model introduced by Phase 08. Replace bloom-only-only language with exact vs adaptive-hybrid vs bloom-only wording. Document the new additive config knobs by their exact names and Phase 08 defaults: `AdaptiveMinRGCoverage: 2`, `AdaptivePromotedTermCap: 64`, `AdaptiveCoverageCeiling: 0.80`, and `AdaptiveBucketCount: 128`, with concise comments describing the promotion threshold, promoted-term cap, coverage ceiling, and bucket count. Update any architecture or comparison sections that currently say the index handles high cardinality \"via bloom filters\" so they instead describe hybrid recovery for hot values plus compact fallback for the tail.</action>
  <acceptance_criteria>
- `README.md` no longer describes high-cardinality handling only as bloom-only fallback.
- `README.md` contains the exact string `adaptive-hybrid`.
- `README.md` configuration examples mention the new additive adaptive knobs by name.
- `README.md` explains that hot values can retain exact row-group pruning on high-cardinality paths.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
rg -n 'adaptive-hybrid|promoted|bucket|coverage ceiling|high-cardinality' README.md</automated>
  </verify>
  <done>The public contract matches the shipped Phase 08 behavior instead of the old bloom-only story.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-memory adaptive index -> serialized bytes | Format drift can corrupt adaptive query behavior after decode |
| adaptive metadata -> CLI output | Hidden or misleading mode reporting makes operational debugging unreliable |
| public README -> maintainer expectations | Stale docs cause users to misconfigure or misunderstand pruning behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | T/R | adaptive encode/decode | mitigate | Persist adaptive config/index state explicitly and add round-trip plus malformed-input tests. |
| T-08-05 | R | CLI path reporting | mitigate | Make mode vocabulary explicit and test output strings directly. |
| T-08-06 | I | README / config docs | mitigate | Update configuration and architecture sections in the same phase as the code change. |
</threat_model>

<verification>
1. Adaptive serialization:
   `go test ./... -run 'Test(AdaptiveConfigRoundTrip|AdaptivePathMetadataRoundTrip)' -count=1`
2. CLI output:
   `go test ./... -run 'Test(PathInfoReportsAdaptiveMode|CLIInfoShowsAdaptiveSummary)' -count=1`
3. Full suite:
   `go test ./... -count=1`
</verification>

<success_criteria>
- Adaptive config and metadata survive `Encode()` / `Decode()`.
- `gin-index info` distinguishes exact, bloom-only, and adaptive-hybrid paths with compact summary counters.
- Public docs stop describing high-cardinality paths as bloom-only only.
</success_criteria>
