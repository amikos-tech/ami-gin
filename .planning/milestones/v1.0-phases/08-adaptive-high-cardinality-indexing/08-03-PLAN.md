---
phase: 08-adaptive-high-cardinality-indexing
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - benchmark_test.go
autonomous: true
requirements:
  - HCARD-05
must_haves:
  truths:
    - Benchmark evidence compares current bloom-only degradation against adaptive-hybrid recovery on the same skewed fixtures.
    - Reported metrics cover pruning effectiveness and encoded-size growth, not just build latency.
    - Fixture naming makes the skew model and path mode explicit so historical comparison is straightforward.
  artifacts:
    - `benchmark_test.go` contains skewed high-cardinality fixture generators with a hot head and long tail.
    - `benchmark_test.go` contains an adaptive benchmark family rooted at `BenchmarkAdaptiveHighCardinality`.
    - Benchmark output reports candidate row-group counts and encoded-size metrics for exact, bloom-only, and adaptive-hybrid modes.
  key_links:
    - Existing scale-cardinality benchmarks are uniform and build-focused, so they do not prove HCARD-05 on their own.
    - Benchmark labels should follow the recent explicit naming style from Phase 07 so mode and shape are visible in sub-benchmark names.
---

<objective>
Produce the benchmark and fixture evidence for Phase 08.

Purpose: prove adaptive-hybrid indexing recovers pruning for hot values on realistic skewed data while keeping size growth bounded relative to the current bloom-only fallback.
Output: skewed high-cardinality fixtures, adaptive benchmark families, and clear metrics for pruning candidate counts and encoded size by mode.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-adaptive-high-cardinality-indexing/08-CONTEXT.md
@.planning/phases/08-adaptive-high-cardinality-indexing/08-RESEARCH.md
@.planning/phases/08-adaptive-high-cardinality-indexing/08-VALIDATION.md
@.planning/phases/07-builder-parsing-numeric-fidelity/07-builder-parsing-numeric-fidelity-02-SUMMARY.md
@AGENTS.md
@benchmark_test.go
@builder.go
@query.go
@serialize.go

<interfaces>
Existing high-cardinality benchmark in `benchmark_test.go`:
```go
func BenchmarkScaleCardinality(b *testing.B) {
	cardinalities := []int{10, 100, 1000, 10000}
	// uniform cardinality build benchmark
}
```

Existing Phase 07 benchmark naming pattern:
```go
name := fmt.Sprintf("parser=%s/docs=%d/shape=%s", parserMode.name, docCount, shape.name)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add skewed high-cardinality fixtures that expose hot-value recovery</name>
  <files>benchmark_test.go</files>
  <read_first>
- benchmark_test.go
- .planning/phases/08-adaptive-high-cardinality-indexing/08-RESEARCH.md
- .planning/phases/07-builder-parsing-numeric-fidelity/07-builder-parsing-numeric-fidelity-02-SUMMARY.md
- AGENTS.md
  </read_first>
  <behavior>
    - Fixtures must contain a hot head and a long tail, not just uniform uniqueness.
    - The same fixture family must be reusable across exact, bloom-only, and adaptive-hybrid modes.
    - Benchmark naming should expose mode and shape directly.
  </behavior>
  <action>In `benchmark_test.go`, add deterministic fixture helpers for one skewed high-cardinality string path such as `$.user_id` or `$.trace_id` with a concrete reusable profile: `96` row groups, `256` docs per row group, `32` hot values that together occupy roughly `60%` of documents and each appear across many row groups, plus a long tail of more than `10_000` unique values that each appear in only `1` or `2` row groups. Use a stable algorithm, not randomness, so results are reproducible. Reuse this exact fixture family across all compared modes by changing only builder config knobs. The config matrix must be explicit in code: exact mode raises `CardinalityThreshold` above observed cardinality (for example `20_000`), bloom-only mode uses the Phase 08 threshold breach but disables adaptive via `AdaptivePromotedTermCap: 0` or `AdaptiveBucketCount: 0`, and adaptive-hybrid mode uses the Phase 08 defaults `AdaptiveMinRGCoverage: 2`, `AdaptivePromotedTermCap: 64`, `AdaptiveCoverageCeiling: 0.80`, and `AdaptiveBucketCount: 128`. Follow the Phase 07 naming style by using sub-benchmark names that contain exact segments like `mode=exact`, `mode=bloom-only`, `mode=adaptive-hybrid`, and `shape=skewed-head-tail`.</action>
  <acceptance_criteria>
- `benchmark_test.go` contains a skewed high-cardinality fixture helper rather than only the existing uniform generator.
- `benchmark_test.go` defines one explicit mode matrix that changes only config knobs between exact, bloom-only, and adaptive-hybrid runs.
- Sub-benchmark names contain the exact segments `mode=` and `shape=`.
- `benchmark_test.go` contains the exact strings `mode=exact`, `mode=bloom-only`, and `mode=adaptive-hybrid`.
- The same fixture helper is used across all three compared modes.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
go test ./... -run '^$' -bench 'BenchmarkAdaptiveHighCardinality' -benchtime=1x -count=1</automated>
  </verify>
  <done>The benchmark harness has a reproducible skewed dataset that actually exercises Phase 08's target behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Report pruning and size metrics for exact vs bloom-only vs adaptive-hybrid modes</name>
  <files>benchmark_test.go</files>
  <read_first>
- benchmark_test.go
- builder.go
- query.go
- serialize.go
- .planning/phases/08-adaptive-high-cardinality-indexing/08-RESEARCH.md
  </read_first>
  <behavior>
    - Benchmarks quantify pruning effectiveness by candidate row-group count, not just latency.
    - Benchmarks quantify encoded-size growth alongside pruning recovery.
    - Output remains runnable with normal `go test -bench` commands.
  </behavior>
  <action>In `benchmark_test.go`, add a benchmark family rooted at `BenchmarkAdaptiveHighCardinality` with sub-benchmarks for a representative hot-value equality probe and a representative tail-value equality probe. For each mode, compute the candidate row-group count returned by `Evaluate([]Predicate{EQ(path, value)})` and report it with `b.ReportMetric` using the exact metric name `candidate_rgs`. Encode the built index and report encoded bytes with `b.ReportMetric(..., \"encoded_bytes\")`. Before the timed loop, compute the hot-probe candidate counts once across the exact, bloom-only, and adaptive-hybrid indexes on the same skewed fixture and fail setup with `b.Fatalf` unless adaptive-hybrid is strictly better than bloom-only for that hot probe; this locks the benchmark to the Phase 08 pruning-recovery claim instead of merely printing numbers. Keep the benchmark runnable entirely through `go test -bench`; do not add external scripts or datasets. Use hot-value probes to show pruning recovery and long-tail probes to show conservative-but-bounded fallback behavior.</action>
  <acceptance_criteria>
- `benchmark_test.go` defines a benchmark function named `BenchmarkAdaptiveHighCardinality`.
- The benchmark reports a metric named `candidate_rgs`.
- The benchmark reports a metric named `encoded_bytes`.
- At least one hot-value probe and one tail-value probe are benchmarked for each mode.
- Benchmark setup asserts that the adaptive-hybrid hot probe yields fewer candidate row groups than bloom-only on the skewed fixture.
- No external benchmark harness or downloaded dataset is introduced.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
go test ./... -run '^$' -bench 'BenchmarkAdaptiveHighCardinality' -benchtime=1x -count=1</automated>
  </verify>
  <done>HCARD-05 has benchmark-proof evidence for pruning recovery and bounded size cost on skewed data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture distribution -> pruning claims | Uniform or unrealistic fixtures can make adaptive results meaningless |
| benchmark metrics -> maintainer decisions | Latency-only reporting can hide that pruning recovery did not actually happen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | R | benchmark fixture realism | mitigate | Use deterministic skewed fixtures with explicit hot and tail probes. |
| T-08-08 | I | benchmark interpretation | mitigate | Report `candidate_rgs` and `encoded_bytes` directly alongside standard benchmark timing. |
</threat_model>

<verification>
1. Adaptive benchmark smoke:
   `go test ./... -run '^$' -bench 'BenchmarkAdaptiveHighCardinality' -benchtime=1x -count=1`
2. Full suite:
   `go test ./... -count=1`
</verification>

<success_criteria>
- Benchmark output compares exact, bloom-only, and adaptive-hybrid modes on the same skewed fixtures.
- HCARD-05 evidence includes candidate row-group counts and encoded size, not just elapsed time.
- The benchmark harness remains repo-local and reproducible with plain `go test -bench`.
</success_criteria>
