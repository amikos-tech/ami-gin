---
phase: 07-builder-parsing-numeric-fidelity
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - benchmark_test.go
autonomous: true
requirements:
  - BUILD-05
must_haves:
  truths:
    - Benchmark output compares `parser=legacy-unmarshal` and `parser=explicit-number` on the same fixtures, so ingest/build deltas are reproducible after the new parser lands.
    - The benchmark family reports both latency and allocation deltas for single-document ingest, batched ingest, and end-to-end build/finalize work.
    - Benchmark names encode parser mode, document count, and fixture shape so historical comparisons are unambiguous.
    - The benchmark matrix includes both `shape=wide-flat` for wide-document staging pressure and a multi-transformer `shape=transformer-heavy`, so the two review-identified slow paths are measured instead of inferred.
  artifacts:
    - `benchmark_test.go` contains a benchmark-only legacy ingest helper that reproduces the pre-Phase-07 parse path.
    - `benchmark_test.go` contains explicit parser benchmark families named `BenchmarkAddDocumentPhase07`, `BenchmarkBuildPhase07`, and `BenchmarkFinalizePhase07`.
    - `benchmark_test.go` contains deterministic Phase 07 fixture generators for int-only, mixed-safe, wide-flat, and transformer-heavy documents.
  key_links:
    - Legacy and explicit parser benchmarks share the same fixture generator and batch sizes, so "before vs after" comparisons are attributable to the parser path rather than to fixture drift.
    - All benchmark families call `b.ReportAllocs()` and use `go test -bench` directly, preserving repo conventions.
    - `benchmarkAddDocumentLegacy` is benchmark-only, private to `benchmark_test.go`, and annotated as the pre-Phase-07 control path so future builder refactors have one obvious place to keep in sync.
---

<objective>
Add benchmark-proof deltas for Phase 07 by comparing the new explicit parser path against a benchmark-only copy of the legacy `json.Unmarshal(..., &any)` path.

Purpose: Satisfy BUILD-05 with reproducible ingest/build latency and allocation measurements that survive after the implementation changes land.
Output: Deterministic Phase 07 benchmark fixtures, legacy-vs-explicit parser benchmark families, and benchmark names that make delta reporting mechanically comparable.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-builder-parsing-numeric-fidelity/07-CONTEXT.md
@.planning/phases/07-builder-parsing-numeric-fidelity/07-RESEARCH.md
@.planning/phases/07-builder-parsing-numeric-fidelity/07-REVIEWS.md
@.planning/phases/07-builder-parsing-numeric-fidelity/07-VALIDATION.md
@.planning/phases/07-builder-parsing-numeric-fidelity/07-01-PLAN.md
@AGENTS.md
@benchmark_test.go
@builder.go
@gin.go
@query.go

<interfaces>
Current builder benchmarks in `benchmark_test.go`:
```go
func BenchmarkAddDocument(b *testing.B)
func BenchmarkAddDocumentBatch(b *testing.B)
func BenchmarkFinalize(b *testing.B)
func BenchmarkBuilderMemory(b *testing.B)
```

Current standard benchmark command pattern:
```bash
go test ./... -run '^$' -bench 'Benchmark...' -benchtime=1x -count=1
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic Phase 07 fixtures and a benchmark-only legacy parser control</name>
  <files>benchmark_test.go</files>
  <read_first>
- benchmark_test.go
- builder.go
- gin.go
- .planning/phases/07-builder-parsing-numeric-fidelity/07-RESEARCH.md
- .planning/phases/07-builder-parsing-numeric-fidelity/07-REVIEWS.md
- .planning/phases/07-builder-parsing-numeric-fidelity/07-01-PLAN.md
- AGENTS.md
  </read_first>
  <behavior>
    - Every benchmark must compare the same fixture under `parser=legacy-unmarshal` and `parser=explicit-number`.
    - Fixtures must cover an int-only shape, a mixed-safe-numeric shape that should promote safely, a wide-flat shape that stresses document-local staging, and a transformer-heavy shape.
    - The legacy control must stay benchmark-only and must not become part of production `AddDocument()`.
  </behavior>
  <action>Addresses review concerns: benchmark-only legacy helper drift, transformer-heavy materialization coverage, and wide-document staging visibility. In `benchmark_test.go`, add deterministic fixture helpers named or equivalent to `generatePhase07IntDoc`, `generatePhase07MixedDoc`, `generatePhase07WideDoc`, and `generatePhase07TransformerDoc`. Use concrete fields and values: an int-only fixture with keys `id`, `account_id`, `count`, and `score_bucket` and large exact integers including `9223372036854775807`; a mixed-safe fixture with keys `sensor_id`, `reading_int`, and `reading_float` using integer values within `[-(1<<53), +(1<<53)]` plus decimals like `1.5`; a wide-flat fixture with root keys `field_0000` through `field_0511` plus `id` and `account_id` so the explicit parser's document-local staging cost is visible on a very wide object; and a transformer-heavy fixture with keys `timestamp`, `event_date`, `version`, `client_ip`, and `build_ref`, wired to `WithISODateTransformer("$.timestamp")`, `WithDateTransformer("$.event_date")`, `WithSemVerTransformer("$.version")`, `WithIPv4Transformer("$.client_ip")`, and `WithRegexExtractIntTransformer("$.build_ref", "build-(\\\\d+)", 1)` so multiple transformer categories are exercised in one shape. Add a benchmark-only helper named `benchmarkAddDocumentLegacy` that reproduces the pre-Phase-07 path exactly enough for comparison: `json.Unmarshal(jsonDoc, &doc)`, legacy eager row-group bookkeeping, and legacy walking/classification behavior local to `benchmark_test.go`. Add an in-file comment directly above that helper with the exact text `KEEP IN SYNC WITH pre-Phase-07 AddDocument control path for BUILD-05`. Keep that helper private to benchmarks and do not call it from production code. Add a small benchmark helper that runs the new production `AddDocument()` path on the same docs. Use shared batch sizes `[]int{100, 1000, 10000}` and shared fixture labels `shape=int-only`, `shape=mixed-safe`, `shape=wide-flat`, and `shape=transformer-heavy`.</action>
  <acceptance_criteria>
- `benchmark_test.go` contains the exact benchmark parser labels `parser=legacy-unmarshal` and `parser=explicit-number`.
- `benchmark_test.go` contains the exact fixture labels `shape=int-only`, `shape=mixed-safe`, `shape=wide-flat`, and `shape=transformer-heavy`.
- `benchmark_test.go` contains the exact integer literal `9223372036854775807` and the exact decimal literal `1.5` in Phase 07 fixtures.
- `benchmark_test.go` contains a private benchmark helper named `benchmarkAddDocumentLegacy` or the exact equivalent spelled out in code comments as the pre-Phase-07 control path.
- `benchmark_test.go` contains the exact comment `KEEP IN SYNC WITH pre-Phase-07 AddDocument control path for BUILD-05`.
- `benchmark_test.go` contains the exact transformer helper calls `WithISODateTransformer`, `WithDateTransformer`, `WithSemVerTransformer`, `WithIPv4Transformer`, and `WithRegexExtractIntTransformer` in the Phase 07 benchmark setup.
- The legacy helper is defined only in `benchmark_test.go` and is not referenced by production `.go` files.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
go test ./... -run '^$' -bench 'Benchmark(AddDocumentPhase07|BuildPhase07|FinalizePhase07)' -benchtime=1x -count=1</automated>
  </verify>
  <done>Phase 07 has deterministic fixtures and an in-repo "before" control, so benchmark deltas remain reproducible after the explicit parser replaces the legacy path.</done>
</task>

<task type="auto">
  <name>Task 2: Add legacy-vs-explicit ingest/build benchmark families with explicit naming and alloc reporting</name>
  <files>benchmark_test.go</files>
  <read_first>
- benchmark_test.go
- builder.go
- .planning/phases/07-builder-parsing-numeric-fidelity/07-VALIDATION.md
- .planning/phases/07-builder-parsing-numeric-fidelity/07-RESEARCH.md
- .planning/phases/07-builder-parsing-numeric-fidelity/07-REVIEWS.md
  </read_first>
  <behavior>
    - Benchmark output must show parser mode, document count, and fixture shape directly in the sub-benchmark name.
    - Each benchmark family must call `b.ReportAllocs()` so BUILD-05 includes allocation deltas.
    - The benchmark suite must cover single-document ingest, batch ingest/build, and finalize cost after the new parser path.
  </behavior>
  <action>In `benchmark_test.go`, add three benchmark families with these exact names: `BenchmarkAddDocumentPhase07`, `BenchmarkBuildPhase07`, and `BenchmarkFinalizePhase07`. For `BenchmarkAddDocumentPhase07`, run one-document ingest loops across both parser modes, all four shapes, and doc-count labels `docs=100`, `docs=1000`, and `docs=10000`. For `BenchmarkBuildPhase07`, build full indexes across the same parser modes, shapes, and doc counts, measuring `NewBuilder` + repeated `AddDocument` + `Finalize`. For `BenchmarkFinalizePhase07`, isolate finalize cost by preloading a builder with each parser mode and then timing `Finalize()` on the resulting builder state. Use sub-benchmark names in the exact format `parser=<mode>/docs=<n>/shape=<label>`. Call `b.ReportAllocs()` in every family. Keep the existing generic benchmarks intact if they still provide value, but Phase 07 benchmark verification must be satisfiable by running `go test ./... -run '^$' -bench 'Benchmark(AddDocumentPhase07|BuildPhase07|FinalizePhase07)' -benchtime=1x -count=1` and seeing both parser modes represented for every shape/count combination.</action>
  <acceptance_criteria>
- `benchmark_test.go` contains benchmark functions named `BenchmarkAddDocumentPhase07`, `BenchmarkBuildPhase07`, and `BenchmarkFinalizePhase07`.
- Every new benchmark family contains sub-benchmark names with the exact segments `parser=`, `docs=`, and `shape=`.
- Every new benchmark family calls `b.ReportAllocs()`.
- Running the Phase 07 benchmark command would enumerate both `parser=legacy-unmarshal` and `parser=explicit-number` for each shape/count combination.
- No custom benchmark binary, shell script, or external dataset is introduced.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
go test ./... -run '^$' -bench 'Benchmark(AddDocumentPhase07|BuildPhase07|FinalizePhase07)' -benchtime=1x -count=1
go test ./... -count=1</automated>
  </verify>
  <done>Phase 07 benchmark output can report ingest/build latency and allocation deltas directly from the repo, with stable names and a same-branch legacy control.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark fixtures -> performance claims | Fixture drift can make latency/allocation deltas meaningless |
| benchmark-only legacy helper -> production code | The control path must remain isolated from runtime behavior |
| benchmark output -> maintainer interpretation | Results must be named clearly enough to compare modes and shapes without ambiguity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | T | fixture selection | mitigate | Use deterministic fixtures and shared batch sizes across both parser modes, including `shape=wide-flat` and a multi-transformer `shape=transformer-heavy`. |
| T-07-06 | R | legacy benchmark helper drift | mitigate | Keep an in-repo benchmark-only legacy helper, annotate it as the BUILD-05 control path, and reuse the same fixture helpers and batch constants as the explicit parser benchmarks. |
| T-07-07 | D | noisy benchmark interpretation | mitigate | Encode parser mode, doc count, and shape directly in every sub-benchmark name and report allocations in all families. |
</threat_model>

<verification>
1. Phase 07 benchmark smoke:
   `go test ./... -run '^$' -bench 'Benchmark(AddDocumentPhase07|BuildPhase07|FinalizePhase07)' -benchtime=1x -count=1`
2. Full suite:
   `go test ./... -count=1`
</verification>

<success_criteria>
- Benchmark output reports ingest/build latency and allocation deltas for legacy vs explicit parser modes on the same fixtures.
- Benchmark names make parser mode, document count, and fixture shape explicit.
- The repo can reproduce BUILD-05 without checking out an older commit or using an external script.
</success_criteria>

<output>
After completion, create `.planning/phases/07-builder-parsing-numeric-fidelity/07-builder-parsing-numeric-fidelity-02-SUMMARY.md`
</output>
