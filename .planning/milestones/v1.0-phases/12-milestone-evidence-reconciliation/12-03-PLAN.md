---
phase: 12-milestone-evidence-reconciliation
plan: 03
type: execute
wave: 2
depends_on:
  - 12-01
  - 12-02
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/v1.0-MILESTONE-AUDIT.md
autonomous: true
requirements:
  - BUILD-01
  - BUILD-02
  - BUILD-03
  - BUILD-04
  - BUILD-05
  - DERIVE-01
  - DERIVE-02
  - DERIVE-03
  - DERIVE-04
must_haves:
  truths:
    - The requirements ledger reflects shipped reality: BUILD rows point to Phase 07 completion, DERIVE rows point to Phase 09 completion, and previously completed PATH/HCARD/SIZE rows remain aligned with their verification artifacts.
    - The refreshed milestone audit no longer fails because of missing Phase 07/09 verification artifacts or stale requirement status.
    - The refreshed audit cites current-tree command evidence instead of copy-forwarding old results.
  artifacts:
    - `.planning/REQUIREMENTS.md` has updated checkbox and traceability status for all BUILD and DERIVE requirements.
    - `.planning/v1.0-MILESTONE-AUDIT.md` is refreshed against the new verification artifacts and current-tree commands.
  key_links:
    - `REQUIREMENTS.md` is updated only after `07-VERIFICATION.md` and `09-VERIFICATION.md` exist.
    - The refreshed milestone audit reads those new verification artifacts plus the existing Phase 06/08/10/11 verification reports when evaluating milestone status.
    - If Phase 07 stays as an accepted validation gap, the exact `Accepted validation gap:` line from `07-VALIDATION.md` is reused verbatim in `07-VERIFICATION.md` and `.planning/v1.0-MILESTONE-AUDIT.md`.
---

<objective>
Reconcile the milestone ledger and audit after the missing Phase 07 and Phase 09 evidence has been rebuilt.

Purpose: make the v1.0 milestone close reflect shipped reality instead of stale planning state.
Output: updated `.planning/REQUIREMENTS.md` and refreshed `.planning/v1.0-MILESTONE-AUDIT.md`.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/12-milestone-evidence-reconciliation/12-RESEARCH.md
@.planning/phases/12-milestone-evidence-reconciliation/12-VALIDATION.md
@.planning/phases/12-milestone-evidence-reconciliation/12-PATTERNS.md
@.planning/phases/07-builder-parsing-numeric-fidelity/07-VALIDATION.md
@.planning/phases/07-builder-parsing-numeric-fidelity/07-VERIFICATION.md
@.planning/phases/09-derived-representations/09-VERIFICATION.md
@.planning/phases/06-query-path-hot-path/06-VERIFICATION.md
@.planning/phases/08-adaptive-high-cardinality-indexing/08-VERIFICATION.md
@.planning/phases/10-serialization-compaction/10-VERIFICATION.md
@.planning/phases/11-real-corpus-prefix-compression-benchmarking/11-VERIFICATION.md
@AGENTS.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Reconcile the requirements ledger to verified shipped reality</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
- .planning/REQUIREMENTS.md
- .planning/phases/07-builder-parsing-numeric-fidelity/07-VERIFICATION.md
- .planning/phases/09-derived-representations/09-VERIFICATION.md
- .planning/phases/06-query-path-hot-path/06-VERIFICATION.md
- .planning/phases/08-adaptive-high-cardinality-indexing/08-VERIFICATION.md
- .planning/phases/10-serialization-compaction/10-VERIFICATION.md
- .planning/ROADMAP.md
  </read_first>
  <behavior>
    - The ledger must reflect the phase that actually delivered each requirement, not the closure phase that repaired the evidence.
    - BUILD and DERIVE rows must not move to complete until the new verification artifacts from Plans `12-01` and `12-02` exist.
    - Existing PATH, HCARD, and SIZE rows must stay aligned with their already-present verification reports.
  </behavior>
  <action>Update `.planning/REQUIREMENTS.md` only after Plans `12-01` and `12-02` finish and both new verification artifacts exist. In the domain sections, change `BUILD-01` through `BUILD-05` from unchecked to checked and append `— validated in Phase 07` wording consistent with the existing PATH/HCARD/SIZE style. Do the same for `DERIVE-01` through `DERIVE-04` with `— validated in Phase 09`. In the `## Traceability` table, change the BUILD rows from `| BUILD-0X | Phase 12 | Pending |` to `| BUILD-0X | Phase 07 | Complete |` and the DERIVE rows from `| DERIVE-0X | Phase 12 | Pending |` to `| DERIVE-0X | Phase 09 | Complete |`. Recalculate the coverage footer from the final checklist/table state rather than by blind string replacement; on this repo that should land at `- Checked off: 20`, `- Mapped to phases: 20`, and `- Unmapped: 0`. Update the footer line to `*Last updated: YYYY-MM-DD after milestone evidence reconciliation*` using the actual execution day. Do not alter the PATH, HCARD, or SIZE phase mappings unless the verification artifacts in the read-first set show a mismatch.</action>
  <acceptance_criteria>
- `.planning/REQUIREMENTS.md` contains checked checklist entries for `BUILD-01` through `BUILD-05`.
- `.planning/REQUIREMENTS.md` contains checked checklist entries for `DERIVE-01` through `DERIVE-04`.
- The `## Traceability` table contains the exact rows `| BUILD-01 | Phase 07 | Complete |` and `| DERIVE-01 | Phase 09 | Complete |` and equivalent `Complete` rows for the other BUILD and DERIVE IDs.
- `.planning/REQUIREMENTS.md` contains the exact line `- Checked off: 20`.
- `.planning/REQUIREMENTS.md` contains the exact lines `- Mapped to phases: 20` and `- Unmapped: 0`.
- `.planning/REQUIREMENTS.md` ends with `*Last updated: YYYY-MM-DD after milestone evidence reconciliation*` using the execution day.
- `.planning/REQUIREMENTS.md` still shows PATH, HCARD, and SIZE rows as complete and mapped to Phases 06, 08, and 10 respectively.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/07-builder-parsing-numeric-fidelity/07-VERIFICATION.md
test -f .planning/phases/09-derived-representations/09-VERIFICATION.md
rg -n '\[x\] \*\*BUILD-01\*\*|\[x\] \*\*BUILD-05\*\*|\[x\] \*\*DERIVE-01\*\*|\[x\] \*\*DERIVE-04\*\*|\| BUILD-01 \| Phase 07 \| Complete \||\| DERIVE-01 \| Phase 09 \| Complete \||- Checked off: 20|- Mapped to phases: 20|- Unmapped: 0' .planning/REQUIREMENTS.md
rg -n '^\*Last updated: 20[0-9]{2}-[0-9]{2}-[0-9]{2} after milestone evidence reconciliation\*$' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>The requirements ledger reflects verified shipped reality and no longer leaves BUILD/DERIVE stranded as pending evidence debt.</done>
</task>

<task type="execute">
  <name>Task 2: Refresh the milestone audit against the reconciled evidence set</name>
  <files>.planning/v1.0-MILESTONE-AUDIT.md</files>
  <read_first>
- .planning/v1.0-MILESTONE-AUDIT.md
- .planning/REQUIREMENTS.md
- .planning/phases/07-builder-parsing-numeric-fidelity/07-VALIDATION.md
- .planning/phases/07-builder-parsing-numeric-fidelity/07-VERIFICATION.md
- .planning/phases/09-derived-representations/09-VERIFICATION.md
- .planning/phases/06-query-path-hot-path/06-VERIFICATION.md
- .planning/phases/08-adaptive-high-cardinality-indexing/08-VERIFICATION.md
- .planning/phases/10-serialization-compaction/10-VERIFICATION.md
- .planning/phases/11-real-corpus-prefix-compression-benchmarking/11-VERIFICATION.md
  </read_first>
  <behavior>
    - The refreshed audit must rerun its underlying current-tree commands instead of copying old outputs forward.
    - Missing-verification and stale-ledger blockers must disappear once Plans `12-01` and `12-02` complete and Task 1 updates the ledger.
    - If Phase 07 validation remains an explicit accepted gap instead of a fully closed debt, the audit must say so clearly without treating missing verification as still unresolved.
  </behavior>
  <action>Refresh `.planning/v1.0-MILESTONE-AUDIT.md` after Task 1 and after Plans `12-01` and `12-02` complete. Re-run the current-tree commands already used by the existing audit: `go test ./... -count=1`, `go run ./examples/transformers/main.go`, and `go run ./examples/transformers-advanced/main.go`. Update the YAML frontmatter to an explicit terminal state rather than prose-only cleanup: set `audited:` to the actual execution UTC timestamp, set `status: passed`, set `scores.requirements: 20/20`, `scores.phases: 6/6`, `scores.integration: 5/5`, and `scores.flows: 3/3`, and make the `gaps:` block explicit with `requirements: []`, `integration: []`, and `flows: []`. If `07-VALIDATION.md` closed cleanly, update Nyquist frontmatter to `compliant_phases: [06, 07, 08, 09, 10, 11]`, `partial_phases: []`, `missing_phases: []`, `accepted_gaps: []`, and `overall: compliant`, and add the exact sentence `Phase 07 validation debt: closed on current-tree evidence.` under both `## Nyquist Coverage` and the pass-state rationale section. If Phase 07 validation remains explicitly accepted rather than closed, still remove the missing-verification blockers and stale requirement blockers, keep `status: passed`, set Nyquist frontmatter to `compliant_phases: [06, 08, 09, 10, 11]`, `partial_phases: [07]`, `missing_phases: []`, add `accepted_gaps:` with the exact `Accepted validation gap:` line copied verbatim from `07-VALIDATION.md`, and keep `overall: partial`. In both branches, update `## Phase Status`, `## Requirements Coverage`, `## End-to-End Flows`, and replace `## Why The Audit Fails` with `## Milestone Pass Rationale` grounded in the new verification artifacts and reconciled ledger. If an accepted gap exists, repeat the exact `Accepted validation gap:` line verbatim under `## Nyquist Coverage` and `## Milestone Pass Rationale` so all three artifacts tell the same story.</action>
  <acceptance_criteria>
- `.planning/v1.0-MILESTONE-AUDIT.md` frontmatter contains `status: passed`, `requirements: 20/20`, `phases: 6/6`, `integration: 5/5`, `flows: 3/3`, and an explicit `gaps:` block with empty `requirements`, `integration`, and `flows` arrays.
- `.planning/v1.0-MILESTONE-AUDIT.md` no longer contains the exact strings `Phase \`07\` is missing \`07-VERIFICATION.md\``, `Phase \`09\` is missing \`09-VERIFICATION.md\``, `verification_status: "missing"`, or `status: gaps_found`.
- `.planning/v1.0-MILESTONE-AUDIT.md` no longer describes BUILD or DERIVE requirements as pending because of missing verification artifacts.
- The refreshed audit cites the exact commands `go test ./... -count=1`, `go run ./examples/transformers/main.go`, and `go run ./examples/transformers-advanced/main.go`.
- The refreshed audit contains the exact heading `## Milestone Pass Rationale` instead of `## Why The Audit Fails`.
- The audit text explicitly states either `Phase 07 validation debt: closed on current-tree evidence.` or an `Accepted validation gap:` line copied verbatim from `07-VALIDATION.md` and `07-VERIFICATION.md`.
- The refreshed audit reads as a rerun of the same milestone audit artifact, not a new artifact class with different headings/scoring vocabulary.
  </acceptance_criteria>
  <verify>
    <automated>go test ./... -count=1
go run ./examples/transformers/main.go >/dev/null
go run ./examples/transformers-advanced/main.go >/dev/null
! rg -F 'Phase `07` is missing `07-VERIFICATION.md`' .planning/v1.0-MILESTONE-AUDIT.md
! rg -F 'Phase `09` is missing `09-VERIFICATION.md`' .planning/v1.0-MILESTONE-AUDIT.md
! rg -F 'verification_status: "missing"' .planning/v1.0-MILESTONE-AUDIT.md
! rg -F 'status: gaps_found' .planning/v1.0-MILESTONE-AUDIT.md
! rg -F '## Why The Audit Fails' .planning/v1.0-MILESTONE-AUDIT.md
rg -n '^status: passed$|^  requirements: 20/20$|^  phases: 6/6$|^  integration: 5/5$|^  flows: 3/3$|^  requirements: \\[\\]$|^  integration: \\[\\]$|^  flows: \\[\\]$|^## Milestone Pass Rationale$|go test ./\\.\\.\\. -count=1|go run ./examples/transformers/main.go|go run ./examples/transformers-advanced/main.go|^Phase 07 validation debt: closed on current-tree evidence\\.$|^Accepted validation gap:' .planning/v1.0-MILESTONE-AUDIT.md
if rg -q '^Accepted validation gap:' .planning/phases/07-builder-parsing-numeric-fidelity/07-VALIDATION.md; then
  GAP_LINE=$(rg '^Accepted validation gap:' .planning/phases/07-builder-parsing-numeric-fidelity/07-VALIDATION.md | head -1)
  rg -F "$GAP_LINE" .planning/phases/07-builder-parsing-numeric-fidelity/07-VERIFICATION.md
  rg -F "$GAP_LINE" .planning/v1.0-MILESTONE-AUDIT.md
fi</automated>
  </verify>
  <done>The milestone audit has been rerun against the reconciled evidence set and no longer fails for missing Phase 07/09 verification or stale BUILD/DERIVE ledger state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new verification artifacts -> requirements ledger | requirements can be marked complete before the supporting verification exists |
| reconciled ledger -> refreshed milestone audit | the audit can report a false pass if it is not regenerated from current-tree evidence |
| accepted Phase 07 validation gap -> milestone close decision | accepted debt can be mistaken for resolved debt if the audit language is ambiguous |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | T/R | requirements ledger | mitigate | Update BUILD and DERIVE rows only after the new verification artifacts exist and cite the actual implementing phases. |
| T-12-08 | R | milestone audit refresh | mitigate | Re-run the audit’s current-tree commands and refresh the existing audit artifact rather than copy-forwarding stale blocker language. |
</threat_model>

<verification>
1. Requirements ledger checks:
   `rg -n '\[x\] \*\*BUILD-0[1-5]\*\*|\[x\] \*\*DERIVE-0[1-4]\*\*|\| BUILD-01 \| Phase 07 \| Complete \||\| DERIVE-01 \| Phase 09 \| Complete \|' .planning/REQUIREMENTS.md`
2. Current-tree audit commands:
   `go test ./... -count=1`
   `go run ./examples/transformers/main.go`
   `go run ./examples/transformers-advanced/main.go`
3. Refreshed audit checks:
   `! rg -F 'Phase \`07\` is missing \`07-VERIFICATION.md\`' .planning/v1.0-MILESTONE-AUDIT.md`
   `! rg -F 'Phase \`09\` is missing \`09-VERIFICATION.md\`' .planning/v1.0-MILESTONE-AUDIT.md`
   `! rg -F 'verification_status: "missing"' .planning/v1.0-MILESTONE-AUDIT.md`
   `! rg -F 'status: gaps_found' .planning/v1.0-MILESTONE-AUDIT.md`
</verification>

<success_criteria>
- `.planning/REQUIREMENTS.md` reflects verified shipped reality for BUILD and DERIVE without regressing PATH/HCARD/SIZE mappings.
- `.planning/v1.0-MILESTONE-AUDIT.md` no longer fails because of missing Phase 07/09 verification or stale BUILD/DERIVE ledger state.
- The refreshed audit explicitly records whether Phase 07 validation debt was closed or accepted, and if accepted the exact `Accepted validation gap:` line matches the Phase 07 validation and verification artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/12-milestone-evidence-reconciliation/12-milestone-evidence-reconciliation-03-SUMMARY.md`
</output>
