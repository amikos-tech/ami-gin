---
phase: 12-milestone-evidence-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/07-builder-parsing-numeric-fidelity/07-VALIDATION.md
  - .planning/phases/07-builder-parsing-numeric-fidelity/07-VERIFICATION.md
autonomous: true
requirements:
  - BUILD-01
  - BUILD-02
  - BUILD-03
  - BUILD-04
  - BUILD-05
must_haves:
  truths:
    - Phase 07 has a current-tree verification report that maps BUILD-01 through BUILD-05 to shipped implementation evidence rather than summary-only claims.
    - Phase 07 validation debt is no longer ambiguous: either `07-VALIDATION.md` is refreshed to a compliant audited state or the remaining gap is explicitly documented for milestone-audit acceptance.
    - The proof surface stays repo-local and reproducible with current-tree commands, including targeted parser/numeric regressions and the Phase 07 benchmark smoke harness.
  artifacts:
    - `.planning/phases/07-builder-parsing-numeric-fidelity/07-VALIDATION.md` records the current validation audit outcome from the execution day.
    - `.planning/phases/07-builder-parsing-numeric-fidelity/07-VERIFICATION.md` exists and follows the completed-phase verification-report structure already used elsewhere in the repo.
    - The verification report cites the current-tree commands needed to support BUILD-01 through BUILD-05.
  key_links:
    - `07-VERIFICATION.md` is grounded in `07-01-PLAN.md`, `07-02-PLAN.md`, both Phase 07 summaries, and the current source/test files that implement the parser/numeric work.
    - `07-VALIDATION.md` and `07-VERIFICATION.md` agree on whether Phase 07 Nyquist debt is closed or must be carried as an accepted audit note.
    - If Phase 07 lands as an accepted gap instead of a clean close, the exact `Accepted validation gap:` line is reused verbatim in `07-VALIDATION.md`, `07-VERIFICATION.md`, and the refreshed milestone audit.
---

<objective>
Reconstruct the missing milestone evidence for Phase 07 and resolve the outstanding validation-state ambiguity.

Purpose: turn the shipped Phase 07 parser/numeric work into auditable milestone evidence instead of leaving BUILD-01 through BUILD-05 stranded behind missing verification.
Output: refreshed `07-VALIDATION.md`, new `07-VERIFICATION.md`, and current-tree proof commands captured against the shipped implementation.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-milestone-evidence-reconciliation/12-RESEARCH.md
@.planning/phases/12-milestone-evidence-reconciliation/12-VALIDATION.md
@.planning/phases/12-milestone-evidence-reconciliation/12-PATTERNS.md
@.planning/phases/07-builder-parsing-numeric-fidelity/07-01-PLAN.md
@.planning/phases/07-builder-parsing-numeric-fidelity/07-02-PLAN.md
@.planning/phases/07-builder-parsing-numeric-fidelity/07-builder-parsing-numeric-fidelity-01-SUMMARY.md
@.planning/phases/07-builder-parsing-numeric-fidelity/07-builder-parsing-numeric-fidelity-02-SUMMARY.md
@.planning/phases/07-builder-parsing-numeric-fidelity/07-VALIDATION.md
@.planning/phases/06-query-path-hot-path/06-VERIFICATION.md
@.planning/phases/11-real-corpus-prefix-compression-benchmarking/11-VERIFICATION.md
@builder.go
@gin.go
@query.go
@serialize.go
@benchmark_test.go
@gin_test.go
@transformers_test.go
@transformer_registry_test.go
@serialize_security_test.go
@AGENTS.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Refresh the Phase 07 validation artifact against the current tree</name>
  <files>.planning/phases/07-builder-parsing-numeric-fidelity/07-VALIDATION.md</files>
  <read_first>
- .planning/phases/07-builder-parsing-numeric-fidelity/07-VALIDATION.md
- .planning/phases/07-builder-parsing-numeric-fidelity/07-01-PLAN.md
- .planning/phases/07-builder-parsing-numeric-fidelity/07-02-PLAN.md
- .planning/phases/07-builder-parsing-numeric-fidelity/07-builder-parsing-numeric-fidelity-01-SUMMARY.md
- .planning/phases/07-builder-parsing-numeric-fidelity/07-builder-parsing-numeric-fidelity-02-SUMMARY.md
- .planning/phases/09-derived-representations/09-VALIDATION.md
- .planning/phases/12-milestone-evidence-reconciliation/12-VALIDATION.md
- benchmark_test.go
- gin_test.go
- transformers_test.go
- transformer_registry_test.go
- serialize_security_test.go
  </read_first>
  <behavior>
    - Phase 07 validation must stop being a stale draft with no current audit outcome.
    - Nyquist closure must be evidence-based: close it only if the current-tree commands and the finished validation map support closure; otherwise preserve the gap explicitly and do not fake compliance.
    - The updated validation artifact must be usable as an input to the refreshed milestone audit.
  </behavior>
  <action>Re-audit `.planning/phases/07-builder-parsing-numeric-fidelity/07-VALIDATION.md` against the current tree. Re-run the Phase 07 targeted regression command for atomic failure, exact-int fidelity, mixed-promotion rejection, transformer numeric compatibility, and decode parity: `go test ./... -run 'Test(AddDocumentRejectsUnsupportedNumberWithoutPartialMutation|NumericIndexPreservesInt64Exactness|MixedNumericPathRejectsLossyPromotion|IntOnlyNumericDecodeParity|TransformerNumericPathExplicitParserCompatibility|TransformerNumericDecodeParity)' -count=1`. Re-run the Phase 07 benchmark smoke harness: `go test ./... -run '^$' -bench 'Benchmark(AddDocumentPhase07|BuildPhase07|FinalizePhase07)' -benchtime=1x -count=1`. Re-run the repo-wide suite cited by the milestone audit: `go test ./... -count=1`. Update `07-VALIDATION.md` using the actual execution day and UTC timestamp, not a hardcoded date. Closure is allowed only if all three commands are green, every row in `Per-Task Verification Map` is `✅ green`, every `Validation Sign-Off` checkbox is checked, and the frontmatter can honestly become `status: verified`, `nyquist_compliant: true`, `wave_0_complete: true`. In that close branch, append `## Validation Audit YYYY-MM-DD` dated with the execution day, summarize the benchmark smoke with concise metrics instead of dumping the full table, set `updated:` to the execution timestamp, and end with `**Approval:** approved YYYY-MM-DD`. If any command or coverage requirement still fails, keep the frontmatter honest (`nyquist_compliant: false`, no false `verified` state), append `## Validation Audit YYYY-MM-DD` with the exact failing command/result, and add one canonical line starting with `Accepted validation gap:` that names the remaining blocker in a form Task 2 and Plan `12-03` can reuse verbatim. In that accepted-gap branch, end the file with `**Approval:** accepted gap YYYY-MM-DD`.</action>
  <acceptance_criteria>
- `.planning/phases/07-builder-parsing-numeric-fidelity/07-VALIDATION.md` contains a `## Validation Audit YYYY-MM-DD` heading dated with the actual execution day.
- `07-VALIDATION.md` explicitly mentions the targeted regression command, the benchmark smoke command, and `go test ./... -count=1`.
- If the audit closes Phase 07 debt, the frontmatter shows `status: verified`, `nyquist_compliant: true`, and `wave_0_complete: true`, all `Validation Sign-Off` boxes are checked, and the file ends with `**Approval:** approved YYYY-MM-DD`.
- If the audit accepts a remaining gap, the frontmatter does not falsely claim Nyquist compliance, the audit section contains an `Accepted validation gap:` line with the exact failing command/result, and the file ends with `**Approval:** accepted gap YYYY-MM-DD`.
- The file no longer relies only on the original 2026-04-15 draft status as the latest validation evidence.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/07-builder-parsing-numeric-fidelity/07-VALIDATION.md
rg -n '^## Validation Audit 20[0-9]{2}-[0-9]{2}-[0-9]{2}$|AddDocumentRejectsUnsupportedNumberWithoutPartialMutation|go test ./\\.\\.\\. -count=1|^\\*\\*Approval:\\*\\* (approved|accepted gap) 20[0-9]{2}-[0-9]{2}-[0-9]{2}$|^Accepted validation gap:' .planning/phases/07-builder-parsing-numeric-fidelity/07-VALIDATION.md
rg -F "Benchmark(AddDocumentPhase07|BuildPhase07|FinalizePhase07)" .planning/phases/07-builder-parsing-numeric-fidelity/07-VALIDATION.md</automated>
  </verify>
  <done>The Phase 07 validation artifact reflects the current tree and states clearly whether Nyquist debt is closed or must be carried as an accepted gap.</done>
</task>

<task type="execute">
  <name>Task 2: Create the missing Phase 07 verification report</name>
  <files>.planning/phases/07-builder-parsing-numeric-fidelity/07-VERIFICATION.md</files>
  <read_first>
- .planning/phases/07-builder-parsing-numeric-fidelity/07-01-PLAN.md
- .planning/phases/07-builder-parsing-numeric-fidelity/07-02-PLAN.md
- .planning/phases/07-builder-parsing-numeric-fidelity/07-builder-parsing-numeric-fidelity-01-SUMMARY.md
- .planning/phases/07-builder-parsing-numeric-fidelity/07-builder-parsing-numeric-fidelity-02-SUMMARY.md
- .planning/phases/07-builder-parsing-numeric-fidelity/07-VALIDATION.md
- .planning/phases/06-query-path-hot-path/06-VERIFICATION.md
- .planning/phases/11-real-corpus-prefix-compression-benchmarking/11-VERIFICATION.md
- builder.go
- gin.go
- query.go
- serialize.go
- benchmark_test.go
- gin_test.go
- transformers_test.go
- transformer_registry_test.go
- serialize_security_test.go
  </read_first>
  <behavior>
    - The report must look like the repo’s other phase-close verification reports, not like a summary or changelog.
    - BUILD-01 through BUILD-05 must each be tied to both implementation artifacts and current-tree command evidence.
    - Any remaining Phase 07 validation debt must be called out explicitly in the report instead of being hidden.
  </behavior>
  <action>Create `.planning/phases/07-builder-parsing-numeric-fidelity/07-VERIFICATION.md` using the same structure and frontmatter contract as `06-VERIFICATION.md` and `11-VERIFICATION.md`: frontmatter with `phase`, `verified`, `status`, `score`, and `overrides_applied`, then `## Goal Achievement`, `### Observable Truths`, `### Required Artifacts`, `### Behavioral Spot-Checks`, `### Requirements Coverage`, and `### Gaps Summary`. Populate the report from the shipped Phase 07 plans/summaries plus the current-tree evidence gathered in Task 1. The observable truths must cover: explicit-number parsing replacing eager whole-document `json.Unmarshal(..., &any)`, exact-int numeric mode and guarded mixed promotion, failure-safe unsupported numbers, transformer numeric compatibility, and benchmark-backed BUILD-05 evidence. In `Behavioral Spot-Checks`, cite the exact current-tree commands from Task 1 and summarize the benchmark smoke with the signal needed for BUILD-05 rather than dumping raw full benchmark tables. If Task 1 closed validation debt, state `None.` in `Gaps Summary`. If Task 1 found a remaining Nyquist issue, carry forward the exact `Accepted validation gap:` line from `07-VALIDATION.md` verbatim under `Gaps Summary` so Plan `12-03` can reuse the identical wording in the milestone audit.</action>
  <acceptance_criteria>
- `.planning/phases/07-builder-parsing-numeric-fidelity/07-VERIFICATION.md` exists.
- `07-VERIFICATION.md` contains the exact heading `# Phase 07: Builder Parsing & Numeric Fidelity Verification Report`.
- `07-VERIFICATION.md` contains the exact headings `### Observable Truths`, `### Required Artifacts`, `### Behavioral Spot-Checks`, `### Requirements Coverage`, and `### Gaps Summary`.
- `07-VERIFICATION.md` mentions every Phase 07 requirement ID: `BUILD-01`, `BUILD-02`, `BUILD-03`, `BUILD-04`, and `BUILD-05`.
- `07-VERIFICATION.md` cites the exact benchmark command `go test ./... -run '^$' -bench 'Benchmark(AddDocumentPhase07|BuildPhase07|FinalizePhase07)' -benchtime=1x -count=1`.
- `07-VERIFICATION.md` contains either `Gaps Summary` `None.` or an `Accepted validation gap:` line copied verbatim from `07-VALIDATION.md`.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/07-builder-parsing-numeric-fidelity/07-VERIFICATION.md
rg -n '# Phase 07: Builder Parsing & Numeric Fidelity Verification Report|BUILD-01|BUILD-02|BUILD-03|BUILD-04|BUILD-05|### Observable Truths|### Behavioral Spot-Checks|^Accepted validation gap:|^None\\.$' .planning/phases/07-builder-parsing-numeric-fidelity/07-VERIFICATION.md
rg -F "Benchmark(AddDocumentPhase07|BuildPhase07|FinalizePhase07)" .planning/phases/07-builder-parsing-numeric-fidelity/07-VERIFICATION.md</automated>
  </verify>
  <done>Phase 07 now has a milestone-grade verification artifact that can support requirements-ledger reconciliation and the refreshed audit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped Phase 07 summaries -> new verification report | summary claims can drift away from current-tree reality if not revalidated |
| current test/benchmark commands -> milestone evidence | copied or stale command evidence can create a false-positive closeout |
| validation artifact -> milestone audit | ambiguous Nyquist state can be misreported as clean if Phase 07 debt is not made explicit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | T/R | Phase 07 verification synthesis | mitigate | Reconstruct `07-VERIFICATION.md` only from shipped plans/summaries plus rerun current-tree commands. |
| T-12-02 | R | requirement mapping | mitigate | Require every `BUILD-*` ID to appear in `07-VERIFICATION.md` with file/command evidence. |
| T-12-03 | I | benchmark evidence | mitigate | Re-run the Phase 07 benchmark smoke harness and cite the exact command in the report. |
| T-12-04 | R | Nyquist closure | mitigate | Refresh `07-VALIDATION.md` honestly and carry any unresolved debt forward explicitly rather than implying compliance. |
</threat_model>

<verification>
1. Phase 07 targeted parser/numeric regressions:
   `go test ./... -run 'Test(AddDocumentRejectsUnsupportedNumberWithoutPartialMutation|NumericIndexPreservesInt64Exactness|MixedNumericPathRejectsLossyPromotion|IntOnlyNumericDecodeParity|TransformerNumericPathExplicitParserCompatibility|TransformerNumericDecodeParity)' -count=1`
2. Phase 07 benchmark smoke:
   `go test ./... -run '^$' -bench 'Benchmark(AddDocumentPhase07|BuildPhase07|FinalizePhase07)' -benchtime=1x -count=1`
3. Artifact checks:
   `test -f .planning/phases/07-builder-parsing-numeric-fidelity/07-VALIDATION.md`
   `test -f .planning/phases/07-builder-parsing-numeric-fidelity/07-VERIFICATION.md`
</verification>

<success_criteria>
- `07-VERIFICATION.md` exists and supports BUILD-01 through BUILD-05 with current-tree evidence.
- `07-VALIDATION.md` is either refreshed to a compliant audited state or captures an explicit unresolved gap for milestone-audit acceptance.
- Phase 07 milestone evidence is no longer blocked on a missing verification artifact.
</success_criteria>

<output>
After completion, create `.planning/phases/12-milestone-evidence-reconciliation/12-milestone-evidence-reconciliation-01-SUMMARY.md`
</output>
