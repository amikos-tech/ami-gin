---
phase: 10-serialization-compaction
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - benchmark_test.go
autonomous: true
requirements:
  - SIZE-01
  - SIZE-02
  - SIZE-03
must_haves:
  truths:
    - Benchmark evidence covers at least one mixed representative fixture, one high-shared-prefix fixture, and one low-shared-prefix or random-like fixture.
    - The evidence reports exact legacy raw-wire bytes and current compact raw-wire bytes separately, not just a single post-compression size.
    - The evidence reports default compressed bytes separately so zstd gains are visible alongside raw-wire gains.
    - The evidence reports encode/decode cost deltas alongside size deltas using a reproducible benchmark configuration.
    - Benchmark coverage includes a post-decode query smoke path so Phase 10 cannot claim byte wins while ignoring query behavior after decode.
  artifacts:
    - `benchmark_test.go` defines Phase 10 compaction fixtures and benchmark cases for size, encode, decode, and post-decode query probes.
  key_links:
    - `benchmark_test.go` already contains prefix microbenchmarks plus generic encode/decode/size benchmarks.
    - Phase 10 context requires raw-wire and zstd-reported deltas, not a single post-compression number.
    - Low-prefix data may see little gain, so the benchmark matrix must make that visible rather than hiding it.
---

<objective>
Produce the benchmark evidence for Phase 10.

Purpose: prove compact path and term encoding actually reduces encoded size on representative fixtures, quantify raw-wire versus compressed gains, and confirm encode/decode or query behavior does not regress silently.
Output: Phase 10 benchmark fixtures plus a benchmark matrix covering size, encode/decode cost, and post-decode query smoke metrics.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-serialization-compaction/10-CONTEXT.md
@.planning/phases/10-serialization-compaction/10-RESEARCH.md
@.planning/phases/10-serialization-compaction/10-VALIDATION.md
@.planning/phases/10-serialization-compaction/10-01-PLAN.md
@AGENTS.md
@benchmark_test.go
@serialize.go
@prefix.go

<interfaces>
Existing benchmark anchors in `benchmark_test.go`:
```go
func BenchmarkPrefixRatio(b *testing.B) { ... }
func BenchmarkEncode(b *testing.B) { ... }
func BenchmarkDecode(b *testing.B) { ... }
func BenchmarkEncodedSize(b *testing.B) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 10 fixture families and exact raw-wire baseline reporting</name>
  <files>benchmark_test.go</files>
  <read_first>
- benchmark_test.go
- serialize.go
- prefix.go
- .planning/phases/10-serialization-compaction/10-CONTEXT.md
- .planning/phases/10-serialization-compaction/10-RESEARCH.md
- AGENTS.md
  </read_first>
  <behavior>
    - Benchmarks cover mixed, high-prefix, and random-like fixture families.
    - Each fixture reports an exact whole-wire pre-Phase-10 raw baseline and the actual compact-format size under `CompressionNone`.
    - Each fixture also reports the default compressed size so zstd gains are visible separately from raw-wire gains.
    - Poor-gain fixtures remain visible; the benchmark does not discard or rewrite them.
  </behavior>
  <action>In `benchmark_test.go`, add deterministic Phase 10 fixture builders with concrete names `buildPhase10MixedFixture`, `buildPhase10HighPrefixFixture`, and `buildPhase10RandomLikeFixture`. Build the mixed fixture so it contains both realistic repeated JSON paths and at least one derived-representation-bearing path; build the high-prefix fixture so it maximizes shared prefixes and includes an adaptive/high-cardinality string path; build the random-like fixture so low-prefix identifiers make fallback or reduced gains visible. Add a benchmark named `BenchmarkPhase10SerializationCompaction` with sub-bench names `Mixed`, `HighPrefix`, and `RandomLike`. For each fixture, report `compact_raw_bytes` from `EncodeWithLevel(idx, CompressionNone)` and `default_zstd_bytes` from `Encode(idx)`. Also compute the exact legacy raw baseline for the full uncompressed wire layout by reconstructing only the bytes that changed in Phase 10: calculate `legacy_string_payload_bytes` as the old per-string formula (`2 + len(value)`) across path names, classic terms, and adaptive promoted terms; calculate `compact_string_payload_bytes` as the actual bytes emitted by the new ordered-string section helper for those same sections; then derive `legacy_raw_bytes = compact_raw_bytes - compact_string_payload_bytes + legacy_string_payload_bytes`. Report `bytes_saved_pct` from `legacy_raw_bytes` versus `compact_raw_bytes`. This avoids fake heuristics while keeping the comparison exact after the old serializer path is gone.</action>
  <acceptance_criteria>
- `benchmark_test.go` contains helpers named `buildPhase10MixedFixture`, `buildPhase10HighPrefixFixture`, and `buildPhase10RandomLikeFixture`.
- `benchmark_test.go` contains `BenchmarkPhase10SerializationCompaction`.
- The benchmark defines fixture sub-benchs named `Mixed`, `HighPrefix`, and `RandomLike`.
- The benchmark reports metrics named `legacy_raw_bytes`, `compact_raw_bytes`, and `default_zstd_bytes`.
- The benchmark reports a percentage metric derived from `legacy_raw_bytes` versus `compact_raw_bytes`.
- The `legacy_raw_bytes` metric is computed from the exact old raw-string layout plus unchanged current-wire bytes, not from a heuristic estimate.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
go test ./... -run '^$' -bench 'BenchmarkPhase10SerializationCompaction' -benchtime=1x -count=1</automated>
  </verify>
  <done>The repo has fixture-backed raw-wire and compressed-size evidence for Phase 10 across the required data-shape matrix.</done>
</task>

<task type="auto">
  <name>Task 2: Measure encode/decode costs with reproducible timing and post-decode query smoke</name>
  <files>benchmark_test.go</files>
  <read_first>
- benchmark_test.go
- serialize.go
- query.go
- .planning/phases/10-serialization-compaction/10-CONTEXT.md
- .planning/phases/10-serialization-compaction/10-RESEARCH.md
  </read_first>
  <behavior>
    - Benchmark output includes encode and decode timing for each Phase 10 fixture.
    - Timing claims are gathered with a stable benchmark configuration, not a single-iteration smoke run.
    - Benchmark output includes a post-decode query smoke probe so size wins are paired with query-behavior evidence.
    - The query smoke uses representative predicates that hit both ordinary raw paths and at least one representation-aware or adaptive path by construction across the fixture set.
  </behavior>
  <action>Extend `BenchmarkPhase10SerializationCompaction` so each fixture also exposes encode and decode timing branches plus a `QueryAfterDecode` branch or metric after decoding compact bytes. Use concrete predicates derived from the fixture data rather than placeholder paths. At minimum, benchmark one ordinary raw string equality probe on every fixture, one representation-aware alias probe on the mixed fixture, and one adaptive/high-cardinality probe on the high-prefix fixture. Keep the benchmark repo-local and single-package. For actual timing evidence, standardize verification on `go test ./... -run '^$' -bench 'BenchmarkPhase10SerializationCompaction' -benchtime=1s -count=3 -benchmem`; the `-benchtime=1x` run from Task 1 is only the harness smoke check for deterministic size reporting. Surface negative or flat results instead of normalizing them away: the benchmark output must make it obvious whether mixed/high-prefix fixtures gained and whether random-like data stayed effectively flat or fell back to raw mode.</action>
  <acceptance_criteria>
- `BenchmarkPhase10SerializationCompaction` includes encode and decode measurement for each fixture.
- `BenchmarkPhase10SerializationCompaction` includes a post-decode query smoke branch or metric labeled `QueryAfterDecode`.
- The query smoke uses concrete predicates derived from the fixture data, not placeholder paths.
- The benchmark covers at least one ordinary raw-path equality probe on every fixture.
- The benchmark covers a representation-aware probe and an adaptive/high-cardinality probe by construction across the fixture set.
- The timing verification command uses `-benchtime=1s -count=3 -benchmem`, not `-benchtime=1x`.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
go test ./... -run '^$' -bench 'BenchmarkPhase10SerializationCompaction' -benchtime=1s -count=3 -benchmem</automated>
  </verify>
  <done>The Phase 10 benchmark evidence pairs size wins with reproducible encode/decode and post-decode query-smoke reporting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture shape -> benchmark claims | biased fixtures can overstate compaction wins |
| raw-reference computation -> reported savings | a wrong baseline makes the percentage deltas meaningless |
| compact bytes -> decoded query smoke | size wins without query evidence can hide functional regressions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | R | benchmark fixtures | mitigate | Use mixed, high-prefix, and random-like fixtures and keep poor-gain cases visible. |
| T-10-08 | T/R | size baseline | mitigate | Compute `legacy_raw_bytes` from the exact pre-Phase-10 raw-string layout for changed sections plus unchanged current-wire bytes. |
| T-10-09 | R | query-regression evidence | mitigate | Pair size metrics with encode/decode timing and post-decode query smoke probes. |
</threat_model>

<verification>
1. Phase 10 benchmark harness smoke:
   `go test ./... -run '^$' -bench 'BenchmarkPhase10SerializationCompaction' -benchtime=1x -count=1`
2. Phase 10 timing evidence:
   `go test ./... -run '^$' -bench 'BenchmarkPhase10SerializationCompaction' -benchtime=1s -count=3 -benchmem`
</verification>

<success_criteria>
- Benchmark output covers the required mixed, high-prefix, and random-like fixtures.
- Benchmark output reports exact legacy raw-wire, compact raw-wire, and default compressed sizes separately.
- Benchmark output includes reproducible encode/decode timing and a post-decode query smoke signal.
</success_criteria>
