---
phase: 10-serialization-compaction
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - serialize.go
  - serialize_security_test.go
autonomous: true
requirements:
  - SIZE-03
  - SIZE-01
  - SIZE-02
must_haves:
  truths:
    - Compact path and term payloads fail closed on corruption, truncation, oversized counts, or unknown section modes.
    - v8 and older payloads remain rebuild-only and are rejected explicitly by the v9 decoder.
    - Compact-format round trips preserve Phase 09 representation alias behavior; compaction changes bytes, not public semantics.
    - Raw fallback mode, if present, remains explicit, deterministic, and decode-safe.
    - Mode selection is deterministic even on equal-size candidates; identical logical input must not produce unstable raw-vs-front-coded output.
  artifacts:
    - `serialize_security_test.go` locks corruption rejection, mode/payload mismatch rejection, deterministic tie-breaking, version mismatch, legacy rejection, and representation-parity coverage for the compact format.
    - `serialize.go` is only touched in this wave if a hardening gap is discovered while implementing the tests.
  key_links:
    - `readHeader()` already enforces exact-version matching and should stay the only compatibility branch.
    - `serialize_security_test.go` already owns malformed payload and version-mismatch coverage for prior wire changes.
    - Phase 09 representation alias parity already has regression coverage that Phase 10 must preserve.
---

<objective>
Harden the compact wire format and lock compatibility semantics for Phase 10.

Purpose: ensure the new compact sections reject malformed input cleanly, preserve rebuild-only version behavior, keep derived-representation semantics unchanged across compact round trips, and prove mode selection is deterministic.
Output: corruption, compatibility, and deterministic-fallback regression tests, plus any narrowly scoped hardening patches discovered while adding those tests.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-serialization-compaction/10-CONTEXT.md
@.planning/phases/10-serialization-compaction/10-RESEARCH.md
@.planning/phases/10-serialization-compaction/10-VALIDATION.md
@.planning/phases/10-serialization-compaction/10-01-PLAN.md
@.planning/phases/09-derived-representations/09-CONTEXT.md
@.planning/phases/09-derived-representations/09-03-SUMMARY.md
@AGENTS.md
@serialize.go
@serialize_security_test.go

<interfaces>
Current strict version gate:
```go
if idx.Header.Version != Version {
	return errors.Wrapf(ErrVersionMismatch, "got version %d, expected %d", idx.Header.Version, Version)
}
```

Current representation parity regression anchor:
```go
func TestDecodeRepresentationAliasParity(t *testing.T) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock corruption rejection and deterministic ordered-string mode semantics</name>
  <files>serialize_security_test.go, serialize.go</files>
  <read_first>
- serialize.go
- serialize_security_test.go
- .planning/phases/10-serialization-compaction/10-RESEARCH.md
- .planning/phases/10-serialization-compaction/10-VALIDATION.md
- AGENTS.md
  </read_first>
  <behavior>
    - Corrupt compact path payloads fail with `ErrInvalidFormat`, not partial decode.
    - Corrupt compact term payloads fail with `ErrInvalidFormat`, not partial decode.
    - Unknown compact-string section modes, decoded-count mismatches, and mode/payload disagreement are treated as format corruption.
    - Raw fallback mode, if present, is validated through the same count and bounds rules as compact mode.
    - Equal-size raw/front-coded candidates resolve deterministically to raw mode.
  </behavior>
  <action>Implement this wave test-first in `serialize_security_test.go`. Add `TestDecodeRejectsCompactPathSectionCorruption` that mutates a compact path-directory payload and asserts `ErrInvalidFormat`. Add `TestDecodeRejectsCompactTermSectionCorruption` that mutates either a classic or adaptive compact term payload and asserts `ErrInvalidFormat`. Add a dedicated mode/payload disagreement case that writes a raw-mode marker over front-coded bytes and a front-coded marker over raw bytes, and assert both fail closed. Add a deterministic tie-break regression test for the ordered-string helper that proves equal-size candidates choose raw mode consistently. Keep all corruption tests uncompressed (`CompressionNone`) so mutations are deterministic and auditable. Only if these tests expose a missing hardening guard may this task patch `serialize.go`; if production code must change here, keep the patch limited to the discovered gap and record it as a deviation from Plan 10-01 in the summary.</action>
  <acceptance_criteria>
- `serialize_security_test.go` contains `TestDecodeRejectsCompactPathSectionCorruption`.
- `serialize_security_test.go` contains `TestDecodeRejectsCompactTermSectionCorruption`.
- `serialize_security_test.go` contains explicit mode/payload disagreement coverage for ordered-string sections.
- `serialize_security_test.go` contains deterministic tie-break coverage proving raw mode wins on equal-size candidates.
- `readOrderedStrings(` rejects unknown compact-string modes with `ErrInvalidFormat`.
- `readOrderedStrings(` rejects decoded-count mismatches with `ErrInvalidFormat`.
- The new corruption tests mutate uncompressed payloads and assert the decode fails rather than partially succeeding.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
go test ./... -run 'Test(DecodeRejectsCompactPathSectionCorruption|DecodeRejectsCompactTermSectionCorruption|DecodeRejectsOrderedStringModePayloadMismatch|WriteOrderedStringsPrefersRawOnTie)' -count=1</automated>
  </verify>
  <done>The compact section readers fail closed on malformed input and the ordered-string fallback rule is deterministic.</done>
</task>

<task type="auto">
  <name>Task 2: Lock rebuild-only compatibility and representation alias parity on the compact format</name>
  <files>serialize_security_test.go</files>
  <read_first>
- serialize_security_test.go
- serialize.go
- gin.go
- .planning/phases/09-derived-representations/09-CONTEXT.md
- .planning/phases/09-derived-representations/09-03-SUMMARY.md
- .planning/phases/10-serialization-compaction/10-CONTEXT.md
- .planning/phases/10-serialization-compaction/10-RESEARCH.md
  </read_first>
  <behavior>
    - Payloads with the pre-Phase-10 version are rejected explicitly by the v9 decoder.
    - Existing legacy rejection tests continue to describe the rebuild-only policy accurately after the v9 bump.
    - Derived-representation alias queries produce the same results before and after compact-format encode/decode.
  </behavior>
  <action>In `serialize_security_test.go`, update or add version-compatibility coverage so the immediately previous version (`8`) is explicitly rejected by the v9 decoder with `ErrVersionMismatch`, alongside the older-version coverage already in the file. Keep the existing `TestDecodeVersionMismatch` / `TestDecodeLegacyRejected` coverage up to date with the new version constant rather than weakening them, and prefer a table-driven shape if it reduces future version-bump churn. Expand `TestDecodeRepresentationAliasParity` so it round-trips a representation-bearing index through the compact format and proves alias queries still match the original index after decode. The test must exercise at least one raw-path query and one alias query on the same source field so Phase 10 cannot accidentally reopen the Phase 09 public contract while changing string bytes on disk.</action>
  <acceptance_criteria>
- `serialize_security_test.go` keeps `TestDecodeVersionMismatch` green against the v9 version constant.
- `serialize_security_test.go` contains explicit rejection coverage for version `8`.
- `serialize_security_test.go` keeps or updates `TestDecodeLegacyRejected` so rebuild-only compatibility remains explicit after the v9 bump.
- `serialize_security_test.go` contains `TestDecodeRepresentationAliasParity`.
- `TestDecodeRepresentationAliasParity` exercises both a raw-path query and an alias query on the same logical source field after compact round-trip decode.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
go test ./... -run 'Test(DecodeVersionMismatch|DecodeLegacyRejected|DecodeRepresentationAliasParity)' -count=1</automated>
  </verify>
  <done>The compact format is explicitly rebuild-only and proven not to change derived-representation query semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compact section bytes -> decoder | malformed lengths or mode markers can cause unsafe allocation or silent misparse |
| prior-version payload -> current decoder | accidental compatibility would blur the explicit rebuild boundary |
| Phase 09 representation metadata -> compact round trip | byte-layout changes could silently break raw/alias query parity |
| equal-size mode choice -> stable output bytes | nondeterministic tie handling would make encoded output unstable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | T/I | compact decoder | mitigate | Add corruption tests for path and term sections and fail with `ErrInvalidFormat`. |
| T-10-05 | R | version contract | mitigate | Keep strict version mismatch rejection for v8 and older payloads. |
| T-10-06 | T/I | representation semantics | mitigate | Extend alias-parity tests across compact encode/decode. |
| T-10-10 | R | deterministic encoding | mitigate | Lock raw-on-tie behavior with direct regression coverage. |
</threat_model>

<verification>
1. Compact corruption rejection and deterministic fallback:
   `go test ./... -run 'Test(DecodeRejectsCompactPathSectionCorruption|DecodeRejectsCompactTermSectionCorruption|DecodeRejectsOrderedStringModePayloadMismatch|WriteOrderedStringsPrefersRawOnTie)' -count=1`
2. Compatibility and alias parity:
   `go test ./... -run 'Test(DecodeVersionMismatch|DecodeLegacyRejected|DecodeRepresentationAliasParity)' -count=1`
3. Full suite:
   `go test ./... -count=1`
</verification>

<success_criteria>
- Corrupt compact sections fail closed.
- The v9 decoder still enforces rebuild-only compatibility.
- Ordered-string mode selection is deterministic.
- Derived-representation raw/alias behavior survives compact round trips unchanged.
</success_criteria>
