---
phase: 06-query-path-hot-path
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - benchmark_test.go
autonomous: true
requirements:
  - PATH-03
must_haves:
  truths:
    - Phase 06 benchmark results can attribute lookup improvements to path resolution because a dedicated path-lookup control benchmark runs on the same fixture family as the integrated EQ, CONTAINS, and REGEX benchmarks.
    - EQ, CONTAINS, and REGEX benchmarks use fixed workload parameters and explicit sub-benchmark names so historical comparisons are reproducible.
    - The benchmark family includes a narrow control tier and wide tiers, plus supported spelling variants, without changing the supported JSONPath surface or benchmark entrypoint workflow.
  artifacts:
    - `benchmark_test.go` contains fixed constants for Phase 06 document count, row-group count, width tiers, selectivity, and query shapes.
    - `benchmark_test.go` contains a dedicated path-resolution control benchmark and integrated `BenchmarkQueryEQ`, `BenchmarkQueryContains`, and `BenchmarkQueryRegex` wide-path benchmark families.
    - `benchmark_test.go` contains reusable log-style wide-fixture helpers shared across the control and integrated benchmarks.
  key_links:
    - One fixture builder produces the same `GINIndex` shape for `BenchmarkPathLookup` and the integrated operator benchmarks, making attribution defensible.
    - Sub-benchmark names encode `paths=<tier>` and `spelling=<variant>` so before/after runs can compare the same workload directly.
    - All benchmark commands remain standard `go test -bench` invocations, preserving the repo’s existing benchmark harness.
---

<objective>
Add fixed-parameter benchmark proof for Phase 06 with explicit lookup attribution and naming discipline.

Purpose: Satisfy PATH-03 and close the review gaps around benchmark attribution, narrow-vs-wide coverage, stable fixture parameters, and spelling-variant measurement.
Output: Reusable wide-path fixture helpers, a dedicated path-lookup control benchmark, and integrated EQ/CONTAINS/REGEX benchmark families in `benchmark_test.go`.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-query-path-hot-path/06-CONTEXT.md
@.planning/phases/06-query-path-hot-path/06-RESEARCH.md
@.planning/phases/06-query-path-hot-path/06-REVIEWS.md
@.planning/phases/06-query-path-hot-path/06-VALIDATION.md
@.planning/phases/06-query-path-hot-path/06-01-PLAN.md
@AGENTS.md
@benchmark_test.go
@query.go
@jsonpath.go

<interfaces>
Existing benchmark helpers in `benchmark_test.go`:
```go
func generateTestDoc(i int) []byte
func generateTestDocWithText(i int) []byte
func generateLargeDoc(i int, numFields int) []byte
func setupTestIndex(numRGs int) *GINIndex
func setupTestIndexWithText(numRGs int) *GINIndex
```

Existing benchmark entry points:
```go
func BenchmarkQueryEQ(b *testing.B)
func BenchmarkQueryContains(b *testing.B)
```

Current query path resolution entry point:
```go
func (idx *GINIndex) findPath(path string) (int, *PathEntry)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fixed Phase 06 fixture constants and a dedicated path-lookup attribution benchmark</name>
  <files>benchmark_test.go</files>
  <read_first>
- benchmark_test.go
- query.go
- jsonpath.go
- .planning/phases/06-query-path-hot-path/06-CONTEXT.md
- .planning/phases/06-query-path-hot-path/06-RESEARCH.md
- .planning/phases/06-query-path-hot-path/06-REVIEWS.md
- AGENTS.md
  </read_first>
  <action>Addresses review concerns: explicit attribution, fixed parameters, and narrow control tier. In `benchmark_test.go`, add Phase 06-specific constants with these exact values: `docs=4096`, `rowGroups=4096`, width tiers `[]int{16, 128, 512, 2048}`, spelling variants `[]string{\"$.service\", \"$['service']\", \"$[\\\"service\\\"]\"}`, EQ target value `\"svc-07\"`, CONTAINS needle `\"timeout shard-03\"`, and REGEX pattern `\"timeout shard-(03|17)\"`. Build a reusable log-style fixture helper that always includes the recognizable base fields `service`, `method`, `status`, `message`, `request_id`, `host`, `region`, `http.path`, `http.status`, `labels.env`, and `labels.team`, then pads each document with deterministic filler fields `extra_0000` through `extra_NNNN` until the requested width tier is reached. Make the target path `service` appear in every document, with `svc-07` matching exactly `64` of `4096` documents (`1/64` selectivity), and make the `message` field include the literal text `timeout shard-03` in exactly `256` of `4096` documents (`1/16` selectivity) plus `timeout shard-17` in another `256` documents for the regex alternation. Add a dedicated benchmark named `BenchmarkPathLookup` that uses the same fixture/index family and measures only `idx.findPath(spelling)` for each width tier and spelling variant, with sub-benchmark names in the exact format `paths=<tier>/spelling=<variant>` where `<variant>` is `canonical`, `single-quoted`, or `double-quoted`.</action>
  <acceptance_criteria>
- `benchmark_test.go` contains the exact values `4096`, `16`, `128`, `512`, and `2048` in Phase 06 benchmark constants or fixture setup.
- `benchmark_test.go` contains the exact strings `$.service`, `$['service']`, `$["service"]`, `svc-07`, `timeout shard-03`, and `timeout shard-(03|17)`.
- `benchmark_test.go` contains a benchmark function named `BenchmarkPathLookup`.
- `benchmark_test.go` contains the base field names `service`, `method`, `status`, `message`, `request_id`, `host`, `region`, `http`, and `labels` in the Phase 06 fixture helper.
- `BenchmarkPathLookup` sub-benchmark names include `paths=` and `spelling=` segments.
- No external fixture file or custom benchmark binary is introduced.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
go test ./... -run '^$' -bench 'Benchmark(PathLookup|Query(EQ|Contains|Regex))' -benchtime=1x -count=1</automated>
  </verify>
  <done>The benchmark file has fixed Phase 06 workload parameters and a dedicated control benchmark that isolates path-resolution cost on the same fixture family used by the integrated operator benchmarks.</done>
</task>

<task type="auto">
  <name>Task 2: Add integrated EQ, CONTAINS, and REGEX wide-path benchmark families with explicit naming</name>
  <files>benchmark_test.go</files>
  <read_first>
- benchmark_test.go
- query.go
- jsonpath.go
- .planning/phases/06-query-path-hot-path/06-01-PLAN.md
- .planning/phases/06-query-path-hot-path/06-RESEARCH.md
- .planning/phases/06-query-path-hot-path/06-REVIEWS.md
  </read_first>
  <action>Addresses review concerns: PATH-03 operator coverage, fixed benchmark parameters, narrow control tier, spelling variants, and explicit names. Extend `BenchmarkQueryEQ`, `BenchmarkQueryContains`, and add `BenchmarkQueryRegex` so each benchmark family iterates over the fixed width tiers `16`, `128`, `512`, and `2048` and over spelling variants `canonical`, `single-quoted`, and `double-quoted`. Use the same Phase 06 fixture/index helper from Task 1 for every operator. For EQ, query the `service` field with `EQ(spelling, "svc-07")` and keep the expected selectivity at `64/4096`. For CONTAINS, query the `message` field with the exact needle `timeout shard-03` and keep the expected selectivity at `256/4096`. For REGEX, query the `message` field with the exact pattern `timeout shard-(03|17)` so regex literal extraction sees two bounded literals and the expected selectivity is `512/4096`. Use sub-benchmark names in the exact format `paths=<tier>/spelling=<variant>/selectivity=<matches>of4096`, for example `paths=512/spelling=single-quoted/selectivity=64of4096`. Keep the benchmark functions targetable through standard commands such as `go test -bench 'BenchmarkQueryEQ'` and `go test -bench 'Benchmark(PathLookup|Query(EQ|Contains|Regex))'`. Do not add unsupported JSONPath forms, and do not replace the integrated benchmarks with microbenchmarks only; the dedicated `BenchmarkPathLookup` is the attribution control, while EQ/CONTAINS/REGEX remain the end-to-end regression proof.</action>
  <acceptance_criteria>
- `benchmark_test.go` contains benchmark functions named `BenchmarkQueryEQ`, `BenchmarkQueryContains`, and `BenchmarkQueryRegex`.
- Each benchmark family includes the width tiers `16`, `128`, `512`, and `2048`.
- Each benchmark family includes spelling variants labeled `canonical`, `single-quoted`, and `double-quoted`.
- `benchmark_test.go` contains the exact selectivity labels `64of4096`, `256of4096`, and `512of4096`.
- `BenchmarkQueryRegex` uses the exact regex pattern string `timeout shard-(03|17)`.
- Sub-benchmark names include `paths=`, `spelling=`, and `selectivity=` segments so output is comparable across runs.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
go test ./... -run '^$' -bench 'Benchmark(PathLookup|Query(EQ|Contains|Regex))' -benchtime=1x -count=1
go test ./... -count=1</automated>
  </verify>
  <done>Wide-path EQ, CONTAINS, and REGEX benchmarks run with fixed parameters, narrow and wide tiers, spelling variants, and an attribution control that isolates the path-lookup change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| synthetic Phase 06 fixture generator -> benchmark claims | The fixture must create real path-count pressure or the benchmark story becomes misleading |
| path-lookup control benchmark -> integrated query benchmarks | Control and integrated workloads must share the same fixture family so attribution is defensible |
| benchmark output -> maintainer interpretation | Automated timings are objective, but maintainers still interpret before/after significance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | T | fixture shape | mitigate | Use fixed constants (`4096` docs/RGs, width tiers `16/128/512/2048`) plus recognizable log-style fields and deterministic filler fields. |
| T-06-06 | R | lookup attribution | mitigate | Add `BenchmarkPathLookup` on the same fixture family so path-resolution improvements are measurable even when trigram/regex work dominates integrated benchmarks. |
| T-06-07 | D | noisy benchmark comparison | mitigate | Encode width, spelling, and selectivity in sub-benchmark names and keep commands on the standard `go test -bench` harness. |
</threat_model>

<verification>
1. Benchmark smoke:
   `go test ./... -run '^$' -bench 'Benchmark(PathLookup|Query(EQ|Contains|Regex))' -benchtime=1x -count=1`
2. Full suite:
   `go test ./... -count=1`
</verification>

<success_criteria>
- Phase 06 benchmark fixtures have fixed document count, row-group count, width tiers, selectivity, and query shapes.
- A dedicated `BenchmarkPathLookup` control benchmark isolates path-resolution cost on the same fixture family as the integrated query benchmarks.
- `BenchmarkQueryEQ`, `BenchmarkQueryContains`, and `BenchmarkQueryRegex` cover narrow and wide path tiers plus canonical, single-quoted, and double-quoted spelling variants.
- Benchmark output names are explicit enough to compare before/after results without ambiguity.
</success_criteria>

<output>
After completion, create `.planning/phases/06-query-path-hot-path/06-query-path-hot-path-02-SUMMARY.md`
</output>
