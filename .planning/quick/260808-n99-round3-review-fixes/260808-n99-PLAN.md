---
phase: quick-260808-n99
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - builder.go
  - gin.go
  - ingest_error.go
  - gin_test.go
  - failure_modes_test.go
  - simd_docs_guard_test.go
  - parser_parity_simd_test.go
  - cmd/gin-index/main.go
  - cmd/gin-index/main_test.go
  - CLAUDE.md
  - docs/simd-deployment.md
  - SECURITY.md
  - CHANGELOG.md
autonomous: true
requirements: [RR3-C1, RR3-C2, RR3-I3, RR3-I4, RR3-I5, RR3-I6, RR3-I7, RR3-I8, RR3-S9, RR3-S10, RR3-S11, RR3-S12]
tags: [builder, observability, simd, cli, docs]

must_haves:
  truths:
    - "AddDocument's staged-path-budget error reports the requirement in the same unit it enforces (total staged paths), so raising the limit always raises the reported requirement."
    - "go test ./... never fails with a Go parse error from the SIMD doc-guard test's cold-cache fallback; a cold module cache produces an honest skip (or a CI-required failure), never a vacuous pass."
    - "SIMD object traversal order (lexical, not document order) is pinned by a test that fails if the sort at parser_simd.go:235 is reversed."
    - "gin-index build exposes the same --max-staged-paths guard as gin-index experiment, and a document that exceeds it fails the build with a diagnostic containing 'staged path budget exceeded'."
    - "WithMaxStagedPaths and Path() godoc describe companion-representation path remapping accurately instead of claiming Path() is always the first path that would exceed the budget."
    - "A soft-skipped companion representation is logged only for documents that actually commit; an uncommitted document logs nothing and NumSoftSkippedRepresentations() stays 0."
    - "Constructing a resource-layer IngestError with a non-empty Value() panics as a programming-bug guard."
  artifacts:
    - path: "builder.go"
      provides: "Budget-honest getOrCreateStagedPath and fold-point soft-skip logging in commitStagedPaths"
      contains: "document requires at least"
    - path: "gin.go"
      provides: "Accurate WithMaxStagedPaths/Path() godoc"
      contains: "Path() is the path whose staging was rejected"
    - path: "ingest_error.go"
      provides: "Panic guard on resource-layer IngestError carrying a non-empty value"
      contains: "must carry no value"
    - path: "simd_docs_guard_test.go"
      provides: "Honest skip/fail on a cold SIMD module cache instead of a synthetic package fallback"
      contains: "t.Skipf"
    - path: "parser_parity_simd_test.go"
      provides: "Lexical-vs-document-order budget rejection regression"
      contains: "TestSIMDParserBudgetRejectsLexicalOrderPath"
    - path: "cmd/gin-index/main.go"
      provides: "--max-staged-paths flag on the build subcommand"
      contains: "max-staged-paths"
  key_links:
    - from: "cmd/gin-index/main.go:runBuild"
      to: "gin.go:WithMaxStagedPaths"
      via: "flag value applied to ginCfg before BuildFromParquet"
      pattern: "WithMaxStagedPaths"
    - from: "builder.go:commitStagedPaths"
      to: "logging.Info"
      via: "soft-skip log emitted only after a successful merge, looped once per buffered skip"
      pattern: "softRepresentationSkips"
---

<objective>
Close all 12 round-3 PR review findings on branch `fix/issue-54` (2 critical, 6 important, 4 suggestions): an honest staged-path budget diagnostic, a SIMD doc-guard test that compiles and skips honestly instead of vacuously passing, accurate godoc/comments, a lexical-order regression for the SIMD object walker, CLI parity for `--max-staged-paths`, a soft-skip log that only fires for committed documents, and a programming-bug guard on resource `IngestError` values.

Purpose: These are review-converged correctness and honesty fixes on code already on this branch — no new features, no scope growth.
Output: Fixed diagnostics/tests/docs in three sequential tasks (core library, test-infra/SIMD, CLI/docs), each independently verifiable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@builder.go
@gin.go
@ingest_error.go
@parser_sink.go
@logging/attrs.go
@logging/doc.go
@cmd/gin-index/experiment.go
@cmd/gin-index/main.go
@docs/simd-deployment.md
@SECURITY.md
@CHANGELOG.md

<interfaces>
Key contracts and constraints the executor needs, verified against current source rather than assumed.

documentBuildState (builder.go:173-185) has rgID int, paths map[string]*stagedPathData, softRepresentationSkips uint64. visiblePathCount int is deleted by Task 1; nothing outside builder.go references it (verified: no test constructs documentBuildState{} by field name).

getOrCreateStagedPath (builder.go:191-218) is the single creation boundary for every staged path: scalars, numerics, MarkPresent (parser_sink.go:55), and companion representations all route through it. isInternalRepresentationPath (gin.go:493) stays; it is still used by remapCompanionIngestErrorPath (builder.go:716) and gin.go:945. Only its use inside getOrCreateStagedPath is removed.

IngestError construction (ingest_error.go): newIngestError(layer, path, value any, err) is a thin wrapper that calls newIngestErrorString(layer, path, fmt.Sprint(value), err). newIngestErrorString is the ONE place that actually builds &IngestError{...} — this is where Finding 8's guard goes. The only production call site passing IngestLayerResource is getOrCreateStagedPath, which already always passes "" for value, so the new guard cannot break it.

Frozen INFO-level logging vocabulary (logging/doc.go, logging/attrs.go) — read this before touching Finding 6. logging/doc.go and logging/attrs.go document, and observability_policy_test.go (TestInfoLevelAttrAllowlist, TestInfoLevelEmissionsUseOnlyAllowlistedAttrs) partially enforce, a hard project invariant: INFO-level log attributes are limited to exactly operation, predicate_op, path_mode, status, error.type. The doc comment is explicit: "Predicate values, field path names, document IDs, row-group IDs, term IDs, and any raw user content must never appear in INFO-level log attributes. Those details may appear only in trace events or behind a LevelDebug guard." Every existing ad-hoc logging.Attr{Key: ...} in the codebase (panic_type/panic_message in runMergeWithRecover, simdCloseErrorAttrKey in parser_simd.go) is emitted at logging.Error, never logging.Info — there is zero precedent for a non-frozen key at INFO level.

Finding 6's literal text asks for "alias and rgID attributes" on the relocated Info log. This plan deliberately does NOT add them: doing so would violate the documented frozen vocabulary and break with every existing INFO-emission precedent in this codebase. Task 1 implements the load-bearing part of Finding 6 (buffer soft-skip emission until the fold point so the log and the counter always agree) using the exact same message and the same 3 frozen attrs (operation, status, error.type) already used today, just relocated and looped once per buffered skip. This deviation is intentional; do not "fix" it back to match the finding's literal wording.

gin_test.go message fixtures (TestMaxStagedPaths, TestStagedPathBudgetPropagatesAllStagingCallSiteErrors) have 7 occurrences of the old message string across lines ~3193-3382. The computed replacements in Task 1's action were derived by manually tracing getOrCreateStagedPath call order for each fixture; verify each one against the actual test failure output after the builder.go fix lands rather than trusting the table blindly, per the finding's explicit instruction.

cmd/gin-index flag parsing: Go's flag package accepts a following arg as a flag's value even when it looks like a negative number (--max-staged-paths -1 parses to -1, confirmed against this Go toolchain). No special quoting/escaping needed in tests.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the staged-path budget diagnostic, soft-skip log timing, remapping godoc, and resource-error value guard</name>
  <files>builder.go, gin.go, ingest_error.go, gin_test.go, failure_modes_test.go</files>
  <action>
Finding 1 (CRITICAL, budget diagnostic unit): in builder.go:getOrCreateStagedPath (builder.go:191-218), delete the "internalPath := isInternalRepresentationPath(canonicalPath)" line and the trailing "if !internalPath { state.visiblePathCount++ }" block. Replace the requiredVisiblePathCount computation with a direct len(state.paths)+1 at trip time, and change the error message to "staged path budget exceeded: limit %d total paths; document requires at least %d" (drop the " caller-visible paths" suffix; both numbers are now total-path denominated). Delete "visiblePathCount int" from the documentBuildState struct (builder.go:173-178) and its increment.

Finding 3 (IMPORTANT, Path() godoc) combined with Finding 1's godoc cleanup: rewrite the WithMaxStagedPaths godoc in gin.go (currently gin.go:468-478). Remove "Path() set to the first path that would exceed the budget" (false when the trip is remapped from an internal __derived: path) and "Cause() contains the limit and visible-path diagnostic" (stale wording). New body: state that Layer() == IngestLayerResource; that Path() is the path whose staging was rejected, and when the rejection occurs inside a transformer's companion representation, Path() reports the source path that owns the companion, never the internal derived path; keep the existing correct sentence about lexical object-key traversal order making the rejected path deterministic; state that Value() is empty and Cause() reports the configured limit and a lower-bound count of the total paths the document requires; keep the closing sentence about ParserFailureMode and Finalize.

Finding 6 (IMPORTANT, soft-skip log timing) combined with Finding 11 (SUGGESTION, why-comment): in stageCompanionRepresentations (builder.go:672-707), delete the logging.Info(...) call in the soft-skip branch; keep state.softRepresentationSkips++ and continue. In commitStagedPaths (builder.go:957-967), after the merge succeeds (after the runMergeWithRecover block, before "b.numSoftRepresentationSkips += state.softRepresentationSkips"), add a one-line why-comment (for example: "Soft-skip logging happens only here, after a successful merge, so a document that is later rejected or never committed never logs a skip.") followed by a loop "for i := uint64(0); i < state.softRepresentationSkips; i++" that calls the same logging.Info with the same message ("builder skipped companion representation after soft transformer failure") and the same 3 attrs (logging.AttrOperation("builder.transform"), logging.AttrStatus("skipped"), logging.AttrErrorType(telemetry.ErrorTypeOther)) that were deleted from the staging site. See the frozen-vocabulary note in the interfaces block above; do not add alias or rgID attrs.

Finding 12 (SUGGESTION, remapCompanionIngestErrorPath comment): in the doc comment above remapCompanionIngestErrorPath (builder.go:709-715), qualify "The offending Value is left untouched because it still reflects the transformed representation that actually failed": it is accurate for transformer/schema/numeric failures but vacuous for resource errors (Value is always empty there). Reword to note the distinction explicitly.

Finding 8 (IMPORTANT, Value()=="" guard): in ingest_error.go, add "github.com/pkg/errors" to the import block (blank line after "fmt", per this repo's import-order convention; see builder.go's import block for the pattern). In newIngestErrorString (the single construction point), after the "if err == nil" check and before building the &IngestError{...}, add: "if layer == IngestLayerResource && value != \"\" { panic(errors.Errorf(\"newIngestErrorString: resource IngestError must carry no value, got %q\", value)) }".

Now propagate the message-format change to every gin_test.go fixture that asserts the old wording (grep confirms only gin_test.go references the string outside builder.go itself; cmd/gin-index/experiment_test.go only does strings.Contains(..., "staged path budget exceeded"), unaffected). Update these 7 occurrences in TestMaxStagedPaths and TestStagedPathBudgetPropagatesAllStagingCallSiteErrors (gin_test.go, current lines ~3193, 3258, 3285, 3304, 3319, 3338, 3382): for each, drop the " caller-visible paths" suffix, and for the "companion paths count toward total cap" subtest (currently at gin_test.go:3258, limit 3, container transformer with width 5/100/5000) change the requirement number from 2 to 4 (this is the actual bug Finding 1 fixes: with the old visible-path formula the reported requirement, 2, was below the limit, 3; with the new total-path formula it becomes len(state.paths)+1 at trip time = 4). All other 6 occurrences keep their existing number, only the wording suffix changes, because they involve no internal/companion paths, so the old and new formulas agree numerically. Verify every one of these 7 against actual "go test -run '^(TestMaxStagedPaths|TestStagedPathBudgetPropagatesAllStagingCallSiteErrors)$' -v ." output before finalizing; do not trust this derivation blindly.

Finally add one new test to failure_modes_test.go (package gin, so it can call the unexported constructor directly): TestNewIngestErrorStringPanicsOnResourceValue — defer a recover-based assertion, then call newIngestErrorString(IngestLayerResource, "$.value", "unexpected", errors.New("boom")) and confirm it panics. Also add a test proving Finding 6's fix: an uncommitted document that soft-skips a companion representation (reuse the "soft representation skip commits atomically" fixture shape from gin_test.go:3323 — WithMaxStagedPaths(2) plus a soft-failing custom transformer on $.a, second field $.b trips the budget) wired to a softSkipInfoLogger (failure_modes_test.go:459), asserting len(logger.entries) == 0 and builder.NumSoftSkippedRepresentations() == 0. The existing TestTransformerFailureModeSoftKeepsRawDocumentAndSkipsCompanion (failure_modes_test.go:866) already covers the committed-document "log(s) AND counter agree" case and needs no changes, since its message/attr assertions are unchanged by this fix.
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; go test -count=1 -run '^(TestMaxStagedPaths|TestStagedPathBudgetPropagatesAllStagingCallSiteErrors|TestSoftSkippedDocumentsAreObservable|TestTransformerFailureModeSoftKeepsRawDocumentAndSkipsCompanion|TestNewIngestErrorStringPanicsOnResourceValue|TestIngestErrorWrappingContract)$' -v . &amp;&amp; gofmt -l builder.go gin.go ingest_error.go gin_test.go failure_modes_test.go</automated>
  </verify>
  <done>getOrCreateStagedPath reports a total-path-denominated requirement that always exceeds the limit; visiblePathCount is gone; WithMaxStagedPaths/Path() godoc is accurate; soft-skip Info logs only fire after a successful merge, verified by a logger-backed test on both the committed and uncommitted paths; resource IngestError construction with a non-empty value panics; all touched tests pass; gofmt is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the SIMD doc-guard cold-cache fallback and pin lexical object traversal order</name>
  <files>simd_docs_guard_test.go, parser_parity_simd_test.go</files>
  <action>
Finding 2 (CRITICAL, non-compiling vacuous fallback): in simd_docs_guard_test.go, delete the knownSIMDLoadingSource function (lines ~232-234). In loadSIMDLoadingSources (lines ~216-230), keep the existing "if os.Getenv(requireSIMDDocumentationUpstreamEnv) == \"1\" { t.Fatalf(...) }" branch (unchanged actionable message) inside the "if moduleDir == \"\"" block, but replace the "return knownSIMDLoadingSource(...), ..." fallback that follows it with "t.Skipf(\"effective pure-simdjson module is not downloaded; run go mod download %s first (set %s=1 to require it, e.g. in CI)\", pureSIMDJSONModule, requireSIMDDocumentationUpstreamEnv)". Leave the function's final "return string(readTestFile(...)), ..." statement (the non-empty-moduleDir case) exactly as-is below the if-block; it remains syntactically reachable so the function still compiles, even though t.Skipf/t.Fatalf halt execution via runtime.Goexit() before reaching it on the empty-moduleDir path. Do not remove effectiveSIMDModule (lines ~253-262): it is still exercised directly by the "versioned replacement is effective" and "unversioned local replacement fails closed" subtests in TestSIMDDocumentationContract, which construct simdModuleListing values in-memory and do not depend on the module cache; it is not dead.

Finding 4 (IMPORTANT, SIMD lexical-order budget unpinned): in parser_parity_simd_test.go (has //go:build simdjson), add TestSIMDParserBudgetRejectsLexicalOrderPath. Use document {"z":{},"a":{},"m":{}} and WithMaxStagedPaths(3). With correct lexical traversal (a, m, z), staging is $ (len 1), $.a (len 2), $.m (len 3), then $.z trips the limit-3 budget; assert IngestError.Path() == "$.z" and builder.numDocs == 0, mirroring the structure of the existing TestSIMDParserPropagatesMarkPresentBudgetFailure (same file) for NewConfig/NewBuilder/newTestSIMDParser wiring. Add a one-line comment noting that document order (z, a, m) would instead reject $.m; this is exactly why the test proves traversal order, not just budget enforcement.

Now perform the mutation check the finding requires (do not leave this change in the final diff): temporarily change "sort.Strings(keys)" at parser_simd.go:235 to "sort.Sort(sort.Reverse(sort.StringSlice(keys)))" (the "sort" package is already imported; reuse it), run "go test -count=1 -tags simdjson -run '^TestSIMDParserBudgetRejectsLexicalOrderPath$' -v ." and confirm it FAILS (rejecting $.m instead of $.z), then revert parser_simd.go back to "sort.Strings(keys)" exactly and re-run the same command to confirm it PASSES again. This proves the new test actually pins traversal order rather than passing vacuously.
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; go vet ./... &amp;&amp; go vet -tags simdjson ./... &amp;&amp; go test -count=1 . &amp;&amp; go test -count=1 -tags simdjson -run '^(TestSIMDParserBudgetRejectsLexicalOrderPath|TestSIMDParserPropagatesMarkPresentBudgetFailure|TestSIMDDocumentationContract)$' -v . &amp;&amp; gofmt -l simd_docs_guard_test.go parser_parity_simd_test.go parser_simd.go</automated>
  </verify>
  <done>simd_docs_guard_test.go compiles and runs unconditionally in the default go test ./... suite; a cold module cache produces an honest t.Skipf naming go mod download, or a t.Fatalf when AMI_GIN_REQUIRE_SIMD_DOCS_UPSTREAM=1; effectiveSIMDModule is retained because its subtests still use it; a new tagged test proves lexical (not document) object traversal order and was confirmed to fail under a temporarily reversed sort, then pass again after revert.</done>
</task>

<task type="auto">
  <name>Task 3: Mirror --max-staged-paths on gin-index build, and fix stale docs/CLAUDE.md/SECURITY.md/CHANGELOG wording</name>
  <files>cmd/gin-index/main.go, cmd/gin-index/main_test.go, CLAUDE.md, docs/simd-deployment.md, SECURITY.md, CHANGELOG.md</files>
  <action>
Finding 5 (IMPORTANT, gin-index build cannot set the budget): in cmd/gin-index/main.go:runBuild (main.go:103-159), register a new flag alongside the existing column/output/embed/key flags: "maxStagedPaths := fs.Int(\"max-staged-paths\", 0, \"Cap total staged JSON paths per document; 0 is unlimited\")" (same description text as experiment.go:54 for consistency). After the existing "-c (column) is required" validation and before building ginCfg, add: "if *maxStagedPaths < 0 { fmt.Fprintln(stderr, \"Error: --max-staged-paths must be greater than or equal to 0\"); return 1 }" (exact same error text as experiment.go:70, for consistency). Where "ginCfg := gin.DefaultConfig()" is set (main.go:126), apply the flag immediately after: "if err := gin.WithMaxStagedPaths(*maxStagedPaths)(&amp;ginCfg); err != nil { fmt.Fprintf(stderr, \"Error: %v\\n\", err); return 1 }" (mirrors experiment.go:90-93). No other flag/usage-string changes are needed: runBuild has no separate manual "Usage: gin-index build ..." string (unlike experiment.go); its usage output is generated from the registered flag descriptions via fs.Usage(), which the new description text already covers.

Add two tests to cmd/gin-index/main_test.go, near TestRunBuildReportsPartialFailures (main_test.go:575): (1) TestRunBuildRejectsNegativeMaxStagedPaths — call runBuild with ["-c", "attributes", "--max-staged-paths", "-1", "does-not-matter.parquet"] and assert a non-zero exit code and stderr containing "--max-staged-paths must be greater than or equal to 0" (Go's flag package accepts "-1" as the value of a preceding non-boolean flag; this is confirmed behavior, not a guess). (2) TestRunBuildSurfacesStagedPathBudgetFailure — use createCLIParquetFile (main_test.go:26) to write one record with Attributes `{"brand":"Toyota"}`, call runBuild with ["-c", "attributes", "--max-staged-paths", "1", parquetPath], and assert a non-zero exit code and stderr containing "staged path budget exceeded" (limit 1 rejects $.brand after $ is staged, mirroring cmd/gin-index/experiment_test.go's newResourceIngestFailure pattern).

Finding 7 (IMPORTANT, stale CLAUDE.md ordering claim): CLAUDE.md line 110 currently reads "builder.go - Derived companion staging in stageCompanionRepresentations() before stageMaterializedValue()." This is backwards: the source path is staged and budget-checked in stageMaterializedValue() (via getOrCreateStagedPath) before stageCompanionRepresentations() runs its derived companions. Reword the sentence to state the correct order.

Finding 9 (SUGGESTION, stale docs/simd-deployment.md wording): at the "## Validation scope" section (currently ~docs/simd-deployment.md:248), replace "Phase 22 validates identical encoded indexes and query results..." with "This repository validates identical encoded indexes and query results..." (remove the internal planning-phase identifier). Also, near the nesting-limit guidance (currently ~docs/simd-deployment.md:213-216, the paragraph ending "...or enforce a 1,023-container maximum before ingest."), add one sentence pointing to WithMaxStagedPaths as the parser-independent per-document resource bound, for example: "Independent of parser choice, WithMaxStagedPaths bounds the number of distinct paths staged per document and is the parser-independent resource guard for this class of input."

Finding 10 (SUGGESTION, SECURITY.md wording): in the "Threat Verification" table's Evidence column (all three edits are within existing table cells, not new rows):
  - T18-06: append a clause to the end of the existing Evidence cell: "The bound caps distinct staged paths, not peak memory: transformer output is fully materialized before rejection, so callers needing a hard memory ceiling must bound input size upstream."
  - T18-11: replace "and the JSON report tests" with "and `TestRunExperimentMaxStagedPathsReportsResourceFailureWithoutValue`" (the actual test name) in the existing Evidence cell.
  - T18-12: the current Evidence cell claims "`Cause` carries resource diagnostics" but Cause()'s godoc says nothing of the sort. Replace that clause so it instead points at the IngestError type doc, which does document Value()/resource semantics: first grep ingest_error.go for the current line range of the "IngestError reports a hard per-document ingest failure." doc comment block (Task 1 added an import line to this file, so the range has shifted from the pre-existing "ingest_error.go:38-43" citation; cite the actual current range), and reference it, e.g. "see the IngestError type doc (ingest_error.go:L-L) for where resource diagnostics live."

Finding 5's CHANGELOG requirement: in CHANGELOG.md's Unreleased section, find the existing bullet beginning "Added `GINConfig.MaxStagedPaths` and `WithMaxStagedPaths(limit)`..." and append one sentence: "Both `gin-index build` and `gin-index experiment` expose this bound via `--max-staged-paths`." Finding 1's diagnostic-wording change does not require a CHANGELOG edit: the existing bullet does not mention the visible-path diagnostic text, so there is nothing stale to correct there.
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; go test -count=1 ./cmd/gin-index/... -run '^(TestRunBuildRejectsNegativeMaxStagedPaths|TestRunBuildSurfacesStagedPathBudgetFailure|TestRunBuildReturnsNonZeroOnBuildFailure|TestRunBuildReportsPartialFailures)$' -v &amp;&amp; gofmt -l cmd/gin-index/main.go cmd/gin-index/main_test.go &amp;&amp; grep -n "Phase 22 validates" docs/simd-deployment.md | test -z "$(cat)"</automated>
  </verify>
  <done>gin-index build accepts --max-staged-paths with the same validation and application as experiment; new negative-flag and over-budget tests pass; CLAUDE.md's companion-staging ordering sentence is accurate; docs/simd-deployment.md no longer names an internal phase and points to WithMaxStagedPaths near the nesting-limit guidance; SECURITY.md's T18-06/T18-11/T18-12 evidence cells are corrected; CHANGELOG.md's MaxStagedPaths bullet mentions both CLI flags.</done>
</task>

</tasks>

<source_audit>

| Source | ID | Feature / constraint | Plan coverage | Status |
| --- | --- | --- | --- | --- |
| REQ | RR3-C1 | Budget diagnostic reports wrong unit (builder.go) | Task 1 | COVERED |
| REQ | RR3-C2 | simd_docs_guard_test.go cold-cache fallback does not compile / is vacuous | Task 2 | COVERED |
| REQ | RR3-I3 | Path() godoc false for companion overflow | Task 1 | COVERED |
| REQ | RR3-I4 | SIMD lexical-order budget behavior unpinned | Task 2 | COVERED |
| REQ | RR3-I5 | gin-index build cannot set the budget | Task 3 | COVERED |
| REQ | RR3-I6 | Soft-skip log fires for never-committed documents | Task 1 | COVERED (attrs deviate from literal wording; see interfaces note and rationale below) |
| REQ | RR3-I7 | CLAUDE.md stale companion-staging ordering claim | Task 3 | COVERED |
| REQ | RR3-I8 | Value()=="" convention unenforced on resource IngestError | Task 1 | COVERED |
| REQ | RR3-S9 | docs/simd-deployment.md stale "Phase 22" wording + nesting-limit cross-reference | Task 3 | COVERED |
| REQ | RR3-S10 | SECURITY.md T18-06/T18-11/T18-12 wording | Task 3 | COVERED |
| REQ | RR3-S11 | One-line why-comment at the soft-skip fold point | Task 1 (merged into RR3-I6's edit, same lines) | COVERED |
| REQ | RR3-S12 | remapCompanionIngestErrorPath comment overclaims for resource errors | Task 1 | COVERED |

Explicitly out of scope per the constraints (not gaps): the 50 pre-existing goconst lint findings in _test.go files (separate cleanup, predates this branch), and the fuzz predicate stdlib-layer check/doc comment (criticality 4, deferred).

Deviation note on RR3-I6: the finding's literal text asks the relocated Info log to carry "alias and rgID attributes." This plan implements the timing fix (log only after a successful merge, one line per buffered skip) but keeps the existing 3 frozen INFO-level attrs (operation, status, error.type) instead of adding alias/rgID, because logging/doc.go documents a hard, partially-test-enforced project invariant that INFO-level attributes are limited to exactly 5 named keys and explicitly forbids row-group IDs and path-like identifiers at INFO level. Every existing ad-hoc attr key in this codebase is emitted at ERROR level only, never INFO. Adding alias/rgID at INFO would contradict this established, documented architecture. The core behavior the finding is about — log and counter always agreeing — is fully fixed.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Caller-configured limit → AddDocument diagnostics | The staged-path budget diagnostic must give callers an accurate, monotonic signal to size retries; a wrong-unit diagnostic causes tuning failures, not exploitable behavior. |
| CI / local dev → SIMD doc-guard test | A vacuous pass on a cold module cache silently disables a documentation-drift guard, letting stale/incorrect SIMD deployment docs merge undetected. |
| gin-index build CLI → builder resource guard | Without --max-staged-paths, the production build command cannot enforce the T18-06 DoS guard that gin-index experiment already exposes. |
| Internal programming contract → IngestError construction | A resource IngestError carrying a document value would violate the documented Value()=="" contract for resource failures; only reachable via a future internal call-site bug, not external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-n99-01 | Denial of Service | builder.go:getOrCreateStagedPath diagnostic | mitigate | Task 1 reports the requirement in the enforced unit (total staged paths) so retry loops converge instead of oscillating or under-reporting. |
| T-n99-02 | Repudiation | builder.go soft-skip logging vs. counter | mitigate | Task 1 relocates the Info log to the post-merge fold point in commitStagedPaths so the log and NumSoftSkippedRepresentations() always agree; verified with logger-backed tests on both the committed and uncommitted paths. |
| T-n99-03 | Tampering | simd_docs_guard_test.go cold-cache fallback | mitigate | Task 2 removes the non-compiling synthetic-source fallback and replaces it with an honest t.Skipf/t.Fatalf, so a cold cache cannot silently pass a documentation-drift guard. |
| T-n99-04 | Tampering | parser_simd.go:235 object key sort order | mitigate | Task 2 adds a lexical-vs-document-order regression proven to fail under a temporarily reversed sort, closing the gap where reversing the sort left the tagged SIMD suite green. |
| T-n99-05 | Denial of Service | gin-index build lacks --max-staged-paths | mitigate | Task 3 mirrors the flag from gin-index experiment onto build, restoring CLI reachability of the T18-06 guard for the production ingest path. |
| T-n99-06 | Information Disclosure | ingest_error.go resource IngestError value contract | mitigate | Task 1 adds a panic guard in newIngestErrorString so a future internal call-site bug that attaches a value to a resource error fails loudly at construction instead of silently violating the documented contract. |
| T-n99-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies are added by this plan; all changes are internal fixes to existing code, tests, and docs. |
</threat_model>

<verification>
Run per-task verify commands first, then the full gate:

1. `go build ./...`
2. `go vet ./...`
3. `go vet -tags simdjson ./...`
4. `go test -count=1 ./...`
5. `go test -count=1 -tags simdjson ./...`
6. `gofmt -l .` (must print nothing across all touched files)
7. `make lint`

Task 2's mutation check (temporarily reverse the sort at parser_simd.go:235, confirm the new test fails, revert, confirm it passes again) must be performed and its result reported in the SUMMARY, but the reversed state must never be committed.
</verification>

<success_criteria>
- All 7 gin_test.go budget-message fixtures pass with the new total-path-denominated wording, each verified against actual test output rather than assumed.
- simd_docs_guard_test.go compiles and runs (or honestly skips) in the default `go test ./...` suite on both warm and cold module caches; effectiveSIMDModule remains because its subtests still use it.
- A new tagged SIMD test proves lexical object traversal order and was empirically confirmed to fail under a reversed sort before being reverted.
- `gin-index build --max-staged-paths` behaves identically to `gin-index experiment --max-staged-paths` for negative-value rejection and over-budget documents.
- WithMaxStagedPaths/Path() godoc, CLAUDE.md's companion-staging sentence, docs/simd-deployment.md, SECURITY.md's three evidence cells, and the CHANGELOG's MaxStagedPaths bullet are all corrected and internally consistent.
- Soft-skip Info logging only ever fires for documents that successfully commit; the counter and the log count always agree.
- A resource-layer IngestError with a non-empty value panics at construction.
- Full `go build ./...`, `go vet ./...`, `go vet -tags simdjson ./...`, `go test ./...`, `go test -tags simdjson ./...`, `gofmt -l`, and `make lint` are green.
</success_criteria>

<output>
Create `.planning/quick/260808-n99-round3-review-fixes/260808-n99-SUMMARY.md` when done, listing commits, the Task 2 mutation-check result, and full verification command output.
</output>
