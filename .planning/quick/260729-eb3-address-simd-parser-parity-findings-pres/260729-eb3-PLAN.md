---
phase: quick-260729-eb3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - builder.go
  - parser.go
  - parser_sink.go
  - parser_simd.go
  - parser_lifecycle_test.go
  - parser_simd_lifecycle_test.go
  - parser_simd_integration_test.go
  - parser_parity_fixtures_test.go
  - parser_parity_test.go
  - docs/simd-deployment.md
  - testdata/parity-golden/*.bin
autonomous: true
requirements:
  - SIMD-REVIEW-FALLBACK
  - SIMD-REVIEW-LIFECYCLE
  - SIMD-REVIEW-PARITY
tags: [simd, parser, parity, lifecycle]
---

<objective>
Resolve the SIMD parser review findings without widening the public API: make
native numeric-rejection fallback preserve stdlib staging policy, make parser
panics terminal for the builder, close the identified parity-test gaps, and
state the deliberate depth and malformed-input limits explicitly.

Purpose: Keep parser choice from changing indexing, failure routing, or builder
lifecycle behavior except where the native parser has a documented limit.
Output: Three focused implementation commits plus quick-task planning/state
artifacts.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve normal staging on native numeric rejection</name>
  <files>parser_simd.go, parser_simd_integration_test.go</files>
  <action>
    After the existing stdlib validation, EOF, and SIMD-depth checks succeed,
    route the document through stdlibParser.Parse rather than directly calling
    StageJSONNumber on one leaf. Return that result immediately from
    simdParser.Parse, including nil, so duplicate values use encoding/json's
    last-key-wins semantics and transformed paths use ShouldBufferForTransform
    and StageMaterialized. Remove the now-redundant bad-number search helper.
    Add regressions for a hard transformer on an overflow value with soft
    numeric policy, a shadowed overflow followed by a valid duplicate, nested
    overflow path reporting, and deterministic staging order.
  </action>
  <verify>go test -tags simdjson -run 'TestSIMDParser(NativeNumeric|NumericRouting|RejectedNumeric)' -count=1 -v .</verify>
  <done>Well-formed native-rejected documents follow the same transformer, numeric, duplicate-key, and staging behavior as stdlib.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve cleanup failures and poison on parser panic</name>
  <files>builder.go, parser_simd.go, parser_lifecycle_test.go, parser_simd_lifecycle_test.go</files>
  <action>
    In AddDocument's existing cleanup defer, recover parser panics only long
    enough to store a tragic builder error, then re-panic with the same value.
    In finishSIMDDocument, retain the existing structured log and, when a walk
    panic coincides with document-close failure, re-panic with a
    parserLifecycleError carrying both the wrapped close error and the original
    panic (or a plain error describing a non-error panic). Add tests proving a
    caller-recovered parser panic leaves the builder unusable, ordinary panic
    identity remains unchanged when close succeeds, and close failure is
    observable through errors.Is/errors.As even with a noop logger.
  </action>
  <verify>go test -tags simdjson -run 'Test(ParserPanic|SIMDDocumentLifecycle)' -count=1 -v .</verify>
  <done>Every parser panic poisons the builder, and a simultaneous native close failure travels with the re-raised panic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pin parity coverage and document intentional limits</name>
  <files>parser.go, parser_sink.go, parser_parity_fixtures_test.go, parser_parity_test.go, parser_simd_integration_test.go, docs/simd-deployment.md, testdata/parity-golden/*.bin</files>
  <action>
    Promote mixed-float-int and single-rg-array-siblings into the authored
    parity suite. Add an authored fixture that buffers an object subtree through
    a soft-failing registered transformer and contains arrays, signed/unsigned
    native integer shapes, whole floats, fractions, and nested numbers, then
    regenerate committed stdlib goldens. Add tagged tests pinning acceptance at
    the SIMD nesting boundary, rejection immediately above it while stdlib
    remains accepting, and the malformed `1e400` plus trailing garbage policy
    asymmetry. Correct the StageUint64 and Parse lifecycle doc comments. Update
    docs/simd-deployment.md with the 1024-depth limitation, the narrow malformed
    input classification edge case, and the validated fallback behavior.
  </action>
  <verify>go test -tags regenerate_goldens -run TestRegenerateParityGoldens -count=1 . &amp;&amp; go test ./... &amp;&amp; go test -tags simdjson -race ./... &amp;&amp; make simd-isolation-check</verify>
  <done>The missing numeric/container paths are covered byte-for-byte, and deployment documentation states every intentional SIMD parity limit tested by the suite.</done>
</task>

</tasks>

<verification>
Run focused tests after each task, then gofmt, default tests, the full
simdjson-tagged race suite, build-tag isolation, and repository lint/build
checks. Commit only each task's declared implementation/test files; the
orchestrator commits PLAN.md, SUMMARY.md, and STATE.md separately.
</verification>

<success_criteria>
- Transformer policy and last-key-wins behavior match stdlib after native numeric rejection.
- Parser panics are re-raised and leave GINBuilder in tragic state.
- Concurrent walk panic and native close failure preserve both causes without relying on configured logging.
- SIMD parity fixtures cover mixed numeric widening and buffered container materialization.
- The 1024 nesting limit and malformed trailing-numeric classification edge case are documented and pinned by tests.
- Parser and sink comments describe only the runtime guards and numeric limits they actually enforce.
</success_criteria>

<output>
Create `.planning/quick/260729-eb3-address-simd-parser-parity-findings-pres/260729-eb3-SUMMARY.md`
and update `.planning/STATE.md` after implementation and verification.
</output>
