---
status: in_progress
created: 2026-04-24
branch: phase/17-failure-mode-taxonomy-unification
source_review: https://github.com/amikos-tech/ami-gin/pull/32#issuecomment-4311554982
---

# Quick Task 260424: Address PR #32 Feedback (Items 1-5)

## Context

Claude's review of PR #32 (Phase 17 — Failure-Mode Taxonomy Unification) raised six observations, all non-blocking. Items 1-5 are janitorial cleanups confirmed valid by research agents. Item 6 (custom-parser wrapping defeats `stageCallbackError` detection) was disproven — `errors.As` walks `%w` chains — so it is intentionally excluded from this task.

## Tasks

1. **Drop redundant `normalizeIngestFailureMode` calls.** `NewBuilder` normalizes `config.ParserFailureMode` and `config.NumericFailureMode` at `builder.go:213-214`. Replace the five redundant calls at `builder.go:422, 649, 683, 754, 770` with direct `==` comparisons against `IngestFailureSoft`.

2. **Expand CHANGELOG.md Unreleased.** Add entries for the new public API surface introduced by phase 17:
   - `WithParserFailureMode(mode)` option
   - `WithNumericFailureMode(mode)` option
   - `NumSoftSkippedDocuments()` method
   - `NumSoftSkippedRepresentations()` method
   - `SoftSkippedDocuments()` deprecated alias for `NumSoftSkippedDocuments()`

3. **Document scientific-notation limitation on `RegexExtractInt`.** The closure at `transformer_registry.go:146-160` uses `parseFloat`, which rejects scientific notation (`"1.5e3"`). Add a short doc comment on `RegexExtractInt` noting that captured strings must be plain decimal, and that unsupported formats result in the document being skipped.

4. **Remove dead-code `p.Group < 0` check inside closures.** Construction-time guards at `transformer_registry.go:116-118` and `139-141` reject negative groups. The closure-internal checks at lines 129 and 152 (`p.Group < 0 || len(matches) <= p.Group`) are unreachable in the `p.Group < 0` branch. Simplify to `len(matches) <= p.Group`.

5. **Clarify `softSkipKindOther` → `softSkipKindParser` remap.** At `builder.go:411-415`, add a short comment documenting the invariant: bare `errSkipDocument` sentinels originate only from the parser path today, and any future non-parser skip site must construct a typed `softSkipDocumentError` with an explicit kind to avoid misclassification.

## Non-Goals

- Item 6 from the review (custom-parser error wrapping). Research confirmed `errors.As` chains through `fmt.Errorf(...%w)` and `errors.Wrap`, so the claimed correctness bug does not exist.
- Behavior changes. All five tasks are source-stable: no API, semantic, or wire changes.
- Broad refactoring of soft-skip taxonomy. The typed-sentinel idea for item 5 is deferred; a comment is sufficient for now.

## Verification

- `go build ./...`
- `go test ./...` — full suite must pass unchanged.
- `golangci-lint run` — no new lint findings.
- Targeted tests:
  - `go test -v -run 'TestNumericFailureModeSoft|TestSoftSkippedDocumentsAreObservable|TestRegexExtract' ./...`

## Files Touched

- `builder.go` — items 1 and 5
- `transformer_registry.go` — items 3 and 4
- `CHANGELOG.md` — item 2
