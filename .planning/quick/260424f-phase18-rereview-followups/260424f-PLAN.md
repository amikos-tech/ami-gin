---
status: complete
created: 2026-04-24
branch: phase/18-structured-ingesterror-cli-integration
---

# Quick Task 260424f: Phase 18 Re-Review Follow-Ups

## Context

Address the second review pass on the Phase 18 follow-up branch. The new review found one real behavioral gap in the tragic-abort path, a set of stale docs/citations after the prior refactor, and a missing end-to-end CLI test for continue-mode tragic closure.

## Tasks

1. Make `aborted:tragic` a real structured report status and preserve grouped failure metadata on tragic abort.
2. Add an end-to-end `runExperiment` tragic test that proves continue mode stops after the first builder-closing failure and still emits the structured JSON report.
3. Refresh stale docs and comments:
   - CHANGELOG `IngestError` API wording
   - SECURITY T18-05 / T18-10 citations
   - helper godoc and layer-order comment
4. Fold in low-risk follow-up coverage:
   - nil-receiver accessor tests for `IngestError`
   - use `internalRepresentationPathPrefix` in companion-path remapping

## Verification

- `go test ./cmd/gin-index -run 'Test(HandleExperimentLineErrorAbortsOnTragicBuilder|RunExperimentOnErrorContinueTragicAbortJSON|RunExperimentOnErrorContinue|ExperimentIngestFailureGroupsDeterministic|RecordExperimentIngestFailureCapsSamplesInArrivalOrder|RunExperimentOnErrorContinueIngestFailures(Text|JSON)|RunExperimentOnErrorContinueMalformedJSONFromFile)'`
- `go test -run 'TestIngestErrorWrappingContract|TestHardIngestFailuresReturnIngestError|TestNumericFailureModeHardReturnsErrors'`
- `go test ./...`
