---
status: complete
created: 2026-04-24
branch: phase/18-structured-ingesterror-cli-integration
---

# Quick Task 260424e: Phase 18 Review Follow-Ups

## Context

Validate and address the Phase 18 review findings for structured `IngestError` handling and CLI experiment reporting. The focus is correctness before merge: fix confirmed critical issues, close the high-leverage API/test gaps, and record any low-cost follow-up cleanups done in the same pass.

## Tasks

1. Fix the confirmed critical issues:
   - remap companion-transformer failures back to source paths
   - capture non-`IngestError` CLI failures instead of silently dropping them
   - stop continue-mode ingest once the builder enters a tragic closed state
   - update the stale parser-wrapping comment in `AddDocument`

2. Address the important structural follow-ups:
   - unexport `IngestError` fields behind accessors
   - exercise schema ordering end-to-end in CLI tests
   - prove the 3-sample cap preserves first-seen ordering
   - broaden the AST guard to discover `stage*` and `+hard-ingest` surfaces and catch more plain-error constructions

3. Fold in low-cost cleanup where it aligns with the above:
   - document `IngestError` value/error-format stability
   - move `formatStagedNumericValue` back next to builder-only numeric staging code
   - switch the non-finite numeric case to `requireIngestError`
   - refresh stale `SECURITY.md` file:line references touched by these edits

## Verification

- `go test`
- `go test ./cmd/gin-index`
- `go test ./...`

