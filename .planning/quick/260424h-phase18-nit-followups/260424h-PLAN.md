---
status: complete
created: 2026-04-24
branch: phase/18-structured-ingesterror-cli-integration
---

# Quick Task 260424h: Phase 18 Nit Follow-Ups

## Context

Address non-blocking review nits around tragic abort error ordering, documented `Unwrap` policy coverage, text-mode tragic output assertions, and companion ingest-error remap godoc completeness.

## Tasks

1. Add a call-site comment preserving the ordering between `recordExperimentIngestFailure` and `newExperimentTragicAbortError`.
2. Add a focused unit test proving `experimentTragicAbortError.Unwrap()` exposes only the builder-closing error, not the original line error.
3. Strengthen the tragic text-mode test to assert exactly one `GIN Index Info:` section.
4. Expand `remapCompanionIngestErrorPath` godoc to include the target-prefix-mismatch no-op case.

## Verification

- `go test ./cmd/gin-index -run 'Test(HandleExperimentLineErrorAbortsOnTragicBuilder|ExperimentTragicAbortErrorUnwrapExposesOnlyBuilderErr|RunExperimentOnErrorContinueTragicAbortText)'`
- `go test ./...`
