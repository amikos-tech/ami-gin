---
phase: quick-260420-h1a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prefix.go
  - serialize.go
  - gin.go
autonomous: true
requirements:
  - PR23-REVIEW-ITEM-2
must_haves:
  truths:
    - "WriteCompressedTerms is no longer an exported symbol in package gin"
    - "The single in-package call site in serialize.go uses the new unexported name"
    - "All doc comment references point at the unexported name"
    - "go build ./... succeeds"
    - "go test -v passes with no regressions"
  artifacts:
    - path: "prefix.go"
      provides: "Unexported writeCompressedTerms declaration and updated doc comment"
      contains: "func writeCompressedTerms("
    - path: "serialize.go"
      provides: "Updated call site for writeCompressedTerms"
      contains: "writeCompressedTerms(&buf,"
    - path: "gin.go"
      provides: "Doc comment references updated to writeCompressedTerms"
      contains: "prefix.go:writeCompressedTerms"
  key_links:
    - from: "serialize.go"
      to: "prefix.go"
      via: "direct function call"
      pattern: "writeCompressedTerms\\(&buf,"
    - from: "gin.go doc comments"
      to: "prefix.go"
      via: "reference string"
      pattern: "prefix\\.go:writeCompressedTerms"
---

<objective>
Unexport `WriteCompressedTerms` → `writeCompressedTerms` in `prefix.go` and update all in-package references. This addresses PR #23 review feedback item 2: the function has zero external callers and its reader counterpart (`ReadCompressedTerms`) was already unexported in commit f933470, so keeping the writer exported falsely implies a stable wire-format contract that cannot actually round-trip externally.

Purpose: Tighten the public API surface — don't export what callers can't usefully consume.
Output: Single commit touching three files (`prefix.go`, `serialize.go`, `gin.go`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<!-- Target declaration and doc-comment site -->
@prefix.go

<!-- Call site (same package) -->
@serialize.go

<!-- Two doc-comment references pointing at WriteCompressedTerms -->
@gin.go

<interfaces>
<!-- Exact occurrences confirmed by grep across the repo (no test references): -->
<!--
prefix.go:25:   // WriteCompressedTerms encodes block entry counts as uint16, so block
prefix.go:152:  func WriteCompressedTerms(w io.Writer, blocks []CompressedTermBlock) error {
serialize.go:524: if err := WriteCompressedTerms(&buf, pc.CompressInOrder(values)); err != nil {
gin.go:564:     // uint16 (see prefix.go:WriteCompressedTerms).
gin.go:708:     // count on the wire (see prefix.go:WriteCompressedTerms).
-->

<!-- Signature stays identical; only the identifier changes from exported to unexported: -->
```go
// before
func WriteCompressedTerms(w io.Writer, blocks []CompressedTermBlock) error

// after
func writeCompressedTerms(w io.Writer, blocks []CompressedTermBlock) error
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename WriteCompressedTerms to writeCompressedTerms across prefix.go, serialize.go, and gin.go</name>
  <files>prefix.go, serialize.go, gin.go</files>
  <action>
Mechanical rename in three files. Do NOT change the function signature, body, or any other code — only the identifier and the four comment references.

1. `prefix.go:152` — change the declaration from `func WriteCompressedTerms(w io.Writer, blocks []CompressedTermBlock) error {` to `func writeCompressedTerms(w io.Writer, blocks []CompressedTermBlock) error {`.
2. `prefix.go:25` — update the doc comment line `// WriteCompressedTerms encodes block entry counts as uint16, so block` to `// writeCompressedTerms encodes block entry counts as uint16, so block`.
3. `serialize.go:524` — update `if err := WriteCompressedTerms(&buf, pc.CompressInOrder(values)); err != nil {` to `if err := writeCompressedTerms(&buf, pc.CompressInOrder(values)); err != nil {`.
4. `gin.go:564` — update `// uint16 (see prefix.go:WriteCompressedTerms).` to `// uint16 (see prefix.go:writeCompressedTerms).`.
5. `gin.go:708` — update `// count on the wire (see prefix.go:WriteCompressedTerms).` to `// count on the wire (see prefix.go:writeCompressedTerms).`.

Do not introduce any other edits. No refactors, no formatting changes beyond what these five edits require. Use `grep -rn WriteCompressedTerms .` after editing to confirm zero remaining occurrences in the repo.

Note: `ReadCompressedTerms` was already unexported (commit f933470); pairing this writer rename with that earlier change finishes the API cleanup for PR #23 feedback item 2.
  </action>
  <verify>
<automated>grep -rn "WriteCompressedTerms" . --include="*.go" | wc -l | grep -q "^[[:space:]]*0$" && go build ./... && go test -v ./... -count=1</automated>
  </verify>
  <done>
- `grep -rn WriteCompressedTerms --include="*.go"` returns no results
- `prefix.go` declares `writeCompressedTerms` (unexported) with the original signature and body unchanged
- `serialize.go:524` calls the unexported name
- Both `gin.go` doc-comment references read `prefix.go:writeCompressedTerms`
- `go build ./...` succeeds
- `go test -v ./... -count=1` passes with no regressions
  </done>
</task>

</tasks>

<verification>
Repo-wide occurrence check and full test suite:

```bash
grep -rn "WriteCompressedTerms" . --include="*.go"   # expect: no output
go build ./...                                       # expect: success
go test -v ./... -count=1                            # expect: all tests pass
```

Public API check (sanity — no exported symbols should reference the old name):

```bash
go doc ./... 2>/dev/null | grep -i "WriteCompressedTerms"   # expect: no output
```
</verification>

<success_criteria>
- Zero occurrences of `WriteCompressedTerms` remain in any `.go` file in the repo.
- `writeCompressedTerms` is declared at `prefix.go` with the original signature and body unchanged.
- Call site at `serialize.go` and two doc-comment references in `gin.go` use the unexported name.
- `go build ./...` and `go test -v ./... -count=1` both succeed.
- Single commit, conventional-commit message (e.g., `refactor: unexport writeCompressedTerms`).
</success_criteria>

<output>
After completion, create `.planning/quick/260420-h1a-unexport-writecompressedterms-to-writeco/260420-h1a-01-SUMMARY.md`.
</output>
