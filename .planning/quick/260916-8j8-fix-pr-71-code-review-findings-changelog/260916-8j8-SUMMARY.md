---
phase: quick
plan: 260916-8j8
subsystem: testing
tags: [regex, trigram, changelog, doc-comment]

requires:
  - phase: quick/260916-88n
    provides: "foldsUnderToLower fix for issue #70 (drop fold-bad case-folded literals)"
provides:
  - "Accurate CHANGELOG.md #70 entry scoping which letters still prune"
  - "Accurate ExtractLiterals doc comment describing whole-literal drop"
  - "Non-misleading Kelvin test comment"
  - "Two new regression cases: (?i)sx/'ſx' and (?i)sX/'ſX'"
affects: [regex.go, regex_soundness_test.go, CHANGELOG.md]

tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - CHANGELOG.md
    - regex.go
    - regex_soundness_test.go

key-decisions:
  - "CHANGELOG.md #70 entry now names s/S as the only fold-bad ASCII letters and calls out Greek/Cyrillic letters also affected, instead of the incorrect blanket 'ASCII and accented letters still prune' claim"
  - "ExtractLiterals doc comment now documents drop-whole-node-on-any-bad-rune behavior instead of the stale 'come back in one case' claim"

requirements-completed: [PR-71-REVIEW]

duration: 12min
completed: 2026-09-16
---

# Quick Task 260916-8j8: Fix PR #71 Code Review Findings Summary

**Corrected CHANGELOG.md and ExtractLiterals doc comment scope claims, reworded a misleading test comment, and added two fold-fragment regression cases to TestRegexNeverPrunesAMatch.**

## Performance

- **Duration:** ~12 min
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- CHANGELOG.md's #70 entry no longer overstates which ASCII letters still prune (only `s`/`S` are fold-bad; `k`/`K` and accented Latin still prune; Greek and a few Cyrillic letters are also affected)
- `ExtractLiterals` doc comment in `regex.go` now accurately describes the whole-merged-literal-node drop, replacing the stale "come back in one case" claim
- `(?i)kkk` test comment reworded from a #70-pin-style comment to a safety-check comment
- Added `(?i)sx`/`"ſx"` (fragment case) and `(?i)sX`/`"ſX"` (whole-literal-drop case) regression cases to `TestRegexNeverPrunesAMatch`

## Task Commits

Both tasks were combined into one atomic commit per the plan's constraint (doc/comment/test-only changes, no production logic):

1. **Task 1 + Task 2: Fix CHANGELOG.md, regex.go doc comment, test comment, add regression cases** - `2029c7e` (fix)

## Files Created/Modified
- `CHANGELOG.md` - Corrected the #70 unreleased entry's letter-scoping claim
- `regex.go` - Rewrote `ExtractLiterals` doc comment to describe whole-node drop behavior
- `regex_soundness_test.go` - Reworded Kelvin comment; added two new `(?i)sx`/`(?i)sX` regression cases

## Decisions Made
- Combined both plan tasks into a single commit since all changes are documentation/comment/test-only with no production logic, matching the executor's atomic-commit instruction for this quick task.

## Deviations from Plan

None — plan executed exactly as written. One out-of-scope, pre-existing test failure was discovered and logged (not fixed) per the deviation-rules scope boundary — see below.

## Issues Encountered

`go test ./...` surfaced a pre-existing, unrelated failure:
`TestAddDocumentDefaultParserErrorStringsPreserved/unterminated-object` in
`parser_test.go` fails because the local Go toolchain is 1.27.1 while
`go.mod` targets 1.25.5 — the stdlib `encoding/json` error message wording
for unterminated objects changed between these versions, breaking a
hardcoded substring assertion. This is unrelated to CHANGELOG.md, regex.go,
or regex_soundness_test.go and was not introduced by this plan. Confirmed
pre-existing by running the test in isolation. Logged in
`deferred-items.md` in this plan's directory; not fixed here (out of scope
per deviation rules).

The plan's own verification target passed cleanly:
`go build ./...` and `go test -run TestRegexNeverPrunesAMatch -v .` both
pass with all 15 cases (13 existing + 2 new) green.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Ready for the orchestrator to review and push `fix/70-regex-unicode-fold`
for PR #71. The pre-existing parser-error-string test failure
(Go-version-dependent stdlib JSON error wording) should be tracked as a
separate follow-up issue/task, independent of this branch.

---
*Phase: quick*
*Completed: 2026-09-16*

## Self-Check: PASSED

- FOUND: commit 2029c7e (git log --oneline --all)
- FOUND: CHANGELOG.md
- FOUND: regex.go
- FOUND: regex_soundness_test.go
- FOUND: deferred-items.md
