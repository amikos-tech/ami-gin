---
phase: quick-260914-n4a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - builder.go
  - gin.go
  - ingest_error.go
  - SECURITY.md
  - cmd/gin-index/experiment.go
  - gin_test.go
  - cmd/gin-index/experiment_test.go
autonomous: true
requirements: [PR-01, PR-02, PR-03, PR-04, PR-05, PR-06, PR-07, PR-08, PR-09, PR-10]
tags: [builder, docs, security-doc, cli, tests]

must_haves:
  truths:
    - "remapCompanionIngestErrorPath's doc comment no longer lists transformer failures (they return directly from stageCompanionRepresentations and never reach this function) and is ~3 lines."
    - "SECURITY.md T18-04, T18-05, T18-07 evidence cells cite function/test names instead of stale line numbers, matching the existing style of T18-06 and T18-11."
    - "TestMaxStagedPaths proves builder recovery: rejecting a document at the limit does not poison the builder, and a smaller retry at the same DocID succeeds, followed by a second DocID and a working Finalize/Evaluate."
    - "The staged-path budget error message tells callers the count includes the root, containers, and derived companion paths, and every existing exact-match and CLI substring assertion still passes."
    - "A 500-element flat array only ever stages two canonical paths ($ and $[*]) regardless of element count; a limit of 1 fails specifically on $[*]."
    - "A two-key companion transformer proves the HasPrefix child-path branch of remapCompanionIngestErrorPath fires when the second staged child key trips the budget."
    - "gin-index experiment rejects --max-staged-paths -1 the same way gin-index build already does."
    - "Querying a concrete array index (\"$.orders[0].id\") after only wildcard paths were staged returns AllRGs (no pruning), not an empty or single-row-group result."
    - "IngestLayerResource's doc comment tells callers to rebuild with a higher limit instead of implying in-place retry."
    - "MaxStagedPaths' field comment, getOrCreateStagedPath's prose below the +hard-ingest marker, and the experiment.go sample-cap comment are each trimmed to one line without removing the +hard-ingest marker."
  artifacts:
    - path: "builder.go"
      provides: "Trimmed remap doc comment, budget-hint error message, trimmed getOrCreateStagedPath prose"
      contains: "count includes the root, containers, and derived companion paths"
    - path: "gin_test.go"
      provides: "New TestMaxStagedPaths recovery/array/companion subtests, prefix-matching budget helper, AllRGs wildcard assertion"
      contains: "requires at least 5"
    - path: "SECURITY.md"
      provides: "Corrected T18-04/T18-05/T18-07 evidence citing function/test names"
      contains: "TestParserContractErrorsRemainNonIngestError"
    - path: "cmd/gin-index/experiment_test.go"
      provides: "Negative --max-staged-paths rejection test mirroring the build subcommand"
      contains: "TestRunExperimentRejectsNegativeMaxStagedPaths"
    - path: "ingest_error.go"
      provides: "Accurate IngestLayerResource guidance"
      contains: "rebuild with a higher limit"
  key_links:
    - from: "gin_test.go:requireStagedPathBudgetError"
      to: "builder.go:getOrCreateStagedPath"
      via: "prefix match on IngestError.Cause().Error() tolerating the new trailing hint"
      pattern: "HasPrefix"
---

<objective>
Address a round of PR review findings on branch `fix/issue-54` (issue #54): trim two over-long comments and fix one inaccurate comment (docs accuracy), replace three stale SECURITY.md line-number citations with function/test names (docs accuracy), add five missing test cases to `gin_test.go` proving existing behavior (builder recovery, budget-hint wording, flat-array wildcard collapsing, companion child-path remap, AllRGs fallback), append a clarifying hint to the staged-path budget error message, and mirror one CLI validation test from `main_test.go` onto `experiment_test.go`.

Purpose: These are review-converged documentation-accuracy and test-coverage gaps on code already on this branch — no new features, no behavior changes beyond one additive error-message hint.
Output: Ten scope items closed across three sequential tasks (comments/docs, error-message + core tests, CLI mirror test), each independently verifiable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@builder.go
@gin.go
@ingest_error.go
@SECURITY.md
@cmd/gin-index/experiment.go

<interfaces>
Key facts verified against current source, not assumed.

getOrCreateStagedPath (builder.go:186-209) is the single creation boundary for every staged path (scalars, numerics, MarkPresent, and companion representations). Its budget error currently reads: "staged path budget exceeded: limit %d total paths; document requires at least %d". Task 2 appends a hint clause; the format verb order (limit, then requires-at-least) does not change.

requireStagedPathBudgetError (gin_test.go:3389-3410) currently does an EXACT match on `ingestErr.Cause().Error()` against `wantCause`. Changing this single comparison to a prefix match (`strings.HasPrefix`) means every existing call site's `wantCause` argument — which already embeds "limit N total paths; document requires at least M" — keeps matching verbatim without editing any of the 7 existing call sites (gin_test.go:3193, 3258, 3285, 3304, 3319, 3338, 3382). Do not touch those 7 call sites.

remapCompanionIngestErrorPath (builder.go:693-716) has 4 branches: exact targetPath match, HasPrefix(path, targetPath+"."), HasPrefix(path, targetPath+"["), and HasPrefix(path, internalRepresentationPathPrefix). Companion target paths are always built by representationTargetPath as `__derived:<source>#<alias>` (gin.go:493), so they already start with internalRepresentationPathPrefix ("__derived:", gin.go:43) — this is why transformer failures (constructed and returned directly inside stageCompanionRepresentations, builder.go:663-687) never reach this remap function at all: only errors returned from the nested `stageMaterializedValue` call (schema/numeric failures) do.

CLI budget-message assertions are all `strings.Contains` (cmd/gin-index/main_test.go:626, cmd/gin-index/experiment_test.go:376,446) and are unaffected by the appended hint; the only call site needing a code change for the message-wording task is the helper in gin_test.go.

runExperiment's negative-flag validation (cmd/gin-index/experiment.go:69-72) fires before `fs.NArg()` is checked (line 78), using the same error text pattern as runBuild's mirror in main.go: "Error: --max-staged-paths must be greater than or equal to 0".

TestRunBuildRejectsNegativeMaxStagedPaths (cmd/gin-index/main_test.go:597-609) is the exact test to mirror: `t.Parallel()`, invoke with a negative flag value plus a placeholder positional arg, assert non-zero exit code and the stderr substring. runExperiment's negative check fires before its NArg() check, so a positional arg is not strictly required, but include one for parity with the build mirror.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix over-long and inaccurate comments, and stale SECURITY.md citations</name>
  <files>builder.go, gin.go, ingest_error.go, cmd/gin-index/experiment.go, SECURITY.md</files>
  <action>
Item 1 - builder.go: rewrite the doc comment above `remapCompanionIngestErrorPath` (currently builder.go:693-701, 9 lines). Drop "transformer" from the failure-type list - transformer errors are constructed and returned directly from `stageCompanionRepresentations` and never pass through this function; only schema and numeric failures reach it via the nested `stageMaterializedValue` call. Trim to ~3 lines stating: it rewrites a leaked companion target path back to the source path (found via `errors.As`) on hard schema or numeric ingest errors, and it is a no-op when `err` is not an `*IngestError`, the path is empty, or the path does not match the companion target/internal-prefix shape.

Item 10a - builder.go: trim the prose below the `// +hard-ingest` marker above `getOrCreateStagedPath` (builder.go:186-190, currently 2 lines of prose after the marker and a blank comment line) to one line: state that it returns the staged state for `canonicalPath`, creating it only within the staged-path budget. Keep the `// +hard-ingest` marker line exactly as-is (ingest_error_guard_test.go's `hardIngestFunctions` AST scan depends on it).

Item 10b - gin.go: trim the `MaxStagedPaths` field comment (currently gin.go:411-414, 4 lines) to one line, pointing to `WithMaxStagedPaths` as the source of truth for the full contract. Do not edit `WithMaxStagedPaths`'s own doc comment at gin.go:468-478 - it stays as-is.

Item 9 - ingest_error.go: in the `IngestLayerResource` constant doc comment, replace the sentence "Callers can adjust the relevant builder limit and retry the document." with "Callers can rebuild with a higher limit." Keep the preceding sentence ("...rejected an otherwise valid document.") unchanged.

Item 10c - cmd/gin-index/experiment.go: collapse the 6-line comment block immediately above `group.Samples = append(group.Samples, sample)` (currently experiment.go:573-578) into one line. Preserve the substance: values are captured verbatim, resource failures carry no value, growth is bounded by sample count (not truncation), and tragic builder-closing failures bypass the cap.

Item 2 - SECURITY.md: in the "Threat Verification" table, replace the Evidence cell contents for rows T18-04, T18-05, and T18-07 (stale references: `builder.go:435`, `failure_modes_test.go:102/184/206/547/604/782`, `ingest_error_guard_test.go:143`) with function/test names, matching the citation style already used in T18-06 and T18-11 (name-only, no line numbers). Use these verified mappings:
  - T18-04 (currently cites builder.go:435, failure_modes_test.go:184, failure_modes_test.go:547): builder.go:435 is the `isParserLifecycleError` check inside `AddDocument` that keeps parser contract checks outside `IngestError`; failure_modes_test.go:184 is now `TestParserContractErrorsRemainNonIngestError`, which verifies contract failures stay non-`IngestError`; failure_modes_test.go:547 is now `TestParserFailureModeSoftKeepsContractViolationsHard`, which proves parser soft mode does not swallow contract errors.
  - T18-05 (currently cites failure_modes_test.go:102, ingest_error_guard_test.go:11, ingest_error_guard_test.go:143): failure_modes_test.go:102 is now `TestHardIngestFailuresReturnIngestError`, the hard-ingest behavior matrix; ingest_error_guard_test.go:11 is `TestHardIngestFunctionsDoNotReturnPlainErrors`, which executes the guard; ingest_error_guard_test.go:143 is now the `hardIngestFunctions` helper, which defines the auto-discovered hard-ingest surface set.
  - T18-07 (currently cites failure_modes_test.go:184/206/547/604/782): the four categories it covers (parser-contract, soft-mode, tragic-state, recovered-panic) now map to `TestParserContractErrorsRemainNonIngestError`, `TestSoftFailureModesDoNotReturnIngestError`, `TestParserFailureModeSoftKeepsTragicStateHard`, and `TestNumericFailureModeSoftKeepsMergeRecoveryTragic` respectively.
  Before finalizing, run a grep for each of these function/test names across failure_modes_test.go, ingest_error_guard_test.go, and builder.go to confirm every name exists before writing the new cells. Do not add or remove table rows; only rewrite the Evidence cell text for these three rows.
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; gofmt -l builder.go gin.go ingest_error.go cmd/gin-index/experiment.go &amp;&amp; grep -n "func TestParserContractErrorsRemainNonIngestError\|func TestHardIngestFailuresReturnIngestError\|func hardIngestFunctions\|func TestNumericFailureModeSoftKeepsMergeRecoveryTragic" failure_modes_test.go ingest_error_guard_test.go</automated>
  </verify>
  <done>All five comment trims/fixes are applied and each cited name is confirmed present via grep; SECURITY.md's T18-04/T18-05/T18-07 evidence cells name functions and tests instead of line numbers; go build and gofmt are clean.</done>
</task>

<task type="auto">
  <name>Task 2: Append the budget-message hint and add the five missing gin_test.go cases</name>
  <files>builder.go, gin_test.go</files>
  <action>
Item 4a - builder.go: in `getOrCreateStagedPath` (builder.go:194-205), append a parenthetical hint to the `errors.Errorf` format string so it reads: "staged path budget exceeded: limit %d total paths; document requires at least %d (count includes the root, containers, and derived companion paths)". The two `%d` verb positions and their arguments (`b.config.MaxStagedPaths`, `len(state.paths)+1`) stay exactly as they are today.

Item 4b - gin_test.go: in `requireStagedPathBudgetError` (gin_test.go:3389-3410), change the Cause() comparison from exact equality to a prefix check: replace the `got != wantCause` equality check with a `!strings.HasPrefix(got, wantCause)` check (fail with a "want prefix %q" message on mismatch). Do not edit any of the 7 existing call sites that pass `wantCause` - their strings already match as a prefix of the new hinted message.

Item 3 - gin_test.go: inside `TestMaxStagedPaths`, add a new `t.Run("builder recovers after a rejected document", ...)` subtest: build a config with `WithMaxStagedPaths(2)` (budget for `$` and `$.a`), get a builder with 2 row groups. Call `AddDocument(0, []byte(`{"a":"x","b":"y"}`))`, assert it fails via `requireStagedPathBudgetError` with path `"$.b"` and cause prefix "staged path budget exceeded: limit 2 total paths; document requires at least 3", then assert `requireUncommittedStagedPathBudgetDocument`. Retry the same DocID 0 with the smaller document `{"a":"x"}` and assert it succeeds. Add DocID 1 with `{"a":"y"}` and assert it succeeds. Call `Finalize()`, then assert `Evaluate([]Predicate{EQ("$.a", "x")})` returns row group `[0]`, `Evaluate([]Predicate{EQ("$.a", "y")})` returns row group `[1]`, and `len(idx.PathDirectory) == 2`.

Item 5 - gin_test.go: inside `TestMaxStagedPaths`, add a new `t.Run("flat array wildcard path counted once", ...)` subtest: build a 500-element flat scalar array document (`"[" + strings.Repeat("1,", 499) + "1]"`). With `WithMaxStagedPaths(2)` (covers `$` and `$[*]`), assert `AddDocument` succeeds on a fresh 1-row-group builder. With a separate `WithMaxStagedPaths(1)` config and a fresh builder, assert `AddDocument` fails via `requireStagedPathBudgetError` with path `"$[*]"` and cause prefix "staged path budget exceeded: limit 1 total paths; document requires at least 2".

Item 6 - gin_test.go: inside `TestMaxStagedPaths`, add a new `t.Run("companion child path exceeds limit on second key", ...)` subtest: build a config with `WithMaxStagedPaths(4)` (covers `$`, `$.email`, the companion container, and its first child key) plus `WithCustomTransformer("$.email", "twokeys", func(any) (any, bool) { return map[string]any{"a": 1, "b": 2}, true })`. Assert `AddDocument(0, []byte(`{"email":"a@b.com"}`))` fails via `requireStagedPathBudgetError` with path `"$.email"` (remapped from the companion's second child key) and cause prefix "staged path budget exceeded: limit 4 total paths; document requires at least 5", then assert `requireUncommittedStagedPathBudgetDocument`. This exercises the `HasPrefix(path, targetPath+".")` branch of `remapCompanionIngestErrorPath` on the second staged child (lexical object-key order stages "a" before "b", so "b" is the one that trips the budget).

Item 8 - gin_test.go: in `TestWildcardArrayQueryReturnsOnlyMatchingRowGroup`, capture `idx := builder.Finalize()` (instead of the current inline `builder.Finalize().Evaluate(...)`) and keep the existing `EQ("$.orders[*].id", "wanted")` assertion against `idx`. Add a new assertion directly after it: `idx.Evaluate([]Predicate{EQ("$.orders[0].id", "wanted")}).ToSlice()` must have length 2 (both row groups - i.e. `AllRGs`, no pruning), since only the wildcard path `$.orders[*].id` was ever staged and a concrete-index query is an unknown path to the index. Add a short comment explaining why the fallback is `AllRGs` rather than an empty or single-row-group result.
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; go test -count=1 -run '^TestMaxStagedPaths$|^TestStagedPathBudgetPropagatesAllStagingCallSiteErrors$|^TestWildcardArrayQueryReturnsOnlyMatchingRowGroup$' -v . &amp;&amp; gofmt -l builder.go gin_test.go</automated>
  </verify>
  <done>getOrCreateStagedPath's error carries the new hint; requireStagedPathBudgetError does a prefix match and all 7 existing call sites plus the 3 new ones pass; TestMaxStagedPaths has the recovery, flat-array, and companion-child-path subtests; TestWildcardArrayQueryReturnsOnlyMatchingRowGroup asserts the AllRGs fallback; all touched tests pass; gofmt is clean.</done>
</task>

<task type="auto">
  <name>Task 3: Mirror the negative --max-staged-paths rejection test onto gin-index experiment</name>
  <files>cmd/gin-index/experiment_test.go</files>
  <action>
Item 7 - cmd/gin-index/experiment_test.go: add `TestRunExperimentRejectsNegativeMaxStagedPaths`, mirroring `TestRunBuildRejectsNegativeMaxStagedPaths` (cmd/gin-index/main_test.go:597-609) for the `experiment` subcommand. Use `t.Parallel()`, call `runExperiment([]string{"--max-staged-paths", "-1", "does-not-matter.jsonl"}, bytes.NewReader(nil), &amp;stdout, &amp;stderr)`, assert the returned code is non-zero, and assert `stderr.String()` contains "--max-staged-paths must be greater than or equal to 0" (the exact text runExperiment emits at experiment.go:70). Place the new test near the other `--max-staged-paths` coverage (`TestRunExperimentMaxStagedPathsReportsResourceFailureWithoutValue`, experiment_test.go:406).
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; go test -count=1 ./cmd/gin-index/... -run '^TestRunExperimentRejectsNegativeMaxStagedPaths$|^TestRunBuildRejectsNegativeMaxStagedPaths$' -v &amp;&amp; gofmt -l cmd/gin-index/experiment_test.go</automated>
  </verify>
  <done>gin-index experiment rejects a negative --max-staged-paths value with the same diagnostic and non-zero exit code as gin-index build; the new test passes alongside the existing build-side test.</done>
</task>

</tasks>

<source_audit>

| Source | ID | Feature / constraint | Plan coverage | Status |
| --- | --- | --- | --- | --- |
| CONTEXT | PR-01 | Trim remapCompanionIngestErrorPath doc comment; drop "transformer" | Task 1 | COVERED |
| CONTEXT | PR-02 | Fix stale SECURITY.md T18-04/T18-05/T18-07 line-number citations | Task 1 | COVERED |
| CONTEXT | PR-03 | TestMaxStagedPaths builder-recovery subtest | Task 2 | COVERED |
| CONTEXT | PR-04 | Budget error message hint + helper prefix match | Task 2 | COVERED |
| CONTEXT | PR-05 | 500-element flat array wildcard-path subtest | Task 2 | COVERED |
| CONTEXT | PR-06 | Two-key companion HasPrefix child-path subtest | Task 2 | COVERED |
| CONTEXT | PR-07 | Mirror TestRunBuildRejectsNegativeMaxStagedPaths onto experiment | Task 3 | COVERED |
| CONTEXT | PR-08 | AllRGs fallback assertion for concrete-index query | Task 2 | COVERED |
| CONTEXT | PR-09 | IngestLayerResource comment wording fix | Task 1 | COVERED |
| CONTEXT | PR-10 | Trim MaxStagedPaths field comment, getOrCreateStagedPath prose, experiment.go sample-cap comment | Task 1 | COVERED |

No GOAL, REQ, or RESEARCH artifacts apply - this is a quick task addressing PR review findings on an existing branch, sourced entirely from the task description.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| PR reviewer feedback -> merged code | Documentation and test-coverage gaps flagged in review must be closed before merge; none of these findings touch untrusted external input paths. |
| Budget error message -> callers/log consumers | The staged-path budget diagnostic is caller-facing text; widening it must not leak internal companion path names (already guarded by remapCompanionIngestErrorPath, untouched by this plan). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-n4a-01 | Repudiation | SECURITY.md T18-04/T18-05/T18-07 evidence cells | mitigate | Task 1 replaces stale line-number citations with function/test names verified via grep, so the audit trail stays accurate as the codebase moves. |
| T-n4a-02 | Information Disclosure | budget error message hint | accept | The added hint is generic guidance (root/containers/companion paths), not a value or internal path name; remapCompanionIngestErrorPath already prevents internal `__derived:` paths from leaking into Path(), and this plan does not touch that guard. |
| T-n4a-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies are added by this plan; all changes are comment, doc, and test edits to existing code. |
</threat_model>

<verification>
Run per-task verify commands first, then the full gate:

1. `go build ./...`
2. `go test -count=1 ./...`
3. `make lint`
4. `AMI_GIN_SIMD_REQUIRED=1 go test -count=1 -tags simdjson ./...`

The pre-existing failure `TestAddDocumentDefaultParserErrorStringsPreserved/unterminated-object` on local Go 1.27 is out of scope and must be ignored (it passes on Go 1.25/1.26 in CI).
</verification>

<success_criteria>
- remapCompanionIngestErrorPath's doc comment is ~3 lines and does not mention transformer failures.
- SECURITY.md's T18-04/T18-05/T18-07 evidence cells cite function/test names, each verified to exist via grep.
- TestMaxStagedPaths gains four new subtests (recovery, flat-array, companion-child-path) plus the existing suite continues to pass with the hinted budget message.
- TestWildcardArrayQueryReturnsOnlyMatchingRowGroup asserts the AllRGs fallback for a concrete-index query.
- gin-index experiment rejects a negative --max-staged-paths value identically to gin-index build.
- IngestLayerResource's doc comment, MaxStagedPaths' field comment, getOrCreateStagedPath's prose, and experiment.go's sample-cap comment are all corrected/trimmed without removing the +hard-ingest marker.
- Full `go build ./...`, `go test -count=1 ./...`, `make lint`, and `AMI_GIN_SIMD_REQUIRED=1 go test -count=1 -tags simdjson ./...` are green (modulo the documented pre-existing Go 1.27 failure).
</success_criteria>

<output>
Create `.planning/quick/260914-n4a-address-pr-review-findings-for-issue-54/260914-n4a-SUMMARY.md` when done, listing commits and full verification command output.
</output>
