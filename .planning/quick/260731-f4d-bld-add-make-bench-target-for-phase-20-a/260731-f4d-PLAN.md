---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Makefile]
autonomous: true
requirements: [ISSUE-42]

must_haves:
  truths:
    - "Running `make bench` executes the full benchmark suite with -benchmem and skips regular tests"
    - "Running `make bench-phase20` scopes to BenchmarkPhase20RealisticJSON only"
    - "BENCHTIME and COUNT are overridable via `make bench COUNT=10 BENCHTIME=2s`"
    - "`make help` lists both new targets, and bench-phase20's line documents the external simdjson opt-in env vars"
    - "Existing required Makefile targets (test, integration-test, lint, lint-fix, security-scan, clean, help) still work unmodified"
  artifacts:
    - path: "Makefile"
      provides: "bench and bench-phase20 targets with BENCHTIME/COUNT overrides, .PHONY declarations, and help text"
      contains: ".PHONY: bench"
  key_links:
    - from: "Makefile bench-phase20 target"
      to: "benchmark_test.go BenchmarkPhase20RealisticJSON"
      via: "go test -bench '^BenchmarkPhase20RealisticJSON$'"
      pattern: "BenchmarkPhase20RealisticJSON"
---

<objective>
Add `make bench` and `make bench-phase20` targets to the Makefile so contributors have a discoverable, consistent way to run Go benchmarks with correct defaults (`-run '^$'`, `-benchmem`, overridable `-benchtime`/`-count`).

Purpose: Closes GitHub issue #42 — today the only way to run benchmarks is a raw, easy-to-get-wrong `go test` invocation. A Make target encodes the right defaults.
Output: Updated `Makefile` with two new `.PHONY` targets, `BENCHTIME`/`COUNT` override variables, both targets listed in `make help` (bench-phase20's help line documents the `GIN_PHASE20_ENABLE_SIMDJSON_EXTERNAL` / `GIN_PHASE20_SIMDJSON_DIR` opt-in).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Makefile
@benchmark_test.go
</context>

<interfaces>
<!-- Confirmed benchmark names exist in benchmark_test.go — no exploration needed by executor -->

Relevant benchmark functions (from `grep -n "^func Benchmark" benchmark_test.go`):
- `BenchmarkPhase20RealisticJSON` (line 2630) — the realistic-dataset suite issue #42 asks to scope to
- Full suite includes many others (BenchmarkEncode, BenchmarkQueryEQ, BenchmarkScale*, etc.) — `bench` target runs `-bench .` to cover all of them, no enumeration needed

External tier opt-in env vars (from `benchmark_test.go:1765-1766`):
```go
phase20ExternalEnableEnvVar     = "GIN_PHASE20_ENABLE_SIMDJSON_EXTERNAL"
phase20ExternalDirectoryEnvVar  = "GIN_PHASE20_SIMDJSON_DIR"
```

Current Makefile structure (existing targets to preserve, not modify): `build`, `simd-isolation-check`, `gotestsum-bin`, `test`, `integration-test`, `check-validator-markers`, `check-notice-version`, `lint`, `lint-fix`, `security-scan`, `clean`, `help`. The `help` target uses `@echo "  name  - description"` lines; new entries follow the same style and column alignment.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add bench and bench-phase20 Makefile targets</name>
  <files>Makefile</files>
  <action>
Edit the Makefile to add two new targets plus override variables, without touching any existing target.

Add near the top (alongside `GOTESTSUM_VERSION ?= v1.13.0`) two overridable variables:
`BENCHTIME ?= 1s` and `COUNT ?= 1`.

Add a `.PHONY: bench` target that runs: `go test -run '^$$' -bench . -benchmem -benchtime=$(BENCHTIME) -count=$(COUNT)`. Note: inside a Makefile recipe, a literal `$` in the shell command must be escaped as `$$` so Make does not try to expand it as a Make variable — this applies to the `^$` regex.

Add a `.PHONY: bench-phase20` target that runs: `go test -run '^$$' -bench '^BenchmarkPhase20RealisticJSON$$' -benchmem -benchtime=$(BENCHTIME) -count=$(COUNT)`.

Place both targets after the existing `test`/`integration-test` targets, before `check-validator-markers`, to keep test-related targets grouped.

Do not add a new "benchmark" package list, new tooling, or `benchstat` as a dependency — plain `go test -bench` only, per the issue's "Notes" section and CLAUDE.md's "keep it radically simple" rule.
  </action>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/custom-gin && grep -c '^\.PHONY: bench$' Makefile && grep -c '^\.PHONY: bench-phase20$' Makefile</automated>
  </verify>
  <done>Makefile contains `bench` and `bench-phase20` `.PHONY` targets using `go test -run '^$' -bench ... -benchmem` with `$(BENCHTIME)` and `$(COUNT)` variables, both defaulting via `?=` (1s, 1). No existing target was removed or altered.</done>
</task>

<task type="auto">
  <name>Task 2: Document new targets in make help and verify the full Makefile</name>
  <files>Makefile</files>
  <action>
Add two `@echo` lines to the existing `help` target, following the current column-alignment style (`"  name    - description"`):

- `bench` line: describe it as running the full benchmark suite, noting `BENCHTIME`/`COUNT` are overridable (e.g. "bench     - Run all benchmarks (-benchmem); override BENCHTIME/COUNT, e.g. make bench COUNT=10").
- `bench-phase20` line: describe it as scoping to the realistic-dataset benchmark, and document the external-tier opt-in inline (e.g. "bench-phase20 - Run BenchmarkPhase20RealisticJSON only; set GIN_PHASE20_ENABLE_SIMDJSON_EXTERNAL=1 and GIN_PHASE20_SIMDJSON_DIR=<path> to include the external corpus tier").

Insert these lines in the same relative position as the targets themselves (after the test/integration-test lines).

After editing, run `go build ./...` (sanity — Makefile changes don't affect Go code, but confirms the repo is untouched/buildable) and `make -n bench` / `make -n bench-phase20` (dry-run, prints the command Make would execute without running it) to confirm the generated shell commands are syntactically correct and use the right benchmark regex and flags.
  </action>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/custom-gin && make -n bench | grep -- "-bench . -benchmem" && make -n bench-phase20 | grep -- "BenchmarkPhase20RealisticJSON" && make -n help | grep -c "bench"</automated>
  </verify>
  <done>`make help` output lists both `bench` and `bench-phase20` with descriptions, `bench-phase20`'s line documents `GIN_PHASE20_ENABLE_SIMDJSON_EXTERNAL`/`GIN_PHASE20_SIMDJSON_DIR`. `make -n bench` and `make -n bench-phase20` print correct `go test -run '^$' -bench ... -benchmem -benchtime=1s -count=1` commands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Makefile → local shell | Contributor-invoked build tooling only; no untrusted input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Tampering | Makefile bench targets | accept | Plain `go test -bench` invocation over trusted local repo code; no new dependency, no network calls, no secrets involved |
</threat_model>

<verification>
Run `make -n bench` and `make -n bench-phase20` to confirm dry-run output matches expected `go test` invocations. Run `make help` to confirm both targets are listed with correct descriptions. Run `make test` (or at minimum `go build ./...`) to confirm no regression to existing required targets.
</verification>

<success_criteria>
- `make bench` runs the full benchmark suite (`-bench .`) with `-benchmem`, skipping regular tests via `-run '^$'`
- `make bench-phase20` runs only `BenchmarkPhase20RealisticJSON`
- `BENCHTIME` (default `1s`) and `COUNT` (default `1`) are overridable on the command line
- `make help` documents both targets, including the external simdjson opt-in env vars for `bench-phase20`
- No existing Makefile target (`build`, `test`, `integration-test`, `lint`, `lint-fix`, `security-scan`, `clean`, `help`, `simd-isolation-check`, `check-validator-markers`, `check-notice-version`) was removed or broken
</success_criteria>

<output>
Create `.planning/quick/260731-f4d-bld-add-make-bench-target-for-phase-20-a/260731-f4d-SUMMARY.md` when done
</output>
