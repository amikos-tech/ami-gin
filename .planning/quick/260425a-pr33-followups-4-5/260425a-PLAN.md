---
status: in-progress
created: 2026-04-25
branch: phase/18-structured-ingesterror-cli-integration
pr: 33
---

# Quick Task 260425a: PR #33 Follow-Ups (Items 4 and 5)

## Context

Address two non-blocking observations from the Claude bot review on PR #33:

- **Item 4** — `+hard-ingest` AST guard directive uses `strings.Contains` matching, so any text containing the substring counts as a directive. Tighten the matcher to require the canonical standalone form `// +hard-ingest`, and update the existing call site to comply. Prevents drift if the directive is copy-pasted with arbitrary trailing text or a near-identical token like `+hard-ingest-foo`.
- **Item 5** — `withExperimentDefaultConfig` test helper overrides a package-global seam but lacks the matching "callers must not run in parallel while the override is active" comment that its sibling `withExperimentBuilderFactory` carries. Add the comment for symmetry.

## Tasks

1. **Tighten `+hard-ingest` matcher and migrate the call site to canonical form.**
   - Update `hasHardIngestDirective` in `ingest_error_guard_test.go:169` to require `// +hard-ingest` as its own trimmed comment line (reject substring matches with trailing text on the same line).
   - Update `builder.go:1009` to use the canonical form: `// +hard-ingest` on its own line, then the descriptive doc text on a separate line.
   - Update the explanatory comment at `ingest_error_guard_test.go:207-208` to mention the canonical-form requirement.
   - Verify guard still fires by running `go test -run TestIngestErrorGuard`.

2. **Add the "no parallel" comment to `withExperimentDefaultConfig`.**
   - Edit `cmd/gin-index/experiment_test.go:138`: insert the same two-line comment that `withExperimentBuilderFactory` has at line 151-152.

## Verification

- `go test -run TestIngestErrorGuard ./...`
- `go test ./...`
- `go vet ./...`
